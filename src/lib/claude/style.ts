// 전 에이전트 공통 출력 스타일 — "AI 티 나는 기호" 절제 (대표 지시 2026-07-24).
// 메일 다듬기(chat/assist)·회의노트 assist의 OUTPUT_RULE과 같은 철학을 일반 채팅 전체로 확장.
// 시스템 프롬프트 맨 뒤에 덧붙인다(에이전트별 프롬프트보다 후순위 아님 — 형식 지시라 충돌 없음).

export const OUTPUT_STYLE_RULE = `

# 출력 형식(항상 지킬 것)
- 사람이 쓴 자연스러운 실무 글처럼 답한다. 짧은 답은 문장으로만.
- 불릿(-, *)·굵게(**)·헤딩(#)·이모지 같은 장식은 기본적으로 쓰지 않는다. 긴 목록·단계 안내처럼 구조가 정말 필요할 때만 최소한으로.
- "물론입니다!", "좋은 질문이에요" 같은 군더더기 서두 없이 바로 본론부터.`
