import { DashboardLayout } from "@/components/dashboard/DashboardLayout"

export default function DashboardPage() {
  // 세션41 대표 확정: 한 화면 배치 — 공지 → [오늘 할 일|작업 제안 + 진행/예정] | [AI 채팅]. 상세는 DashboardLayout.
  return <DashboardLayout />
}
