# 첫 사용 경험(Activation) 개선 — 제품 점검 결과와 작업 계획

> **현재 상태·다음 할 일의 SSOT는 `HANDOFF.md`다.** 이 문서는 *왜 이걸 하는가*와 *무엇이 남았는가*를 담는다.
> 둘이 어긋나면 HANDOFF가 맞다. (`BILLING-PLAN.md`와 같은 역할·같은 이유로 레포에 둔다 — 세션이 끊겨도 이어가려고.)
>
> ### 진행 상황 (2026-08-19)
> | 단계 | 상태 |
> |---|---|
> | 3-4 컴피 예시칩 생성형 교체 | ✅ |
> | 3-5 에이전트 핀 안내 | ✅ |
> | 3-7 task-suggestions Gmail 죽은 코드 제거 + 온보딩 제안 | ✅ |
> | 3-10 문서 정정(lint 베이스라인·I31) | ✅ |
> | 3-1 시드 에이전트 3개 | ✅ `734b7c1` |
> | 3-6 초대 진입로 + 좌석 · 3-8 시작하기 카드 | ✅ `0233966` |
> | 3-2 예시 갤러리 · 3-9 가치 노출 | ✅ `2d1119d` |
> | 3-3 빈 상태 표준화 | ✅ `5cfa37f`·`bd29262`·`3cf59ba` |
> | §4 컴피 쓰기 툴(선택) | ⬜ 대표 판단 |
>
> **코드 작업은 전부 끝났다. 남은 것은 §5 실제 신규 워크스페이스 검증뿐.**
> **DB 마이그레이션은 이 트랙 전체에서 0건이다(실제 0건으로 끝냈다).**

---

## Context — 왜 하는가

결제 연동이 자격증명 대기로 멈춘 사이, 대표 지시로 제품 자체를 점검했다.

한 문장 진단: **기능이 부족한 게 아니라, 기능이 17개인데 실제로 쓰이는 건 5~6개이고 신규 팀의 첫 5분이 비어 있다.**

심사가 통과되면 곧 외부 고객이 들어온다. 첫 사용 경험이 가장 먼저 부딪히는 곳인데 지금은 **연쇄 실패**한다.

---

## 1. 점검 결과 (2026-08-19 · 코드 + 프로덕션 DB 실측)

### 1-1. 🔴 신규 팀의 첫날이 연쇄 실패한다

| 지점 | 실제 동작 | 근거 |
|---|---|---|
| 온보딩 문구 | *"기본 에이전트가 함께 준비됩니다"* 라고 **약속** | `OnboardingView.tsx:81` |
| 실제 | `clone_seed_agents`가 `return 0` → **에이전트 0개** | `130_clone_seed_agents_noop.sql:13-15` |
| 대시보드 | 5개 카드 **전부 빈칸** | `AnnouncementsBoard`·`TodayTasks`·`TaskSuggestions`·`WorkOverview` |
| 컴피 첫 질문 | 예시칩 3개가 **전부 조회형** → 0건이면 "없어요"만 | `lib/agentTools.ts`(툴 4개 전부 조회) |
| 워크플로우 | 에이전트가 0이라 **잠김** | `NewWorkflowModal.tsx:62` |
| 제품 투어·체크리스트 | **코드 0건** | — |

**대표가 못 느낀 이유**(DB 실측): 자사 메인 워크스페이스 `EQURIA`엔 직접 만든 에이전트 2개가 있다.
신규 고객은 0개로 시작한다. 옛 테스트 워크스페이스 2곳에만 퇴역 시드 8개가 남아 착시가 있었다.

### 1-2. ✅ 해결책의 절반이 이미 코드에 있다 (연결만 빠졌다)

| 이미 있는 것 | 위치 | 상태 |
|---|---|---|
| 직무별 에이전트 예시 **5종** | `src/lib/agentTemplates.ts` | 🔴 **import 0건 — 죽은 코드.** 주석엔 "clean-slate 후 빈 화면 대신 예시에서 시작하게 한다"고 적혀 있는데 UI 연결 누락 |
| `EmptyState`(아이콘+제목+설명+CTA) | `src/components/shared/States.tsx` | 26개 파일이 `States`를 쓰는데 `EmptyState`는 8곳만 |
| 에이전트 **자동 핀** | `AgentBuilderForm.tsx:248` | ✅ 이미 동작 |

### 1-3. 🔴 자사 워크스페이스조차 안 쓰고 있다 (DB 실측)

**0행 기능**: 전자결재 3테이블 전부 · 휴가신청 · 지출결의서 · 세금계산서 · 프로젝트 업무 ·
에이전트 지식파일 · 공지 · 회의노트 폴더 · 근태 열람위임 · HR설정 · 현금흐름 이체

**마지막 사용일**: 워크플로우 `7/16` · 회의노트 `7/22` · 캘린더 `7/30` · DM·어시스턴트 `8/11` · 에이전트 `8/14`

표본이 자사 1곳뿐이라 "기능이 나쁘다"가 아니라 **"첫 진입이 안 만들어졌다"** 로 읽는 게 맞다.

### 1-4. 🔴 컴피는 읽기만 하고 아무것도 만들지 못한다

툴이 **조회 4개뿐**(구성원·프로젝트·일정·오늘할일). 쓰기 툴 0개.
"AI가 대신 해준다"는 약속이 반쪽이고, day-0엔 조회할 게 없어 가치가 0이 된다. → §4

---

## 2. 대표 결정 (2026-08-19)

| 질문 | 결정 |
|---|---|
| 무엇부터? | **첫 사용 경험(Activation)** — 원가·커스터마이징·정리는 다음 사이클 |
| 제품 범위? | **다 유지하고 완성도만 올린다** — 기능 삭제·숨김 없음 |
| 시트 한도? | **가격은 그대로, UX만 고친다** — 결제 스키마 무변경 |

---

## 2-1. 🎯 골든패스 — "우리가 의도한 사용" (대표 결정 2026-08-19)

이게 없으면 체크리스트가 **"만들 수 있는 것들의 나열"** 이 된다. 안내·빈 상태·체크리스트는 **전부 이 3단계를 향해야** 한다.

| 순서 | 행동 | 왜 이게 핵심인가 | 완료 판정(파생) |
|---|---|---|---|
| **1** | **팀원 초대** | 혼자면 협업 도구가 의미 없다. 여기서 못 넘어가면 무조건 이탈 | `workspace_members > 1` |
| **2** | **AI로 진짜 일 한 번** (회의록 요약 **또는** 영수증 촬영) | "오, 이게 되네"가 나오는 유일한 지점. 손으로 하던 걸 30초에 대신해준다 | `meeting_notes > 0` **또는** `business_cards > 0` **또는** OCR 출처 `finance_entries > 0` |
| **3** | **우리 회사 반복 업무를 에이전트로** | 여기까지 오면 안 떠난다. 우리 제품만의 차별점 | 아래 ⚠️ 참조 |

> ⚠️ **3번 판정의 함정**: 3-1에서 시드 에이전트 3개를 자동 생성하므로 "에이전트 수 > 0"으로 판정하면
> **가입하자마자 3번이 완료로 찍힌다.** 시드는 세어선 안 된다.
> **해법(DDL 0)**: 시드 생성 시 `agents.category`(마이그103에서 자유 텍스트가 됨)에 `'시작하기'`를 넣고,
> 판정은 `category <> '시작하기'`인 에이전트가 1개 이상인지로 한다.

**무료/유료 안내 원칙 (대표 결정)**
- 골든패스 1~3은 **무료로 전부 가능**해야 한다. 처음부터 결제를 조르면 2번(가치 경험)에 도달하지 못한다.
- 업그레이드 안내는 **두 순간에만** 띄운다: ① **자리가 부족할 때**(초대하려는데 좌석 초과) ② **AI 사용량이 한도에 가까워질 때**.
- 그 외 상시 노출·배너·팝업은 만들지 않는다.

---

## 3. 남은 작업 (의존: `1→2`, `6→8`, `8→9`)

### ✅ 3-1. 시드 에이전트를 앱에서 3개 생성 (M) ⚠️ **이 트랙 최고 위험**

**DB 함수 부활은 하지 않는다.** `130_...noop.sql`의 clean-slate 사유(옛 8종은 이큐리아 전용 ·
앱 데이터를 못 읽는 "혼자 글쓰기 도우미" · 컴피가 대체)는 **지금도 유효**하다.
`seed.sql:34-42`에 'K-뷰티 번역'·'Higgsfield' 같은 특정 회사 유물이 그대로 있다.

- **어디**: `OnboardingView.tsx:49` — `create_workspace` 성공 직후
- **무엇**: `src/lib/agentTemplates.ts`에 `systemPrompt` 필드를 **추가**하고 3개 생성
  (경리 마감 · 고객 문의 1차 응대 · 회의록 요약). 8개는 컴피와 겹쳐 인지부하만 늘린다
- **재사용**: `AgentBuilderForm.tsx:210-248`의 insert 3종 세트(`agents`→`agent_versions`→`user_agent_pins`)
  — RLS 통과가 검증된 경로. 정적 프롬프트라 **가입 시점 AI 비용 0**
- **동시에**: `OnboardingView.tsx:81` 문구를 실제 동작과 일치시킨다(지금은 사실이 아니다)

> ⚠️ **위험**: 클라이언트에서 3×3=9번 insert가 일어나는데 중간 실패 시 롤백이 없다
> (에이전트는 있는데 버전이 없으면 채팅이 깨진다).
> 완화 ① 전부 best-effort로 감싸고 실패해도 `/dashboard`로 진행 ② `agent_versions` 실패 시 그 `agents` 행 즉시 삭제 ③ 리다이렉트를 막지 않는다

**실제 구현(계획과 다른 점)**: 프롬프트는 `agentTemplates.ts`가 아니라 **`src/lib/seedAgents.ts`**에 뒀다.
갤러리 템플릿은 위저드가 AI로 프롬프트를 만드는 용도라 성격이 다르고, 정적 프롬프트 3개(약 150줄)를
갤러리 파일에 섞으면 읽기 어려워진다. 이름·설명은 `AGENT_TEMPLATES`에서 id로 가져오므로 SSOT는 하나다.
완화책 3개는 전부 구현했고, 여기에 **12초 타임아웃**(응답이 없어도 온보딩이 멈추지 않게)을 더했다.
`is_public: true`로 만든다 — 나중에 초대된 팀원도 시작 키트를 볼 수 있어야 한다.

### ✅ 3-2. 에이전트 예시 갤러리 연결 (S)
`app/(app)/agents/page.tsx`의 "내 에이전트" 빈 상태를 `AGENT_TEMPLATES` 카드 그리드로 교체 →
클릭 시 `/agents/new?template=<id>`. `AgentWizard.tsx:37`이 이미 `mcpPrefill`을 받으므로
`templatePrefill`을 **같은 방식으로 추가**(기존 시그니처 무파괴).

**실제 구현(계획과 다른 점)**: "빈 상태 자리"가 아니라 **별도 섹션**으로 뺐다. 3-1 때문에 신규 오너의
"내 에이전트"는 시드 3개로 이미 차 있어서, 빈 상태에만 걸면 정작 오너에게는 갤러리가 안 보인다.
조건은 **직접 만든(= `category <> '시작하기'`) 에이전트가 0개일 때** — 골든패스 3단계 판정과 같은 식이다.
프롭 이름은 `templatePrefill`이 아니라 `templateId`(URL 파라미터를 그대로 넘긴다. 없는 id면 무시).

### ✅ 3-3. 빈 상태 표준화 (M)
신규 컴포넌트 만들지 말고 기존 `EmptyState`로 통일. 각 화면 CTA 1개씩.
대상: `AnnouncementsBoard:167` · `TodayTasks:153` · `WorkOverview:86,97` ·
**`CalendarView`(현재 안내 문구 0건)** · 근태·결재·회의·파일·명함·재무.
> 파일 3~4개씩 나눠 커밋 — 10여 파일 동시 수정은 lint 신규 에러 유입 위험.

### ✅ 3-6. 초대 진입로 + 좌석 잔여 (M)
`InviteLinksCard.tsx:120-137`이 `planOf`·`seatsFull`로 이미 좌석 게이지를 그린다.
그 블록을 `SeatMeter`로 **추출만** 하고 ① 사이드바 `CreditMeter` 아래 "팀 초대" 링크 ② 체크리스트에서 재사용.
→ 오너가 링크 발급 **전에** 잔여 좌석을 본다(지금은 초대받은 사람이 참여 화면에서 처음 알게 됨). **가격 무변경.**

### ✅ 3-8. 시작하기 카드 (M) — DDL 0
**항목은 §2-1 골든패스 3단계 그대로**(5개 나열 금지 — 3개여야 "뭘 먼저 할지"가 보인다).
완료 상태는 **전부 파생**하고(§2-1 표의 판정식), 저장하는 건 "닫기" 여부뿐 →
`localStorage("equria:onboarding-dismissed")`.
위치는 **대시보드 최상단 카드**(`DashboardLayout.tsx:65`, `AnnouncementsBoard` 위). 3개 완료 시 자동 소멸.

### ✅ 3-9. 첫날 즉시 가치 노출 (S)
골든패스 **2번**을 클릭 한 번으로 갈 수 있게 한다: "회의록 AI 요약 → `/meetings`" ·
"영수증 사진 올리기 → `/finance`" · "명함 찍기 → `/cards`".
문구는 `lib/config/features.ts:113,124,158`의 description 재사용(SSOT 중복 금지).

---

## 4. 선택 — 컴피에 **쓰기 툴** 붙이기 (L, 대표 판단 대기)

이번 점검에서 나온 **유일한 "새 기능" 제안**이다.

지금 컴피는 조회만 한다. "다음 주 월요일 10시 팀 회의 잡아줘"라고 하면 **텍스트만 답하고 실제로는 아무것도 안 생긴다.**
쓰기 툴 2개(**할 일 추가**, **일정 등록**)를 붙이면 day-0에 "이거 좋다"가 처음 발생한다.
도구 호출 인프라(`lib/agentTools.ts` + MCP 런타임)가 이미 있어 신규 구조가 필요 없다.

**단서**: 쓰기 툴은 **실행 전 확인 UX가 반드시 필요**하다(의도하지 않은 데이터 생성 방지).
그 확인 흐름 때문에 규모가 L이고 3-1~3-9와 성격이 달라 분리 판단을 권한다.

---

## 5-0. ✅ DB 실측 검증 결과 (2026-08-19 · 프로덕션 DB · 정리 완료)

브라우저 E2E는 **가입이 구글 전용**이라 신규 구글 계정이 필요하다(대표 보류 항목). 그래서 세션46과 같은 방식으로
**실제 프로덕션 DB에 검증용 워크스페이스 `검증01`을 만들어** RLS와 판정식을 직접 돌렸다.
(`set local role authenticated` + `request.jwt.claims` + `x-workspace-id` 헤더 = 앱 클라이언트와 같은 조건)

| 확인 | 결과 |
|---|---|
| `create_workspace('검증01')` | ✅ 워크스페이스+오너 멤버십+전체방 생성 |
| **시드 3종 생성**(agents→agent_versions→user_agent_pins) | ✅ **3/3/3** — 일반 authenticated 권한으로 RLS 전부 통과 |
| 시드 3개가 채팅 가능한가 | ✅ 각 1개의 `is_current` 버전 보유(=`chat/route.ts` 404 조건 아님) + 3개 모두 자동 핀 |
| **가입 직후 시작하기 카드** | ✅ **0/3** (초대 ✗ · AI ✗ · 에이전트 ✗) |
| 🔴 **시드 오판정 함정** | **`category` 제외 없이 세면 `agents=3` → 3단계가 즉시 완료로 찍힌다.** 제외식은 `0`. 실측으로 확인 |
| 2단계 전환(회의록 1건) | ✅ `step2_used_ai = true` |
| 3단계 전환(직접 만든 에이전트 1개) | ✅ `step3_built_agent = true` |
| 1단계(혼자) | ✅ `step1_invited = false` — 좌석은 Basic 3석 중 1 사용 |
| 정리 | ✅ 6개 테이블 전수 스캔 후 삭제 → **잔여 0**, 워크스페이스 목록이 검증 전과 동일 |

> ⚠️ 시뮬 중 `agent_versions` insert가 한 번 42501로 막혔는데, **앱 버그가 아니라 시뮬 방식 문제**였다.
> 3개 insert를 하나의 CTE로 묶으니 `av_insert` 정책의 `exists(select … from agents)`가 **문장 시작 시점 스냅샷**을
> 봐서 방금 넣은 agents 행을 못 봤다. 앱은 요청을 나눠 보내므로 해당 없음(순차 문장으로 재실행 → 통과).

**아직 안 한 것(브라우저 필요)**: 화면 렌더 확인(카드 레이아웃·닫기 후 localStorage·컴피 칩·"제안 받기"의
Gmail 403 부재). 로직·권한·판정은 위 표로 검증됐고, 남은 건 눈으로 보는 부분이다.

---

## 5. 검증 — 실제 신규 워크스페이스로

1. 시크릿 창 → 구글 로그인(신규 계정) → `/onboarding` → 워크스페이스 "검증01" 생성
2. `/dashboard` 즉시: 시작하기 카드 **3/3 중 0개 완료**(⚠️ 시드 에이전트 때문에 3번이 미리 ✓로 찍히면 버그), 컴피 칩 3개,
   공지·할일·진행중 카드가 전부 **EmptyState + CTA**
3. 컴피 칩 "우리 팀 첫 공지 초안 써줘" → **데이터 0건인데 결과가 나오는가**
4. 우하단 위젯 → 에이전트 3개가 **핀 조작 없이** 떠 있는가
5. "제안 받기" → 빈칸이 아니라 온보딩 제안 3개. **네트워크 탭에 Gmail 403이 없는가**
6. **사이드바**(설정 아님)에서 "팀 초대" → 좌석 게이지 → 한도 초과가 **오너 화면에서 먼저** 보이는가
7. `/calendar` → 빈 안내 + "첫 일정 추가" CTA (현재 안내 0건)
8. 시작하기 카드 3개(골든패스) 완료 후 새로고침 → 카드 자동 소멸
9. 게이트: `npx tsc --noEmit` **0** · `pnpm lint` **29/0(신규 0)** · `pnpm build` 성공
10. 검증용 워크스페이스·계정은 확인 후 삭제(`safe-changes` 관례)

---

## 6. 다음 사이클 후보 (이번 트랙 밖)

1. **원가·안전** — AI 라우트 **6곳에 예산 게이트 없음**(잔액 0에서도 무제한 호출, OCR 2곳은 단가 최고) ·
   프롬프트 캐싱 **2/20**(18곳이 회당 원가 약 90% 절감 미적용) · 레이트리밋 0건 ·
   무경계 쿼리(`select("*")` + limit 없음) 다수
2. **회사별 커스터마이징** — 비용 8분류에 **네이버스마트·컬리·쿠팡** 하드코딩(`lib/finance.ts:7,19`) ·
   결재 서식·휴가 종류가 프론트 상수(`approval/templates.ts`) ·
   에이전트 프롬프트에 **"Complow"** 박힘(`lib/agentBuilder.ts:248` — 모든 테넌트의 에이전트가 Complow를 자칭)
3. **정리** — 죽은 코드 약 1,500줄 + API 8개(MailShell 510줄·Gmail 읽기 5·Drive 2 등) · `/mail` 노출 여부 결정
4. **반쪽 채우기** — 워크플로우 툴 확장 · 에이전트 상세에서 바로 대화 · 세금계산서 실발행
