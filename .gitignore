# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env (절대 커밋 금지)
.env
.env.local
.env*.local

# supabase
/supabase/.temp

# claude code (개인 권한 설정만 제외, .claude/skills 는 공유)
/.claude/settings.local.json

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# 대표 전달용 PDF 산출물(레포 추적 제외 — 파일은 로컬 보존)
/Complow-구글연동-인증전략-소개서.pdf
/Complow-제품소개서.pdf
