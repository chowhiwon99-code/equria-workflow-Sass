# WORKLOG — 작업 로그 (최신이 위)

> 매 작업마다 **무엇을·왜·쪼갠 내용·예상이슈 체크**를 기록한다(대표 리뷰용). 현재 상태/정책은 `HANDOFF.md`.
> 작업 방식(합의): ① 단계별로 잘게 진행(한 번에 큰 배치 금지, 코드 안 꼬이게) ② 매 작업 후 예상이슈 점검 ③ 쪼개서 설명 ④ 이 파일 갱신(세션 종료 시 아래 목차에도 한 줄 추가) ⑤ 기존 디자인 유지 · 사용자 편의 최우선.
> **세션33 이하 + 세션번호 도입 전 초기 기록**은 `docs/archive/WORKLOG-초기.md`로 옮겼다(2026-09-03 분리).

## 세션 목차 (세션34~55, 최신이 위 — Cmd+F로 세션 번호 검색해 점프)

- 2026-09-04 · 세션55 — 문서 정리 + 파일용량상한 + 신규가입 E2E(구글가입 전면차단 버그 해소) + 워크플로우 기능 전면삭제
- 2026-08-27 · 세션54 — 랜딩 시안에 실제 제품 화면 3장 반영(데모 워크스페이스에서 직접 캡처)
- 2026-08-27 · 세션53 — 나이스페이 재문의 대응 + 대시보드 UI 버그 근본수정 + 랜딩페이지 리디자인 시안
- 2026-08-25 · 세션52 — 전체 코드리뷰(신규 발견 9건) + 연간상품 삭제(나이스페이 심사 대응)
- 2026-08-24 · 세션51 — 기능별 요금제 게이팅(마케팅 개시 조건 A) + 나이스페이 심사 대응
- 2026-08-22 · 세션50 — 웹훅 등록 + 조회 API 실검증
- 2026-08-22 · 세션49 — 환불·취소 완성 + AI 원가 계획 재실측(마이그142)
- 2026-08-21 · 세션48 — 매달 자동청구를 크론에 연결
- 2026-08-19 · 세션47 — 첫 사용 경험(Activation) 2차: 남은 6개 완료
- 2026-08-20 · 세션47-2 — 나이스페이 PG 규격 전면 전환
- 2026-08-19 · 세션46 — 결제 복구 경로 완성 + 프로덕션 결제 차단 버그 수정(마이그141)
- 2026-08-10 · 세션44 — 원가 레버 재조준 + 하이브리드 한도 + 크레딧 표현 정리(마이그134)
- 2026-08-08~10 · 세션43 — 프롬프트 캐싱 → AI 크레딧 → PWA(마이그131~133)
- 2026-08-07 · 세션43 — 세션42 배포
- 2026-08-01 · 세션42 — 회사별 HR(근태) 설정 + 범용 비서 '컴피'(마이그128~130)
- 2026-08-01 · 세션42 — 후속 정리(기술부채): app-ambient 중복 제거 + 죽은 컬럼 drop
- 2026-08-01 · 세션41 후속 — 회의노트 토글(접기) 블록 + 메일 상태 확인
- 2026-07-31 · 세션41 후속 — 에이전트 생성 단순화·시트 게이팅(125)·오늘할일 회사분리(126)·초대 역할 피커
- 2026-07-31 · 세션41 후속 — /code-review 12건 수정·배포
- 2026-07-30 · 세션41 후반 — 대표 실시간 dogfood 대개편(마이그122~124)
- 2026-07-30 · 세션41 — 앱 ↔ 랜딩 디자인 통일("밝은 미니멀" 리디자인, 마이그121)
- 2026-07-30 · 세션40 — 결제/가격 B3 트랙(요약)
- 2026-07-28 · 세션39 후속 — 트랙2: 대화 요약 압축(마이그117)
- 2026-07-28 · 세션39 후속 — B2 노션식 초대 개방(마이그113~116)
- 2026-07-28 · 세션39 후속 — B1-b 쓰기 격리 Phase A-1~A-4
- 2026-07-28 · 세션39 후속 — 가입=구글 전용 전환 + 오너 '구성원 초대'
- 2026-07-28 · 세션39 후속 — 구글 로그인 1차
- 2026-07-28 · 세션39 후속 — 파비콘 둥근 사각형 새 로고 교체
- 2026-07-27~28 · 세션39(이어서) — 랜딩 완성 + SEO/서치콘솔
- 2026-07-27 · 세션39 — 손익 탭 통일 + DateInput 전면 정리 + 분류 연동
- 2026-07-24 · 세션38(이어서) — UI 다이어트 + AI 출력 스타일
- 2026-07-24 · 세션38 — 트랙1: MCP 정적 OAuth 클라이언트 인프라(구글·Slack·PayPal)
- 2026-07-24 · 세션37(이어서3) — 자동기억 품질 고도화 + Exa 활성화 + MCP 연결 로드맵 분석
- 2026-07-24 · 세션37(이어서2) — 로딩바 + 기억 정리(마이그106) + 대화 히스토리 3종
- 2026-07-24 · 세션37(이어서) — 품질 루프 강화: 채팅 원클릭 기억 + 제작 되물음 인터뷰
- 2026-07-24 · 세션37 — 에이전트 자동 기억(v2) + 마이그105 적용
- 2026-07-22 · 세션36(이어서) — B1-b 착수 + 프로젝트 협업/허브
- 2026-07-22 · 세션36 — 맹점 5종 수정 배포(마이그104)
- 2026-07-16 · 세션35 — P2.2 출력형식 재설계 + 채팅 진입 하단 스크롤 + 학습/기억 기획 착수
- 2026-07-15 · 세션34 — 에이전트 시스템 재설계(리서치 기반 5단계 계획 + Phase 1)

---

## 2026-09-04 · 세션55 — 문서 정리 · 파일용량상한 · 신규가입 E2E(치명적 버그 해소) · 워크플로우 전면삭제

**무엇/왜:** 대표 지시 4갈래 — ① 전체 코드리뷰 요청으로 시작해 문서 전반 정리로 확장(중복·구식 정리,
B1-DESIGN 등 완료문서 아카이브, WORKLOG 2,108→1,269줄 분리+목차) ② 마케팅 개시 조건 B(파일 용량 상한)
착수 ③ 조건 C(제3자 신규가입 E2E) 실행 중 프로덕션 버그 발견·해소 ④ "다른 AI 서비스 대비 알맹이만
남기자"는 대표 판단으로 워크플로우 기능 전면 삭제.

- **문서 정리**(4개 커밋): README/CLAUDE/HANDOFF 사실오류 정정(마이그카운트·lint베이스라인·브랜드문구),
  `B1-DESIGN.md`·`PRODUCTIZATION.md`의 B1/B2 리스크 섹션을 `docs/archive/`로, `known-issues.md`
  가지치기(I6·I22·I31·세션36해소분 삭제), `WORKLOG.md` 세션33 이하를 `docs/archive/WORKLOG-초기.md`로
  분리 + 세션 목차 신설. `docs/build/mail.md`·`mobile-responsive.md` 실코드 재검증(README 상태 표기가
  낡아 있었음 — mail은 이미 완료, mobile은 1·3차 완료·2차만 남음).
- **랜딩 버그 수정**: Reveal.tsx 하이드레이션 불일치(useMediaQuery 훅으로 교체) · 히어로 글로우 모바일
  가로스크롤+z-index 역전 · 워크플로우 스크린샷 카드 하단 빈공간(이후 그 섹션 자체가 삭제됨) · 외부연동
  카피-스크린샷 불일치.
- **메일 작성창 서식 툴바에 글자색·정렬 추가**: `@tiptap/extension-text-style`+`extension-text-align`
  신규 설치, 프리셋 5색 스와치+정렬 3버튼. (같은 세션 후반부에 "메일은 에이전트로만 쓸 것"으로 대표
  결정 — 사이드바 비노출 확정, `/mail` 라우트는 유지.)
- **파일 용량 상한(마케팅 조건 B) 완료**: 마이그144(버킷 파일당 50MB, 6개 버킷 전부·서버측 강제) +
  마이그145~146(워크스페이스 총량 500MB, `workspace_storage_status()` RPC — premium은 무제한 예외.
  EQURIA 실사용량이 이미 156MB라 요금제 구분 없이 걸었으면 내부 워크스페이스가 막혔을 뻔함).
  RLS 시뮬레이션 3케이스(본인/타워크스페이스/premium) 전부 확인. 알려진 한계(I35~I37, LOW): storage
  경로에 workspace_id가 없어 클라단 강제, 다중워크스페이스 유저 중복집계, 명함·지식파일 미적용.
- **신규가입 E2E(마케팅 조건 C) — 치명적 버그 발견·해소**: 새 구글 계정으로 실가입 테스트 중 Supabase
  Auth **Site URL이 `http://localhost:3000`으로 방치**돼 있어 로그인 후 매번 `ERR_CONNECTION_REFUSED`로
  끊기는 걸 발견 — 가입이 구글 전용이라 **그 시점까지 신규 고객이 단 한 명도 가입 못 하는 상태**였음.
  Site URL→`https://complow.kr` + Redirect URLs에 와일드카드 추가로 해소, 재시도 성공(온보딩→워크스페이스
  생성→대시보드 완주, DB로 workspace/멤버십/시작하기 에이전트 3개 확인). 테스트 워크스페이스는 검증 후
  트랜잭션 시뮬레이션 뒤 CASCADE 삭제(계정 자체는 재사용 가능하게 보존). 마케팅 개시 조건 A~C 전부 완료,
  남은 건 D(카드사 심사, 시한 9/17)뿐.
- **워크플로우 기능 전면 삭제**: 대표 판단 "다른 AI 서비스가 발전한 만큼 실질적으로 쓰이는 것만 남기자,
  워크플로우 빼고 에이전트에 집중" → 3갈래 Explore 조사(프론트/API 코드·DB스키마+실사용량·문서/마케팅)
  후 Plan mode로 승인받아 실행. DB: `workflows`(8행)·`workflow_runs`(16행) DROP(다른 테이블 FK 참조 0건
  확인, 실사용 전부 EQURIA 자체 dogfood 또는 랜딩용 데모 데이터, 실고객 데이터 없었음). 코드: 페이지 2·
  API라우트 1·컴포넌트 5·lib 2개 삭제, `isSafeWebhookUrl`(SSRF방지)만 `lib/safeFetch.ts`로 이관(MCP
  연결 검증이 재사용 중이라 보존). 랜딩페이지 "실제화면 2—워크플로우" 섹션 삭제(3장→2장), 요금제표
  "워크플로우·MCP연동"→"MCP연동". 문서(CLAUDE·HANDOFF·known-issues I2/I10 등) 전반 정리.

**검증(전 구간 공통):** tsc 0 · lint 27/0(신규 0 — WorkflowsView.tsx 삭제로 기존 부채도 29→27로 줄어듦,
새로 고친 게 아니라 파일째 사라진 것) · build 0 · DB 변경마다 `get_advisors` 신규 이슈 없음 확인.

**커밋(main, 순서대로):** 랜딩버그수정 → 문서정리1(대분류) → docs/build 재검증 → 문서정리 마무리 →
메일 색상/정렬 기능 + 문서반영 → 메일 사이드바 비노출 확정 → 파일당 상한 → 워크스페이스 총량 상한 →
신규가입 E2E 버그 해소 기록 → 워크플로우 전면삭제(코드+DB+문서, 이 세션 마지막 커밋).

## 2026-08-27 · 세션54 — 랜딩 시안에 실제 제품 화면 3장 반영(데모 워크스페이스에서 직접 캡처)

**무엇/왜:** 세션53 끝에 대표가 "시안이 손그림 목업뿐이라 마케팅에 의미 없다"고 재평가 →
AI 에이전트 도구 호출·컴피·워크플로우 캔버스를 **실제로 캡처한 스크린샷**으로 시안을 보강.

- **데모 워크스페이스 신규 생성**(대표 확인 후 진행 — 기존 EQURIA/이큐리아2/테스트는 실제 회사
  데이터라 그대로 찍으면 마케팅 페이지에 노출됨). 계정 `데모매니저`(admin API로 생성 — 가입이
  구글 전용으로 바뀌어 있어 기존 이름·비번 폼은 죽은 코드; 로그인 폴백만 살아있음을 확인),
  워크스페이스 `샘플컴퍼니`(온보딩 RPC로 생성, 시작 에이전트 3개 자동 시드).
  워크플로우·MCP 연결이 Pro 이상 게이트라 `workspaces.plan='pro'`로, Context7 커넥터 연결이
  관리자 전용이라 `set_member_role` RPC로 `profiles.role='admin'` 부여(둘 다 이 데모
  워크스페이스에만 스코프 — 실제 고객 플랜에 영향 없음).
- **캡처 3장**(Playwright 임시 설치 — 세션53과 동일 검증된 방법):
  ① 고객 응대 도우미에 Context7(무인증 MCP) 연결 후 실제 대화로 `resolve-library-id`→
     `query-docs` 2단계 도구 호출을 라이브로 유도(Stripe 문서 조회) — 도구 사용 칩이 메시지
     parts에만 존재하고 DB에는 최종 텍스트만 저장됨을 먼저 코드로 확인했기 때문에, 히스토리
     재생이 아니라 스트리밍 중 캡처가 유일한 방법이었음. ⚠️ 원래 예시(메일→캘린더→회신)는
     Gmail·Calendar가 실제 구글 OAuth 계정을 요구해 데모 계정으론 재현 불가 — 대체재로
     무인증 커넥터(Context7) 기반 실제 2단계 호출을 캡처(가짜보다 진짜가 우선이라는 판단).
  ② 대시보드 컴피에 실제 회의 공지 초안 요청 → 진짜 생성된 답변 캡처.
  ③ 워크플로우 캔버스 — 실제 UI(단계추가)로 에이전트 3개(고객응대→회의록→경리마감)를
     연결해 저장 후 캡처(첫 시도는 "저장" 안 누르고 브라우저 닫아 유실 — 재현 후 성공).
  3장 모두 PIL로 팔레트 양자화(32~40색)해 각 20~57KB로 압축(원본 캡처는 200~280KB).
- **시안 반영**: `design` 스킬로 기존 캔버스(`3395df82-...`)를 `--extract`해 작업 파일 복원 →
  AI 섹션에 새 좌우교차 블록 2개 추가(컴피/워크플로우, 기존 `.split`/`.app-card` 클래스 그대로
  재사용 — 새 CSS 없음) + 기존 "외부 도구 연동" 블록의 손그림 mock을 에이전트 도구호출
  스크린샷으로 교체. 정렬·타이포·스크롤리빌은 손대지 않음(대표 지시대로 콘텐츠만 변경).
  같은 URL로 재게시 완료(새 캔버스 만들지 않음).

### 검증
데스크톱(1600px)·모바일(420px) 두 폭에서 Playwright로 새 블록 3곳 전부 스크린샷 확인 —
`.split.rev` 모바일 미디어쿼리가 기존 블록과 동일하게 자동 적용되는 것 확인. 코드(`LandingPage.tsx`)는
**아직 안 건드림** — 대표 지시대로 콘텐츠 확정 후 다음 단계에서 반영 예정.

### 다음 세션 참고
- 데모 워크스페이스(`샘플컴퍼니`, workspace id `edb928a6-...`)와 데모 계정은 그대로 남겨둠 —
  필요 없으면 대표 판단으로 정리(삭제 스크립트는 아직 안 만듦).
- Playwright/Chromium은 스크래치패드에 남아있음(세션53처럼 작업 완전히 끝나면 정리 대상).
- 발견한 기존 버그(이번 작업 범위 밖, 손대지 않음): `mcp_servers.name`에 워크스페이스
  스코프 없는 전역 UNIQUE 제약 — 다른 회사가 먼저 "Context7"이란 이름으로 커넥터를 연결해두면
  같은 이름으로 못 만든다(지금은 다른 이름을 쓰는 우회로 해결).

### (같은 세션, 날짜 넘어감 2026-08-28) 추가 작업 2건

**1) 대시보드 컴피 사이드바 리사이즈 — 드래그 되돌림(스냅백) 느낌 추가 (실제 코드, `DashboardAssistant.tsx`)**
대표 지적: "드래그를 다시 올리면 되돌아가는 느낌 넣어줘." 어느 드래그인지 애매해서(랜딩 목업/워크플로우
노드/사이드바 리사이즈 3개 후보) AskUserQuestion으로 확인 → 대시보드 AI채팅 사이드바 폭 리사이즈(세션41
기능)가 맞았다. 경계(160~420px) 밖으로 끌면 초과분의 1/4만 반영하는 고무줄 저항(`elasticSidebar`)을
추가하고, 손을 떼는 순간(`onUp`)엔 `sidebarResizing`을 먼저 꺼서 다음 렌더가 220ms
`cubic-bezier(0.34,1.56,0.64,1)` 트랜지션을 타게 해 clamp 값으로 튕겨 돌아오게 했다(드래그 중엔
`transition:none`이라 끌 때는 그대로 1:1 추종). **검증**: `npx tsc --noEmit` 0에러 ·
`pnpm lint` 29 errors/0 warnings, 신규 0 — 대표가 요청한 "30/0" 기준은 구버전 수치였고
`known-issues.md`(2026-08-19 실측)에 이미 **29/0으로 정정**돼 있어 그 기준과 정확히 일치함을 확인.
`DashboardAssistant.tsx`에 있던 기존 lint 에러 3건(26·91·106행)은 이번 변경 전부터 있던 베이스라인
부채로, 내가 건드리지 않은 부분. 아직 배포 전(`/deploy` 대기, 코드는 로컬 완료).

**2) 랜딩 시안에 "이런 분들에게 추천드려요" 타겟 섹션 신설 (같은 캔버스, 코드는 여전히 미착수)**
대표: "우리 타겟 기억해?" → Explore 서브에이전트로 HANDOFF·CLAUDE.md·WORKLOG·메모리·git 로그
전수 조사(결과: HANDOFF.md:404-410의 2026-08-13 대표 진술이 유일하고 현재도 유효한 타겟 정의 —
"기업이 아니라 소규모 팀·스타트업·대학생 수준 조직", 판단기준 "5~10명 팀에게 필요한가". CLAUDE.md의
K-뷰티/이큐리아 내부용 문구는 본문 자체에 "구버전 원본 기록"이라고 이미 명시돼 있어 최신이 아님을 확인).
Plan 모드로 전환해 AskUserQuestion 2개로 확정: ① 페르소나는 역할이 아니라 **규모·조직형태 기준**(대표
확정 — "대학생 수준"은 문자 그대로 타겟명이 아니라 규모감 기준이라 카피에 "대학생"을 직접 쓰지 않음)
② 배치는 **요금제 섹션 바로 위**(가격 보기 전에 "왜 이 정원인지" 먼저 납득). 계획 승인 후 `Main.dc.html`에
보안 섹션과 같은 왼쪽정렬+`icon-grid-3` 골격(신규 CSS 없음)으로 3장 추가: IT 담당자 없는 5~10인 팀 /
여러 SaaS 구독료가 부담스러운 초기 스타트업 / AI는 도입하고 싶은데 뭐부터 할지 모르는 조직. 세 번째 카드
아이콘은 세션54 앞부분에서 만든 "컴피" 카드의 스파클 아이콘을 그대로 재사용, 나머지 둘은 페이지 기존
스타일(스트로크 SVG, 이모지 금지)에 맞춰 새로 그림. 데스크톱(1600px)·모바일(420px) 스크린샷으로
`icon-grid-3` 모바일 2열 래핑까지 확인 후 같은 URL로 재게시.

**곧바로 후속 수정 — "IT 담당자 없는 5~10인 팀" 카드를 더 본질적인 페르소나로 교체.** 대표가
채팅으로 페르소나 브레인스토밍을 이어가며 "조직적으로 AI를 쓰고 싶은데 안 되는 작은 조직·구성원을
같이 관리하면서 AI를 쓰고 싶은 사람들"을 제시 — 이게 HANDOFF 미션 문장("작은 조직이 AI를
프로페셔널하게·조직적으로 다루게 하는 것")을 거의 그대로 옮긴 페르소나라 첫 카드 자리를 교체하기로
합의(대표: "ㄱㄱㄱ"). 새 카드: "직원 각자 AI를 따로 쓰는 대신, 팀 전체가 하나로 씁니다" — "구성원마다
ChatGPT를 따로 켜놓는 대신 회사 지식을 아는 같은 에이전트를 다 같이 쓰고, 사용량도 회사 차원에서
보인다"는 설명으로 실제 기능(구성원 관리·워크스페이스 공용 에이전트·크레딧 사용량)과 바로 연결.
아이콘은 작은 원 4개가 가운데 원 하나로 모이는 새 SVG(각자 흩어진 사용이 하나로 합쳐짐을 시각화,
페이지의 스트로크 아이콘 스타일 그대로). 데스크톱 확인 후 같은 URL로 재게시.

### (같은 세션 계속, 날짜 넘어감 2026-08-31~09-01) 사이드바 스냅백 배포 → 랜딩 캔버스 전체를 실코드로 이식·배포까지

**배포 전 확인 — "배포함? 심사에 문제 없나?"**: 확인해보니 이 시점까지 아무것도 배포 안 됨
(사이드바 스냅백은 로컬 미커밋, 랜딩은 캔버스에만 있었음). HANDOFF 확인 결과 나이스페이
카드사 승인심사가 진행 중(2026-09-17 시한)이고 포스타트가 실제로 **홈페이지 내용을 직접
심사해서 미비사항을 지적한 전례**가 있어 대표에게 알림(연간상품 삭제 요구·"충전·크레딧·포인트"
금지 문구 등). 사이드바 스냅백은 로그인 뒤 대시보드 화면이라 PG 심사(홈페이지)와는 무관하다고
판단 근거까지 설명.

**1차 배포 — 사이드바 스냅백만**: 대표 "배포 할래?" → `/deploy` 절차대로 진행.
tsc 0 · lint 29/0(신규 0 — 대표가 요청한 "30/0"은 구버전 수치, `known-issues.md`
2026-08-19 실측이 29/0으로 이미 정정돼 있음을 확인 후 보고) · build 통과 → 커밋 분리(코드/문서)
→ `main` push → Vercel READY 확인(`dpl_DgGRM6d…`) → 스모크(`/` 200 · `/dashboard` 307) →
HANDOFF 프로덕션 SHA `b2d55ab`(롤백후보 `f48d813`)로 갱신 → 그 갱신 자체를 기록하는 후속
커밋(`e58fe2d`)도 배포.

**타겟 카드 페르소나 브레인스토밍 2라운드**: 대표 "타겟 카드 마저 다듬자" → 카드2·3에 대한
추가 아이디어 요청("아이디어 더") → 규모/단계·AI 이해도·업무 페인포인트·"조직 단위로 AI
써보고 싶은 팀" 등 여러 축으로 후보를 제시(HANDOFF §트랙3 "남은 것"에 후보 목록 기록,
아직 미확정 — 카드1만 앞서 확정해 반영됨).

**최종 반영 — 캔버스 전체를 처음으로 `LandingPage.tsx`에 이식 + 배포**: 대표 "일단 디자인부터
배포 가능함?" → 이 시점까지 캔버스의 어떤 것도 코드에 반영된 적이 없었음(정렬·타이포·스크롤리빌·
스크린샷 3장·타겟 섹션 전부 캔버스에만 존재)을 설명 → 대표 "ㅇㅇ 일단 진행하고 나중에 수정".

- 기존 `LandingPage.tsx`(549줄)를 전체 읽고 구조 파악 — `FEATURES`/`AI_BLOCKS`/`SECURITY`/
  `PLANS`(lib/plans.ts SSOT 파생)/`PLAN_ROWS`/`FAQS` 데이터 배열 + 4개 심화 split 블록이
  이미 캔버스와 거의 1:1 대응돼 있어(같은 좌우교차·아이콘그리드 패턴) 전면 재작성이 아니라
  타겟 패치로 접근 가능하다고 판단.
- 새 `src/components/landing/Reveal.tsx` — IntersectionObserver 기반 스크롤 리빌
  컴포넌트(`prefers-reduced-motion` 대응, reduced-motion 여부를 useState 지연 초기값으로
  계산해 effect 안 동기 setState를 피함 — 처음 구현 땐 이 패턴 때문에 lint 29→30으로
  올라갔다가 수정).
- 히어로에 배경 글로우 + 미니 바차트(7개 막대) + 목업 3중 레이어 그림자 추가.
- AI·기능·보안 3개 섹션 제목 가운데→왼쪽 정렬(`text-center`/`mx-auto` 제거).
- AI 섹션에 새 좌우교차 블록 2개(컴피·워크플로우, 실제 스크린샷), 기능 섹션 "외부 도구 연동"
  블록 mock을 에이전트 도구호출 스크린샷으로 교체, 보안 섹션 바로 뒤·요금제 바로 앞에
  "이런 분들에게 추천드려요" 타겟 섹션 신설 — 전부 `Reveal`로 감싸 스크롤 리빌 적용.
- 스크린샷 3장을 원본 캡처에서 다시 고화질로 크롭·리사이즈(팔레트 양자화 안 함, 캔버스용
  20~57KB 압축본과 달리 실코드엔 155~314KB 원본 품질 사용 — Next/Image가 서빙 시 최적화)해
  `public/marketing/{compi-panel,agent-tool-chain,workflow-canvas}.png`로 저장.
- 검증: tsc 0 · lint 29/0(신규 0) · build 통과. Playwright로 로컬 dev 서버를 데스크톱(1440px)·
  모바일(390px) 두 폭 모두 전체 페이지 스크린샷 확인(청크로 잘라서 새 섹션·이미지·정렬·모바일
  스택 하나하나 검사) — 스크롤 위치를 wheel count로 어림잡다가 새로 늘어난 페이지 길이 때문에
  한 번은 푸터까지 건너뛰어 스크린샷을 다시 찍음.
- 커밋 분리(코드/에셋 vs 문서) → `main` push → Vercel READY 확인(`dpl_J2b7iG8…`, 빌드
  1분) → `/marketing/*.png` 3장 200 확인 → HANDOFF 프로덕션 SHA `536609a`(롤백후보
  `e58fe2d`)로 갱신, 트랙3을 "시안 확정 중" 상태에서 "코드 반영·배포 완료"로 압축 정리
  (이 파일 자체 "과거 세션 블록을 쌓지 말 것" 규칙에 맞춤) → 커밋(`866881c`) 배포.

**세션 정리 페이지**: 대표가 "우리 뭐하고 있었는지 까먹었어"라고 해서, 이번 세션 전체
타임라인 + 실제 스크린샷 3장 + 남은 일 A/B/C를 담은 별도 정리 Artifact를 신규 발행
(`9f857ec4-59cd-46cf-b18e-f638ec914a90`, 캔버스와 다른 별개 문서 — 시안 캔버스 아님).
다음부터 진행 상황은 이 방식(타임라인+스크린샷+한 것/남은 일 구분)으로 정리하기로 함.

### 다음 세션 시작할 것
1. 타겟 섹션 카드 2·3 문구 확정(HANDOFF §트랙3에 브레인스토밍 후보 정리돼 있음).
2. 데모 워크스페이스 `샘플컴퍼니` 정리(삭제) 여부 결정.
3. Playwright/Chromium은 스크래치패드에 남아있음 — 완전히 끝나면 정리 대상(세션53 관행).

---

## 2026-08-27 · 세션53 — 나이스페이 재문의 대응 + 대시보드 UI 버그 근본수정 + 랜딩페이지 리디자인 시안

**무엇/왜:** ① 08-26 나이스페이 재회신(심사계정 미도달·가격조정 폭 문의) 대응 + 대표 요청으로 테스트
구독(9/21 예정 자동청구) 해지. ② 대표가 스크린샷으로 제보한 대시보드 카드 겹침·결제화면 미비 3건을
Chrome 확장 미연결 한계를 Playwright 임시 설치로 우회해 **직접 재현·수정·재검증**까지 완료.

### 1) 나이스페이 대응
- `billing_request_cancel` RPC로 테스트 워크스페이스(`42f70fa1-…`) 구독 정식 해지 —
  `auto_renew=false`·`next_charge_at=null`·`cancel_effective_at=2026-09-20`. 9/21 청구 취소됨.
- `nicepayreview` 계정 비밀번호 재발급(담당자가 기존 발급분 미도달 문의) + `signInWithPassword` API
  직접 호출로 실제 로그인·세션발급 검증(브라우저 클릭 재현은 Chrome 확장 미연결로 여전히 불가).
- 회신 메일 초안: 계정 재발급 통보·가격조정 폭(통상 10~20%, 구체계획 없음)·실사업자전환 문구 재질의.

### 2) UI 버그 3건 — 대표 제보 → Playwright로 직접 검증
대표가 결제/가격 화면과 대시보드 스크린샷 여러 장으로 문제 제기. Chrome 확장이 이 세션 내내
연결 안 돼 코드 근거로만 수정해왔는데, 프로덕션에서도 재발 신고가 들어와 **Playwright(headless
Chromium)를 스크래치패드에 임시 설치**해 실제 로그인·렌더까지 직접 확인하는 방식으로 전환.

- **가격 취소선(오판정 방지 사례)**: `nicepayreview` 계정으로 로그인해 `/billing` 스크린샷 +
  `getComputedStyle` 확인 결과 `textDecoration: none` — 앱 코드엔 원인이 없었다. 확장 없는 깨끗한
  브라우저 스크린샷에도 취소선이 없어 **사용자 브라우저 확장(쿠폰/가격비교류) 문제로 최종 판정**,
  코드 수정 안 함. (증거 없이 "코드엔 없다"로 끝내지 않고 스크린샷으로 직접 반증했다는 점이 핵심.)
- **대시보드 카드 겹침(진짜 버그, 2단계로 근본수정)**: 1차 시도(`overflow-y-auto`를 좌측 컬럼
  전체에 추가)만으론 부족했음 — Playwright로 AI채팅 폭을 드래그해 실제 재현한 결과, 상단 그리드가
  `flex-1`로 세로공간이 눌리는데 카드(Surface) 자체엔 넘친 내용을 잘라내는 처리가 없어 폭이 좁아지면
  내용이 카드 밖으로 흘러넘쳐 옆 카드와 겹쳤다. `TodayTasks`·`TaskSuggestions`의 Surface에
  `overflow-y-auto` 직접 추가 + 그리드에 `min-h-[260px]`(최소한 제목+입력줄은 보이게) 추가.
  **수정 후 같은 재현 스크립트를 다시 돌려 겹침이 사라진 것까지 스크린샷으로 확인**(로컬 →
  배포 후 프로덕션 complow.kr에서도 재확인).
- **"현재 요금제" 미표시**: `/billing` 페이지가 워크스페이스를 "가장 오래된 멤버십"으로 판단하던
  버그(결제 라우트에서 이미 한 번 사고 났던 것과 같은 패턴, 이 화면만 안 옮겨져 있었음) 수정 +
  Basic(무료) 요금제 사용자에게도 "지금 Basic 요금제를 쓰고 있어요" 명시 + 사이드바 하단
  `PlanBadge` 상시 표시 추가 + 결제화면 접근 CTA가 `/#pricing`(마케팅 랜딩)이 아니라 `/billing`으로
  가도록 2곳(PlanGate·CreditMeter) 수정 + 설정 > 결제 카드에 "요금제 변경" 버튼 추가(지금까지
  해지 말고는 할 게 없었음 — `billing_apply_payment` upsert가 이미 안전하게 처리함을 코드로 확인 후 추가).

### 검증
tsc 0 · lint 29/0(신규 0) · build 통과(매 커밋마다). Playwright 스크립트로 로컬 dev(41일째 켜져
있던 stale 서버 재시작 후) → 배포 후 프로덕션 순으로 동일 시나리오 재현해 실제 렌더 확인. 임시 설치한
Playwright/Chromium은 작업 종료 후 정리(프로젝트 의존성에 남기지 않음).

### 3) 배포 누락 발견 + 마저 배포 + 아이콘 근본수정
`6157704` 배포 후 대표가 여전히 깨진다고 재제보 — 확인해보니 §2의 세 번째 항목(요금제 오판정·Basic
미표시·요금제 변경 버튼)이 **로컬 커밋만 되고 push가 안 된 채로 방치**돼 있었다(다음 작업으로 바로
넘어가며 누락 — [[working-style]]에 2026-07-07 지적된 것과 같은 실수의 재발, 메모리 갱신함).
`88b996c`로 마저 커밋·배포. 그래도 카드 겹침이 재발하자 대표가 "그냥 아이콘 지우고 깔끔하게 가자"
지시 → `TodayTasks`·`WorkOverview`의 `EmptyState` 장식 아이콘(size-8+py-16, 카드 최소높이를
계속 키우던 원인) 3곳 제거(`2755c3b`). 반복 재발 UI 버그는 CSS 미세조정보다 장식 요소 제거를
우선한다는 판단 기준을 메모리에 신규 기록([[ui-bug-persistent-simplify]]).

### 4) 회신 발송 + 통과 여부 안내
계정 재발급(비밀번호 재검증 완료)·가격조정 답변(10~20% 이내, 구체계획 없음)·실사업자전환 재질의를
담은 회신 메일 발송 완료(대표 발송). 심사 통과 여부는 확답 불가 — 담당자가 이미 2회 왕복(연간상품
삭제 요구 → 계정미도달·가격질문)했고 이번 회신의 재질의에도 아직 답이 없어 추가 왕복 가능성 있음을
그대로 전달([[verify-before-asserting-external-domains]] 원칙대로 통과를 단정하지 않음).

### 배포
main 직접 커밋 6개(`b4fe158`·`47abf9b`·`6157704`·`88b996c`·`2755c3b` + docs 3개) → push → Vercel
READY(최종 `dpl_EiTLPcj…`, `complow.kr` 별칭 연결) → 프로덕션에서 Playwright로 최종 재검증.

### 5) 랜딩페이지 리디자인 시안 (코드 미착수 — Claude Design 캔버스로만)
대표 요청: 정렬 정리·"더 있어보이게"·스크롤 리빌 모션·좌우교차 이미지블록 강화. "design 에이전트 쓰셈"
지시에 따라 `/design` 스킬로 실제 프로덕션 콘텐츠(`LandingPage.tsx` 549줄 — 카피·숫자·요금제·FAQ 전부
그대로) 기반 시안을 별도 Artifact로 제작. **`LandingPage.tsx`는 이번 세션에 한 줄도 안 건드렸다** —
방향을 먼저 시안으로 확정한 뒤 실제 코드 이식은 다음 단계.

시안 링크: https://claude.ai/code/artifact/3395df82-a5e2-4d1e-a2ad-a2f3ccd09c1a

반복 개정(대표 피드백 순서대로): ① 초안 게시 후 백그라운드 검증에서 FAQ 2~6번 답변 누락 발견·수정
(6개 전부 실제 클릭되는 네이티브 `<details>`로) ② 미니 UI 카드가 "작게 떠 있다" 지적 → 옆 텍스트 칸
높이만큼 꽉 채우고 콘텐츠 보강(막대그래프·대화턴·연동활동 추가) ③ 가운데 정렬 아쉬움 → AI·기능·보안
3개 섹션 제목을 왼쪽 정렬로 통일(그리드와 정렬 체계 일치) ④ "색상·글씨가 작다" → 마케팅 카피 전반
폰트 크기·명암비 상향(목업 카드 안쪽 미니 UI 라벨은 실제 앱처럼 보이려고 의도적으로 작게 유지).
스크롤 리빌은 실제 IntersectionObserver로 구현해 시안 안에서도 진짜로 작동함(정적 목업이 아님).
Pretendard 대신 시스템 폰트 사용(캔버스가 구글 폰트만 허용 — 실제 코드 이식 땐 원래 폰트 그대로).

**세션 끝 대표 재평가**: 정렬·타이포는 됐는데 "마케팅하기엔 의미 없는 랜딩" — 손그림 CSS 목업뿐이라
컴플로우의 실제 차별점(꼬리물기·컴피·워크플로우)이 안 보인다는 지적. 다음 세션 우선순위를
"코드 이식"에서 "실제 제품 화면 캡처로 콘텐츠 교체"로 변경(§HANDOFF 트랙3에 상세·프롬프트 기록).

### 다음 세션 이월
개인 MCP 커넥터 Pro게이트 우회 재확인 · 다운그레이드 후 지속사용 · 경량 묶음(§HANDOFF 참고) ·
설정 > "요금제 변경" 버튼 실사용 확인(유료 워크스페이스 계정으로, 테스트 계정은 해지돼 확인 못함) ·
나이스페이 회신 대기 · **랜딩페이지 — 꼬리물기/컴피/워크플로우 실제 화면으로 시안 보강 후
`LandingPage.tsx`에 실제 반영**(§HANDOFF 트랙3, 우선순위 최상단).

---

## 2026-08-25 · 세션52 — 전체 코드리뷰(신규 발견 9건) + 연간상품 삭제(나이스페이 심사 대응)

**무엇/왜:** ① 세션43~52(결제 연동·기능게이팅) 구간이 `known-issues.md` 커버리지 밖이라 4개 에이전트
병렬 리뷰(결제·플랜게이팅·API보안·채팅코어) 진행 — CRITICAL 1건(웹훅이 tid↔order_id 결속을 검증 안 해
위조 웹훅으로 유료 고객 강등 가능) 포함 신규 9건 발견 → 그중 3건(웹훅 위조·SSRF+IDOR·응답소실)을
같은 세션에 수정·적대검증·배포까지 완료(아래 §BCD 수정). ② 나이스페이가 "서비스 제공기간 3개월
초과 상품 판매불가"로 연간 결제 삭제 요구 → 대표 결정(삭제) → 즉시 반영.

### 배포한 것 — 연간 결제 삭제
- 랜딩(`LandingPage.tsx`) 프로모션 문구, 결제화면(`CheckoutView.tsx`) 월/연 토글 제거.
- `api/billing/checkout/route.ts`: `body.cycle`을 무시하고 항상 `"monthly"`로 고정 — **UI만 지우면
  크래프팅된 요청(`cycle:"yearly"`)으로 여전히 연간 결제가 통과하던 서버측 구멍**을 리뷰에서 발견해 같이 막음.
- DB 제약조건·`refund.ts`(연간 환불 계산)·`renew.ts`(연간 자동갱신)·약관 문구는 안 건드림 — 기존 연간
  가입자가 없어(유일한 실결제 고객=월간, 청구액 29,000원으로 확인) 위험 없고, 되돌리기 쉽게.
- 검증: `tsc` 0 · `pnpm lint` 29/0(신규 0) · `pnpm build` 통과 · 커밋 `d044e9a` → main push →
  Vercel READY(`dpl_C9e2RB6…`) → 프로덕션 curl로 "연간 결제" 문구 0건 확인.

### ✅ 배포한 것 — BCD 수정 (같은 세션, 커밋 `a018842`·`b44e3f7`·`9c3bd97`)

**B — 결제 웹훅 위조 취약점(CRITICAL, 수정 완료)**. `provider.ts`의 `InquiryResult`에 `orderId` 필드
추가(나이스페이 공식 매뉴얼 `nicepay-manual/api/status-transaction.md`로 조회 응답 필드명 확인 —
`orderId`·camelCase·필수). `nicepay.ts` `readInquiry()`가 파싱하고, `webhook/route.ts`의 취소·정산
두 분기 모두 RPC 호출 전에 `inq.orderId === moid` 대조. **3개 에이전트 적대검증(반증 시도) 3/3 통과**
— 핵심 공격 시나리오(자기 자신의 무관한 tid로 남의 order_id 지정한 위조 통보)는 전부 막힘.
검증 중 나온 보완 2가지도 반영: ① `orderId`가 null(응답 필드 누락)이면 "불일치"가 아니라 "모른다"로
보고 `retryLater`(첫 결제 `CPL-` 주문은 이 웹훅이 유일한 복구경로라 잘못 거부하면 승인은 됐는데 서비스가
영영 안 열릴 수 있었음). ② 불일치 감사로그에서 공격자 자신의 원본 조회응답(PII 가능)을 통째로 남기지
않고 식별용 최소 필드만 기록(피해자 워크스페이스 로그로 공격자 정보가 섞이는 것 방지).

**C — SSRF + IDOR(수정 완료)**. `lib/mcp/connect.ts`: MCP 서버 연결이 문자열 정규식(`isSafeWebhookUrl`)
만으로 가드돼 공개 도메인이 DNS로 사설/내부 IP(클라우드 메타데이터 등)로 풀리는 리바인딩을 못 막았음 —
`safeFetch.ts`의 `assertPublicHost`(실제 DNS 조회로 공인 IP 확인)를 export해 연결 직전에 한 번 더 검증.
`api/finance/tax-invoice/route.ts`: `finance_entries`를 `workspace_id` 필터 없이 id로만 조회해 RLS(내가
속한 워크스페이스 전부 허용)에만 의존 — 여러 회사에 속한 사용자가 A사 재무 항목으로 B사 세금계산서를
만들 수 있었음. 활성 워크스페이스로 명시 스코프.

**D — 응답소실(수정 완료)**. `api/assistant/route.ts`·`api/workflows/[id]/run/route.ts` 둘 다 멀티스텝
도구호출로 턴이 끝나면 AI SDK의 `text`(마지막 스텝만)가 빈 문자열이 되어 응답이 통째로 유실/체인이
조용히 깨지는 버그 — `agents/[id]/chat` 라우트가 2026-08-14에 이미 겪고 고친 것과 동일 클래스인데
그때 이 두 라우트엔 안 옮겨졌었음. 같은 패턴(`steps` 전체 텍스트 이어붙임) 적용.

검증(BCD 공통): `tsc` 0 · `pnpm lint` 29/0(신규 0) · `pnpm build` 통과 · C·D는 재검토 에이전트가
"correct" 판정. 배포: main push → Vercel READY(`dpl_6cM1vbp…`) → 프로덕션 스모크 정상.

### 아직 안 한 것 (다음 세션 후보)
4. `mcp_user_connections`(개인 MCP 커넥터)가 Pro 게이트를 API 직접호출로 완전 우회 가능 — 마이그143에서
   "애매해 제외"했던 원래 결정이 이 사실을 몰랐던 상태였음, 재확인 필요.
5. 다운그레이드 후에도 기존 워크플로우 실행·MCP 연결 지속 사용 가능(마이그143 트리거가 INSERT만 막음).
6. 경량: 예산체크 누락 2곳(`generate-prompt`·`chat/assist`) · cron secret 비timing-safe 비교 ·
   OAuth state 세션 미결속 · `renew.ts` 비-NETWORK 실패코드 조회미검증 · Gmail 첨부 헤더 클라값 신뢰.

### 🔴 신규 미확인 — 나이스페이 "바로오픈" 안내
같은 심사 메일에 "정기결제는 실사업자전환일부터 사용 가능"이라는 문구가 있음. 실사업자 전환은 심사
**완료 후**인데, 이미 08-20에 빌키 실결제 1건이 성공했고 **09-21에 29,000원 자동청구가 예정**돼 있다 —
이 문구가 그 청구에 영향이 있는지 확인 안 됨. 회신 메일에 반드시 같이 물어볼 것(추측으로 단정 금지).

### 다음 세션 착수 후보
A. **담당자 회신 메일 발송**(대표 액션, 초안 있음 — 연간삭제 완료·재심사 여부·2차인증 답변·바로오픈
문구 질문 4개 포함) 및 회신 확인 후 카드사 심사 접수 확인 — 시한 9/17 임박, 최우선.
B. 위 §아직 안 한 것 4(개인 MCP 커넥터 Pro 게이트 우회) — 대표 재확인 필요한 안건.
C. 위 §아직 안 한 것 5(다운그레이드 후 지속사용) 수정.
D. 위 §아직 안 한 것 6(경량 묶음) — 급하지 않음.

---

## 2026-08-24 · 세션51 — 기능별 요금제 게이팅 (마케팅 개시 조건 A) + 나이스페이 심사 대응

**무엇/왜:** 랜딩 요금표는 전자결재·근태·회의노트·명함·비용매출·워크플로우·MCP를 유료로 팔고 있었는데
막는 코드가 한 곳도 없었다(HANDOFF 세션50이 코드 전수 확인으로 남긴 과제). 서버 강제 없는 게이팅은
게이팅이 아니라는 원칙으로, 화면 대신 DB 트리거를 1차 강제 지점으로 설계했다.

### 1) 설계 확인 — 대표에게 3가지 질문 후 진행
- 그랜드파더: **안 함**(테스트·이큐리아2 모두 대상 테이블 0건이라 소급 충돌 없음을 먼저 확인하고 물음)
- 사이드바: **그대로 노출, 클릭 시 업그레이드 카드**(숨기지 않음 — 전환 유도)
- `mcp_user_connections`(직원 개인 커넥터): **이번엔 제외**(workspace_id 컬럼이 없어 기준이 애매함)

### 2) 구현
- 마이그143: `plan_rank()` + 공용 트리거 함수 `enforce_plan_feature_gate()`(tg_argv로 최소플랜·문구 전달) +
  16개 테이블에 `BEFORE INSERT` 트리거. `agent_version_enforce_model_plan`(134)·`plan_seat_limit`(125)과
  같은 패턴 — 이 테이블들은 API 라우트 없이 클라이언트가 직접 insert해서 화면 가드만으론 우회된다.
- `lib/plans.ts`: `planRank`·`meetsMinPlan` 추가(화면 안내용, SSOT는 여전히 트리거).
- `lib/config/features.ts`: `Feature.minPlan` 필드 추가, 대상 7개 라우트에 값 부여.
- `components/shared/PlanGate.tsx` + `hooks/usePlanGate.ts`: `GuestGuard`와 같은 방식(현재 경로 ↔
  `FEATURES`)으로 미달 시 `InviteLinksCard` 시트 게이팅과 같은 톤의 업그레이드 카드로 대체.
  9개 page.tsx(목록+상세)에 씌움 — View 컴포넌트 내부는 무변경.

### 3) 검증
- DB: 적용 전 3개 워크스페이스 전부 대상 테이블 조회로 그랜드파더 불필요 확인 → 적용 →
  `begin...rollback` 트랜잭션 시뮬레이션으로 테스트(standard)가 워크플로우는 막히고 회의노트는
  통과하는지, EQURIA(premium)는 워크플로우까지 통과하는지 확인 후 롤백(잔여 데이터 없음).
  `get_advisors`로 새 보안 경고 없음 확인.
- 앱: `tsc` 0 · `pnpm lint` 29/0(신규 0) · `next build` 통과.
- ⚠️ **브라우저 실사용 확인은 못 함** — Claude-in-Chrome 확장이 이 세션에 연결되지 않아 로그인 후
  업그레이드 카드가 실제로 뜨는지는 눈으로 못 봤다. 대신 미인증 상태 curl 스모크(로컬+프로덕션)로
  라우트가 500 없이 정상 리다이렉트되는 것만 확인.

### 4) 배포
main 직접 커밋(`be2a6e7`) → push → Vercel 프로덕션 READY 확인(`dpl_k2b7F2gf…`, `complow.kr` 별칭 연결) →
프로덕션 curl 스모크(`/` 200 · 게이팅 라우트 5개 전부 미인증 307). 롤백 후보 `aa753fb`.

### 5) 나이스페이 포스타트 회신 — 2건 중 1건만 처리
담당자가 ① 심사용 테스트 계정 ② **연간상품(3개월 초과 서비스 제공) 삭제**를 요구.
①은 `admin_create_workspace`(마이그115, 도입 문의 기업용으로 이미 있던 RPC)로 **격리된 새 워크스페이스**를
만들어 실회사 데이터 노출 없이 계정을 발급(이름 `nicepayreview`, free 플랜). ②는 **연간 결제가 빌링키
미승인 상태의 우회로로 일부러 넣은 것**(2026-08-18 결정)이라 대표 판단 없이 지우지 않았다 — HANDOFF
§새 블로커에 기록, 다음 세션 최우선.

**예상이슈 체크**
| 항목 | 결과 |
|---|---|
| 그랜드파더 데이터 충돌 | 확인함 — 0건, 문제 없음 |
| 트리거가 API 라우트 우회 케이스까지 막는가(mcp_servers 등) | 확인함 — 트리거는 RLS/service_role 무관하게 항상 발동 |
| 기존 사이드바·View 컴포넌트 깨짐 | 없음 — page.tsx만 감쌌고 내부 무변경, tsc/lint/build로 확인 |
| 브라우저 실사용 확인 | **못 함**(확장 미연결) — 다음 세션 또는 대표가 직접 확인 필요 |

---

## 2026-08-22 · 세션50 — 웹훅 등록 + 조회 API 실검증 (DDL 0 · 코드 변경은 주석뿐)

**무엇/왜:** 세션49가 남긴 결제 트랙 4건 중 **대표 액션에 막혀 있던 2건**을 닫았다.
코드로 새로 만든 건 없다 — 있는 코드가 실물에서 도는지 확인한 세션이다.

### 0) 먼저 발견한 사고: 작업 디렉터리가 죽은 브랜치였다

`/Users/johwiwon/equria-workspace`가 `feat/toss-ui-refresh`(`4b7fe08`)에 머물러 있었고
**main보다 74커밋 뒤**였다. 그래서 처음 읽은 `HANDOFF.md`가 **세션44 시점의 480줄 옛 파일**이었고,
"기능별 게이팅 미구현·PG 거절" 같은 이미 끝난 과제를 다음 할 일로 잘못 집계했다.
미커밋 3건(`credits.ts` 문구·랜딩 Basic 카드·HANDOFF PG 블록)도 전부 main에 이미 반영돼 있었다
(`credits.ts`는 글자까지 동일). → `main`으로 이동, 로컬 변경 폐기(패치 백업 후).

**교훈**: 세션 시작 시 `git rev-parse HEAD` vs `origin/main`을 먼저 대조할 것.
HANDOFF를 읽어도 **낡은 HANDOFF면 소용이 없다.**

### 1) 웹훅(URL 통보) 등록 — 대표 실행

상점관리자 > 개발정보 > 웹훅에 `https://complow.kr/api/billing/nicepay/webhook` 등록.
**결제수단 4종 전부 체크.** 처음엔 "쓰는 건 신용카드뿐이니 카드만"으로 안내했다가 정정했다 —
체크 비용은 0이고(모르는 주문번호는 `route.ts:101`이 `OK`만 돌려준다), 빠뜨렸을 때 비용이 크다.
특히 **가상계좌는 입금이 나중에 일어나 이 콜백이 입금을 아는 유일한 경로**라, 나중에 열면서
깜빡하면 "고객은 입금했는데 서비스는 안 열리는" 조용한 실패가 난다.

⚠️ 같은 화면의 **IP보안(REST API 접근제한)은 비워 뒀다** — Vercel은 아웃바운드 IP가 고정이 아니라
등록하면 승인 요청이 통째로 막힌다. 라우트 주석에 이 경고를 옮겨 적었다(등록 위치 오기도 함께 정정 —
"가맹점관리자 > 가맹점정보"는 실제로 "개발정보 > 웹훅"이었다).

### 2) `inquireByOrderId` 실API 왕복 검증 — 통과

`.env.local`에 키 2줄 입력 후, 스크래치 스크립트로 **실제 provider 코드를 그대로 태워** 호출했다
(curl로 흉내내면 PG만 검증되고 우리 코드는 검증되지 않는다).

| 확인한 것 | 결과 |
|---|---|
| `GET /v1/payments/find/{orderId}?orderDate=20260820` | `resultCode "0000"` ✅ |
| Basic 인증 헤더 조립 | 통과 ✅ |
| `readInquiry` 해석 | `approved=true` · `canceled=false` · 29,000원 · tid `UT0036025m01162608201556531803` ✅ |
| DB 기록과 대조 | 카드 하나(외환) 끝 2590 · `paidAt 2026-08-20T15:56:53+0900` 일치 ✅ |
| 시크릿키 길이 | **32자** → 카드 암호화가 AES-256-CBC 경로를 탄다(ECB 폴백 아님) ✅ |

### 3) 🔴 이 검증에서 새로 나온 구멍 — 부분취소 금액 필드명

`readInquiry`는 `raw.cancelledAmount ?? raw.canceledAmount`를 읽는데 **실물 응답에 둘 다 없었다.**
승인 거래라 정상일 수 있지만 **확인된 게 아니다.** 못 읽으면 `canceledAmountKrw=null` →
`billing_apply_cancellation`이 **전액 취소로 간주** → **부분환불이 전액환불로 처리돼 유료 고객이 강등**된다.

응답에 **`balanceAmt`(잔액 29,000)**와 **`cancels`(배열, 지금은 null)**가 실재하므로,
필드명이 다르면 **`amount − balanceAmt`로 유도**하는 편이 철자 추측보다 안전하다.
확정은 실취소 1건 필요 → 카드사 심사 통과 후 HANDOFF §남은 것 3번과 함께.

**예상이슈 체크**
| 항목 | 결과 |
|---|---|
| 조회가 돈을 움직이나 | 아니오 — 읽기 전용 GET ✅ |
| `.env.local`이 커밋되나 | `.gitignore:17` `.env*.local` ✅ |
| tsc / lint / build | 0 · 29 errors 0 warnings(베이스라인·신규 0) · 통과 ✅ |
| DDL | 없음 |

**배포**: 프로덕션 `6d7a025` READY(`dpl_FyQvB2r5…`) · 롤백 후보 `51f4100`.
스모크 6종 통과(`/` 200 · 웹훅 GET 200 · reconcile **401** 크론 가드 · `/refund` 200 · `/terms/billing` 200 · `/billing` 307).

### 4) "마케팅 시작해도 되나" 판정 → **아직 아니다**

대표 질문에 기억이 아니라 **코드 전수 확인**으로 답했다. 그 과정에서 하나가 새로 드러났다.

**🔴 새로 드러난 것 — 기능별 게이팅이 아예 없다**

| 확인한 곳 | 결과 |
|---|---|
| `src/lib/config/features.ts` plan 조건 | **0건** |
| Sidebar 메뉴 숨김 | **0건** |
| `workflows/`·`approval/` 페이지 가드 | **0건** |
| `lib/plans.ts` 실사용처 | 좌석·AI 사용량·Opus **3가지뿐** |

랜딩은 결재·근태·회의노트·재무·워크플로우를 유료로 파는데 코드는 안 막는다 →
**무료 가입자가 유료 기능을 전부 쓴다.** 마케팅을 켜도 전환이 안 된다. → HANDOFF §다음 세션 착수로 승격.

**나머지 개시 조건 4가지**(HANDOFF §마케팅 개시 조건 표에 정리):
정산 미개통(심사 대기·시한 9/17) · Vercel Hobby 비상업 약관 · 파일 용량 상한 부재(1GB 초과 시 **중단**) ·
제3자 신규가입 E2E 미검증.

**부수 산출물**: 대표용 그림 설명서 2장(Artifact) — 크레딧/요금제 구조 · 결제 안전장치("두 번 긁지 않기").
결제 쪽은 `nicepay.ts`·`renew.ts`·`refund.ts`·webhook·reconcile 헤더 주석을 근거로 그렸다.

---

## 2026-08-22 · 세션49 — 환불·취소 완성 + AI 원가 계획 재실측 (마이그142 · 프로덕션 `89a5dcb`)

**무엇/왜:** 결제 트랙의 마지막 구멍 두 개를 막았다. 그리고 다음 과제였던 "AI 원가 절감"을
착수 전에 실측했는데, **계획에 적혀 있던 항목 2개가 이 워크로드에선 틀렸다**는 게 드러나 방향을 바꿨다.

### 1) 환불·취소 (`8b4a188` · 마이그142)

**뚫려 있던 것**: 나이스페이에서 결제를 취소해도 웹훅이 감사 로그만 남기고 끝냈다 →
**돈은 돌려줬는데 그 회사는 계속 유료 요금제를 썼다.** 세션47이 이걸 미룬 이유는 환불 금액
계산과 한 묶음이어야 해서인데, 그 짝이 이번에 생겨 함께 넣었다.

- `lib/billing/refund.ts` — `/refund`에 **이미 공표한 조항을 그대로** 코드로. 분기마다 근거 조항 주석.
  대표 결정: 이용 기간은 **날짜로 쪼갠다**(달 올림 아님) · **AI 사용량은 금액에서 공제 안 한다**.
- 마이그142 `billing_apply_cancellation` — 영수증·구독 종료·요금제 강등을 한 트랜잭션으로.
- 웹훅 취소 분기도 **본문을 안 믿는다** — 주문번호만 알면 가짜 취소 통보로 유료 고객을 강등시킬 수
  있었다. TID로 조회 API를 때려 실제 취소인지 확인한 뒤에만 반영한다.

**예상이슈 체크(프로덕션 트랜잭션 시뮬 → 전부 rollback)**
| 확인한 것 | 결과 |
|---|---|
| 환불 12개 시나리오 | 월 3일차 26,100 / 월 8일차 0 / 연 3개월15일 188,937 / 연 11개월 0 · **전부 환불+공제=결제액** ✅ |
| 재통지 멱등 | 부분12,000 → `already` → 잔액취소 `canceled` → 재전송 `already` → 낮은 금액에도 29,000 유지 ✅ |
| 옛 결제 취소가 현재 구독을 끊는가 | 안 끊김(`current_period_start` 일치 검사) ✅ |
| 미승인/모르는 주문 | `noop` / `unknown` ✅ |
| advisors | 신규 ERROR 0 (billing_keys 정책 0개는 설계상 의도) ✅ |

### 2) AI 원가 — **착수 안 함. 계획을 정정했다** (`1cc6709`·`89a5dcb`)

🔴 **내가 중간에 오진했다.** `agent_usage` 60일치를 보고 "캐싱이 사실상 안 걸린다"고 판단했는데,
**캐싱이 08-08에 배포됐다는 걸 놓쳤다.** 대부분이 캐싱 이전 호출이었다.
`created_at >= '2026-08-09'`로 다시 보니 어시스턴트 4건 중 3건 적중 · 회당 $0.0116 —
**기존 기록("1/9 절감")이 맞았다.** 다음 세션이 같은 함정에 빠지지 않게 HANDOFF에 필터 조건을 적었다.

라이브 API 프로브로 코드 경로도 확인: 1차 캐시쓰기 3,774 → 2차 캐시읽기 3,774 = **정상**.

**계획에서 뺀 것 2개**
- ~~캐시 TTL 5분→1시간~~ — 호출의 **67~73%가 이미 5분 이내**. 1시간으로 늘려도 어시스턴트 +12%p ·
  **Notion clean +0%p**인데 쓰기 비용은 1.25→2배. 이 워크로드에선 손해다.
- ~~캐시 커버리지 24곳 전부~~ — OCR·generate-prompt처럼 입력이 매번 다른 일회성 라우트는
  읽기 0 + 쓰기 1.25배 = **순손실**. 큰 공통 프리픽스를 반복하는 곳만 대상이다.

**진짜 이유**: 캐싱 배포 이후 프로덕션 AI 호출이 **총 6건**이다. 깎을 돈 자체가 없다.

**고친 것 1개**: MCP 도구 **이름순 정렬**. 도구는 프롬프트 맨 앞에 렌더돼 1바이트만 달라도 전체
캐시가 무효인데, MCP 서버 응답 순서·재연결 경로 때문에 호출마다 순서가 바뀔 수 있었다.
⚠️ 표본이 6건뿐이라 **"이것 때문에 무효화됐다"는 실측으로 증명 못 했다** — 규격상 소지가 분명하고
수정 비용이 0이라 미리 막은 것이다.

**모델별 최소 캐시 프리픽스**(문서에 없던 것): Sonnet 4.6 **1,024** · Opus 4.7 **2,048** ·
Haiku 4.5 **4,096**토큰. 미달이면 마커를 걸어도 **조용히 캐시되지 않는다**(에러도 안 난다).

**게이트**: `tsc` 0 · `pnpm lint` 29/0(신규 0) · `pnpm build` 성공 · Vercel READY(`dpl_74g6Zq…`).
**스모크**: complow.kr 200 · `/api/billing/reconcile` 401(크론 가드) · 웹훅 GET 200 · `/refund` 200 ·
billing RPC 11개 전부 `authenticated`·`anon` 실행 불가 확인.
**롤백 후보**: `e70e0ee`.

---

## 2026-08-21 · 세션48 — 매달 자동청구를 크론에 연결 (마이그 0건 · 프로덕션 `e70e0ee`)

**무엇/왜:** 8/20 실결제로 첫 결제는 증명됐지만, **한 달 뒤 갱신 청구를 아무도 안 하고 있었다.**
그대로 두면 9/20에 기간이 끝나고 유예 3일 뒤 `free`로 강등된다 — 돈 낸 고객이 자동으로 무료가 되는 상태.
부품(빌키 저장·`provider.charge`·정산 RPC·매일 도는 크론)은 다 있었고 **연결만 없었다.**

**가장 어려웠던 지점 = 중복 청구 방지.** 원래 계획서(BILLING-PLAN)가 못 본 함정이 하나 있었다 —
청구 요청이 타임아웃되면 **승인 여부를 모르는데**, 그 상태를 담을 자리가 스키마에 없다.
`ready`로 두면 6시간 뒤 `billing_fail_stale_checkouts`가 `failed`로 바꿔버려 "확정된 실패"와
구분이 사라지고, 다음날 다시 긁으면 이중 결제다.
→ **판단 근거를 상태가 아니라 `refund_reason` 접두사로 옮겼다**(`renew:` = 확정 / `pending:` = 미결).

**쪼갠 내용(커밋 3개)**
1. `d21716a` **갱신 엔진 + 크론 연결** — `src/lib/billing/renew.ts` 신규(약 400줄, 대부분 주석).
   · 중복 청구 방어 4겹: ①주문번호 = 워크스페이스×한국날짜(`RNW-42F70FA1-20260921`, order_id unique가 뮤텍스)
     ②확정 표식 없으면 전부 미결 → 그 회사는 청구 제외 ③미결은 PG **주문번호 조회**로 확정
     (`GET /v1/payments/find/{orderId}?orderDate=`) ④청구 판정은 `current_period_end` 하나만
   · "거래 없음"은 응답코드(`A118·A243·A251·U107·U126`)로만 판정 — `2000`(DB오류)·`A299/U303`(API지연)은
     "결제 안 됨"이 아니라 "모르겠다"다. 이걸 실패로 굳히면 승인된 카드를 또 긁는다.
   · 새 기간은 **직전 기간이 끝난 시점부터** — `now()`로 잡으면 결제일이 매달 뒤로 밀린다.
   · 금액은 구독 행의 `amount_krw`(고객이 동의한 금액). 가격표를 다시 읽지 않는다(가격 변경은 7일 전 고지가 먼저).
   · 🔴 실제 청구는 **크론(GET)만** — 화면 진입 시 지연 대사(POST)는 로그인한 아무나 부를 수 있다.
   · `checkout`: 정산 성공 **직후** `auto_renew`를 켠다. 사전 동의 RPC는 결제 **전**에 부르는데
     그 시점엔 구독 행이 없어 UPDATE가 0행이었다 — 8/20 실결제가 `auto_renew=false`로 남은 원인.
   · `checkout`·`cancel`: 대상 회사를 `getUserWorkspaceId()`(활성 워크스페이스 쿠키 우선)로 교체.
     "가장 오래된 멤버십"을 고르던 탓에 실결제가 EQURIA가 아닌 '테스트'로 들어갔었다.
2. `ec109d7` 문서 — HANDOFF(방어 4겹 표·시뮬 결과·남은 1건) · BILLING-PLAN(원 설계가 못 본 함정 기록)
3. `e70e0ee` 다음 세션 시작 시 알릴 것(주문번호 조회 API 실검증 — 대표 보류 항목)

**예상이슈 체크(프로덕션 DB 트랜잭션 시뮬 → 전부 `rollback`)**
| 확인한 것 | 결과 |
|---|---|
| 같은 날 2회차 청구 시도 | `duplicate key` 차단 ✅ |
| 금액 위조(10원으로 정산) | `amount mismatch: expected 29000, got 10` 예외 ✅ |
| 정산 재통지(웹훅 중복) | `already` no-op · 결제행 1건 유지 ✅ |
| 정상 정산 | 기간 연장 · plan 유지 · 사용량 750 재설정 · 장부 1건 ✅ |
| 청구 실패 | `past_due`(**플랜 유지**) → 유예 3일 → `expired`+`free` ✅ |
| 성공한 구독이 만료 처리에 휩쓸리는가 | 안 건드림 ✅ |
| 7일 전 고지 | "곧 구독료가 결제돼요 · 8월 26일에 29,000원" · 같은 기간 중복발송 0 ✅ |
| 프로덕션 라우트 | `/api/billing/reconcile` 401(인증 가드 정상) · 웹훅 GET 200 ✅ |

**아직 못 한 것(정직하게)**: `inquireByOrderId` **실API 왕복 미검증**. 규격·응답코드는 매뉴얼로 맞췄지만
`.env.local`에 나이스페이 키가 없어 실호출을 못 했다. 대표가 "다음 세션 열 때 알려달라"로 보류.

**배포 후 처리**: 기존 구독 `auto_renew` ON(대표 승인). → 9/13 고지 · **9/21 29,000원 자동 청구**.

**게이트**: `tsc` 0 · `pnpm lint` 29/0(신규 0) · `pnpm build` 성공 · Vercel READY(`dpl_83jFzG…`).
**롤백 후보**: `a616c19`.

---

## 2026-08-19 · 세션47 — 첫 사용 경험(Activation) 2차: 남은 6개 완료 (마이그 0건)

**무엇/왜:** `ACTIVATION-PLAN.md`의 남은 6개(3-1·3-2·3-3·3-6·3-8·3-9)를 전부 구현했다.
한 문장 목표: **신규 팀이 안내를 따라 골든패스 3단계(팀원 초대 → AI로 진짜 일 한 번 → 반복 업무 에이전트화)를 스스로 밟게 만든다.**

**쪼갠 내용(커밋 6개)**
1. `734b7c1` **3-1 시드 에이전트 3개** — `src/lib/seedAgents.ts` 신설. 온보딩에서 `create_workspace` 직후
   경리 마감·고객 응대·회의록 정리를 앱이 직접 만든다(정적 프롬프트 → 가입 시점 AI 호출 0).
   온보딩 문구("기본 에이전트가 함께 준비됩니다")가 실제로는 0개를 만들던 거짓말을 고쳤다.
2. `0233966` **3-8 시작하기 카드 + 3-6 초대·좌석** — 대시보드 최상단 골든패스 3단계 카드,
   사이드바 '팀 초대'(오너만) + `SeatMeter` 추출 + `useSeats` 훅.
3. `2d1119d` **3-2 예시 갤러리 + 3-9 가치 노출** — 죽은 코드였던 `agentTemplates.ts` 8종을
   `/agents`에 연결(`?template=<id>`로 위저드 프리필). 2단계 미완료일 때만 회의록·영수증·명함 바로가기.
4. `5cfa37f`·`bd29262`·`3cf59ba` **3-3 빈 상태 표준화** — 파일 3~4개씩 3커밋으로 분리.
   대시보드 3개 / 캘린더·파일 / 명함·재무·근태·휴가·지출.

**예상이슈 체크:**
- **최고 위험이던 9회 insert(3-1)**: 에이전트 1개 단위로 격리했다. `agent_versions` 실패 시 그 `agents` 행을
  즉시 하드 삭제한다 — 버전 없는 에이전트는 채팅 API가 **404**를 준다(실측 확인: `chat/route.ts`의 `if (!agentVersion)`).
  `seedStarterAgents`는 throw하지 않고, 12초 타임아웃을 둬 어떤 경우에도 `/dashboard` 진입을 막지 않는다.
- **판정 오작동 방지**: 시드에 `category='시작하기'`를 넣고, 골든패스 3단계와 예시 갤러리 노출을
  `category <> '시작하기'`로 센다. 안 그러면 **가입 즉시 3단계가 완료로 찍혀** 안내가 통째로 무의미해진다.
- **RLS 경로 재사용**: `AgentBuilderForm`과 같은 3종 세트(agents→agent_versions→user_agent_pins).
  마이그043의 `agents_insert`가 `created_by = auth.uid()`를 강제하므로 `created_by`를 반드시 넣는다.
  쿠키를 쓴 뒤 `createClient()`를 **다시** 만든다 — 그래야 `x-workspace-id` 헤더가 붙는다.
- **대시보드 레이아웃**: 한 화면 고정(`h-[var(--app-content-height)]`)이라 `EmptyState`의 `py-16`을
  `py-4~5`로 눌러 썼다(`cn`이 twMerge라 덮인다). 카드는 3단계 완료 또는 닫기 시 사라져 자리도 안 차지한다.
- **재무 빈 상태**: 데이터가 없는 경우와 필터 때문에 빈 경우를 갈랐다. 같은 문구면 필터가 걸린 걸 못 알아챈다.
- **계획과 다른 점 2개**(ACTIVATION-PLAN에 기록): 프롬프트는 `agentTemplates.ts`가 아니라 `seedAgents.ts`에 뒀고,
  예시 갤러리는 빈 상태 자리가 아니라 별도 섹션으로 뺐다(시드 3개 때문에 오너에게는 빈 상태가 안 뜬다).
- **마이그레이션 0건** — 약속대로. 판정은 전부 기존 데이터에서 파생, 저장은 localStorage 하나뿐.
- 게이트: `tsc` 0 · `pnpm lint` **29/0(신규 0)** · `pnpm build` 성공.

**검증(주장 말고 실측):** 브라우저 E2E는 가입이 구글 전용이라 새 구글 계정이 필요해(대표 보류 항목)
세션46과 같은 방식으로 **프로덕션 DB에 `검증01`을 실제로 만들어** 돌렸다.
`set local role authenticated` + jwt claims + `x-workspace-id` = 앱 클라이언트와 같은 조건.
- 시드 3종 생성 **3/3/3** RLS 통과 · 각 `is_current` 버전 1개 보유(채팅 404 조건 아님) · 3개 모두 자동 핀
- 가입 직후 시작하기 카드 **0/3**. **시드를 제외하지 않으면 `agents=3`이라 3단계가 즉시 완료로 찍힌다**(실측)
- 회의록 1건 → 2단계 완료 전환 · 직접 만든 에이전트 1개 → 3단계 완료 전환 · 혼자면 1단계 미완료
- 시뮬 중 42501 한 번 — **앱 버그 아님**. 3개 insert를 CTE로 묶으니 `av_insert`의 `exists`가 문장 시작
  스냅샷을 봐서 방금 넣은 agents를 못 봤다. 앱은 요청을 나눠 보내므로 해당 없음(순차 재실행 → 통과)
- 정리: workspace_id를 가진 전 테이블 스캔 → 6개 테이블 삭제 → **잔여 0**, 목록이 검증 전과 동일

**배포(2026-08-20):** 게이트 3종 재확인(tsc 0 · lint 29/0 · build) → **main 먼저** 푸시(`bf529cc..f02864e` ff)
→ Vercel 프로덕션 `dpl_CRLY3PE1…` BUILDING→**READY**(icn1) → `complow.kr` **HTTP 200** 확인.
**롤백 후보 = `bf529cc`**. 브랜치 푸시는 앞서 같은 SHA로 끝나 있어 중복제거 없이 프로덕션이 정상 승격됐다.
> 정리: `feat/toss-ui-refresh`(`4b7fe08`)는 **origin/main보다 뒤처진 낡은 브랜치**임을 확인했다
> (`origin/main..feat` = 0커밋). 미배포 작업이 남아 있지 않으므로 이번 배포에서 병합 대상이 아니었다.

**남은 것:** 화면 렌더 눈확인(브라우저) · §4 컴피 쓰기 툴(대표 판단 대기).

---

## 2026-08-20 · 세션47-2 — 나이스페이 PG 규격 전면 전환 (마이그 0건 · `ac1cf27`·`9ae7883`)

**무엇/왜:** 대표가 나이스페이 관리자에서 KEY 발급 화면을 열었고, 그 화면과 담당자 회신 덕분에
**세션46의 설계 가정 2개가 틀렸다는 게 드러났다.** 규격을 확인하고 PG 경계를 통째로 갈았다.

**⓪ 화면 하나가 방향을 바꿨다**
- 대표가 "KEY 재발급" 모달(결제창 승인 방식 / API 인가 방식)을 캡쳐해 보내며 **누르기 전에 물었다.**
  잘못 골랐으면 코드를 다시 짜야 했다 → 우리 코드를 읽어 `Server 승인`(ReturnURL로 받아 서버가 승인) 확정,
  `Basic 인증`은 Access Token이 30분 만료·갱신 불가라 서버리스에 부적합해 선택.
- 그 과정에서 공식 매뉴얼(github.com/nicepayments/nicepay-manual)을 확인 →
  **우리 코드는 `MID+상점키+SignData` 옛 결제창 규격인데 실제 상점은 `clientKey/secretKey`+Basic** 이었다.
- 담당자 회신이 같은 날 도착해 **독립적으로 확증**: *"포스타트에서는 MID가 아닌 클라이언트키/시크릿키
  통하여 연동이 가능합니다."* + *"자동결제 기능, **카드정보입력창** 등은 귀사에서 직접 개발"*
  → 정기결제는 **결제창을 아예 쓰지 않는다**(비인증 빌키 방식).

**① 교체 내용**
- `provider.ts`: buildCheckout/approve/netCancel(결제창 3종) → issueBillingKey/charge/expireBillingKey/cancel(빌키 4종).
  `CardInput` 타입 신설 + 취급 규칙을 타입 주석에 못박음.
- `nicepay.ts`: 전면 재작성. `api.nicepay.co.kr` · Basic 인증 · `resultCode==="0000"` 판정.
  encData는 **AES-256-CBC** 우선(ECB는 같은 평문→같은 암호문이라 카드정보에 부적합), 시크릿키가
  32자가 아닐 때만 AES-128-ECB 폴백. 평문 형식은 문서 그대로 `cardNo=..&expYear=..&expMonth=..&idNo=..&cardPw=..`.
- `checkout/route.ts`: 빌키 발급 → 저장 → 첫 결제 → 정산을 한 흐름으로. 정산 실패 시 `cancel()`로 즉시 되돌린다.
- `CheckoutView.tsx`: 결제창 SDK 호출부 제거 → **카드정보 입력창 4칸**(심사 5번 요건).
- `nicepay/return` 라우트 삭제(결제창 전용).

**예상이슈 체크:**
- 🔴 **웹훅이 조용히 죽을 뻔했다.** `mid !== process.env.NICEPAY_MID` 비교가 남아 있으면 없는 env와
  비교해 **모든 통보를 ok()로 무시**한다(승인 유실 복구 경로 사망, 로그도 안 남음).
  발신자 확인을 clientId로 바꾸고 **필드가 없으면 통과**시켰다 — 통보 규격이 불확실한데 여기서 막으면
  정상 통보까지 버린다. 실제 방어선은 "우리가 만든 주문인지"와 "조회 API 재확인"이다.
- **카드 원문**: 브라우저 state → 요청 본문 → 서버 암호화 직전까지만. DB·로그·raw 어디에도 없고
  저장은 `bid`와 끝 4자리뿐. 성공 즉시 state를 비우고 `autoComplete="off"`·password 타입.
- **자동결제 동의를 필수로 바꿨다** — 빌키가 실제로 자동 갱신을 켜므로(법적 의무①).
  ⚠️ 그런데 **매달 갱신 청구는 아직 없다**(다음 작업 1번). 실패 방향은 안전하다(`billing_lapse_due`가
  만료 → free 강등). 유료 고객 0명이라 시급하지 않지만 반드시 채워야 한다.
- **마이그레이션 0건**: 빌키 저장은 단일 행이라 원자성 요구가 없어 RPC 없이 service_role로 직접 insert.
  같은 워크스페이스의 옛 빌키는 revoked로 내린다(카드 교체 시 옛 카드로 청구되면 안 된다).
- 옛 EUC-KR 우려는 **해소**(REST는 JSON/UTF-8). 대신 **웹훅 실물 규격 확인**이 새 숙제로 남았다.
- 게이트: tsc 0 · lint 29/0(신규 0) · build 성공.

**배포·실검증(2026-08-20):** main `913cc16` → 프로덕션 READY. 자격증명 반영은 프로덕션 웹훅이
**503(credentials_missing) → 200** 으로 바뀐 것으로 확증(추측 아님).
그 직후 **대표가 실제 카드로 결제** — 빌키 발급 → 승인 → 정산 → 요금제 승급이 **전 구간 작동**했다.
29,000원 · 하나(외환) 끝 2590 · 구독 active(8/20~9/20) · '테스트' 워크스페이스 standard 승급.
**DB에 카드번호 전체는 없다**(bid + 끝 4자리만) — 설계대로.

**실결제가 드러낸 3가지(다음 세션 작업):**
1. `auto_renew: false` — 동의는 받는데 구독 행에 안 켜진다. **9/20에 갱신이 없어 free로 강등된다.**
2. `checkout/route.ts`가 결제 대상 워크스페이스를 **"첫 멤버십"**(created_at 오름차순 limit 1)으로 고른다
   → 여러 회사에 속한 오너는 엉뚱한 곳에 결제된다. 이번에도 EQURIA가 아니라 '테스트'로 들어갔다.
   활성 워크스페이스 쿠키(`x-workspace-id`)를 우선하도록 고칠 것.
3. 카드 승인 문자 가맹점명이 **"나이스정보통신"** — PG 대표가맹점 방식이라 정상이고 코드로는 못 바꾼다.
   담당자에게 개별 가맹점 등록 절차를 문의해야 한다.

**남은 것:** ① **매달 자동청구 연결(⏰ 9/20 기한)** ② 결제경로 PPT 제작·회신(대표) ③ 웹훅 URL 등록(대표)
④ 심사용 계정 준비(대표) ⑤ 공정환불 계산식 ⑥ 취소 통보 처리 ⑦ 워크스페이스 선택 로직 수정.

---

## 2026-08-19 · 세션46 — 결제 복구 경로 완성 + 프로덕션 결제 차단 버그 수정 (마이그141 · 배포 `56a858b`)

**무엇/왜:** 대표 지시는 "나이스페이 자격증명 확인, 못 찾으면 대안 3개(크론 대사·웹훅·랜딩 가격 파생화)". 자격증명은 관리자 로그인이 필요해 못 했고, 대안 3개를 하면서 **나이스페이 공식 문서를 처음으로 직접 확인**했다. 그 결과 설계 가정 3개가 틀렸고 **프로덕션 결제가 아예 작동 불가 상태**였음이 드러났다.

**⓪ 규격을 먼저 확인한 게 이 세션의 방향을 바꿨다**
- 마이그139·140과 BILLING-PLAN은 **조회 API·결제통보 규격을 못 본 상태**에서 쓰였다. 실제 문서(manual-status.php · manual-noti.php) 확인 결과:
  - **노티에 서명 필드가 없다** → 설계했던 `timingSafeEqual` 서명검증이 불가능. 대신 "MID 일치 + TID로 조회 API 재확인"으로 대체(더 강한 검증이다).
  - **조회 API는 TID로만 조회되고 응답에 금액이 없다** → `ready` 고아 행은 조회로 대사할 수 없다. TID를 물어다 주는 **노티가 유일한 복구 경로**.
  - **노티 재전송이 최대 10회·1~10분 간격**으로 명시 → "항상 200 반환" 지침을 뒤집었다. 판정 못 한 경우엔 일부러 OK를 안 줘서 공짜 재시도를 쓴다.

**① 🔴 프로덕션 버그 — 결제가 조용히 전부 실패하는 상태였다**
- `proxy.ts` matcher가 `/api/*`까지 걸리는데, 나이스페이 결제창이 리턴 URL로 보내는 POST는 **크로스사이트**라 Supabase 세션 쿠키(SameSite=Lax)가 안 실린다 → 미인증 판정 → `/login` 리다이렉트 → **승인 API 호출 자체가 실행되지 않는다.**
- 말로 넘기지 않고 **실서버를 띄워 재현**: OPEN_PATHS에 없는 API는 쿠키 없는 요청에 `307 → /login`. 수정 후 결제 3경로는 401/503/303으로 각자 핸들러 도달.
- 결제 서버간 통신 3경로를 OPEN_PATHS에 추가. 셋 다 **요청자 신원을 믿지 않는** 경로라(주문번호로 DB 조회 + PG 재확인 + CRON_SECRET) 공개해도 권한이 열리지 않는다.

**② 🔴 결제창이 실제로 뜨지 않았다**
- `CheckoutView.start()`가 checkout API만 부르고 끝났다. 자격증명을 넣어도 **카드사 심사 스크린샷 4·5번을 못 찍는 상태**.
- SDK 로드 + `goPay(form)` + 전역 콜백(`nicepaySubmit`/`nicepayClose`) 연결. **form을 React가 아니라 DOM에 직접 만든다** — SDK가 인증 결과를 form 안에 써 넣는데 리렌더가 그걸 지우면 승인이 조용히 실패한다.

**③ 마이그141 — 140의 구멍 3개**
- `billing_apply_payment`에 **'failed' → 'paid' 복구 허용**. 승인 응답이 유실되면 돈은 빠졌는데 140은 `status<>'ready'`면 예외를 던져 복구가 막혀 있었다. 금액 대조·tid unique·for update는 그대로라 실패가 성공으로 뒤집히지 않는다(앱은 조회 API가 `Status='0'`이라 답한 경우에만 호출).
- `billing_lapse_due` — `billing_expire_due`는 **해지 예약만** 봐서 기간이 끝나도 `active`로 남았다 = **1회 결제로 평생 이용**. 유예 3일 후 `expired` + `free`.
- `billing_notice_upcoming` — 법적 의무②(7일 전 고지)를 보내는 코드가 **아예 없었다**.
- `billing_fail_stale_checkouts` — 승인 안 된 `ready` 정리. 30분이 아니라 **6시간** 기본값(노티 재전송 최악 100분 고려).
- 예상이슈 점검: ⓐ `billing_events.kind` 화이트리스트에 2개 추가하며 **기존 11개 전부 재선언**(마이그104·139 관례). ⓑ 유예<3일·정리기한<30분은 함수가 예외로 막는다(잘못 부르면 유료 고객이 끊긴다).
- 검증: 테스트 워크스페이스 2개 생성 → **25개 시나리오 시뮬 전부 통과** → 정리(잔여 0 확인). 시뮬이 잡은 것은 내 테스트 셋업 버그(기간 제약 위반) 1건.

**④ 자동 점검 시계 · 웹훅**
- `/api/billing/reconcile` — GET=크론(`Bearer CRON_SECRET`) · POST=지연 대사(로그인). 프로덕션 DB 상대로 실제 호출해 4개 RPC 전부 오류 0 완주 확인.
- `vercel.json` 크론은 **하루 1회**. Vercel **Hobby는 하루 1회가 상한이고 더 잦은 식은 배포가 실패**한다(문서 확인). 플랜을 API로 확인할 수 없어 양쪽에서 다 배포되는 값으로 맞췄다. 실시간 복구는 크론이 아니라 웹훅이 맡는다. Pro면 schedule 한 줄만 조이면 된다.
- 취소 통보(StateCd 1·2)는 **감사 로그만** 남긴다. 자동 강등은 환불(부분취소·일할)과 한 묶음이어야 하고, 반쪽만 만들면 회수 분쟁이 생긴다.

**⑤ 랜딩 가격 파생화 + return 라우트 보강**
- `LandingPage.tsx`의 가격·인원 3중 하드코딩을 `lib/plans.ts`에서 파생. tsx 스크립트로 **원본과 글자 단위 동일**함을 7/7 확인(마케팅 카피는 그대로 두고 숫자만 파생).
- return 라우트: 네트워크 오류를 `failed`로 굳히지 않는다 — **승인 여부를 모르는 상태**라 실패로 찍으면 돈만 빠진다. `ready`로 두고 노티/크론이 처리.

**배포:** 게이트 tsc 0 · lint 29/0(신규 0) · build 성공 → main 먼저 push(`688d0ec`→`56a858b`) → 프로덕션 READY 확인 → feat 브랜치 push. 롤백 후보 `688d0ec`.
**프로덕션 검증:** `/api/billing/reconcile` 401 · `/api/billing/nicepay/webhook` 503(자격증명 없음) — 둘 다 `/login` 리다이렉트 없이 핸들러 도달.

**남은 것:** `computeFairRefundKrw()`(마지막 미구현 법적 의무) · 취소 통보 처리 · 대표 액션(MID·상점키 확인 → Vercel 환경변수 3개 → 노티 URL 등록 → 심사 스크린샷).

---

## 2026-08-10 · 세션44 — 원가 레버 재조준 + 하이브리드 한도 + 크레딧 표현 정리 (마이그134)

**무엇/왜:** 세션43 인계의 다음 순서(①모델 라우팅 ②하이브리드 한도 ③크레딧→사용량% ④랜딩 정정)를 진행. **착수 전 실측을 먼저 했고, 그 결과 ①의 전제가 틀렸음을 확인**해 방향을 바꿨다.

**⓪ 실측 먼저 — "Haiku 라우팅 27% 절감"은 성립하지 않았다**
- `agent_usage` 60일 집계: **Opus 에이전트 채팅 $10.60(61%)** · 어시스턴트/리서치 등 큰 프롬프트 $4.48(26%) · 중간 보조 $1.66(10%) · **소형 보조·OCR $0.08(0.5%)**.
- 인계문서가 Haiku로 보내자던 "요약·분류·간단질의"가 바로 그 **0.5%**다. 전부 옮겨도 $0.05 절감 — 27%는 나오지 않는다.
- **캐싱이 계산을 한 번 더 뒤집는다**(`claude-api` 스킬로 단가 확인): Sonnet 캐시읽기 **$0.30**/MTok vs Haiku 정가입력 **$1.00**/MTok → **캐시 걸린 Sonnet이 캐시 없는 Haiku보다 3.3배 싸다**. 게다가 **Haiku 4.5는 캐시 최소 프리픽스가 4,096토큰**(Sonnet 1,024)이라 작은 라우트는 Haiku로 옮기면 캐시가 아예 안 걸린다.
- Models API로 Haiku 4.5 실확인: 비전·PDF·구조화출력 모두 지원(=OCR 이전은 기술적으로 가능), 컨텍스트 200K/출력 64K.
- → **대표 결정: Haiku 라우팅 보류, Opus 게이팅으로 전환.**

**① 고급 모델 Pro 전용 게이팅(`4dd987d`·마이그134)**
- 원가 61%가 Opus인데 `AGENT_MODELS`가 **전 플랜에 열어두고** 있었다. 랜딩 요금표는 Opus를 Pro 전용으로 팔고 있어 **코드와 판매 약속이 어긋난 상태**.
- **왜 트리거인가**: 에이전트 버전은 API 라우트가 아니라 **클라이언트에서 Supabase로 직접 insert**된다(`AgentBuilderForm`). 서버 라우트가 없으니 앱에서 막으면 우회 가능 → `agent_versions` BEFORE INSERT 한 곳으로(마이그133 크레딧 차감과 같은 논리).
- **fail-closed 설계**: 모델 id를 나열하지 않고 이름(opus/fable/mythos)으로 매칭. 나열식이면 새 고가 모델이 목록 누락으로 무료 플랜에 열린다.
- **grandfather**: 이미 그 모델을 쓰던 에이전트는 계속 편집 가능(무료 '테스트'의 기존 Opus 1건 보존) — 추가 우선·기존 파괴 금지.
- 예상이슈 점검: ⓐ 트리거 함수가 `/rest/v1/rpc`로 직접 호출 가능하다는 advisor 경고 → `revoke execute` (safe-changes §5). **PUBLIC 기본 grant 때문에 anon/authenticated만 revoke하면 실제로는 안 걷힌다**는 걸 권한 조회로 확인하고 public까지 revoke. ⓑ revoke 후에도 트리거가 도는지(호출자 EXECUTE 권한 불필요) **재시뮬로 확인**.
- 검증: 5경로 트랜잭션 시뮬 전부 통과(무료+Opus 차단 / 무료+Sonnet 통과 / grandfather 편집 통과 / premium+Opus 통과 / 미지의 고가모델 차단), 잔여 데이터 0.
- 폼의 중복 모델 select 2곳 → 공용 필드 1개로 통합(safe-changes §6).

**② 하이브리드 한도(`aa69d68`·마이그 불필요)**
- 대표 결정: 노션과 같은 구조(일반 AI 무제한·공정사용 / 자율 에이전트만 한도). 노션 Business 5명 $100 vs 우리 Standard 5명 ≈$21로 **1/5 가격**이라 전 기능 무제한은 불가능하지만, **채팅이 갑자기 멈추는 경험만은 없앤다**.
- `credit_consume`이 이미 음수 잔액을 허용하고 있어 **DDL 없이 TS만으로** 됐다. 차감은 종류와 무관하게 그대로(원가 관측 유지), **차단 시점만** 달라진다.
- `checkBudget(userId, kind)` — **기본값을 "automated"(엄격)로 둔 게 핵심**. 새 라우트가 표시를 빼먹으면 막히는 쪽으로 실패해야지 조용히 무제한이 되면 안 된다.
- interactive 6곳(채팅·컴피·수식·기억하기·제작인터뷰·재무코칭) / automated 기본값(워크플로우 자동실행·작업제안·기억정리·다단계 리서치 5종). 리서치는 사용자가 눌러 시작해도 그 뒤로 스스로 여러 호출을 돌아 automated로 분류.
- 예상이슈 점검: 채팅 무제한 = 원가 무한 노출 → **공정사용 안전밸브 `FAIR_USE_MULTIPLIER=3`** 추가(포함량의 3배에서 정지). 없으면 무료 워크스페이스 하나가 원가를 무제한으로 태울 수 있다.
- 검증: 경계값 11케이스 실행 전부 통과(free/std 소진·초과·공정사용 한계·premium 무제한).

**③④ '크레딧' → 'AI 사용량' + 랜딩 정정(`3e66d01`)**
- KCP 거절 사유 ①이 "구독료에 크레딧 포함 = 환금성". 크레딧을 없애도 사유 ②(업력 2년)가 남아 KCP는 안 되지만, **다음 PG에서 이 사유가 생기지 않게** 지금 정리. DB·트리거·차감 로직은 그대로, 표시만 변경.
- 랜딩이 실제 동작과 달랐던 것 정정: "월 500 크레딧" → 실제는 매일 충전(상한 500). "포함 크레딧/월" 한 줄 → "AI 채팅·보조(공정사용)" + "자동 실행" 두 줄로 분리해 ②의 구조를 요금표가 그대로 설명하게. FAQ의 **"추가 구매하거나" 삭제**(top-up 판매 = 환금성 위험) + "갑자기 막히지 않나요?" 문항 추가.
- 게이지: "잔액/상한" → "N% 남음". 음수 구간(채팅 공정사용)은 0%로 바닥 처리 + "자동 실행만 멈췄어요, 채팅은 그대로" 안내.
- **차감 계수는 $0.01 유지**(대표 결정). 캐싱으로 크레딧 1개가 사는 대화량이 이미 ~4배 늘어(턴당 ~5.1→~1.2크레딧) 계수를 건드릴 필요가 줄었다. $0.005로 낮추면 마진은 오르지만 실질 한도가 절반이 된다.

**⑤ AI 사용량 기록 누락 7곳 봉합(`b20ac6d`)** — 무료 AI 후보를 원가순으로 뽑다가 발견. AI 호출 23곳 중 **7곳이 `agent_usage` 기록을 안 했다**(명함/영수증 OCR·채팅/메일/회의록 다듬기·프롬프트 생성·MCP 도구 요약).
- 마이그133이 차감을 INSERT 트리거에 걸어 "누락을 구조적으로 불가능하게" 만들었지만, **트리거는 기록하는 라우트만 잡는다.** 기록 자체를 안 하면 그물 밖 → 원가 집계 누락·크레딧 미차감·예산 미적용. **⓪의 60일 원가 분석도 이만큼 작았다(스스로 정정).**
- 원인이 "20줄짜리 insert 복붙"이라 신규 `lib/aiUsage.ts`로 한 줄 호출 통일(cost 계산 포함). 곁다리로 하드코딩 모델 문자열 2곳 → `MODELS.default`.
- 예상이슈 점검: ⓐ 처음엔 헬퍼가 에러를 삼키게 짰는데, **그러면 컬럼/RLS가 깨질 때 전 라우트 사용량이 통째로 유실돼도 아무도 모른다** — 방금 고친 버그와 똑같은 실패 모드라 `console.warn` 추가. ⓑ 스트리밍에 붙인 `consumeStream`이 응답을 먹으면 다듬기 3종이 빈 화면이 되므로 **실호출로 확인**(본문 108자 수신 + onFinish 실행).
- 검증: 전수 재확인 23/23 · DB 시뮬로 두 insert 조합 유효 + 차감 0.81(=$0.0081) 정확·실패 미차감(롤백).

**검증 게이트**: 각 커밋 tsc 0 · lint 29/0(신규 0) · build 0. 마이그134 원격 적용 + 파일화 둘 다 완료.

**배포(2026-08-10)**: main-first 푸시 → 프로덕션 **`b20ac6d`** READY(빌드 107초·icn1). 롤백 후보 `0560ea1`.
라이브 검증: 랜딩 신규 문구 6종 전부 노출·구 문구 4종 0건, 앱 경로 정상(`/`·`/login`·`/download` 200, `/dashboard` 307).
**⚠️ 미검증(대표 dogfood 필요)**: 로그인이 필요한 화면 — ⓐ 무료 워크스페이스('테스트')에서 Opus 잠김 ⓑ 자동실행 소진 후 채팅 지속 ⓒ 사이드바 % 표기.

**부수로 확인된 것(다음 세션 과제)**: 기능별 게이팅 미구현(무료가 결재·근태·회의노트·재무를 다 씀) · 인원 초과 과금이 랜딩과 코드 불일치(마이그125가 6번째 초대 거부) · Supabase 무료 플랜(스토리지 14.5%·초과 시 과금 아닌 **중단**) → `docs/ops/infra-limits.md` 신설.

---

## 2026-08-08~10 · 세션43 — 프롬프트 캐싱 → AI 크레딧 → PWA (마이그131~133)

**무엇/왜:** 세션42 배포 후 대표 질문("모델 새로 나오지 않았나 / 토큰 비용 정리")에서 시작해, **원가 구조 → 과금 → 패키징** 순으로 이어짐. 순서를 캐싱 먼저로 잡은 건 대표 판단("캐싱부터 넣고 크레딧 붙이자")이었고, 결과적으로 그게 맞았다(아래 ①).

**① 프롬프트 캐싱 + 캐시 인지 원가계산(`3f1b417`·마이그131)**
- 착수 전 `latest-stack.md` 확인 → AI SDK 캐시 API가 불확실해 context7로 조회(`providerOptions.anthropic.cacheControl`, system 역할 메시지로 breakpoint).
- **`usage.inputTokens`가 캐시를 포함하는지**가 관건이라 문서 대신 **설치된 패키지 소스를 직접 확인**: `@ai-sdk/anthropic`의 `convertAnthropicMessagesUsage`가 `total = input_tokens + cache_creation + cache_read`로 합산(Anthropic 원본은 캐시 제외 잔여만). **기존 computeCostUsd로 캐싱을 켰으면 캐시 읽기를 10배 과다 청구** → 크레딧을 그 위에 얹었으면 고객 크레딧이 부당 차감됐을 것. 원가 계산을 먼저 고친 이유.
- `pricing.ts`: 캐시 배수(write 1.25×·read 0.1×, 입력 단가에만) + **fable/mythos 티어 신설**(없으면 sonnet 폴백 → 3.3배 과소계상).
- 라우트 2곳(채팅·컴피)의 시스템 프롬프트를 **안정/변동 구간으로 분리**. 지식파일·기능레지스트리(큰 고정분)를 기억·요약·스냅샷(변동분) **앞**에 두고 안정 구간 끝에만 `cacheControl`. 컴피는 스냅샷이 첫 턴에만 붙어 **턴1↔턴2 프리픽스가 갈라지고 있었음** → 스타일 규칙을 앞으로 이동해 해소.
- **실호출 검증**(컴파일은 검증이 아님 — Gmail 교훈): 동일 프리픽스 2회 → 1회차 write 14,560 / 2회차 **read 14,560**, `inputTokens 14,617 = 57 + 14,560`으로 합산 구조까지 실측 확인. 대화 1회 원가 **~90%↓**.
- 🔒 **부수 발견**: SDK 경고를 계기로 `UIMessage.role`에 `'system'`이 포함됨을 확인 → 클라이언트가 system 메시지를 섞어 보내면 시스템 지시를 덮어쓰는 **프롬프트 주입**이 가능했음(변경 전부터 존재). 이력을 user/assistant로 필터링. 컴피는 네이티브 도구 권한이 있어 영향이 컸다.

**② AI 크레딧(`a0bf25b`·마이그132·133 / 게이지 `6192f91`)**
- 대표 결정: **소진 시 완전 차단** + **하루 지나면 조금씩 충전**(리텐션·abuse 방어 동시 달성).
- 무료 500 지급 → **매일 17 충전·상한 500** / 유료 월 일괄·이월 없음 / premium 무제한.
- **충전을 크론이 아니라 lazy 계산으로**: 크론 지연·실패 시 고객이 크레딧을 못 받는 게 더 나쁘고, 쓰지 않는 워크스페이스는 비용이 0. 기준일 KST.
- **차감을 앱이 아니라 `agent_usage` INSERT 트리거로**: AI 라우트가 14곳이라 앱에서 하면 하나는 반드시 누락되고, 앞으로 추가될 라우트가 조용히 무료가 된다. 공통 지점 한 곳에 걸어 **누락을 구조적으로 차단**.
- `checkBudget`에 크레딧 게이트 통합 → 라우트 14곳 자동 적용(차단 문구도 사유별 분리). 관리자 예외 없음(예산은 내부 통제지만 크레딧은 요금제로 판 것).
- **DB 실검증**(전부 통과·검증데이터 정리): 최초500 → 같은날 중복지급 0 → 100차감 → 3일경과 +51 → **60일경과는 1,020이 아니라 상한 부족분 49만** / 트리거 $0.045 → 정확히 4.5크레딧·실패호출 미차감.
- ⚠️ 배포 전 발견: 그대로 두면 **자사 팀도 500에서 막힘** → EQURIA·이큐리아2를 `plan='premium'`으로(세션13 결정). **'테스트'는 free 유지 = 게이팅 실검증 경로.**

**③ PWA(`c8c3a90`·`9ec03a4`·`cdb3fdc`)**
- 대표 요청 "다운로드해서 사용". Electron은 코드사이닝(Apple $99/년+윈도우 인증서)·스토어 심사로 1~2주+연비용인데 **고객 0명** → 같은 체감을 오늘 주는 PWA 우선(네이티브 전환 시 버리는 작업 없음).
- manifest(start_url=/dashboard)·아이콘 3종(기존 로고에서 `sips`로 생성, maskable은 80% 안전영역 패딩)·`sw.js`·`/download` 안내+`InstallButton`·푸터 연결.
- ⚠️ **캐시 정책이 최대 위험이었다**: 멀티테넌트에서 응답을 캐시하면 타사 데이터가 기기에 남아 **RLS로 막은 걸 캐시가 뚫는다** → 대상을 `/_next/static/`·`/icons/`(내용해시 불변·공개)로만 한정하고 `/api/*`·`/auth/*`·**모든 HTML**(SSR이라 본문에 사용자 데이터)은 캐시도 가로채기도 안 함.
- ⚠️ **proxy OPEN_PATHS 누락 시 조용히 실패**: 브라우저가 로그인 전에 가져가는 파일이라 /login으로 튕기면 설치 버튼이 안 뜬다(sitemap·robots 전례). PWA 5종 추가 후 로컬 실검증 5경로 200·리다이렉트 0.

**④ 격리 재감사** — 구조: `workspace_id` 51테이블·RLS off 0·sentinel DEFAULT 0·인덱스 누락 0. 동작: RLS 시뮬로 신규 크레딧 테이블의 **타사 잔액·원장 0행** 확인.

**⑤ 결제 트랙 — KCP 거절(2026-08-10)**
- 사유 ① 구독료에 크레딧 포함 → **환금성 분류로 심사 강화** ② **업력 2년+재무제표** 미충족.
- **크레딧을 없애도 KCP는 안 됨**(②가 남음) → 표현 정리는 "다음 PG를 위한" 작업으로 재배치.
- 벤치마크 확인: **노션도 자율 에이전트는 크레딧·소진 시 정지**($10/1,000). 단 노션 Business는 **인당 $20**이고 우리 Standard는 **5명 ₩29,000(≈$21)** = **1/5 가격** → 같은 AI 여유는 구조적으로 불가.
- 🚨 함께 드러난 것: 1크레딧=원가 $0.01이라 **Standard 3,000 소진 시 원가 $30 > 매출 ≈$21 = 구조적 적자**. 지금은 breakage에 기대는 중 → 차감 계수 조정이 필요하나 실질 한도가 절반이 되므로 한도 설계와 **함께** 정해야 함.

**예상이슈 체크:** 마이그131~133 전부 추가형·프로덕션 적용 완료(코드와 동시 배포라 어긋남 없음). **오늘 배포분은 대표 육안 확인이 하나도 없다** — 특히 크레딧 게이팅은 대표 계정이 premium이라 '테스트' 워크스페이스로 전환해야 보인다. 캐싱은 실호출로, 크레딧은 DB로 검증했지만 **라이브 사용자 경로(사이드바 게이지·차단 화면)는 미검증**.

---

## 2026-08-07 · 세션43 — 세션42 배포(프로덕션 `9c6bb59`)

**무엇/왜:** 세션42 작업(후속정리·HR 설정/근태 잔여·컴피)이 로컬 14커밋으로 쌓이고 DB만 마이그130까지 앞서 있던 상태 → 대표 지시로 `/deploy`. 코드·DB 정합 회복이 목적.

**쪼갠 내용:**
1. **검증 게이트** — `tsc --noEmit` 0 · `pnpm lint` **29 errors/0 warnings**(베이스라인 유지·신규 0) · `pnpm build` 통과. 마이그 drift 확인: `list_migrations` 130까지 원격 적용 + `supabase/migrations/` 파일 존재(둘 다 = SSOT 규칙 충족).
2. **작업트리 정리** — `docs/build/mail.md`에 남아 있던 의미 없는 빈 줄 1개 revert(clean 상태로 배포).
3. **main-first 배포** — `main` ff-merge(`0930c5f`→`9c6bb59`, 로컬 main이 52커밋 stale이었으나 feat가 후손이라 ff 성공) → `git push origin main`(원격 `8fd5b16..9c6bb59`) → **Vercel 프로덕션 빌드 생성 확인** → 간격 두고 `feat/toss-ui-refresh` push. 중복제거 회피 규칙 준수.
4. **확인** — 프로덕션 `dpl_FrFb534q…` **READY**(target=production·SHA `9c6bb59`). 롤백 후보 = **`8fd5b16`**(직전 프로덕션·Vercel isRollbackCandidate=true).

**라이브된 것:** 회사별 HR 설정(오너 카드·입사일)·근태 연차/월차 잔여 표시·범용 비서 컴피(스냅샷+네이티브 도구 4종)·신규 워크스페이스 clean-slate·app-ambient 제거·죽은컬럼 drop·/code-review 7건 수정.

**후속 — Gmail MCP 프리뷰 차단 해제 검증(같은 날):** 대표가 "구글 메일이 안 온다"고 해 확인 → **승인 메일은 7/28 17:06에 이미 도착**(수신함이 `complow@complow.kr`이라 미확인·신청 당일 승인). 독촉 메일 초안까지 썼다가 불필요로 폐기. 진단 스크립트(scratchpad `probe_gmail_mcp.mjs`: .env.local→service_role로 `mcp_user_connections`/`mcp_oauth_clients` 조회→AES-256-GCM 복호화→refresh_token 갱신→Streamable HTTP JSON-RPC, 토큰 미출력)로 실호출 검증: initialize 200 → tools/list 13개 → **`list_drafts` tools/call 200·isError=false·실데이터**. 경계도 확인 — `search_threads`는 403이나 사유가 `insufficient authentication scopes`(스코프 부족)로, 7/28의 `caller does not have permission`(프리뷰 미등록)과 **다름** = 프로젝트 차단 해제 확정. 읽기 403은 CASA 회피로 의도한 설계. 잔여: 캘린더는 연결 행 자체가 없어 재연결 후 재검증 필요 · 검증은 대표 계정 1건 기준 · 판매는 GA 후(Pre-GA 약관).

**예상이슈 체크:** DDL은 이번 배포에 신규 0(127~130은 이미 적용됨) → 앱-DB 동기 배포 리스크 없음. 마이그130 no-op은 이제 코드와 함께 라이브 — 신규 워크스페이스에 시드 에이전트 없이 컴피만 제공되는 게 의도대로인지 **첫 신규 가입 시 확인 필요**. 기존 워크스페이스 활성 시드 16개는 그대로(I30·컴피 검증 후 소프트삭제). **대표 dogfood 미검증**: HR 설정 저장·잔여 표시·컴피 도구호출 정확도(모바일/다크 포함).

---

## 2026-08-01 · 세션42 — 회사별 HR(근태) 설정 + 범용 비서 '컴피' (플랜 승인·마이그128~130·로컬 커밋 미푸시 → **세션43에서 배포됨**)

**무엇/왜:** 대표 지시 — "기본 에이전트를 사이드바 기능 제어형으로 싹 교체 / 근태 에이전트가 각 인원 연차·반차 잔여 답변(→회사별 HR 기준 설정 선행) / 워크스페이스를 다 아는 개인 전용 범용 에이전트(이름 추천)". 플랜모드(Explore 3+대표 결정 4)로 계획 승인(`~/.claude/plans/memoized-sprouting-cascade.md`). **대표 결정**: 범용 비서 1개·이름 **컴피(Compi)**·근태·HR 먼저·HR 범위 4종. **탐색 핵심**: 에이전트는 외부 MCP 도구만 호출(앱 데이터 네이티브 도구 전무)·연차 잔여/부여/입사일 개념 DB에 전무 → 신설.

**트랙1 — 회사별 HR + 근태 잔여(각 tsc0·lint29/0·build0·main-first 커밋):**
1. **마이그128(`hr_settings`)** — workspace_id PK+jsonb(leave_policy/work_policy/holidays·읽기=멤버·쓰기=오너 RLS `auth_is_workspace_owner`) + `profiles.hire_date` + `attendance_records.status`/`leave_requests.leave_type`에 '월차' additive. begin/rollback 시뮬(기존데이터 위반0)·advisors 신규 ERROR 0.
2. **마이그129 RPC + `lib/hr.ts`** — `set_member_hire_date`(owner_can_set_role)·`attendance_balances`(can_view_attendance 게이트·인원별 기준연도창+연차/반차/월차 used). granted/remaining 법정 산식은 `lib/hr.ts` 순수함수(근로기준법 기본값·resolve* 병합). 실호출 검증: 오너=6명 집계(실근태 반영)·비오너=0행.
3. **HR 설정 UI** — 오너 전용 `HrSettingsCard`(휴가기준·근무시간·휴무일, `SettingsView` 삽입) + 구성원 '입사일' date 칸(`MemberInfoRow`+`set_member_hire_date`). `/api/hr-settings` 라우트는 불필요(RLS 오너 게이팅·클라 upsert)로 단순화.
4. **AttendanceAdmin 잔여** — `attendance_balances`+`hr_settings`→`lib/hr` 산식으로 인원별 연차 잔여 표시(상세 '잔여 N/부여 M·월차 K', 목록행 '연차 잔여 N'). '월차'를 BAR_STYLE·ABSENT_SPAN·REASON_STATUSES·STATUS_BADGE 반영.

**트랙2 — 범용 비서 컴피:**
5. **`workspaceContext.ts`**(featuresOverview·buildWorkspaceSnapshot·kstDate) + **`agentTools.ts`**(네이티브 도구 4종: get_attendance_balances·list_projects·list_calendar_events·list_my_tasks — 전부 RLS·활성 워크스페이스 스코프).
6. **assistant 라우트 → 컴피 승격** — 페르소나 + 기능 레지스트리 + 현황 스냅샷 주입 + 도구 병합(streamText tools·stepCountIs5) + 게스트 403 fail-closed. "누가 연차 며칠 남았어" → 도구로 근태 RPC 조회해 정확 응답.
7. **컴피 브랜딩**(DashboardAssistant 인사말·클릭 예시 칩·플레이스홀더).

**옛 8종:** 신규 워크스페이스 clean-slate — **마이그130** `clone_seed_agents` no-op(옛 시드 복제 중단, 컴피가 대체). **기존 워크스페이스 활성 시드 16개(3워크스페이스)는 유지** — 대표 결정 "컴피 검증 후 숨기기"(별도 처리·is_active 토글로 되돌림 쉬움).

**/code-review 후속 수정(2026-08-03, 7건 전부 실결함):** ① 컴피 도구 `list_projects`·`list_calendar_events`·`list_my_tasks`가 활성 워크스페이스 미스코프 → 명시 `.eq(workspace_id)`(개인전용 RLS라 이큐리아/이큐리아2 혼입·마이그126 회귀 방지) ② `buildWorkspaceSnapshot`도 4소스 전부 workspace_id 스코프+시그니처에 wsId 추가 ③ 컴피 라우트 onFinish `usage`→`totalUsage`(도구 다단계 토큰 과소집계) ④ 스냅샷을 첫 턴에만 주입(매 턴 4쿼리·프롬프트캐시 무효 완화) ⑤ 빈 답변 시 assistant_messages 미저장(빈 말풍선 방지) ⑥ `lib/hr` 근속 가산 산식이 start_year 무시(비법정 설정서 오산) → `floor((years-start_year)/every)+1`. 각 tsc0·lint29/0·build0.

**예상이슈 체크:** ⚠️ **상태 주의**: 마이그 127~130은 **프로덕션 DB 적용됨**, 코드는 **전부 로컬 커밋(미푸시)** — 배포 코드는 여전히 `a274fab`. 적용 마이그는 전부 additive라 배포 코드 무영향(hr_settings/hire_date/월차/RPC 미참조). 단 130(clone no-op)은 라이브 신규 워크스페이스 생성 시 시드 없음 — 가입 게이팅 상태라 실영향 미미. **컴피 라이브 도구호출 스트리밍·HR 설정 저장·잔여 표시 = 대표 dogfood 미검증(배포 후).** 후속(known-issues): 이월 잔여 반영·본인 잔여 셀프조회·재무/회의 도구·task-suggestions↔workspaceContext 통합·옛 8종 숨김.

---

## 2026-08-01 · 세션42 — 후속 정리(기술부채): app-ambient 중복 제거 + 죽은 컬럼 drop (로컬 커밋 `76b70c9`·`4d6a6c1`)

**무엇/왜:** 세션41 개편 소개서 PDF 산출(`~/Desktop/complow/Complow_세션41_업데이트_소개서.pdf`) 후, 대표 선택으로 **후속 정리(기술부채)** 착수. HANDOFF 세션41 다음액션 #3 중 안전·고가치 항목만 잘게 처리. 각 변경 전 safe-changes(추가>수정>삭제·파괴는 검증 후) 준수.

**쪼갠 내용:**
1. **app-ambient 중복 배경 제거(`76b70c9`)** — 세션41 랜딩 통일로 `.app-ambient`가 flat `var(--background)`만 칠하게 평탄화됐고 `body`가 이미 동일 배경을 칠함 → `(app)/layout.tsx`의 `fixed -z-10` 마운트 div + `globals.css` `.app-ambient`/`.dark .app-ambient` 블록 완전 중복. 둘 다 제거(참조 grep 0 확인·레이아웃/픽셀 무변화).
2. **죽은 컬럼 drop(`4d6a6c1`·마이그127, I22)** — `workspaces.finance_snapshot_open`(마이그121)은 세션41 대시보드 개편(6349e84)에서 FinanceSnapshot 기능 제거로 미사용. 검증: 코드 참조 0(생성 types.ts 3줄 제외)·RLS 정책/함수/뷰/제약 의존성 전부 0(MCP execute_sql)·`begin; drop; rollback` 시뮬 통과 → `apply_migration`로 원격 drop + `127_drop_finance_snapshot_open.sql` 파일화(SSOT) + types.ts Row/Insert/Update 3줄 제거. 롤백=마이그 주석의 add column.

**보류(위험/가치 판단):** ① **Surface glass variant** — `.glass`가 이제 솔리드로 렌더되지만 `AnnouncementsBoard`가 아직 `variant="glass"` 1곳 사용 중 → 솔리드 치환이 시각 동일한지 확인 전엔 보류(기존 디자인 유지). ② **랜딩 리터럴 토큰화** — 랜딩은 의도적 리터럴 하드코딩(토큰 변동 격리)이라 재커플링 가치 낮고 공개 마케팅 페이지 시각 변동 위험. ③ **날짜헬퍼 통합(I26)** — 지역 헬퍼는 문자열(YYYY-MM-DD) 기반, `lib/calendar`는 Date 기반이라 통합 시 타임존 off-by-one 위험(드래그 로직) → 별도 정밀 작업으로 이월.

**예상이슈 체크:** tsc0·lint29/0(신규0·베이스라인 유지)·build0. 마이그127 advisors 신규 ERROR 0(WARN 68·INFO 1 전부 기존 I9 범주). **⚠️ 상태 주의**: 코드 2커밋은 **로컬(미푸시)**, 마이그127은 **프로덕션 DB 적용됨** — 배포 코드(`a274fab`)가 해당 컬럼을 미참조하므로 라이브 무영향. 배포(푸시)는 대표 확인 후.

---

## 2026-08-01 · 세션41 후속 — 회의노트 토글(접기) 블록 + 메일 상태 확인 (`a274fab`)

**무엇/왜:** 대표 지시 — "회의노트에 노션식 슬래시 기능 추가·부실한 기능 파악/수정·**안되는 건 하지마셈 확실하게**" + "메일은?". ① 회의노트 슬래시 메뉴가 노션 대비 **토글(접기) 블록**만 비어 있어 추가(제목 옆 셰브론으로 펼침/접힘·본문에 임의 블록). Callout/FileBlock와 동일한 커스텀 노드 패턴 — `summary`·`open`을 `data-*`로 직렬화해 `getHTML()` 왕복 안전, 읽기 전용(editable=false)에서도 같은 확장으로 렌더되어 로컬 상태로 접기/펼치기 동작. ② **메일**은 이미 완성(Gmail OAuth·3분할·읽기/별표/보관/휴지통/검색·작성/답장+AI 도우미·첨부·DOMPurify) — 부실 아님. 초대는 대표 확정대로 **링크 방식 유지**(메일 발송 안 함, 별개 기능)로 정리 보고.

**제외(확실하게 — 대표 지침):** 수학공식(KaTeX 미설치)·차트·데이터베이스/보기(데이터 엔진 필요)·웹 북마크(서버 OG 크롤)·동영상/오디오 임베드·목차·버튼/탭/동기화 블록(노션 전용 런타임) — 리치텍스트에서 신뢰성 있게 구현 불가라 손대지 않음.

**신규/변경:** `meetings/editor/Toggle.tsx`(신규) · `extensions.ts`(등록) · `slashItems.ts`(기본 블록에 '토글 목록' 추가). tsc0·lint0·build0 → main-first 배포(프로덕션 READY·`complow.kr`). **⚠️ 대표 육안 미검증**(회의노트 `/`→토글, 읽기 화면 접기).

---

## 2026-07-31 · 세션41 후속 — 에이전트 생성 단순화·시트 게이팅(125)·오늘할일 회사분리(126)·초대 역할 피커

**무엇/왜:** 대표 dogfood 연속 지시. 각 잘게 커밋·tsc0·lint29/0·build0·main-first.
1. **근태 월별 리스트 정리(`560ee5a`)** — 근무시간 없을 때 '—' 제거·지난날 미퇴근 '출근' 접미사 제거(오늘만 '근무 중').
2. **에이전트 생성 첫 화면 단순화(`47f6ac7`)** — 다크모드 부스러기 장식 아이콘 제거→예시 칩 5종(브랜드 모노 라인 아이콘)+타이핑 플레이스홀더.
3. **원샷 생성 시도→반려(`f7e824b`→revert `cd543f3`)** — '한 줄→즉시 완성' 만들었으나 **대표 결정: "이 과정(단계별)을 보여주는 게 우리 장점"** → 기존 15필드 가이드 플로우 유지, quick-generate 라우트 삭제. (교훈: 기존 플로우 임의 대체 금지.)
4. **플랜/시트 게이팅(`bae01b8`·마이그125)** — `lib/plans.ts` SSOT(Basic 3석/Standard 5석/Pro 10석/Premium 무제한·랜딩 정합)+`seatsFull`/`nextPlan`. `accept_workspace_invite`에 **서버 강제 시트 한도**(비게스트≥한도면 거부·우회 불가·게스트 미소비·기존 grandfathered). InviteLinksCard 사용량 바+초과 시 노션식 업그레이드 프롬프트. JoinView 초과 안내.
5. **오늘 할 일 회사별 분리(`849bf35`·마이그126)** — `personal_tasks.workspace_id` 추가(추가형·기존은 이큐리아 sentinel 백필). TodayTasks/TaskSuggestions가 활성 워크스페이스로 필터/삽입 → 이큐리아/이큐리아2 겹침 해소. **에이전트 개인 기억은 개인 유지**(대표 결정 — 대화 성향 학습이라 회사 무관).
6. **초대 역할 노션식 피커(`40d55b6`)** — 단순 select→역할 카드(소유자/멤버/제한된 멤버, 아이콘+설명+선택 체크·레퍼런스 카피). 시트 소비 역할은 무료 초과 시 '↑플러스' 배지+잠금(게스트는 항상 가능·마이그125 연동). **초대 링크 방식 유지**.

**⚠️ 대표 육안 미검증**: 시트 초과 업그레이드 UI·역할 피커·회사 전환 시 오늘 할 일 분리·에이전트 첫 화면.

---

## 2026-07-31 · 세션41 후속 — /code-review 12건 수정·배포 (`6e27693`)

**무엇/왜:** 세션41 배포분에 `/code-review`(오케스트레이터는 지출한도로 중단·하위 에이전트 A/C/D 완료) → 12건 발견, 실버그 전부 수정. **HIGH급 정합**: ① 부가세 falsy-zero(어제 M2 수정이 "부가세를 0으로 못 바꾸는" 새 버그를 만듦) → 기록 다이얼로그가 이번 달 기존 부가세 프리필로 근본 해결, auto-restore 제거 ② 캘린더 모바일 프로젝트 막대가 `📁`만 표시(이모지가 첫 토큰) → 제목 기준 ③ 자동 동기화가 미래달 명시 기록 시 이번 달 유령 항목 생성 → 미래 기록 가드. **에러 핸들 5종**: 분류 저장·프로젝트 기간·할일 드래그·날짜 편집 실패 시 조용한 스냅백 → 롤백+토스트. **작업 제안 3종**: res.ok 전 json 파싱(504 HTML→SyntaxError)·suggested_due 날짜 정규화(비ISO→null)·안정 _id 키(리마운트/포커스 소실). **근태**: 월별 뷰 배지=오늘 상태 별도 조회(일별 그래프 날짜 오염). **route**: 워크플로우 날짜 KST. tsc0·lint29/0·build0. **⚠️ 대표 육안 미검증**(모바일 캘린더·부가세 0 변경·근태 배지).

---

## 2026-07-30 · 세션41 후반 — 대표 실시간 dogfood 대개편 (마이그 122~124 · 3중 리뷰 · 배포)

**무엇/왜:** 디자인 통일 직후 대표가 라이브로 화면을 보며 연속 지시 — 손익 UX 단순화, 프로젝트 노션식 타임라인, 대시보드 flex식 개편, 근태 그래프, 캘린더 통합, 리사이즈 일습. 각 요청을 잘게 커밋(총 46커밋). 요지: ① 손익=요약/펼침 행·분류 관리(마이그122)·**계산값=결과값(입력→장부 자동 동기화)**·장부연동 제거 ② 프로젝트=목록/상세 타임라인 드래그·일정별 파일(마이그123)·태스크 색(124)·AI 연동=생성 시 ③ 대시보드=한 화면(좌 카드|우 AI)+**AI 작업 제안**(`/api/task-suggestions`) ④ 근태 일별 그래프+사유 기입 ⑤ 캘린더 프로젝트 막대+드래그 ⑥ 사이드바/AI/캔버스 리사이즈.

**리뷰(배포 게이트):** 적대 에이전트 3(데이터·UI·보안) → **HIGH 3**(KST 종일 이벤트 누락 / 자동 동기화가 수동 기록 소실 / 게스트 예산 우회) + **MED 6**(sync 병렬 중복·부가세 소실·파일빼기 no-op·사유기입 unique 충돌·pointercancel 유령 드래그·어시 리마운트) 전부 수정(`9b5eaef`). 보류 LOW = known-issues I19~I24.

**예상이슈 체크:** 각 커밋 tsc0·lint29/0·build0 · 마이그 4개 전부 추가형·멱등·advisors 신규 0 · 자동 동기화는 슬롯별 직렬화로 경합 차단 · 드래그 3종(타임라인2·캘린더)+리사이즈 5종 pointercancel 정리 · task-suggestions=수동 호출만+budget+게스트 403. **⚠️ 대표 육안 미검증 잔여**: 모바일 드래그 실기기·작업 제안 실호출 품질·근태 그래프 실데이터.

---

## 2026-07-30 · 세션41 — 앱 ↔ 랜딩 디자인 통일("밝은 미니멀" 리디자인, 마이그 121)

**무엇/왜:** 대표 지시 — 랜딩 목업(불투명 흰 카드·헤어라인·파스텔 필)과 실제 앱(세션31 글래스모피즘)의 통일감 부재. 가입 개방·가격표 라이브 상태라 신규 유저가 랜딩에서 본 것과 다른 첫 화면을 만나는 문제. 플랜모드(Explore 3+Plan 1) 계획 대표 승인(`gleaming-chasing-tiger.md`). **대표 결정 5:** ①글래스 완전 제거 ②다크모드 유지 ③대시보드 목업 정합 포함 ④손익 필=기본 관리자/오너 전용+공개 토글(오너·관리자 제어) ⑤어시스턴트 축소 유지. **로컬 커밋 4개(`bbe5b00`→`0fdbcb7`)·미푸시(배포는 대표 결정 대기).**

**쪼갠 내용:**
1. **커밋① 토큰 리프레시(`bbe5b00`, globals.css 단일 파일)**: 글래스 토큰 불투명화(.force-light 방식 :root/.dark 승격 — `.glass` 소비처 20파일 무수정으로 솔리드화) · @supports backdrop-filter 블록 삭제(blur 0도 fixed containing block 형성 — 세션32 드로어 사고 근원 제거) · 배경 0.961→0.977(#f7f8fa) · 보더 검정 알파 헤어라인(0/0.08) · primary INK #111 근사 · 신호색 hue 정합(success 150→163 emerald · info 250→263 blue, 라이트/다크) · .app-ambient 평탄화(마운트 div 유지) · .force-light 싱크.
2. **커밋② 셸 디테일(`954d2a5`)**: Sidebar 필 rounded-full→lg 3곳 · FloatingAgentChat FAB `bg-primary/85+blur`→solid 3곳 · SelectionBar solid. 오버레이 스크림(Modal 등) blur는 유지(랜딩 AuthModal과 같은 오버레이 언어 — 표면 재질 아님).
3. **커밋③ 백엔드(`b43f625`)**: 마이그 **121** `workspaces.finance_snapshot_open`(추가형·멱등·MCP 적용+파일·advisors 신규 0) · `/api/finance-snapshot` GET/PATCH(/api/budget 패턴 미러·관리자/오너 재검증) · types 수동 반영.
4. **커밋④ 대시보드 UI(`0fdbcb7`)**: `FinanceSnapshot` 신설 — 이번 달 손익 필 3개(KRW만·financeAgg.aggregateByCurrency 재사용)+최근 기록 5건 · 게이팅(게스트 숨김/관리자 항상/멤버=토글 ON) · dashboard/page.tsx 재배치(공지→필→2열→어시스턴트 26rem 고정) · 설정 '대시보드 손익 공개' 스위치.

**예상이슈 체크:** 각 커밋 tsc0·lint29/0(신규 1건은 기존 disable 선례로 처리)·build0 · 랜딩은 리터럴 하드코딩이라 픽셀 무변화(실소비 토큰=radius·모션뿐, 둘 다 미변경) · 알파 보더 중첩 진해 보이면 solid 근사 폴백 1줄 · info 채도 과하면 0.16 후퇴 1줄 · 마이그121은 DB 적용됨(기본 false라 구 코드에 무해). **⚠️ 대표 육안 미검증(라이트/다크/모바일 3벌·대시보드 필 값↔재무 탭 대조·토글 동작) — 배포(/deploy)는 대표 확인 후.** 후속 기록: app-ambient div 완전 제거·Surface glass variant 정리·랜딩 리터럴 토큰화·rounded-xl>2xl 역전.

---

## 2026-07-30 · 세션40 — 결제/가격 B3 트랙 (요약 — 상세는 HANDOFF 세션40 블록·git)

**무엇/왜:** B3 슬라이스(대표 "에바 아님" 승인). 4티어 가격 확정(Basic ₩0/Standard ₩29k·5석/Pro ₩49k·10석/Premium 문의·초과 ₩4k/석·연 2개월 무료) → 랜딩 가격표 공개(`503166f`) · 푸터 사업자정보(`ea3d5dc`) · X 링크(`185a93f`) · 사이드바 로고 이미지(`a761bd3`). 비용 조사(Claude 단가·agent_usage 실측 — 입력이 원가 93~99%·유저당 월 ~$2) · DB 멀티테넌트 재감사 통과(47테이블 구멍 0). 산출물: 가격 논의자료 PDF·회사소개서 PDF(Desktop/complow). 행정: PortOne PG 검토중·통신판매업 신고번호 발급 대기. 신규 마이그 0.

---

## 2026-07-28 · 세션39 후속 — 트랙2: 대화 요약 압축 (마이그 117)

**무엇/왜:** 대표 지시. HISTORY_WINDOW(10턴) 슬라이딩 윈도우라 오래된 턴이 뚝 잘려 "질문하다 앞 내용 까먹음" — 윈도우 밖으로 밀려난 턴을 Haiku로 압축 요약해 시스템 프롬프트에 주입("멍청 게이지" 반려의 정직한 대안, HANDOFF 트랙2 원안 그대로). **배포 `9f77e33`·롤백=직전 SHA+마이그117 컬럼 drop.**

**쪼갠 내용:** ① 마이그117 `conversations.summary/summary_upto`(증분 커서·추가형) ② `lib/conversationSummary`(주입 블록·턴 직렬화 600자 컷·generateText Haiku 온도0·12줄 규칙·agent_usage 비용 기록 — agentMemoryExtract 동일 골격) ③ 채팅 라우트: 기존 대화 요약 로드→주입 + onFinish 백그라운드 증분 갱신(미요약 오버플로 ≥4턴일 때만·실패 무영향) ④ types 재생성.

**예상이슈 체크:** 요약은 다음 턴부터 반영(백그라운드 지연 — 의도) · summary_upto는 클라 메시지 배열 커서라 히스토리 복원과 수 개 어긋나도 무해 · 매 턴 호출 방지(≥4턴 게이트) · 어시스턴트(대시보드) 대화는 미적용(후속). **⚠️ 실검증=14턴 이상 대화 후 앞 내용 질문(대표 dogfood).**

---

## 2026-07-28 · 세션39 후속 — B2 노션식 초대 개방 (마이그 113~116 + 온보딩·조인·초대 링크·게스트)

**무엇/왜:** 대표 지시 — 노션처럼 링크 초대·호스트/게스트·도입문의 별도 격리·다운로드 자리. 승인 플랜(`prancy-whistling-tome.md`) Phase B·C 실행. 각 단계 tsc0·lint29/0·build0·main-first. **컷오버 스위치(가입 ON)만 대표 대기.**

**쪼갠 내용:**
1. **마이그 113** 초대 링크: `workspace_invites`(192bit 토큰·만료·횟수·회수)+guest role+RPC 4종(발급/회수/미리보기[anon]/수락[FOR UPDATE·강등방지·owner 무주공산 승계·guest→project_members]). RLS 시뮬: 오너발급O·비관리자 거부·프로젝트 없는 guest 거부.
2. **마이그 114** 게스트 RLS: 핵심 헬퍼 2개 guest 제외(fail-closed·45테이블 일괄 차단)+is_project_member/can_access_project로 프로젝트 경로만 개방+shares_workspace_with 재작성+notifications 자기행 완화. 시뮬: 멤버 회귀0·게스트 재무/회의록/DM 0건·초대 프로젝트만 1건.
3. **마이그 115** create_workspace(slug·오너 멤버십·전체방·시드 8종 복제[이큐리아/EQURIA/K-뷰티 치환])+admin_create_workspace(운영자 셋업·owner 링크 반환)+create_group_room 픽스(guest 제외·p_workspace). 종합 시뮬: 생성→복제→타사 0건 격리→owner 승계.
4. **앱 `9b74af8`(B-4a)**: /onboarding(만들기/참여 2카드)·/join/[token](미리보기·구글 로그인 복귀·명시적 수락)·proxy /join·layout 멤버십0→온보딩+role 로드. **types.ts 재생성 → workspace_id가 Insert 타입 필수화**(조건부 스프레드 49곳→필수 명시·전체행 wrap 14곳 원상·withWorkspace 필수 반환) — 🔥 ProjectDetail 일정 redo/복원이 좁은 select라 재삽입 실패했을 실버그 발견·픽스.
5. **앱 `5f430af`(B-4b)**: 설정 'InviteLinksCard'(멤버/게스트·프로젝트 칩·발급 즉시 복사·회수)·구 이메일 초대 라우트 410(컷오버 R2 차단)·GuestGuard+Sidebar 필터+AI 위젯 게스트 미렌더(guestAllowed SSOT).
6. **마이그 116** handle_new_user v2(equria 자동등록 제거 — 가입 ON 전 필수 선행) + **랜딩 푸터 다운로드 자리**(iOS&Android/Mac&Windows '준비 중', 5컬럼).

**예상이슈 체크:**
- 컷오버 순서 안전장치: 116을 앱 배포 뒤에 적용(체크리스트 준수)·구 초대 라우트 선차단. 가입 ON 전까지 신규 유저 진입 불가 상태 유지.
- 게스트 링크 발급은 이미 가능하지만 수락은 구글 인증 가능 계정만(테스트 사용자) — 심사 통과 후 전면 개방.
- 라이브 스모크: /join 미리보기 렌더·미로그인 온보딩 가드 307 확인.
- **패스트팔로우**: Header 워크스페이스 스위처·마이그117(MCP 크리덴셜 회사별)·guest→member 승급 분기.

---

## 2026-07-28 · 세션39 후속 — B1-b 쓰기 격리 Phase A-1~A-4 (클라 59+서버 5+가드 2+presence)

**무엇/왜:** 대표 지시 "격리 공사 시작"(가입 개방·노션식 초대의 선행 차단막). 플랜모드 계획(`prancy-whistling-tome.md`) 승인 후 실행. Explore 2에이전트 조사로 **B1-b가 이미 1/3 진행**(WorkspaceProvider·lib/workspace 헬퍼·서버 32곳 기배선) 확인 → 잔여만 정밀 타격. 4묶음+1커밋 = 5회 main-first 배포(각 tsc0·lint29/0·build0). **최종 `3ec50d8`.**

**쪼갠 내용:**
1. **묶음1 재무 15**(`b26fe62`): CashFlowView 9(**하드코딩 sentinel 상수 제거**)·FinanceView 3·FinanceEntryModal 2·CalcTypeBuilder 1(상수 제거).
2. **묶음2 프로젝트·캘린더·파일 19**(`5eebc56`): ProjectDetail 9·ProjectsView 5·CalendarView 3·FilesView 2. project_tasks는 의도적 제외(부모 EXISTS 상속).
3. **묶음3 채팅·결재·회의록 13**(`d79462d`): 채팅 낙관적 로컬 행 `workspace_id:""`→wsId 정합 포함.
4. **묶음4 에이전트·근태·공지 12**(`5bc71d2`): 클라 59/59 완료.
5. **A-2·A-3**(`3ec50d8`): OCR 3라우트 배선 + **mcp/servers 2라우트 admin(service_role) 멤버십 가드**(RLS 우회 구멍) + **presence 채널 워크스페이스 분리**(RLS 없는 유일한 실질 격리 위험).

**A-4 검증(A-5 게이트):** ① 코드 grep 감사 — 스코프 테이블 insert/upsert 미명시 0건(오탐 2건=변수 행빌더, 육안 확인) ② DB 인벤토리 — sentinel DEFAULT 보유 **정확히 45개 테이블**(전부 NOT NULL) ③ **drop default 시뮬** — DO 루프 45개 무오류 실행→롤백 정상 ④ RPC 경유 쓰기(064 attendance_viewers 등) workspace 명시 확인·cash_transfers 앱 쓰기 없음.

**예상이슈 체크:**
- 명시값=현 DEFAULT와 동일이라 **동작 불변**·provider null(이론적)이면 스프레드 생략→DEFAULT 폴백.
- useCallback 의존성 배열 5곳에 wsId 추가(신규 lint 경고 0 유지).
- ~~다음 = A-5~~ → **✅ A-5 실행 완료(대표 승인·2026-07-28)**: 마이그 112 적용 — 강제 ON 스모크(미명시 insert 거부·명시 insert 성공, 롤백 트랜잭션) 통과. Phase B 마이그는 113~117로 시프트.

---

## 2026-07-28 · 세션39 후속 — 가입=구글 전용 전환 + 오너 '구성원 초대'

**무엇/왜:** 대표 결정 — "공용비번 가입 폼 빼고, 다들 구글로 로그인하게". 함정 보고: 가입 OFF 상태라 탈퇴 후 구글 재가입 경로가 없음 → **초대 방식**(오너가 구글 이메일 사전등록)으로 해결. 가입 차단 유지=외부인 차단 그대로. DDL 0. tsc0·lint29/0·build0·main-first. **배포 `35813ed`·롤백 `d29f1fb`.**

**쪼갠 내용:**
1. **`/api/members/invite` 신설**(오너 게이트=members/[id] DELETE 동일 패턴) — `admin.createUser`(실제 Gmail·email_confirm·이름) → handle_new_user 트리거가 프로필+멤버 자동 등록 → 당사자는 'Google로 계속하기'만 누르면 **이메일 일치 자동 연결·로그인**(비밀번호 없는 계정). 가입 OFF여도 admin API라 동작.
2. **설정(오너) '구성원 초대' 카드** — 이름+구글 이메일 입력. 테스트 모드 동안 구글 콘솔 '테스트 사용자' 추가 필요 안내 포함.
3. **AuthForm 가입 모드=구글 전용** — 폼 제거·안내문("관리자가 등록했다면 위 버튼으로 바로")·signup 분기/`signupAction` import 정리(파일은 유지=롤백 용이). 로그인 모드=이름+비밀번호 폴백 유지(구글 미연결 기존 계정용).
4. **콘솔 설정 완료(대표 실행·내가 서버 검증)** — provider ON 후 authorize 302→accounts.google.com 확인("missing OAuth secret" 중간 상태도 프로브로 잡아 안내). Redirect URLs 2개 확인.

**예상이슈 체크:**
- **잠금 방지 순서**: 기존 멤버 강제 탈퇴(대표 의사)는 ①대표 구글 연결+재로그인 검증 ②직원 1명 초대 실검증 **후에** 설정 UI에서 직접. 삭제=개인 데이터 연쇄(파괴적)라 자동화 안 함.
- 테스트 모드 100명 제한: 초대해도 콘솔 테스트 사용자 미등록이면 구글이 차단 → 게시(브랜드 검증) 통과 시 해제.
- 초대 계정은 비밀번호가 없어 이름+비밀번호 폴백 로그인 불가(구글 전용) — 의도된 동작.
- 기존 멤버 비밀번호=공용비번(가입 시 동일값 설정) → 전원 구글 전환·정리 후 공용비번 이슈 자연 소멸.
- ~~미검증(대표): 초대→구글 로그인 실왕복~~ → **✅ 실검증 성공(2026-07-28)**: 대표 'Google로 계속하기' 실로그인 확인. 과정: ① Supabase에 잘못된 secret → auth 로그 `invalid_client` 진단 → 로컬 값(구글 token 엔드포인트 검증)으로 교정 ② 마이페이지 연결 단계 대신 **오너 이메일 직접 교체**(`c6817c63`: 합성→`chowhiwon99@gmail.com`, auth.users/identities/profiles 3곳·롤백=역방향) → 이메일 일치 자동 연결로 구글 직행. 직원들도 이메일 교체 방식 권장(데이터 보존).

---

## 2026-07-28 · 세션39 후속 — 구글 로그인 1차 (버튼 + 마이페이지 계정 연결)

**무엇/왜:** 대표 결정 — 소셜 로그인은 구글 먼저(애플·카카오=B6 포장 단계), B1-b 전이므로 **"로그인 수단 추가"와 "가입 개방"을 분리**(개방은 B1-b 후). DDL 0·RLS 변경 0·전부 추가형. tsc0·lint29/0(신규0)·build0·main-first. **배포 `d29f1fb`·롤백 `7f12fc6`.** ⚠️ 실동작은 대표 콘솔 설정 5단계 후(HANDOFF '구글 로그인 활성화').

**쪼갠 내용:**
1. **SocialButtons 개편** — 세션39에 만들어놓고 미사용이던 3버튼(구글·애플·카카오)을 구글 단독 'Google로 계속하기'로. `AuthForm` 상단에 연결 → 로그인·가입 페이지+랜딩 모달 3곳 동시 반영(공용화 덕).
2. **콜백 라우트 보강**(`/auth/callback`) — Supabase 에러 파라미터 처리(`signup_disabled`→"등록된 멤버가 없어요" 안내·`access_denied`→취소), `next` 검증(`/`시작·`//`차단 = open redirect 방지), 연결 흐름 에러는 `?link_error=`로 원래 화면 전달. 로그인 페이지에 `?error=` 문구 매핑 표시.
3. **🔥 잠복 버그 픽스** — proxy OPEN_PATHS에 `/auth/callback` 추가. 콜백은 code 교환 "전"이라 세션이 없는데 프록시가 /login으로 튕겨 **OAuth가 구조적으로 불가능**했음(세션39 sitemap 차단과 동류·라우트 미사용이라 미발현).
4. **마이페이지 '로그인 방법' 카드**(`SocialAccountsCard`) — 멤버 계정 이메일이 합성 주소(`u<hex>@equria.local`)라 구글 자동 연결 불가 → `linkIdentity` 수동 연결/해제. supabase-js API(context7 문서 검증). 마지막 로그인 수단 해제 금지·해제 2단계 확인·`identity_already_exists` 안내.

**예상이슈 체크:**
- **보안 핵심**: `handle_new_user`(043)가 OAuth 신규 계정도 equria에 자동 등록 → **Supabase '신규가입 허용' OFF가 provider ON보다 선행 필수**(HANDOFF에 순서 명시). OFF해도 공용비번 가입은 `admin.createUser` 경유라 안 깨짐(코드 확인).
- 미등록 구글 계정 로그인 시도 → `signup_disabled` → 로그인 화면 안내문("공용비번 가입 후 마이페이지에서 연결") 정상 유도.
- lint 신규 1건(`set-state-in-effect`) → McpCredentialsCard와 동일한 disable 주석 규약으로 처리(29/0 유지).
- /login이 searchParams로 dynamic 전환(기존 static) — proxy가 어차피 전 경로 실행이라 영향 없음.
- **미검증(대표)**: 실 OAuth 왕복(연결→로그아웃→구글 재로그인)·미등록 계정 차단 확인. 콘솔 설정 전엔 버튼 클릭 시 "시작하지 못했어요" 에러가 정상.

---

## 2026-07-28 · 세션39 후속 — 파비콘 둥근 사각형 새 로고 교체

**무엇/왜:** 대표 리포트 — 새 로고(CP 마크)가 탭에서 각진 흰 정사각형으로 보임, "둥근 사각형으로" 요청. 1차=검정 둥근 사각형+흰 마크(`94775bf`) → **대표 피드백 "모양 OK, 색 반전" → 흰 둥근 사각형+검정 마크(#111) 확정(`7f12fc6`)**. + **favicon.ico(16/32/48) 신설** — 구글 등 검색엔진·외부 서비스가 `/favicon.ico`를 직접 가져가는 경우도 동일 아이콘 노출("다른 곳에서 검색해도 똑같이" 요청 대응). tsc0·lint29/0(신규0)·build0·main-first ×2. **배포 `7f12fc6`·롤백 `e4f2a1f`.**

- **`src/app/icon.png` 교체(512px)**: `public/brand/logo-mark.png`(흑마크·흰배경)에서 마크를 알파 추출→흰색 반전→검정 라운드 스퀘어(radius 24%) 위 66% 크기 중앙 배치. 4배 슈퍼샘플링→다운스케일로 모서리 안티앨리어싱. 32px 축소 시뮬레이션으로 가독 확인.
- **`src/app/apple-icon.png` 신설(180px)**: iOS 홈화면용 풀블리드 검정 정사각(iOS가 자체 라운딩·투명 모서리 금지 규칙 대응). Next 파일 컨벤션이라 layout 메타 코드 변경 0.

**예상이슈 체크:**
- **프록시 안 막힘 확인**: `proxy.ts` matcher가 `.png` 종료 경로 제외 → `/icon.png`·`/apple-icon.png` 공개 접근 OK(세션39 sitemap 차단 같은 문제 없음).
- **구글 검색결과의 옛 'C' 원형 아이콘 = 구글 파비콘 캐시** — 코드 문제 아님, 재크롤 시 자동 갱신(수일~수주). Search Console 색인 재요청이 앞당길 수 있음.
- 브라우저 탭도 캐시 강함 — 대표 확인 시 하드 리프레시(⌘⇧R) 필요할 수 있음.
- 다크 탭바에서 검정 배경 대비 낮지만 흰 마크가 가독 담당(구 검정 원 아이콘과 동일 조건). 흰 배경+검정 마크 반전 원하면 스크립트 한 줄 변경으로 즉시 가능.

---

## 2026-07-27~28 · 세션39(이어서) — 랜딩 완성(공개→노션 GNB·모달→잔디식 콘텐츠) + SEO/서치콘솔

**무엇/왜:** 대표 지시 연속 반영 — 랜딩 공개·새 로고·모노톤 미니멀·잔디식 모션 → **구독제 피벗**(가격은 토큰 원가 측정 후, UX 틀만) → 노션 GNB/로그인 모달 → 잔디식 본문·푸터 → 한글 '컴플로우' 검색 미노출 대응. 각 tsc0·lint29/0(신규0)·build0·main-first. **프로덕션 `e4f2a1f`.**

- **노션식 GNB+CTA(`d1a51cc`)**: 로그인+"Complow 무료로 사용하기", 60px·중앙 nav. → **GNB·본문 완전 분리+모달(`4441691`)**: LandingHeader(전체폭)·AuthModal(블러+카드·ESC/배경 닫기·모달 내 로그인↔가입 전환)·**AuthForm 공용화**(/login·/signup과 로직 단일, 가입 약관 죽은 # 링크 픽스).
- **잔디식 콘텐츠(`50bdaf7`)**: 숫자 스트립(8종·10+·100%)·AI 심화 3블록·기능 심화 4블록(손익/협업/결재/연동 미니 데모)·보안 4항목·**요금별 기능 비교표**(대표 요청)·FAQ 5(details)·**다크 푸터 4컬럼**(잔디 레퍼런스, 없는 하위 페이지='준비 중'). 소셜 아이콘(`d534850`→인스타 @complow `1f7dcf7`, X=임시 x.com).
- **로그인 상태 랜딩 열람(`d5b8891`)**: proxy 루트 예외(대표 결정) — GNB '워크스페이스 열기'·본문 CTA는 로그인 시 대시보드로.
- **SEO(`e29a4f0`)**: 원인=①구글이 랜딩 공개 전 /login을 색인 ②사이트에 한글 '컴플로우' 부재 ③서촌 카페 키워드 선점. 조치=메타 한글 병기·JSON-LD(alternateName 컴플로우)·본문 15회·robots.ts/sitemap.ts·(auth) noindex. **픽스(`e4f2a1f`)**: sitemap/robots가 프록시 로그인 리다이렉트에 막힘("Sitemap이 HTML입니다") → OPEN_PATHS 추가.
- **Search Console(대표 실행)**: 도메인 인증 ✅(가비아 TXT 추가, 기존 구토큰과 별개 신규 토큰) · 루트 색인 생성 요청 ✅ · 사이트맵=구글 자동 재시도 대기.

**예상이슈 체크:**
- '무료로 사용하기'는 아직 공용비번 게이트라 외부인 실가입 불가 — 실효는 B2(가입 개방) 공사 후. UX 틀로는 완성.
- 공개 표면 확대(루트 상시 공개·OPEN_PATHS 5개) — 앱 경로 보호는 불변(미인증→/login 리다이렉트 회귀 없음, robots도 앱 경로 Disallow).
- 한글 '컴플로우' 단독 상위 노출은 수주 소요(카페 선점) — 조합 검색·외부 링크(인스타 프로필에 사이트 링크 권장)부터.
- 푸터 미기재(대표 제공 대기): 사업자등록번호·주소·통신판매업신고번호·X 핸들.

---

## 2026-07-27 · 세션39 — 손익 탭 통일(대표 결정) + DateInput 전면 정리 + 분류 연동

**무엇/왜:** 대표 "요약이랑 현금흐름이 왜 통일이 안 되냐(손익은 하나여야)" → 선택지 제시 후 **"탭 합쳐 완전 통일" 결정**. + 날짜 UI "연도. 월. 일." 전면 정리 지시. 마이그109 1개(추가형). 각 tsc0·lint29/0(신규0)·build0·main-first. **배포 `571855a`·롤백 `c9bd6fe`.**

- **손익 탭 통일(`90051bd`)**: 비용·매출 탭 4→3(**손익/내역/세금계산서**, 기본=손익). 손익=계산기(CashFlowView) 단독·헤더 KPI 매출·비용·순이익·가용현금 한 벌(유일한 헤드라인). 구 요약의 분석(환산·합계·추세·분류분해)은 **내역 탭 상단**으로(장부 기록의 집계로 명확화). 요약 전용 코드(pnl 카드·스트립·중복 fetch) 제거. 기간 컨트롤은 손익 탭에서 invisible(자리 유지).
- **공용 DateInput(`c1bc178`)**: 빈 날짜칸의 네이티브 "연도. 월. 일." 노출 제거 — 투명 처리+"날짜 선택" 오버레이·포커스 시 원복·다크 color-scheme. **12곳 교체**(프로젝트·오늘할일·지출결의·휴가·비용매출·세금계산서·캘린더·전자결재) + 종료일 min=시작일 연결. 회의노트 인라인 2곳은 유지.
- **장부 연동 분류 필터(`571855a`·마이그109)**: `cash_accounts.ledger_category`(널=전체). ledger 슬롯이 특정 분류만 자동 합산(예: "식비" 슬롯=내역 식비 이번달 합계). 동기화가 분류별 소계(byCat) 집계·그리드에 '분류' 셀렉트. 어드바이저 생략 사유=정책/함수 무변경 널러블 컬럼.
- **내역 탭 KPI 카드 제거(`25cd01f`)**: 대표 "두 화면 숫자 달라 헷갈림" — 내역 탭 상단 큰 매출/지출/순수익 카드·원화환산 카드를 "이 기간 기록 합계" 한 줄로 축소('순수익' 표기 제거). 손익처럼 보이는 숫자는 손익 탭이 유일. 전월대비 배지·미사용 코드 정리.
- **⚠️ 실사용 육안 미검증=대표 dogfood.** 회귀 주의 지점: 내역 탭이 길어짐(분석+표)·손익 탭 진입 시 기간 컨트롤 안 보이는 게 의도된 동작. [안내] 손익 탭 캔버스의 큰 숫자(예: 매출 3.6억)는 대표가 직접 입력한 계획 슬롯 — 실제 기록만 반영하려면 수동 슬롯 삭제 후 '장부 연동' 슬롯만 사용.

### 세션39 이어서 — 재무 완전 통합(원장 SSOT) ★대개편
**무엇/왜:** 대표 최종 취지 "다 연동되어야 한다"(캔버스에서 기록→전체 반영, 세금계산서→매출, 어떤 업종이든). 계획 승인(`~/.claude/plans/structured-popping-raven.md`) 후 실행. Explore·Plan 에이전트로 설계 검증. **배포 `e186e8f`·마이그 001~111·롤백 `1aa2e44`(코드)/111 기록 delete(데이터).**

- **모델**: `finance_entries` = 유일한 원장. 슬롯 = 계정(+기록 입력기) — **휴면 FK `account_id`(마이그078) 재활용**. 수식(개수×단가·채널)은 '계산 도우미'로 강등(기록 프리필). 내역·추세·분류·손익 전부 같은 테이블 파생 = 정의상 일치.
- **이중 집계 해결(핵심)**: 이번 달 기록을 슬롯 귀속(claimed: account_id+구분·통화 일치)/미귀속(잔여)로 **분할** — ledger 슬롯은 잔여만 합산 → **pool = Σ귀속+잔여 = 장부 전체**(항등식·구조적 불가). 통화불일치·슬롯삭제·kind변경은 잔여로 흡수(소실 없음).
- **커밋**: `00c98ee` 마이그110(source 'invoice') · `511b311` 파생 전환+기록 UI(2단 집계·updateSlot amount 축소[reserve만]·recordEntry+RecordEntryDialog·그리드/캔버스 [기록] 버튼·"이번 달 금액" 헤더) · `e186e8f` 세금계산서 확정→매출 자동 기록(조건부 UPDATE 가드·롤백·Undo·FinanceView reload 리스너).
- **이관(마이그111·코드 배포 후 실행)**: 슬롯 5건·4.133억 → 장부 기록 이관(`metadata.slot_migration` 멱등 가드). **검증**: 사전 5건/4.133억=사후 일치 · 재실행 0건 · 슬롯별 귀속 합계가 원금액과 정확 일치(네이버 앰플 1.8925억 등 5/5) · 이번 달 KRW 장부 매출 3.595억/비용 5,380만=캔버스와 동일.
- **한계(주석 명시)**: xlsx 내보내기 계산형 행은 수식 유지(의미 혼재·후속 개선) · invoice 기록 모달 재편집 시 total 재계산 한계 · 슬롯 금액은 "이번 달" 파생이라 매월 0 시작(과거는 내역·추세 보존).
- **⚠️ 대표 육안 미검증** — 확인 포인트: 손익 탭 숫자 유지(이관 후 동일값), [기록] 버튼 플로우, 내역에 이관 5건 노출, 세금계산서 "확정·장부 기록".

- **슬롯 장부 분류(`a71bfb6`)**: 대표 제안 — 캔버스/표에 '분류' 자유입력(+datalist 제안=기본+실사용 분류, 구분별). `recordEntry`가 슬롯 분류로 기록(비우면 슬롯명) → 내역·분류 차트가 회사 분류 체계로 집계.
- **급여 템플릿 8종(`8805fc9`)**: 대표 "급여도 비용" — 계산 유형에 '급여' 카테고리 신설(정규직 월급·총비용[퇴직적립]·연봉제·시급제·알바 주휴·일용직·프리랜서 3.3%·성과급). 2026 요율 조사 반영(사업주 약 10.8% 안내·수식 하드코딩 없이 %칸). DDL 0.
- **급여 요율 자동 적용(`f4f62e2`)**: 대표 지적 "10.8% 알아서 안 들어감" — CalcField에 def(기본값) 도입, 유형 배정 시 field_values 자동 채움(기존값 안 덮음)·빌더 미리보기 프리필·기존 '정규직 월급' 유형 SQL 픽스. 기본값=사업주 10.8%·퇴직 8.33%·주휴 20%(법정 요율만).
- **슬롯 ₩0 혼동 해소(`d6d02d6`)**: 캔버스 금액에 "이번 달" 라벨+0이면 흐리게, 기록 전이면 [기록] 버튼 프라이머리 강조(대표 "왜 ₩0?" — 장부 파생 의미가 화면에서 안 읽히던 것).
- **수식 자연어 플로우(`cf6bcc5`)**: 암묵 곱셈("급여 3.3%"→급여×0.033·토큰0·"해석:" 줄로 투명화) + 파서 실패 시+문장 감지 시(`890f004` — 암묵곱셈이 문장을 성공 처리해 버튼 안 뜨던 것) AI 변환 버튼(신규 /api/finance/formula·Haiku 원샷·파서가 최종 검증).
- **계산 유형 '직접 만들기' 재설계(`b51a508`)**: 대표 "직접 만들 때 불편"+"AI 토큰 대신 그냥 타이핑" → 신규 `lib/formulaParse`(순수 파서·토큰 0). 수식 한 줄 타이핑=끝(쓴 이름이 곧 칸·%칸은 이름%·친절한 한국어 에러·편집 왕복). 실값 라이브 미리보기(가짜 예시 대체). 스텝 조립 UI 전부 삭제. 파서 실검증(패턴 7+엣지+에러 5, node 실행).

**다음 후보**: 대표 육안 확인(SSOT 전환·기록 플로우·수식 타이핑) · 브랜드 검증 준비물(/privacy·/terms) · 트랙2 대화 요약 압축 · Slack/PayPal 크리덴셜(대표).

---

## 2026-07-24 · 세션38(이어서) — UI 다이어트 + AI 출력 스타일 (대표 피드백 일괄)

**무엇/왜:** 트랙1 검증 직후 대표가 화면 5곳 피드백("글이 많으니 디자인이 구려진다"·"AI 티 나는 기호"). 마이그 불필요·전부 추가형/축약. tsc0·lint29/0(신규0)·build0·main-first. **배포 `270c598`·롤백 `6480720`.**

- **AI 출력 스타일(`218b73d`)**: 신규 `lib/claude/style.ts` OUTPUT_STYLE_RULE — 불릿/굵게/헤딩/이모지 절제·군더더기 서두 금지. 에이전트 채팅+어시스턴트 시스템 프롬프트 공통 주입(메일·회의노트 assist 철학의 전면 확장).
- **UI 다이어트(`270c598`)**: ①MCP 앱 크리덴셜 — 한 줄 접기+①콘솔②URI③값 3단계 ②워크플로우 최근 실행 — 기본 3개+전체 보기, 'N단계' 제거 ③결재 날짜 오늘 기본값('연도.월.일.' 제거)+배너 축약 ④파일함 Google Drive 탭 제거 ⑤현금흐름 탭에 '실제 장부' 참고 스트립(연동 v1).
- **재무 심층 연동 v2 = 장부 연동 슬롯(배포 `7b6fe2a`·마이그108)**: 대표 "요약이랑 현금흐름이랑 안 맞는다" → `item_type='ledger'` 슬롯 도입. '장부 연동' 버튼이 장부 매출/비용(이번 달) 슬롯 생성, `load()`가 열 때마다 이번 달 finance_entries 합계(통화별)로 amount 동기화(요약 탭과 같은 집계). 그리드 '장부 자동' 배지+금액 읽기전용·캔버스 읽기전용·해제=삭제. 마이그108=item_type CHECK에 'ledger' 추가(제약 재생성·적용·drift0).
- **요약 탭 손익 계산기 표시(배포 `bbd7181`)**: 대표 정정 — 연동 방향은 "계산기에서 편집→요약 반영". 요약 탭 최상단 '손익 계산기' 카드(매출·비용·순이익, 통화별·진입마다 재조회) + '현금흐름 탭에서 편집→' 바로가기 + 장부 KPI에 '실제 장부·내역 기준' 라벨. 어제 만든 장부→계산기 슬롯(마이그108)과 합쳐 양방향 대조 완성.
- **디테일 4종(배포 `022bf5e`)**: 워크플로우 실행시각 "7월 16일 23:54 · 52초" 포맷 / 이모티콘 제거(🔒→Lock 아이콘·✅⚠️❌→lucide·파일 라벨 텍스트만, 채팅 반응 피커는 유지) / 회의노트 그리드↔표 토글 오른쪽 고정(정렬 셀렉트를 왼쪽으로) / 설정 구성원 본인 행 삭제버튼 placeholder 정렬.

---

## 2026-07-24 · 세션38 — 트랙1: MCP 정적 OAuth 클라이언트 인프라 (구글·Slack·PayPal 연결)

**무엇/왜:** 세션37 분석의 트랙1 실행. 구글(Gmail·Cal·Drive)·Slack·PayPal의 원격 MCP는 **DCR 미지원** → 대표가 각 서비스에 OAuth 앱을 등록해 받은 client_id/secret을 넣어야 직원이 자기 계정으로 연결 가능. 마이그107 1개(추가형). tsc0·lint29/0(신규0)·build0·main-first. **배포 `a8b832f`·롤백 `9b17bff`.** ⚠️ 대표 OAuth 앱 등록 선행 필요(아래 액션) · 실사용 육안 미검증.

- **핵심 근거(@ai-sdk/mcp 1.0.45 소스 직접 검증)**: `auth()`는 `clientInformation()`이 값을 반환하면 **DCR(동적 등록)을 건너뛴다**(index.js ~1021) → 정적 client를 seed하면 DCR 재등록이 아예 안 일어나 정적값이 안 덮임. 토큰 교환 auth 메서드는 우리 `clientMetadata`가 아니라 **secret 유무로 자동 결정**(`selectClientAuthMethod` ~748, secret 있으면 `client_secret_post`) → **구글 confidential 클라이언트도 코어 수정 없이 작동**. 스코프는 `provider.clientMetadata.scope`에서 옴(~1094). 구글식 `access_type=offline`은 SDK가 안 붙임(`offline_access`만 처리) → 연결 라우트에서 주입.
- **엔진(커밋 `0f3a0b9`)**: 마이그107(`mcp_oauth_clients.is_static` + `mcp_user_connections.custom_url`, 추가형·멱등, Supabase MCP 적용·drift0) · `oauth.ts` 가드: is_static이면 redirect_uri 자가치유(마이그102) 건너뛰어 정적값 무효화 방지 + `persistClientInformation`이 정적 행을 DCR 결과로 안 덮음 + `credKey()`로 credentialKey 해석(구글 3형제=한 앱 공유) + `clientMetadataFor`에 커넥터별 `oauthScope` · connect 라우트가 인가 URL에 커넥터별 `authorizationParams`(구글 `access_type=offline·prompt=consent`=refresh_token) 주입 · `lib/mcp.ts` Connector 타입 확장(requiresAppCredential·credentialKey·oauthScope·authorizationParams·customUrl)+`credentialKeyFor()`.
- **오너 크리덴셜(커밋 `8e6ed14`)**: 신규 `/api/mcp/oauth-clients`(오너 게이트, `workspaces.owner_id`) GET(그룹 현황·`configured`는 전 로그인유저=McpView 게이팅용, client_id 유무는 오너만·**secret 값 절대 미반환**)·POST(등록·secret 미입력 시 기존값 보존, `is_static=true` 저장)·DELETE · 자체 완결 `McpCredentialsCard`(그룹별 client_id/secret 입력 + **콘솔에 등록할 리디렉션 URI 복사** + 콘솔 링크·스코프 안내) · SettingsView 오너일 때만 렌더.
- **카탈로그+게이팅(커밋 `a8b832f`)**: 카탈로그에 **Gmail·Google캘린더·Google드라이브**(credentialKey `google` 공유·엔드포인트 `*mcp.googleapis.com/mcp/v1`·스코프 명시)·**Slack**(`mcp.slack.com/mcp`)·**PayPal**(`mcp.paypal.com/mcp`)을 available·oauth·requiresAppCredential로 추가(웹조사 실검증 2026-07-24). McpView: requiresAppCredential인데 대표 크리덴셜 미설정이면 **'관리자 설정 필요'** 표시(깨진 연결 방지)·`/api/mcp/oauth-clients`로 설정 여부 조회.
- **⚠️ 대표 액션(값 주면 배선 끝·병렬 가능)**: ①⭐구글 — Google Cloud Console(`console.cloud.google.com/apis/credentials`)에서 OAuth 동의화면 구성(스코프: gmail.readonly·gmail.compose·calendar.events.readonly·calendar.events.freebusy·drive.readonly·drive.file) + '웹 애플리케이션' OAuth 클라이언트 생성 → **리디렉션 URI 3개**(`https://complow.kr/api/mcp/oauth/google-gmail/callback`·`.../google-calendar/callback`·`.../google-drive/callback`) 등록 → 앱 '게시(프로덕션)' 전환(테스트 모드=refresh 7일 만료) → 설정 'MCP 앱 크리덴셜'에 client_id/secret 입력. ②Slack — api.slack.com 앱+리디렉션 `.../oauth/slack/callback`. ③PayPal — developer.paypal.com 앱+리디렉션 `.../oauth/paypal/callback`.
- **남긴 것(fast-follow)**: **Zapier 커스텀 URL** — URL 자체에 시크릿이 포함돼 저장 스키마(암호화·`auth_method` 제약·nullable token) 별도 조정 필요 → 준비중 유지(customUrl 타입·custom_url 컬럼은 미리 넣어둠). 구글 등 OAuth 정적경로 완결 우선.

- **🟢 실연결 검증 완료(2026-07-24 · 대표 dogfood):** 대표가 Google Cloud OAuth 앱 등록(client_id/secret) → 설정 'MCP 앱 크리덴셜' 입력 → **Gmail·Google캘린더 실제 연결 성공.** access+**refresh token** 저장 확인(access_type=offline 주입 작동), **Gmail 13도구·캘린더 9도구 디스커버리 성공**(토큰이 `*mcp.googleapis.com` 실인증) = 정적 client→DCR 건너뜀 인프라 100% 검증. 계정 `complow@complow.kr`·External+Testing(테스트 사용자 4명).
- **린 구성 확정(배포 `f99edcc`):** 대표 결정 "100명 넘겨도 돈 드는 건 안 켠다" → Gmail=compose만·Drive 커넥터 제거·네이티브 메일 탭 숨김(제한스코프 CASA 비용 회피). 남은 스코프 2개 다 '민감'→ 게시 시 가벼운 브랜드 검증만.
- **[전략 정리]** 멀티테넌트 자동 연동 = **전역 앱 1개 + 사용자별 토큰 격리**(OAuth 앱 공유해도 데이터 격리 유지, Zapier/Notion 방식)가 정답. "회사별 앱 등록"은 자동연동을 깨므로 반려. 자가서비스는 **브랜드 검증 1회**로 켜짐(런치 전 병렬 처리).

**다음 후보**: **브랜드 검증 준비물**(`/privacy`·`/terms` 페이지·도메인 인증·로고·스코프 사유) → 자가서비스 자동 연동 · 에이전트에 Gmail 바인딩해 실사용 · Slack/PayPal 크리덴셜(대표) · Zapier 커스텀URL · 트랙2 대화 요약 압축.

---

## 2026-07-24 · 세션37(이어서3) — 자동기억 품질 고도화 + Exa 활성화 + MCP 연결 로드맵 분석

**무엇/왜:** 대표 dogfood 연속. 마이그 불필요(106 재사용). 각 tsc0·lint29/0(신규0)·build0·main-first.

- **자동기억 품질 고도화(배포 `4e8fb6f`)**: 자동추출이 ① **중요도 자동 매김**(`memoryExtractionSchema`에 importance 1~3 추가 → 핵심 규칙=높음, insert 반영 → 중요도순 주입 즉시 반영) ② **근접중복 차단**(`dedupeCandidates`를 정확일치→`similarMemory`=포함관계+토큰 자카드≥0.6로 확장, 기존·이번 후보 양쪽 대비 → "보고서는 표로" ~ "보고서는 항상 표로 정리" 사실상 중복 안 쌓임). `clampImportance` 방어. 추출 프롬프트 중요도 규칙 추가.
- **Exa 커넥터 활성화(배포 `58b6783`)**: 웹조사 확인 — Exa 원격 MCP(`mcp.exa.ai/mcp`)가 `Authorization: Bearer <API키>` 지원 → 기존 bearer 프리셋과 일치. '인증 방식 상이'로 준비중이던 것 → **available 전환**. 직원 각자 Exa 키로 원클릭. 대표 등록 불필요.
- **MCP 연결 로드맵 분석(코드 변경 없음·조사)**: 대표 "구글 MCP 연결"·"준비중 왜 안 되나" 질문에 실제 웹조사로 답. **핵심 발견**: ① 구글은 이제 **공식 원격 MCP 있음**(내 이전 "없다" 정정) — DCR 없어 대표 OAuth 앱 등록 필요 ② 우리 OAuth가 `mcp_oauth_clients`에서 client 정보 읽어 **정적 client_id/secret 주입 시 DCR 건너뛰고 작동**(Slack·구글·PayPal 공통 경로) ③ Exa=bearer(즉시), Zapier=계정별 URL(커스텀URL 필요), Vercel=승인게이트, Figma=사실상 막힘(DCR 403·승인클라 전용). → 상세·대표 액션·개발 계획 = **HANDOFF "다음 세션 착수" 블록**.

**다음(HANDOFF 착수 블록):** 트랙1=MCP 정적 클라이언트 인프라(구글·Slack·PayPal, 대표 OAuth 앱 등록 선행) · 트랙2=대화 요약 압축. context 정리 후 다음 세션 진행.

---

## 2026-07-24 · 세션37(이어서2) — 로딩바 + 기억 정리(AI·마이그106) + 대화 히스토리 3종

**무엇/왜:** 대표 dogfood 연속 피드백. 마이그106 1개(추가형). 각 tsc0·lint29/0(신규0)·build0·main-first.

- **답변 로딩 인디케이터(배포 `a419dba`)**: 유튜브식 얇은 상단 바(`equria-loadbar`, 헤더 밑 2px 좌→우, submitted/streaming 중, 끝나면 사라짐). reduced-motion=정적 폴백. '생각 중…' 텍스트 대체.
- **기억 정리(AI·배포 `fe810cb`, 마이그106)**: 대표 지적 "기억 쌓이면 지저분(쓰레기·중복·분류 뭉개짐)". `agent_memories.importance`(1~3) 추가 + 채팅 주입을 중요도순으로. '기억 관리'에 **'AI로 정리'**(수동) → 신규 `/memory/organize`가 Sonnet으로 병합·중복제거·쓰레기제거·재분류·중요도 매김 제안(읽기전용) → **before→after 미리보기** → `/organize/apply`가 새목록 insert + 기존 soft-delete(휴지통 복구·RLS 본인만). 패널을 **종류별 그룹+중요도 배지**로 재구성. 자동추출·suggest 프롬프트에 명령문·일회성 제외 명시(쓰레기 원천 차단). ⚠️마이그106 Supabase MCP 적용·drift0.
- **대화 히스토리 3종(배포 `4e0ba45`)**: 대표 지적 "위젯 닫으면 대화 사라져 앞 내용·의도 파악 끊김". 실은 DB(conversations/messages)에 다 저장돼 있고 화면 복원만 없던 것 → 불러오기 배선(DashboardAssistant 검증 패턴 이식). ① **재오픈 복원**(열 때 이어지던 대화 메시지 하이드레이션) ② **새로고침 유지**(conversationIdByAgent 메모리→localStorage 영속) ③ **과거 대화 열람**(신규 `/agents/[id]/conversations` 목록·`/[convId]` 메시지·DELETE, 소유권 가드 + 위젯 하단 '새 대화'·'대화 목록' 버튼·목록 패널). 마이그 불필요.
- **[객관적 판단 기록]** 대표 아이디어 "대화량↑=AI 멍청 게이지"는 **반려**: 채팅 라우트가 최근 10개(`HISTORY_WINDOW`)만 보내는 슬라이딩 윈도우+1M 컨텍스트라, 게이지는 "차오름" 오해를 줌. 실제 문제는 **망각(truncation)**·불연속. 정직한 대안=히스토리 복원(이번 배포)·요약 압축(다음)·넛지. 대표 승인=히스토리 먼저.

**다음 후보**: 요약 압축(길어져도 연속성)·정직한 기억 경계 표시 · 자동기억 품질 고도화(유사 병합) · 공용비번 강화·Gmail/Drive 복구(대표) · B1-b Step1b.

---

## 2026-07-24 · 세션37(이어서) — 품질 루프 강화: 채팅 원클릭 기억 + 제작 되물음 인터뷰

**무엇/왜:** 자동기억 v2 배포 후 대표 요청 2건. ① 채팅 중 좋은 답변을 복사·붙여넣기 없이 원클릭으로 기억 저장 ② 에이전트 제작 시 더 디테일한 기획을 요구("정확한 기획 → 정확한 결과"). 계획=`~/.claude/plans/reactive-napping-wall.md`(대표 승인). 마이그 불필요(agent_memories/agent_versions 재사용), 전부 추가형. 신규 AI 라우트는 checkBudget+agent_usage+withWorkspace 기존 패턴 준수.

- **Part A — 채팅 원클릭 '기억하기'**: 답변 버블 hover에 복사 옆 '기억하기'(Brain) → **Haiku가 그 답변에서 오래 쓸 한 줄 추출**(신규 `/api/agents/[id]/memory/suggest`, `memorySuggestSchema`) → 인라인 확인·수정·kind 선택 → 기존 memory POST로 저장. 첫 답변 시 **1회성 안내**(localStorage dismiss). `Bubble`에 agentId/conversationId prop, sourceConversationId best-effort. 복사·붙여넣기 루프 제거. 위젯 '기억 관리' 패널은 무변경(자동 노출).
- **Part B — 에이전트 제작 AI 되물음 인터뷰 + 항목 강화**(전략문서 §4 온보딩 인터뷰의 첫 실체): 위저드 마지막 질문 → 생성 직전 새 phase `interview`. 신규 `/api/agents/interview`가 **Sonnet으로 빈틈**(예시없음·성공기준모호·엣지케이스·회사규칙) **2~4개 되물음**(`interviewSchema`) → 답하거나 건너뛰기 → `clarifications`를 generate-prompt에 반영(SKILL_MD_SYSTEM 규칙8=최우선 근거). **성공 기준**(`successCriteria`) 필드 신설(required)·예시 강력권장. `SKILL_MD_SYSTEM`: **'성공 정의' 섹션 추가**·측정가능 합격선 지시·**K-뷰티 하드코딩 가드레일을 업종무관으로 중립화**(브랜드 정책 정합, P2.3 일부). 인터뷰 실패/빈틈없음이면 바로 생성(막지 않음).
- **파일**: 신규 `api/agents/[id]/memory/suggest`·`api/agents/interview` · `lib/claude/schemas.ts`(memorySuggest·interview) · `lib/agentBuilder.ts`(successCriteria·메타프롬프트) · `api/agents/generate-prompt`(clarifications) · `components/agent-chat/FloatingAgentChat.tsx`(Bubble 버튼·확인행·힌트) · `components/agents/AgentWizard.tsx`(interview phase).
- **검증**: tsc0 · lint29/0(신규0) · build0 · 신규 라우트 2개 등록 확인. main-first 배포(코드 `d0e24ab`, 롤백=직전 프로덕션 `311cf4c`). **⚠️ 실사용 육안 미검증(로그인 비번 없음)=대표 dogfood.**

**다음 후보**: 위 2종 dogfood 후 고도화(추출 품질·유사기억 병합·인터뷰 질문 튜닝) · 공용비번 강화·Gmail/Drive 복구(대표) · B1-b Step1b.

---

## 2026-07-24 · 세션37 — 에이전트 자동 기억(v2) + 마이그105 적용 + Stripe 오식별 정정

**무엇/왜:** 쉬고 온 뒤 이어서. ① 세션36 "Stripe 키 폐기" 대표 액션이 실제로 유효한지 역추적 → **오식별로 판명·종결**(별도 커밋 `beaf222`, 위 정정 블록). ② 미적용이던 **마이그105 프로덕션 적용**(참고사항 메모·멤버 수정권한이 죽어 있던 것 복구). ③ 워크스페이스 고도화 첫 착수 = **에이전트 자동 기억(학습 v2)**.

- **마이그105 적용·검증(Supabase MCP)**: `projects`에 `notes`·`deleted_at` 컬럼 + `projects_update` RLS에 project_members OR 추가. 롤백 지점(035 원문=created_by/owner_id만) 사전 확보. 적용 후 컬럼·정책 재조회로 검증. 코드는 이미 배포돼 있어 재배포 불필요 — 참고사항 메모·멤버 중요도/상태 변경이 라이브에서 살아남.
- **에이전트 자동 기억 v2 (수동 '기억해두기' → 자동 추출)**: 세션35 v1은 수동이라 실사용에서 거의 안 쓰이던 문제. **대화가 쌓이면 요점을 자동 추출·저장**하게. 마이그 불필요(099 `agent_memories` 재사용).
  - **설계**: 트리거=채팅 라우트 `onFinish`(응답 끝난 뒤 백그라운드, 사용자 대기 0) · 빈도=사용자 턴 **3의 배수**마다(비용/지연 방어) · 모델=**Haiku**(`MODELS.cheap`=claude-haiku-4-5, $1/$5) · 중복방지=기존 기억을 프롬프트에 주입("겹치면 만들지 마") + 코드 정규화 비교(`normalizeMemoryContent`) **2중** · 안전=budget 이미 상단 통과·`agent_usage` 성공/실패 기록·try/catch로 채팅 절대 안 깨짐.
  - **파일**: `lib/claude/client.ts`(cheap 별칭) · `lib/claude/schemas.ts`(`memoryExtractionSchema`) · `lib/agentMemory.ts`(순수 헬퍼: `EXTRACTION_SYSTEM`·`buildExtractionPrompt`·`dedupeCandidates`·`normalizeMemoryContent`·`ExtractTurn`) · **신규 `lib/agentMemoryExtract.ts`**(generateObject→dedup→insert, 기존 기억 최근40 비교, RLS 본인만) · `api/agents/[id]/chat/route.ts`(userTurns 카운트·`turnsForExtraction` 800자컷·onFinish 배선). agent_memories는 개인용(워크스페이스 컬럼 없음)이라 withWorkspace 미사용.
  - **위젯 무변경**: '기억 관리' 패널이 agent_memories를 그대로 조회 → 자동 추출분도 자동 노출.
  - **예상이슈**: onFinish가 Haiku 호출만큼(~1-3s) 길어짐=사용자 무영향(텍스트 이미 렌더·maxDuration 60s 내) / 할루시네이션 중복=2중 방어 / 마이그 불필요.
  - **검증**: tsc0 · lint29/0(신규0·베이스라인 30에서 오히려 -1) · build0. **⚠️ 실사용 육안 미검증(로그인 비번 없음) — 대표 dogfood 필요.**
- **Stripe 키 정정(커밋 `beaf222`)**: 세션35 "Vercel WORKSPACE_PASSWORD=Stripe 라이브 키" 단정을 트랜스크립트 역추적 → 대표가 보낸 Vercel 스크린샷의 잘린 값을 읽은 **추정**이었음(당시 기록도 "값 Sensitive라 못 봄"·"앞부분만 잘려 보임"·확인은 유도질문). 실제 키값 트랜스크립트 0건 + 대표 확인 Stripe 가입 이력 없음 → **폐기 대상 없음, 종결**. 레포 clean(전수검색 0건). HANDOFF 미해결①=✅종결, 교훈=스크린샷 판독은 추정으로 표기.

**다음 후보**: 자동 기억 dogfood 후 고도화(추출 품질·병합 로직·프로젝트공유 v1.5) · 공용비번 `4321`→강한값(대표) · Gmail/Drive 복구(대표) · B1-b Step1b(나머지 ~50곳 배선).

---

## 2026-07-22 · 세션36(이어서) — B1-b 착수 + 프로젝트 협업/허브 (대표 dogfood 피드백)

**무엇/왜:** 맹점 배포 후 대표 실사용 피드백으로 연속 작업. 매 건 본보기코드+검증(tsc0·lint29~30/0·build0).

- **B1-b 쓰기격리 착수(무위험)**: `WorkspaceProvider`/`useWorkspace`(Step0, 서버레이아웃 멤버십 로드) + `lib/workspace.ts`(`getUserWorkspaceId`·`withWorkspace`) → **AI 대화·사용량 도메인 서버 22곳**에 `workspace_id` 명시(Step1a: chat·assistant·cashflow-coach·workflows/run·리서치5). 단일 테넌트엔 값==DEFAULT라 동작 변화 0. 감사 finding 1D(agent_usage 예산우회) 선제 해소. 남은 도메인(client·projects·finance 등 ~50곳)·DEFAULT 제거는 후속.
- **프로젝트 협업 개선(마이그105)**: `projects_update` RLS에 project_members OR 추가(멤버도 중요도/상태 변경) + `deleted_at`(소프트삭제) + `notes`(참고사항) + 상태 배지 **점→아이콘**(완료=CircleCheck 등) + 목록/상세 삭제 버튼(생성자). ⚠️마이그105 프로덕션 직접적용 필요.
- **프로젝트 삭제 하드전환**: 소프트삭제가 미적용 마이그105 컬럼 의존이라 안 되던 걸 **하드삭제(기존 DELETE RLS=created_by, 마이그 불필요)** 로. FK 검증 = finance/files/calendar SET NULL(보존)·members/tasks CASCADE(정상). Undo=행 재삽입.
- **프로젝트 상세 = 일정·정산 허브(죽은 요약카드 살리기)**: 요약카드 3개가 연결 UI 부재로 항상 0이던 근본원인 규명(2병렬 조사) → ① 연결된 일정 섹션(calendar_events.project_id 인라인 추가) ② 비용·매출 정산 섹션(FinanceEntryModal 재사용+projectId 주입, **통화별 합계+순익**, 통화 섞어 더하던 버그 수정=aggregateByCurrency+money) ③ 요약카드 라이브+클릭 스크롤. **마이그 불필요**(project_id 컬럼 이미 존재). 계획=`~/.claude/plans/snazzy-booping-muffin.md`.

---

## 2026-07-22 · 세션36 — 맹점 5종 수정 배포 (신뢰성 4 + 보안 SSRF 1) + 마이그104

**무엇/왜:** 대표 요청 "맹점 파악·우선순위대로 하나씩 수정". 코드 3병렬 감사(API보안·멀티테넌시 쓰기격리·AI/신뢰성)로 발견한 **실사용 버그·보안구멍**을 우선순위대로 수정. 매 수정 **본보기 코드(Before→After)** 제시. tsc0·lint30/0·build0·main-first.

- **#1 워크플로우 도구 CHECK 위반 (HIGH · 마이그104)**: `save_file`(source='workflow')·`notify`(type='workflow')가 CHECK 제약에 없어 매번 INSERT 실패 → 파일은 `files` 버킷에 **고아**로 남고 "저장됨" **거짓 보고**(save_file은 insert 에러조차 미캡처였음). 관례대로 CHECK 확장(files 004·notif 076 기준으로 'workflow' 추가) + `run/route.ts` insert 에러 캡처·실패 시 고아 파일 `remove`. UI는 이미 `lib/files.ts`에 "워크플로우 결과" 라벨 보유 → 값만 열어주면 정합. **마이그104는 이 세션 Supabase MCP/CLI 부재로 프로덕션 SQL Editor에 대표가 직접 적용 완료(Success).**
- **#2 채팅 비용 과소집계 (HIGH)**: MCP 다단계(도구) 에이전트가 `usage`(마지막 스텝만)로 과금 → `totalUsage ?? usage` 합산(`run/route.ts:274`와 동일). 예산 한도 정확도 회복.
- **#3 OAuth 토큰 저장 (MED · Gmail/Notion 끊김 유력원인)**: `google/client.ts` `auth.on("tokens", …)`가 `void admin.update()`라 **HTTP 미전송**(lazy thenable, safe-changes §5) → 회전 refresh_token 유실 → 시간 지나면 Gmail/Drive 조용히 끊김. 핸들러 async+await로 실제 저장 + 에러 로깅.
- **#4 알림 읽음 저장 (MED)**: `NotificationBell.open()`도 동일 `void update()` → 새로고침 시 배지 오표시. async+await로 수정(`markAllRead`는 이미 정상이라 불일치 해소).
- **#10 SSRF 하드닝 (MED)**: 가드(`safeHttpUrl`/`isSafeWebhookUrl`)가 **최초 URL 호스트명 문자열만** 검사 → 302 리다이렉트·DNS 리바인딩으로 내부IP(169.254.169.254 등) 우회 가능. 공용 **`safeFetch`**(`lib/safeFetch`): ① DNS 실제 IP 공인검증(v4/v6 사설·loopback·링크로컬·CGNAT·IPv4-mapped) ② 리다이렉트를 fetch에 안 맡기고 **수동 매 홉 재검증**. 웹훅=`maxRedirects:0`(리다이렉트 차단), 리서치 이미지 2곳=3홉(CDN 허용·재검증). 3 sink(webhook·research/images·image-import) 배선.

**예상이슈 체크(수행):** ① 마이그104 미적용 상태로 코드 배포해도 무해 — save_file이 이제 깔끔히 실패처리+고아정리(적용 후 정상저장). ② `open()` async화 → 호출부 `onClick={() => open(n)}`라 반환 promise 무시=안전. ③ `safeFetch`의 `redirect:"manual"`은 Node(undici) fetch에서 3xx 실제 응답+Location 반환(브라우저 opaque와 다름) — 수동 추적 성립. ④ 잔여 위험: check→connect 사이 초고속 DNS-flip 리바인딩은 IP핀 없이는 잔여(LOW·내부툴, known-issues I18). 검증 tsc0·lint30/0·build0.

**롤백:** 직전 프로덕션 **`a0e86bd`**. 마이그104 롤백=두 constraint 배열에서 'workflow'만 제거 재적용.

---

## 2026-07-16 · 세션35 — P2.2 출력형식 재설계 + 채팅 진입 하단 스크롤(4곳) + 학습/기억 기획 착수

**무엇/왜:** ① 세션34 다음 우선순위 P2.2 진행 ② 대표 지시 "채팅 들어가면 바로 최근 대화(하단)에 있어야 함" ③ 대표 신규 방향: 예시 갤러리 불필요·개인 맞춤 우선 + **작업할수록 학습/기억(많은 사용자에도 스케일)** + 결과물 저장·확인 + 기술 병목 분석 후 기획·설계.

- **P2.2 출력형식 재설계 (배포 `cf5684e`)**: `agentBuilder.ts`에 `OUTPUT_FORMATS`(SSOT) + `recommendOutputFormat(직무)`. 추상 9개 multiselect → **업무언어 6개 단일선택**(바로쓰는초안/한눈요약/정리된표/단계별안내/완성형문서/자유롭게) + 각 **한줄설명·미니예시** + step1 **직무 기반 '추천' 배지**. `WizardField.hint` 추가, 위저드에 outputFormat 전용 카드 렌더(제네릭 select 분기), '직접 입력' 이스케이프 유지. 템플릿/직접작성폼은 outputFormat 미참조 → 영향 국소.
- **채팅 진입 하단 스크롤 4곳 정합 (배포 `aed63d9`)**: 원인 갈림 —
  - **DirectChat(1:1)**: 루트 `h-full`이 세션31 리디자인 때 추가된 `PageTransition`(`animate-page-in`, auto-height) 래퍼 밑에서 **높이 사슬 끊김** → 내부 `overflow-y-auto`가 bounded 안 됨 → `<main>`이 스크롤 → 진입 시 최상단. **회귀 근본원인.** → 루트 `h-[var(--app-content-height)]`(대시보드·그룹챗 관례)로 bounded 복원.
  - **GroupChat**: bounded지만 `[roomId]` 페이지에 key 없어 방 전환 시 remount 안 됨(`didInitialScroll` ref 유지). → `key={roomId}`.
  - **DashboardAssistant·FloatingAgentChat 위젯**: 히스토리/스트리밍 모두 `smooth`만. → `jumpToBottom` ref로 진입·대화전환=즉시(`auto`), 스트리밍만 `smooth`.
- **학습/기억 기획 착수(설계 前)**: 대표 워크플로우(higgsfield+Claude 반복 학습→퀄↑, 결과물 저장·확인)를 Complow에서 쉽게. **기술 병목 집중 분석 리서치 2갈래**(① 개인 메모리 at scale ② RAG/생성 파이프라인 at scale) 병렬 → 종합 후 기획 문서화 예정. 핵심 = 파인튜닝 아님, **앱레벨 메모리(추출+검색 하이브리드)로 스케일**, 격리는 RLS가 강제. **v1 착수: `AGENTS-LEARNING-DESIGN.md` 작성 + 마이그 099(`agent_memories` 개인용·본인만 RLS) 적용·격리검증(A본인=1·B타인=0)·drift0.** 대표 결정=갤러리 인터뷰교체·학습v1 먼저·기억=사용자×에이전트(+프로젝트공유 v1.5). → ✅ **수동 기억 루프 배포(`a2d73be`·롤백 `ebd30d2`)**: `lib/agentMemory`(buildMemoryBlock)·기억 API(GET/POST/DELETE, RLS 본인만)·채팅 라우트 주입(활성 최근30, 지식파일 다음, 없으면 스킵)·위젯 `AgentMemoryPanel`('기억 관리' 토글=추가/목록/삭제·soft-delete)·types.ts agent_memories. tsc0·lint30/0·build0. **대표 방침 = 만들고 쓰면서 점검 후 고도화**(내부 dogfood). **개념 정리(대표 문답): "학습"=파인튜닝 아닌 앱레벨 기억 / 저장은 무제한(로그처럼)·주입만 소수 선별 / 로그·기억 모두 에이전트별·사용자별(conversations.agent_id+user_id, agent_memories RLS) / 프로젝트 공유는 v1.5.** 다음=자동 추출(대화 종료 시 요점)·중복병합.
- **에이전트 만들기 진입 재설계 (배포 `3395892`→`844e79f`→`d5ea4b9`, 대표 dogfood 피드백 반영)**: ① 출력형식 카드→열린 입력 전환 → ② 예시 **완전 제거** + 배경 **떠다니는 에이전트 아이콘 7개**(`animate-float` 재사용, opacity 0.16, motion-safe) → ③ **MCP 연결 스텝을 직무 다음에 추가**(`McpConnectorPicker`: `/api/mcp/user-connections`에서 내 커넥터 선택, `/mcp` 새탭 링크, 결과폼에 `mcp_connectors` 전달=세션34 바인딩 재사용). `WizardFieldType`에 `mcp` 추가·`serializeInputs`서 제외. `AGENT_TEMPLATES`/`pickTemplate` 제거. 대표 지침: "만드는 건 쉽고 잘 되게가 일"(MCP 연결≠좋은 결과, 품질은 dogfood로).
- **MCP UX·프라이버시·직접작성 제거 (배포 `369ce2c`→`f1dccea`+본커밋)**: ① **MCP 스텝 재설계**(`McpConnectorPicker`) — 연결한 것만→**사용 가능 커넥터 전체**(연결됨=선택 / 미연결='연결하면 쓸 수 있는 도구 N개' 펼침+`/mcp` 새탭+계정필요 배지). ② **도구설명 AI 한국어 요약**(`lib/mcp/summarize`, generateObject·sonnet-4-6, 테스트/연결 시 1회→`mcp_user_connections.tools`에 `summaryKo` 캐시, 표시=summaryKo||MCP_TOOL_KO||description·방어적). ③ 커넥터 **실제 로고**(McpView `ConnectorLogo` 공용 추출→위저드 MCP 스텝도 도메인 파비콘, emoji 폴백). ④ **프라이버시 3종**: 지식파일 개인전용(마이그**100** `is_personal`·ak_select 재작성·RLS시뮬 소유자2/타인1, KnowledgeFilePicker 파일별 개인전용/공유 토글) · 관리자 구성원별 사용량(마이그**101** RPC `admin_usage_by_member` **오너전용** security definer·게이팅시뮬 오너4/비오너0, SettingsView 표·**대화 내용 미노출**) · 개인 파일 '🔒 개인(나만)' 라벨(044 재사용). 합의=AI채팅·기억·개인연결·개인전용지식=본인만·관리자는 사용량만. ⑤ **직접작성(manual) 모드 완전 제거**(대표: 가이드 마법사만) — `mode`/manual블록/`PenLine`/'직접 작성으로 전환' 2곳 제거, **아이콘 선택은 결과폼 AgentBuilderForm에 그대로**, 갤러리 '이걸로 만들기'→**'다음'**(말이 이상). **Notion 연결 401**=재연결 안내(refresh는 코드상 배선됨 `McpOAuthRuntimeProvider`, 토큰·갱신토큰 무효 시 재연결 필요). 각 tsc0·lint30/0·build0. 대표 문답: 새 사용자 가입=이름+공용비번(`WORKSPACE_PASSWORD`)·AI채팅 본인만 격리 확인.
- **구성원 삭제(오너 전용, 배포별도)**: `/api/members/[id]` DELETE — 오너 검증(workspaces.owner_id) + 대상=내 워크스페이스 멤버 + 본인 아님 → `admin.auth.admin.deleteUser`. profiles=`auth.users` on delete cascade라 그 사람 **개인 데이터(대화·기억·연결)만** 연쇄삭제, **공유 자원(에이전트·워크플로우·프로젝트 created_by=set null)은 보존**(FK 확인). 설정 구성원 행에 🗑 + **2단계 확인**(오삭 방지). tsc0·lint30/0·build0.
- **⚠️ 공용비번 사건(대표 dogfood 중 발견)**: 프로덕션 Vercel `WORKSPACE_PASSWORD`가 실수로 **Stripe 라이브 시크릿 키(`sk_live_…`)** 로 설정돼 있어 신규 가입 전부 실패(기존 계정 로그인은 됨). → 값을 **4321로 교체·재배포**, `test`계정 가입 성공(DB 확인)으로 검증. **대표 액션: 그 `sk_live_`가 진짜 라이브 키면 Stripe에서 폐기(Roll) · 4321은 약하니 강한 값으로 교체 권장.**
  - **🔧 [정정 2026-07-23] 위 "Stripe 라이브 키" 단정은 오식별.** 근거를 역추적하니 **대표가 보낸 Vercel 설정 스크린샷의 잘린 값을 읽은 추정**이었음(당시 세션 기록에도 "Vercel 비번 값은 Sensitive라 못 보고"·"화면엔 앞부분만 잘려 보여서"라고 적혀 있고, 확인 방식도 "지금 값이 `sk_live_`로 시작하죠?" **유도질문**). 실제 키 값은 **트랜스크립트 어디에도 없음**(`sk_live_`+실제값 매칭 0건, 전부 서술뿐). **대표 확인 = Stripe 가입 이력 없음 → 폐기 대상 없음, 종결.** 재확인 불가(스크린샷 캐시 삭제·Vercel env 값 히스토리 없음·현재값 4321, 로컬 `.env.local`도 4321로 동기). 잔여 리스크 ≈ 0(노출은 대표 Vercel 계정 한정, **레포 clean** — git 히스토리·현재 파일·`.env.local` 전수검색 0건, public 레포라 중요). **교훈: 스크린샷 판독 결과를 문서에 "사실"로 적지 말 것 — 추정은 추정으로 표기하고 근거를 함께 남긴다.** ※ 4321 → 강한 값 교체는 여전히 유효.
- **OAuth·env 도메인 이사 대응 + 자가치유·시스템 점검 (배포 `492d9f5`·`35e3a1f`·`43f06b1`, 마이그 102·103)**: 옛 도메인(`*.vercel.app`)→`complow.kr` 이사 후유증 연쇄 해결.
  - **Notion 연결**: 401→재연결→`invalid mcp state`(쿠키)→`invalid redirect_uri`(옛 등록) → `mcp_oauth_clients`·개인연결 DB **등록 초기화(2회)** + Vercel `NEXT_PUBLIC_APP_URL`=`https://complow.kr` 교체·재배포 → **연결 성공**.
  - **MCP OAuth 자가치유(마이그102)**: `mcp_oauth_clients.redirect_uri` 저장 → `loadClientInformation`이 현재값≠저장값이면 무효 처리 → `auth()`가 새 주소로 DCR 재등록. **수동 DB 삭제 반복 근절.**
  - **카테고리 저장 버그(마이그103)**: 세션34에서 카테고리 자유입력으로 바꿨는데 `agents_category_check`(옛 8슬러그)가 남아 목록 밖 값 저장 시 위반 → **제약 제거**(더 permissive라 데이터 무영향).
  - **시스템 점검(오너 전용 `/api/health` + 설정 카드)**: 앱주소·공용비번(`sk_`/장문=키 의심)·구글콜백 호스트=앱 호스트 일치·AI/Supabase 키를 **사전 대조** → ✅/⚠️/❌ + 고치는 법. 값은 미반환·상태만. **대표 지시: "이런 거 미리 검증하는 시스템".**
  - **Gmail·Drive `invalid_grant`**(저장 토큰 죽음) = 진단만, **대표 액션 필요**: Vercel `GOOGLE_OAUTH_REDIRECT_URI`=`https://complow.kr/api/google/callback` + **Google Console '승인된 리디렉션 URI' 등록**(구글 사전등록 엄격) → 재연결. 테스트모드면 refresh 7일 만료→게시.
  - **아키텍처 문서 산출**: 이해용 HTML(artifact) + **PDF**(`~/Downloads/Complow-아키텍처.pdf`, Chrome 헤드리스 print-to-pdf). 14섹션(레이어·기능·AI파이프라인·데이터모델·보안·멀티테넌시·연동·배포·env·로드맵).
  - 각 tsc0·lint30/0·build0 · RLS/게이팅 시뮬. **다음 세션 인계 = HANDOFF 최상단 🔴 미해결 4개**(Stripe키 폐기·공용비번 강화·Gmail/Drive 구글콘솔·점검 ⚠️) 먼저 챙기고 시작.

**검증:** tsc0 · lint30/0(신규0) · build0. 커밋 2분리(P2.2/채팅). main-first 배포(`006719b`→`aed63d9`, 롤백후보 `006719b`), Vercel 프로덕션 BUILDING 확인 후 feat push. **⚠️ 채팅 스크롤·출력형식 = 대표 실기기 육안 미검증(로그인 비번 없어 자동확인 불가).**

---

## 2026-07-15 · 세션34 — 에이전트 시스템 재설계 (리서치 기반 5단계 계획 + Phase 1)

**무엇/왜:** 대표 지시 — 기본 에이전트 전부 제거·회사가 직접 만들게 + 만들기/삭제 기능 + 만들기 과정 이해하기 쉽게 + 지식파일 첨부 + **다른 AI(클로드·커서·제미나이·그록·코덱스)로 내보내기** + 출력형식 UX 개선 + 애플급 UI + **워크플로우 사람 검토·승인 게이트(파일 산출→사람 승인→다음 에이전트 인계)**.

- **리서치(6갈래 병렬 서브에이전트)**: 에이전트 CRUD/빌더 코드맵 · 가이드라인/의존성 · AI에이전트/AX 도입 트렌드(Anthropic 컨텍스트엔지니어링·OpenAI 에이전트가이드·McKinsey/BCG/MIT·한국 정부 AX) · 이식/내보내기 표준(SKILL.md·AGENTS.md) · 지식파일+출력UX(풀컨텍스트 vs RAG) · 워크플로우 HITL 패턴. → **5단계 계획** = `~/.claude/plans/stateful-zooming-quiche.md`(대표 승인).
- **Phase 1 — 클린슬레이트 + CRUD (배포 `af87697`, 롤백 `ecadc69`)**:
  - **마이그 096**: 기본 8개 `is_active=false` 소프트삭제(`created_by is null`). 복구 가능(`set is_active=true`). agent_versions/pins/usage/conversations 보존. `seed.sql` 에이전트 시드 블록 RETIRED 배너.
  - **삭제 UI**: 목록 카드(내 에이전트, hover 🗑)·상세(수정 옆) 삭제 버튼 — 소프트삭제+⌘Z 복구+`equria:agents-changed`(위젯 자동반영). RLS(036)가 소유자 강제. 빈 '기본 에이전트' 섹션 숨김.
  - 검증: tsc0·lint30/0·build0. 라이브 DB 확인(활성 0/비활성 8). ⚠️ 마이그는 원격=프로덕션 DB에 즉시 적용됨(앱이 원격 읽음).
- **리서치 핵심(다음 Phase 반영)**: 가이드=구조화 템플릿+가드레일 기본탑재(환각완화) · 출력형식=업무언어 소수+미니예시+자동추천 · 지식=풀컨텍스트(RAG 아님)+cache_control · 이식=시스템프롬프트MD(어디서나)+SKILL.md+AGENTS.md · HITL=동기→재개형 실행이 유일한 큰 작업(전자결재 RPC·알림·save_file 재사용). **🐞 발견: `save_file`(source="workflow")·`notify`(type="workflow")가 CHECK 위반으로 현재 깨짐 → Phase 5 선행 수정.**

- **StepPicker 가독성**(배포 `c4614f5`): MCP 도구를 서버별 접이식 그룹.
- **Phase 2 부분(배포 `595558a`·`9adcddd`)**: ① **예시 갤러리**(`lib/agentTemplates` 백오피스 8종): '새 에이전트'→"어떤 일을 맡길까요?"→선택 시 입력 프리필→즉시 AI 생성(빈 화면 해소). ② **카테고리·업무영역·직무 자유입력**(select→자유입력+datalist / multiselect 커스텀 태그 / select+직접입력) — 대표 "틀에 갇힘" 반복 피드백. ③ **업계특화(K-뷰티) 문구 → 업종 중립**(placeholder·시스템프롬프트 예시). ④ **위젯 빈상태 드래그 버그수정**(`EmptyAgentWidget`가 `useDragWidget` 안 써서 못 움직이던 것 — 기본 에이전트 제거로 드러남).
- **Phase 3 지식파일(배포 `9adcddd`, 마이그 097)**: `agent_knowledge`(부모 agents EXISTS 상속·기존 files 버킷 재사용). 위저드 '필요한 데이터' 스텝 + 빌더에 **파일 첨부**(AI-readable만: PDF·이미지·txt/md/csv/json). 채팅 라우트가 텍스트는 시스템에, PDF/이미지는 파일파트로 **풀컨텍스트 주입**(admin-sign, RAG 아님). 신규 `lib/agentKnowledge`·`KnowledgeFilePicker`.
- **MCP 바인딩(배포 `23cca30`, 마이그 098)**: `agent_versions.mcp_connectors`(개인 커넥터 슬러그). 빌더 '내 MCP 연결'에서 내가 연결한 개인 커넥터(Notion 등) 선택. 채팅·워크플로우 실행 모두 "전부"가 아니라 **바인딩된 커넥터만** 실행자 본인 연결로 해석(공유 에이전트도 쓰는 사람 계정 기준). ※발견: 워크플로우/채팅 모두 원래 개인연결을 전부 로드하던 것을 선택-only로 일관화.

**검증:** 각 배치 tsc0·lint30/0·build0. 마이그 096·097·098 원격 적용·drift0.

**남은 것(다음):** MCP **3b**(워크플로우에 개인 MCP 툴노드로 추가=StepPicker 개인연결 표시+실행엔진, 노드모델 변경)·**3c**(에이전트 노드의 바인딩 MCP 배지) · P2.2 출력형식 재설계(9개 추상→업무언어 소수+미니예시) · P2.3 가드레일 기본탑재 · P2.4 위저드↔직접작성 통합 · P2.5 애플UI · Phase 4 내보내기(SKILL.md·AGENTS.md·MCP스니펫) · Phase 5 워크플로우 사람검토(재개형 실행+승인RPC, +save_file/notify CHECK 버그수정). **아이콘**=가이드 결과폼 상단에 이미 있음(대표 확인 필요).


---

> 세션33 이하(2026-05~07-13, 초기 기록)는 `docs/archive/WORKLOG-초기.md` 참고.
