# HANDOFF — 회사 AX(AI 전환) 도입 플랫폼 (사내 AI 워크스페이스 · B2B 전환 중 · 브랜드 = Complow)

> **새 세션 읽기 순서:** 이 파일 → 아래 **📂 문서 지도** → `CLAUDE.md` → `.claude/skills/{safe-changes,latest-stack,known-issues}.md`
> 이 파일은 **"현재 상태 · 다음 할 일 · 합의된 정책"만** 담는다. 깊은 내용은 전용 문서(지도 참조), 과거 상세는 git 커밋 메시지에.
> 최종 업데이트: **2026-07-28 (세션 39 후속 — 프로덕션 `5f430af`+마이그 116 직후 코드 · 마이그 001~119 · B1-b/B2 롤백=각 마이그 파일 주석 · 재무 데이터 롤백=`delete from finance_entries where metadata->>'slot_migration'='true'`)**. ★**B2 노션식 초대 개방 — 코드·DB 전부 완료, 컷오버 스위치만 대기(아래 🔴)**: 마이그 113(초대 링크 `workspace_invites`+guest role+RPC4종·RLS시뮬 통과)·114(게스트 RLS — 헬퍼 fail-closed+프로젝트 경로만 개방·멤버 회귀0/게스트 0건 시뮬 통과)·115(create_workspace+시드 8종 복제·이큐리아 중립화+admin_create_workspace 운영자 셋업+create_group_room guest/멀티WS 픽스 — 종합 시뮬: 생성→복제→타사 격리→owner 링크 승계 전부 통과)·116(handle_new_user v2 — equria 자동등록 제거) + 앱 `9b74af8`(온보딩·/join·SocialButtons next·layout 멤버십0→온보딩·**types 재생성=workspace_id 컴파일 강제**·ProjectDetail 일정 redo 실버그 픽스)·`5f430af`(설정 '초대 링크' 카드[게스트=프로젝트 칩]·구 이메일 초대 410·GuestGuard+네비/위젯 게스트 게이팅)·랜딩 푸터 다운로드 자리(iOS&Android/Mac&Windows 준비 중). 라이브 스모크: /join 미리보기·로그인·온보딩 가드 확인. **패스트팔로우**: Header 워크스페이스 스위처+last-ws 쿠키(멀티멤버십 시)·마이그117(mcp_oauth_clients 회사별 스코프 — 플랜 B-5). ★**B1-b 쓰기 격리 Phase A-1~A-4 완료(대표 승인 계획 `~/.claude/plans/prancy-whistling-tome.md`)**: 클라 59곳(`b26fe62`재무15·`5eebc56`프로젝트캘린더파일19·`d79462d`채팅결재회의록13·`5bc71d2`에이전트근태공지12)+서버5·admin가드2·presence 분리(`3ec50d8`) — 전부 `useCurrentWorkspaceId()`/`getUserWorkspaceId()` 명시(현재 명시값=DEFAULT라 동작 불변·DDL 0). **A-4 감사 통과**: 코드 grep 0건(오탐 2 육안검증)·DB sentinel DEFAULT 45개 테이블 인벤토리·drop default 시뮬 롤백 검증·064 RPC workspace 명시 확인. **✅ A-5 완료(대표 승인 후 실행) = 마이그 112 `b1b_drop_workspace_default`**: 45테이블 sentinel DEFAULT 제거 — **쓰기 격리 강제 ON**(미명시 INSERT=NOT NULL 거부 확인·명시 INSERT=성공 확인, 롤백 스모크). 롤백=마이그 파일 주석의 set default 루프. **B1-b Phase A 전체 완료 — 이제 신규 워크스페이스 데이터가 equria로 오귀속될 수 없음.** 🔜 Phase B(가입 개방·초대 링크·게스트) = 플랜 파일 설계 확정본, 마이그 번호는 113~117로 시프트. ★**대화 요약 압축(트랙2 완료 `9f77e33`·마이그 117)**: 10턴 윈도우 밖 오래된 턴을 Haiku 증분 요약(onFinish 백그라운드·오버플로≥4턴)→시스템 프롬프트 '지금까지의 대화 요약' 주입 — '앞 내용 까먹음' 정면 해결. `conversations.summary/summary_upto`·`lib/conversationSummary`. 어시스턴트 대화는 후속. ⚠️14턴+ 대화 실검증=대표 dogfood. ★**가입=구글 전용+구성원 초대(`35813ed`·롤백=`d29f1fb`·DDL 0)**: 공용비번 가입 폼 제거 → 오너 설정 '구성원 초대'(구글 이메일 사전등록)로만 신규 진입(가입 OFF 유지=외부인 차단). 콘솔 설정은 대표 완료·서버 302 검증. 상세=아래 '✅ 구글 로그인' 블록. ★**구글 로그인 1차(`d29f1fb`·롤백=`7f12fc6`·DDL 0)**: [대표 결정] 소셜=구글 먼저(애플·카카오=B6)·**"로그인 수단 추가"≠"가입 개방"(개방은 B1-b 후)** — 'Google로 계속하기' 버튼(로그인·가입·랜딩 모달 3곳, AuthForm 공용) + 마이페이지 '로그인 방법' 카드(**멤버 이메일이 합성 `u<hex>@equria.local`이라 자동연결 불가 → linkIdentity 수동 연결** 필수) + 콜백 에러 처리(미등록 계정 안내·취소·open redirect 방지) + **proxy OPEN_PATHS에 /auth/callback**(세션 없는 콜백이 /login으로 튕겨 OAuth 불가하던 잠복 버그 픽스). **⚠️ 실동작=아래 '🔴 구글 로그인 활성화' 대표 콘솔 설정 5단계 후**(특히 **신규가입 OFF를 provider ON보다 먼저** — 순서 틀리면 아무 구글 계정이나 워크스페이스 진입). 실연결 육안 미검증(브라우저 OAuth 왕복=대표만). ★**파비콘 새 로고 교체(`94775bf`→색반전 `7f12fc6`·롤백=`e4f2a1f`)**: 탭 흰 정사각 → 둥근 사각형, 대표 피드백으로 **흰 배경+검정 CP 마크** 확정(icon.png 512·apple-icon.png 180·**favicon.ico 16/32/48 신설**=구글 등 /favicon.ico 직접 가져가는 서비스 대응 — 구글 검색결과 옛 'C' 아이콘은 구글 캐시라 재크롤 대기). ★**재무 완전 통합(원장 SSOT·대표 승인 계획)**: `finance_entries`=유일한 원장, 캔버스 슬롯=계정+[기록] 입력기(휴면 FK `account_id` 재활용), 슬롯 금액=이번 달 자기 기록 합계 파생, 수식=기록 프리필 도우미. **이중 집계 해결**=귀속/잔여 분할(pool=장부 전체 항등식). 세금계산서 확정→매출 자동 기록(source 'invoice'). 기존 슬롯 5건·4.133억 장부 이관 완료(멱등·슬롯별 5/5 정확 일치 검증). 마이그 110(source check)·111(이관). ⚠️ 대표 육안 미검증. 상세=WORKLOG 세션39. ★**랜딩 완성(2026-07-28)**: 노션식 GNB(전체폭·중앙 nav·리소스 드롭다운)+로그인/가입 모달(`AuthModal`·`AuthForm` 공용화, /login·/signup과 로직 단일) → 잔디식 콘텐츠(숫자 스트립·AI 심화 3블록·기능 심화 4블록·보안·요금별 기능 비교표·FAQ·다크 푸터 4컬럼+소셜[메일·인스타 @complow·X 임시]) → **로그인 상태도 랜딩 열람**(proxy 루트 예외, GNB='워크스페이스 열기'). ★**SEO(한글 '컴플로우' 검색 대응)**: 메타 한글 병기·JSON-LD Organization(alternateName 컴플로우)·본문 한글 15회·robots.ts/sitemap.ts(+proxy OPEN_PATHS에 /sitemap.xml·/robots.txt — 리다이렉트에 막히던 픽스 `e4f2a1f`)·(auth) noindex. **Search Console**: 도메인 인증 ✅+루트 색인 생성 요청 ✅(2026-07-28 대표 실행)·사이트맵은 구글 자동 재시도 대기(1~2일 내 '성공 4페이지' 확인). 푸터 미기재=사업자등록번호·주소·통신판매업신고번호·X 핸들(대표 제공 대기). 세션39: **손익 탭 통일**(대표 결정 — 비용·매출 탭 4→3 손익/내역/세금계산서, 계산기=손익의 단일 기준·KPI 한 벌, 구 요약 분석은 내역 탭 상단으로) + **공용 DateInput**(빈 날짜칸 "연도. 월. 일." 전면 정리·12곳) + **장부 연동 분류 필터**(마이그109 `ledger_category` — "식비" 슬롯=내역 식비만 자동 합산). ⚠️ 대표 육안 미검증. 상세=WORKLOG 세션39. / 이전 세션38: 후속2: **장부 연동 슬롯**(`7b6fe2a`·마이그108 — 현금흐름 계산기에 '장부 연동' 버튼 → 이번 달 내역 합계 자동 반영 슬롯, 요약↔현금흐름 수치 일치) + **디테일 4종**(`022bf5e` — 실행시각 포맷·이모티콘 제거[🔒→Lock 등]·회의노트 토글 자리 고정·구성원 행 정렬). 후속1: **UI 다이어트+AI 출력 스타일**(`270c598`·`218b73d` — MCP 크리덴셜 3단계 접이·워크플로우 실행 접기·결재 날짜 기본값·파일함 Drive 탭 제거 / 전 에이전트 기호 절제 OUTPUT_STYLE_RULE). 🔜 **다음 세션은 바로 아래 "다음 세션 착수" 블록부터 읽을 것.** 세션38: **MCP 정적 OAuth 클라이언트 인프라 완성·배포**(구글 Gmail·Cal·Drive·Slack·PayPal을 대표가 OAuth 앱 등록[client_id/secret]하면 직원이 자기 계정으로 연결 — DCR 미지원 서비스 지원). @ai-sdk/mcp 소스 검증: 정적 client seed 시 DCR 건너뜀·secret 있으면 confidential 자동. 마이그107(`is_static`·`custom_url` 추가형). 오너 설정 'MCP 앱 크리덴셜' UI + 카탈로그 추가 + '관리자 설정 필요' 게이팅. **⚠️ 대표 OAuth 앱 등록 선행 필요(아래 트랙1 액션) · 실연결 육안 미검증(브라우저 OAuth 왕복은 대표만).** Zapier 커스텀URL=fast-follow(준비중). 상세=WORKLOG 세션38. 후속 배포분: ㉱ **자동기억 품질 고도화**(`4e8fb6f` — 자동추출이 중요도 자동 매김·근접중복(포함관계+토큰 자카드) 차단) ㉲ **Exa 커넥터 활성화**(`58b6783` — bearer API키, GitHub과 동일) ㉳ **MCP 연결 로드맵 분석 완료**(구글·Slack·PayPal·Vercel·Figma·Zapier 현황 검증 → 다음 세션 착수 블록). 이전(같은 날): ㉮로딩바 ㉯기억정리(AI·마이그106) ㉰대화 히스토리 3종. 대표 dogfood 연속 배포(각 tsc0·lint29/0·build0·main-first, 마이그106 1개 추가형): ㉮ **답변 로딩바**(유튜브식 상단 얇은 바) ㉯ **기억 정리(AI·마이그106 `importance`)** — '기억 관리'에 'AI로 정리'(병합·중복제거·쓰레기제거·재분류·우선순위) before→after 미리보기 후 적용(휴지통 복구)·종류별 그룹+중요도 배지·채팅 주입 중요도순·추출 프롬프트 명령/일회성 차단 ㉰ **대화 히스토리 3종** — 재오픈 복원·새로고침 유지(localStorage)·과거 대화 열람(신규 conversations 라우트+'새 대화'/'대화 목록'). **⚠️ 실사용 육안 미검증=대표 dogfood.** [객관 판단] "대화량 게이지"는 반려(10개 슬라이딩 윈도우라 오해 유발) → 히스토리 복원으로 대체, 다음=요약압축. / 이전 세션37이어서: 품질 루프(채팅 원클릭 기억+제작 되물음 인터뷰, `d0e24ab`). **품질 루프 2종**(마이그 불필요·추가형·각 tsc0/lint29·0/build0·main-first): ⓐ **채팅 원클릭 '기억하기'** — 답변 버블 hover에 복사 옆 '기억하기' → Haiku가 오래 쓸 한 줄 추출(신규 `memory/suggest`) → 확인·수정·저장(복사붙여넣기 제거) + 첫답변 1회성 안내. ⓑ **에이전트 제작 AI 되물음 인터뷰**(전략 §4 온보딩 인터뷰 첫 실체) — 위저드 마지막→생성 직전 새 phase, 신규 `api/agents/interview`가 Sonnet으로 빈틈(예시·성공기준·엣지·회사규칙) 2~4개 되물음→답/건너뛰기→clarifications 반영 + **성공기준 필드 신설**·SKILL_MD '성공 정의' 섹션·**K-뷰티 가드레일 업종무관 중립화**(P2.3 일부). **⚠️ 실사용 육안 미검증=대표 dogfood.** / 이전 세션37 초반: ① **에이전트 자동 기억 v2**(세션35 수동 '기억해두기'→**자동 추출**): 채팅 `onFinish` 백그라운드(사용자 대기 0)·사용자 턴 3의 배수마다·**Haiku**(`MODELS.cheap`)·중복 2중방어(기존기억 프롬프트주입+`normalizeMemoryContent`)·budget통과·`agent_usage`기록·try/catch로 채팅 무영향. 신규 `lib/agentMemoryExtract.ts`+`agentMemory.ts` 헬퍼+`memoryExtractionSchema`. 마이그 불필요(099 재사용). 위젯 '기억 관리'가 자동분도 노출. **⚠️ 실사용 육안 미검증(로그인 비번 없음)=대표 dogfood.** ② **마이그105 적용**(세션36에서 미적용이던 것 — `projects.notes`·`deleted_at`+멤버 수정권한 RLS, Supabase MCP로 적용·검증): 참고사항 메모·멤버 중요도/상태 변경 라이브 복구. ③ **Stripe 키 폐기건=오식별로 종결**(스크린샷 잘린값 추정이었고 대표 Stripe 가입이력 없음·레포 clean, HANDOFF 미해결①=✅). 각 tsc0·lint29/0(신규0)·build0·main-first. 세션37 상세=WORKLOG. / 이전 **2026-07-22 (세션 36 — 맹점 5종 수정 배포 · 마이그 001~104)**. 코드 3병렬 감사로 찾은 실사용 버그·보안구멍 우선순위 수정(각 tsc0·lint30/0·build0·main-first, 본보기코드 제시): ①**워크플로우 도구 CHECK 위반+고아파일**(마이그**104** `files.source`·`notifications.type`에 'workflow' 추가 · save_file insert에러 캡처·고아 remove — 배포된 기능이 실제로 안 되던 버그) ②**채팅 비용 과소집계**(MCP 다단계 `usage`→`totalUsage` 합산) ③**구글 OAuth 토큰 저장**(`google/client.ts` `void`→`await`, lazy-thenable로 회전 refresh_token 유실=Gmail/Drive 조용히 끊김 유력원인) ④**알림 읽음 저장**(`NotificationBell` `void`→`await`) ⑤**SSRF 하드닝**(공용 `safeFetch`=DNS 실제IP 공인검증+리다이렉트 매홉 재검증 / 웹훅 리다이렉트0 · 리서치이미지 2곳 3홉). **⚠️ 마이그104 프로덕션 SQL Editor로 직접 적용 완료(이 세션 Supabase MCP 부재).** **합의된 다음 큰 공사 = B1-b 쓰기격리**(다음 사용자 받기 선행 필수, ~72곳 INSERT 배선·`useWorkspace()` 신설·presence 채널 네임스페이싱). 세션36 상세=WORKLOG. / 이전 **2026-07-16~22 (세션 35 — 프로덕션 `43f06b1`(+env 재배포)·READY `complow.kr`·마이그 001~103, 롤백=직전 프로덕션 SHA·WORKLOG 참조)**. 대표 dogfood 피드백 다수 반영(각 tsc0·lint30/0·build0·main-first). **배포분:** ①P2.2 출력형식 재설계(업무언어 단일선택+미니예시+직무추천) ②채팅 진입 하단스크롤 4곳(DM 루트 `h-full`→`h-[var(--app-content-height)]`=세션31 PageTransition 높이사슬 회귀수정·그룹 `key=roomId`·어시/위젯 `auto`) ③**학습/기억 v1**(마이그**099** `agent_memories`·본인RLS·격리검증 / 위젯 '기억 관리'(추가·삭제)+채팅 주입 / **지금은 수동 '기억해두기', 다음=자동추출**) ④**만들기 진입 재설계**(예시 갤러리 **제거**→열린 입력+떠다니는 아이콘 / **MCP 연결 스텝**[직무 다음]: 사용가능 커넥터 **전체**+미연결 '연결하고 오세요'·개인계정 안내) ⑤**MCP 도구설명 AI 한국어 요약**(`lib/mcp/summarize`, 커넥터 테스트/연결 시 1회 생성·캐시) ⑥커넥터 **실제 로고**(공용 `ConnectorLogo` 파비콘) ⑦**지식파일 개인전용**(마이그**100** `agent_knowledge.is_personal`·RLS검증 소유자2/타인1 — 공유 에이전트여도 올린 본인만) ⑧**관리자 구성원별 AI 사용량**(마이그**101** RPC `admin_usage_by_member` **오너전용**·게이팅검증 오너4/비오너0·설정 표, 내용은 미노출·사용량만) ⑨개인 파일 '🔒 개인(나만)' 명확화 ⑩**직접작성(manual) 모드 완전 제거**(가이드 마법사만·아이콘은 결과폼 유지) ⑪**오너 전용 구성원 삭제**(`/api/members/[id]` DELETE, admin deleteUser·개인데이터만 cascade·공유자원 set null 보존·2단계 확인) ⑫**MCP OAuth 자가치유**(마이그**102** `mcp_oauth_clients.redirect_uri`—주소 바뀌면 자동 재등록, 'invalid redirect_uri' 수동삭제 반복 방지) ⑬**카테고리 자유입력 정합**(마이그**103** `agents_category_check` 제거—저장 막히던 버그) ⑭**시스템 점검**(오너 전용 `/api/health`+설정: 앱주소·공용비번·구글콜백·키 사전 대조 ✅/⚠️/❌). **기획문서** `AGENTS-LEARNING-DESIGN.md` · **아키텍처 PDF/HTML** 산출(`~/Downloads/Complow-아키텍처.pdf`). **⚠️ 마이그 099~103 프로덕션 DB 즉시적용 · 대표 dogfood 중(실기기 육안 미검증).**
> **🔴 미해결 = 대표 액션(다음 세션 전/중 필수):** ① ~~💳 Stripe 키 폐기~~ → **✅ 종결(2026-07-23 · 오식별로 판명)**: 세션35의 "Stripe 라이브 키" 단정은 **확인된 사실이 아니라 추정**이었음(대표가 보낸 Vercel 설정 **스크린샷의 잘린 값**을 읽은 것 — 당시 기록에도 "값은 Sensitive라 못 봄"·"앞부분만 잘려 보임"·확인은 "`sk_live_`로 시작하죠?" 유도질문). **대표 확인 = Stripe 가입 이력 없음** → 폐기할 키 자체가 없음. 재확인 불가(스크린샷 캐시 삭제·Vercel은 env 값 히스토리 없음·현재값=4321). **잔여 리스크 ≈ 0**: 노출 범위는 대표 Vercel 계정 한정, **레포는 clean**(git 히스토리·현재 파일·`.env.local` 전수검색 0건 — public 레포라 중요). ② 🔑 공용비번 **4321 → 강한 값**(여전히 유효). ③ 📧 **Gmail·Drive 복구**: Vercel `GOOGLE_OAUTH_REDIRECT_URI`=`https://complow.kr/api/google/callback` **+ Google Console '승인된 리디렉션 URI' 등록**(구글 사전등록 엄격). 앱 '테스트'모드면 refresh 7일 만료→'게시'. ④ 시스템 점검 ⚠️ 항목 정리. **[이번 세션 해결]** 공용비번 가입(4321·`test`계정 생성 검증)·Notion 연결(DB 등록초기화+`NEXT_PUBLIC_APP_URL`=complow.kr·OAuth 자가치유)·카테고리 저장. **합의**: AI 채팅·기억·개인연결·개인전용지식=**본인만**(관리자도 내용 못 봄) / 관리자는 **사용량·비용만** 열람. **다음**: 학습 **자동추출**·**프로젝트공유 기억(v1.5)**·파일 자료추천·대용량 RAG. 세션35 상세=WORKLOG. 이전 **2026-07-15 (세션 34 — **에이전트 시스템 재설계** — 리서치(6갈래) 기반 **5단계 계획** `~/.claude/plans/stateful-zooming-quiche.md`(대표 승인) + **배포됨(순차)**: Phase 1(기본 에이전트 8개 소프트삭제·마이그**096**·목록/상세 삭제UI·빈섹션 숨김·seed RETIRED) → StepPicker MCP 서버별 접이식 → Phase 2(예시 갤러리 백오피스8종·카테고리/업무영역/직무 **자유입력**·업계특화 문구 중립화·위젯 빈상태 드래그 버그수정) → Phase 3(에이전트 **지식파일 첨부** 마이그**097** `agent_knowledge`, AI-readable만[PDF·이미지·txt/md/csv/json], 채팅 풀컨텍스트 주입) → **MCP 바인딩**(마이그**098** `agent_versions.mcp_connectors`, 빌더서 내 개인 MCP 선택, 채팅·워크플로우 실행 일관). 프로덕션 **`23cca30`**·롤백 `9adcddd`·READY(`complow.kr`). **⚠️ 마이그 096~098 원격(프로덕션) DB 즉시 적용.** **남은 것**: P2.2 출력형식 재설계·P2.3 가드레일 정합·P2.4 위저드↔직접작성 통합·P2.5 애플UI · MCP **3b**(워크플로우 개인 MCP 툴노드 피커)·**3c**(에이전트 MCP 배지) · Phase 4 내보내기(SKILL.md·AGENTS.md) · Phase 5 워크플로우 사람검토 게이트(+save_file/notify CHECK 버그수정) · **아이콘**(가이드 결과폼에 이미 있음-대표 확인). 세션34 상세=WORKLOG. 이전 세션 33(2026-07-13): MCP 커넥터 11종 추가(available: Cloudflare Docs·Globalping[무인증]·Neon·Asana·Atlassian·Intercom·Square·Webflow·Wix·Canva·Prisma[OAuth+DCR 개방] / coming_soon: Slack·Figma·Vercel·Exa·Zapier·PayPal[화이트리스트·DCR없음·인증불일치]) — 카탈로그 추가만으로 원클릭 연결(연결흐름 데이터주도, 코드0줄) · AI 코칭 실장부 6개월 추세 인식(finance_entries) · 오늘할일 위젯 축소 · **워크플로우 사용성 개선**(예시 템플릿 5종·통합 '단계 추가' 피커·자동연결 명확화) · 프로덕션 `34b9a61`·롤백 `8a8d902`·READY(`complow.kr`)). 세션33 상세=WORKLOG. 이전 세션 32(2026-07-11): 오늘할일 시각알림 pg_cron(마이그 095) + 앰비언트 그라데이션 완화 + 모바일 픽스[iOS 채팅 자동확대·**사이드바 드로어 portal 탈출**(헤더 backdrop-filter가 fixed containing block 되던 회귀)·Modal body portal·viewport maximum-scale] · 프로덕션 **`0a44085`·롤백 `c954435`**·READY(`complow.kr`)).** 세션32 상세=WORKLOG. **⚠️ 실기기 육안 미검증(대표): 채팅 확대 해소·사이드바 드로어·그라데이션 톤·오늘할일 알림 도착.** 이전 **세션 31(2026-07-10)**: 프로젝트 체크리스트(마이그 **094** `project_tasks`, 부모 projects 워크스페이스 격리 EXISTS 상속·협업 CRUD) + **글래스모피즘 전면 리디자인**(P1~5, 토큰 우선 `globals.css` `--glass-*`·`.glass`/`.glass-panel`·`.app-ambient`·신규 `shared/Surface`·`PageTransition`; 캔버스·채팅·에디터·입력은 solid 유지) + 디테일(글래스 강도·카드 칸구분·호버 통일) + 비용·매출(요약 레이아웃 추세2/5+분류3/5·추세 막대 그라데이션·'매출 분류' 자유입력+자동제안). 프로덕션 `4e501d3`·롤백 `c87e091`. **미검증(대표 육안): 3테마·Safari 블러 성능.** **이전 세션(≤30) 상세 = WORKLOG.md·git 커밋**(굵직한 것만): 세션30 프로젝트 중요도(091)·오늘할일 위젯(092)·구성원 부서 부여(093) + **도메인 `complow.kr`+`www.complow.kr` LIVE**(가비아 DNS+Vercel, 프로덕션 `43552ec`) · 세션29 **MCP 개인연결 대개편**(마이그 088~090 `mcp_user_connections`+OAuth 커넥터, `AGENTS-MCP-STRATEGY.md`) + 모바일 반응형 골격(`MobileNav`·`useMediaQuery`·`h-dvh`) · 세션28 MCP 커넥터 디렉터리·bearer 토큰 암호화(086)·AI 비용 예산 한도(087) · 세션27 Drive 탭·Gmail 리치작성/AI 다듬기 · 세션26 **브랜드 EQURIA→Complow 확정**(§합의된 정책) · 세션25 손익 계층표 · 세션15~21 **현금흐름 손익계산기**(마이그 078~085, §현금흐름). **미픽스 기술부채·MCP 마이너 = `known-issues.md`(I17 등).**
> ⚙️ **작업 방식(하네스/루프):** `.claude/skills/work-harness.md`(작업 SOP·검증 게이트·멀티에이전트 기준) + `/deploy`·`/verify` 명령 + push 전 tsc/lint 훅. 매 작업 = 잘게 순차 → tsc 0·lint 30/0·build 0·(DDL이면 RLS 시뮬) → main-first 배포 → 보고.

---

## 🔜 다음 세션 바로 착수 (세션45[구글 로그인 분리·CASA 회피·나이스페이·AI 원가 진단] 인계 · 2026-08-12~14 — 이 블록부터 읽기)

> **✅ 프로덕션 = `9f29785` (2026-08-14 · Vercel READY · `complow.kr`/`www.complow.kr` alias 확인) · 롤백 후보 = `c11dba6`**
> 게이트(매 커밋): tsc **0** · eslint **29/0**(베이스라인 유지·신규 0) · `next build` 성공 · main-first 푸시. **DDL 0 — 이번 세션 마이그 추가 없음**(마이그 001~134 그대로).
> 작업 브랜치 `worktree-gmail-scope-narrow` = main과 동일 내용, origin 푸시됨.
>
> **라이브된 것(세션45 전체)**: 구글 로그인 전용 GCP 프로젝트 분리(인원 상한 제거) · 브랜드 검증 통과(동의화면 `Complow`) · Gmail 스코프 `gmail.send`만(CASA 회피) · `/mail` 발송 전용화 · 로그인/가입 문구 줄바꿈 + 폴백 폼 접기 · 통신판매업신고번호 푸터 게재 · 화면 "충전" 표현 제거 · 푸터 "앱 설치"(PWA) 라벨 정합 · `Notion clean` Sonnet 전환 · 캐시 무효화 잠복 버그 수정 · 다단계 턴 빈 답변 저장 버그 수정.
>
> ⚠️ **대표 메인 체크아웃 정리 필요**: `LandingPage.tsx`·`credits.ts`("충전" 문구)는 **내가 브랜치에서 이미 커밋·배포**했으므로 대표 로컬 수정분은 중복이다 → `git restore` 후 `main`으로 이동:
> `git -C /Users/johwiwon/equria-workspace restore HANDOFF.md src/components/landing/LandingPage.tsx src/lib/credits.ts && git -C ... checkout main && git -C ... pull`
> ⚠️ **`feat/toss-ui-refresh`는 main보다 뒤처짐** — 정리(main으로 리셋 또는 삭제) 필요.

### ✅ 구글 OAuth 프로젝트 분리 — 로그인 100명 상한 제거
- **문제**: 기존 GCP 프로젝트 `complow`(ID `influencer-automation-489610`)는 프로덕션 게시·**브랜드 검증 통과**지만 **데이터 액세스(스코프) 검증 미완** → **OAuth 사용자 한도 100명이 실제 적용 중**(4명 사용 · **재설정 불가**). 로그인을 여기 두면 96명에서 신규 가입이 영구 차단될 위험.
- **해결**: 로그인 전용 프로젝트 **`complow-login`** 신설 → 스코프 `openid`·`userinfo.email`·`userinfo.profile` **3개(전부 비민감)** · 민감·제한 **0개 확인** → **프로덕션 게시 완료**. 근거 = 구글 콘솔 원문 *"사용자 한도는 **승인되지 않은 민감·제한 범위를 요청할 때** 제한"* → 비민감 전용 앱은 상한 미적용.
- OAuth 클라이언트 `complow-web-login`(리디렉션 URI = `https://dutovtfdckhayyvhtuxu.supabase.co/auth/v1/callback`) → **Supabase Google provider client id/secret 교체 완료**.
- **✅ 실검증**: 대표 시크릿창 로그인 성공 — 기존 계정이 새 client로 그대로 붙음(구글 `sub`는 계정 단위 고정이라 identity 유지). 회귀 없음.
- 브랜딩 입력 완료(로고 `icon.png`·홈페이지·`/privacy`·`/terms`·승인도메인 `complow.kr`).
- **✅✅ 브랜드 검증 통과·게시 완료(2026-08-13)** — 인증 센터 "브랜딩이 인증되었으며 사용자에게 표시되고 있습니다". **동의화면 실물 확인**: 제목 `Complow 서비스로 로그인` + CP 로고 + `Complow의 개인정보처리방침 및 서비스 약관`. 어제까지 `dutovtfdckhayyvhtuxu.supabase.co`가 노출되던 문제 **해소**.
  - 요청 권한 = 이름·프로필 사진 + 이메일 **2개(최소 구성)**. 최초 1회 동의화면은 구글 정책상 생략 불가(정상). client_id를 교체했으므로 기존 사용자도 1회 재동의 필요.
  - **→ Supabase 커스텀 도메인(월 $10 애드온 + Pro $25) 불필요.** 검증 통과로 목적 달성. **선결제 금지.**
  - 절차 기록(재사용): 브랜딩 페이지에서 정보 저장 → **"브랜딩 확인"** 클릭(자동 심사 수 분·수동 검토 시 2~3영업일) → **"브랜딩 게시"까지 눌러야 반영**되고 **통과 결과는 7일만 유효**(미게시 시 재확인 필요).
  - 앞선 세션의 "승인 도메인(`complow.kr`)과 표시 도메인(`supabase.co`) 불일치로 반려될 수 있다"는 우려는 **과대평가였다** — 공식 문서상 검사 대상은 승인 도메인·홈페이지·개인정보처리방침이고 리디렉션 호스트는 검사 항목이 아니다.

### ✅ 스코프 정책 확정 — CASA 연간 감사 회피 (대표 결정 A안)
- **구글 공식 확인**: `gmail.modify`·`gmail.compose`·`gmail.readonly`·`drive.readonly` = **제한(restricted)** → 프로덕션 공개 시 **CASA Tier 2 연간 보안감사(제3자 감사기관·유료)** 강제. `gmail.send`·`calendar.events.readonly` = **민감(sensitive)** → 검증만, CASA 없음.
- ⚠️ **기존 HANDOFF 기록 "Gmail=compose만 → 민감(제한 아님)"은 오류였다** — `gmail.compose`는 제한이다. 이 기록으로 정정.
- **`oauth.ts` `GOOGLE_SCOPES` = openid·email·profile·`gmail.send`** (커밋 `1da3c14`).
  - Drive: `GoogleDriveTab.tsx`가 **어디서도 임포트되지 않는 죽은 코드**라 `drive.readonly` 제거로 깨지는 기능 0. 파일·`/api/google/drive/*` 라우트는 **보존**(파괴적 삭제는 별도 검증 후).
  - **✅ `/mail` 발송 전용화 완료(커밋 `84f5e4d`)**: 신규 `MailSendView`(연결 확인 + '새 메일 쓰기' → 기존 `MailCompose` 우하단 플로팅 재사용, 첨부·서식·AI 다듬기 유지·디자인 무변경). `MailShell.tsx`와 읽기 라우트(`threads`·`labels`·`attachments`)는 **보존**(grep으로 호출자 0건 확인 → 도달 불가 상태. 되돌리려면 페이지만 `MailShell`로 교체. 파괴적 삭제는 별도 검증 후 = safe-changes §2).
  - ⚠️ **`features.ts` `hiddenFromNav: true`가 유지 중이라 사용자가 발송 화면에 도달할 경로가 없다.** 에이전트에도 메일 발송 도구가 없어 실질적으로 발송 기능이 잠긴 상태 → **노출 여부 = 대표 결정 대기**(노출하려면 `hiddenFromNav` 제거 한 줄).
  - 기존 연결 사용자 토큰은 부여 당시 범위 유지(축소는 재동의 시점부터 적용).
- ⚠️ **기존 프로젝트의 "제한된 범위" 칸 미확인**(스크린샷 스크롤 잘림). Gmail 계열이 남아 있으면 그 프로젝트 스코프 검증 시 CASA가 재발한다 → 확인 필요.

### ✅ 온보딩 경로 코드 검증 (가입 개방 선행조건)
- `handle_new_user` v2(마이그116) = `profiles`만 생성·**워크스페이스 자동등록 없음** → 외부인이 기존 회사로 침입 불가
- `(app)/layout.tsx:62` 멤버십 0 → `/onboarding` → `create_workspace` RPC(workspaces plan `free` + 오너 멤버십 + 전체방)
- 신규 워크스페이스 시드 에이전트 **0개**(마이그130 no-op — 의도된 clean-slate, 컴피가 범용 비서)
- **⚠️ 실제 신규 가입 E2E는 미검증**(신규가입 OFF라 테스트 불가). 켠 직후 검증 필수.

### ✅ UI (커밋 `53d176e`)
- 한글이 음절 단위로 잘리던 것(`로그인됩/니다`·`동/의하는`) → `break-keep` 3곳(`AuthForm.tsx`). 문구·레이아웃 무변경.
- 로그인 화면 이름·비밀번호 폼 **기본 접기 + '기존 계정으로 로그인' 토글**. **제거 불가 근거 = `auth.users` 7명 중 구글 연결 2명 / 미연결 5명이고 그 5명 전원이 워크스페이스 멤버 + 로그인 이력 보유(실사용자). 합성 이메일 `u<hex>@equria.local`이라 이메일 자동 연결도 불가 → 제거하면 5명 영구 잠김.** 5명이 마이페이지에서 구글 연결을 마치면 분기 통째 삭제 가능.

### 💰 AI 원가 진단 (2026-08-14 · 결제 연동 전 필수 선행) — 문서 `https://claude.ai/code/artifact/be3ff135-6603-4823-99d1-e027083fa054`

**측정 기준**: 프로덕션 `agent_usage` 165건(2026-06-14~08-11) · 총원가 $17.44 · 환율 ₩1,514.45/$(`fx_rates` 2026-07-07) · 단가 Sonnet4.6 $3/$15 · Haiku4.5 $1/$5 · Opus4.7 $5/$25.

**확인된 사실 (DB·코드 실측)**
- 평균 호출: 입력 **32,276토큰** / 출력 810 → **원가의 89%가 입력**. 평균 $0.1057 · 중간값 $0.0221 · p90 $0.2247 · 최대 $1.17.
- 세그먼트: **에이전트 채팅 39건이 원가의 64%**($11.18), 내부·단발 126건이 36%($6.26).
- **단일 최대 항목 = `"Notion clean"` 에이전트(Opus 4.7) 26건 $10.60 = 전체의 61%.** 평균 입력 **76,522토큰**인데 **지식파일 0개** → 정체는 Notion MCP 도구 정의 + 도구 결과 누적. 한 대화 추적: 37,960 → 40,997 → 44,601 → 45,366(4턴).
- **캐시 커버리지 2/24 (8%)** — 캐시 코드가 있는 곳은 `assistant/route.ts:96-103`, `agents/[id]/chat/route.ts:269-276` 뿐. 나머지 22곳(워크플로우·리서치5·OCR2·다듬기3·백그라운드3 등)은 구조적으로 히트 0.
- 🔄 **정정(2026-08-14 재검증) — "캐시 무산" 진단은 오판이었다.** 프롬프트 캐싱은 커밋 **`3f1b417`(2026-08-08)** 에 배포됐고, **에이전트 채팅 실측 59건은 전부 08-07 이전 = 배포 전 데이터**다. 캐시가 무산된 게 아니라 **배포 후 에이전트 채팅을 한 번도 안 썼을 뿐**이다.
  - **배포 시점 기준 재집계**: 배포 전 162건 → 히트율 **0%** · 평균 원가 **$0.1074**. 배포 후 3건 → 캐시 쓰기 2·읽기 3 · 히트율 **32.8%** · 평균 원가 **$0.0122**(배포 전의 **1/9**).
  - → **캐싱은 정상 작동 중이다.** 히트율 0.3%는 분모에 배포 전 162건이 섞인 착시.
  - ✅ **에이전트 채팅 경로도 검증 완료(2026-08-14 15:20~15:21, 대표 실사용 2턴)**:
    | 턴 | 일반입력 | 캐시쓰기 | 캐시읽기 | 출력 | 원가 |
    |---|---|---|---|---|---|
    | 1 | 668 | **46,014** | 0 | 327 | $0.2991 |
    | 2 | 25,346 | 0 | **230,070** | 2,463 | $0.3033 |
    2턴에서 **캐시로 230,070토큰을 읽었다**(캐시 없으면 그것만 $1.15 → 실제 $0.115). **에이전트 채팅에서도 캐싱 정상 작동 확인.**
    부수 검증: Opus 단가로 역산한 값($0.299/$0.303)이 기록 원가와 소수점까지 일치 → **`pricing.ts` 원가 계산 로직도 정확**. `tokens_input`은 total(캐시 포함)이고 `plainInput = total − read − write`로 계산된다.
    ⚠️ **다만 2턴 총 입력이 485,486토큰**이다(1턴 46,682 → 10배). Notion 도구 결과가 `stepCountIs(5)`로 누적된 결과 → **도구 결과 누적은 여전히 실재하는 문제**.
  - ⚠️ 배포 후 표본 3건은 통계적으로 약하다. 히트율 32.8%를 확정치로 쓰지 말 것.
  - ⚠️ **이 절의 원가율 156%/216%는 2026-08-14 §포함량 재설계로 대체됐다**(→ 39.2%/40.2%). 아래 서술은 재설계 **이전** 기록이며, 당시 계산은 공정사용 3배를 빼먹어 진짜 최악값(470%/649%)을 놓쳤다. **원가율 계산 방식 자체(포함 크레딧 × $0.01)는 유효**하다. 다만 크레딧은 실제 `cost_usd`로 차감되므로 **캐싱이 원가를 1/9로 줄이면 같은 포함량으로 약 9배 더 쓸 수 있다** → "AI를 별로 못 쓴다" 문제는 상당 부분 이미 해소된 상태일 수 있다.
- 지식파일 주입: **개수 상한 없음**(`chat/route.ts:146-149` `.limit()`·`.order()` 둘 다 없음), 파일당 20만자 컷만(`agentKnowledge.ts:41`). PDF/이미지는 **캐시 breakpoint 밖**(`:170-176`)이라 매 턴 재과금.
- 기억 30건(최대 ~12,000자)·대화 요약도 **breakpoint 뒤(volatile)** → 매 턴 정가.
- 채팅 1턴 = 최대 **3회 과금**(본 응답 + 기억추출 haiku 3턴마다 + 요약 haiku overflow 4+).
- `MODELS.complex`(opus-4-7)는 **코드에서 참조 0건** — Opus는 에이전트 버전의 DB `model` 값으로만 들어온다.

**캐시 규격(공식)**: 읽기 0.1× · 쓰기 1.25×(5분)/2×(1시간) · 손익분기 2회/3회 · **기본 TTL 5분**(`ttl:"1h"` 미사용) · **최소 캐시 길이 1,024~2,048토큰, 미달 시 에러 없이 조용히 무시**.

**흑자 기준(대표 결정)**: 원가율 **40%** · Haiku 라우팅은 **사용자에게 안 보이는 내부 작업만**(기억추출·대화요약·MCP도구요약).
→ 허용 AI 원가: **Standard $7.66/월**(현 포함 3,000크레딧=원가 $30·원가율 156%) · **Pro $12.94/월**(현 7,000=$70·216%).
→ 캐시 복구 + Opus 정리 + 내부 Haiku로 호출당 **$0.106 → $0.030** 가능 = **포함량을 안 깎고도 흑자 전환 여지**.

**실행 순서(2026-08-14 재검증 후 갱신)**:
① ~~캐시 무산 원인 확정~~ → **✅ 완료: 무산 아니었음(위 정정).** 에이전트 채팅 경로도 **대표 실사용 2턴으로 실효 검증 완료**(캐시 읽기 230,070토큰). **이 항목은 종결.**
①-b ✅ **완료(2026-08-14)**: **`Notion clean` → Sonnet 4.6 전환**(대표 결정 — "연동된 Notion 워크스페이스를 정리해주는 에이전트"라 정리·분류 작업 ⇒ Opus 불필요). **DB 데이터 변경**(DDL 아님·마이그 불필요):
  `update public.agent_versions set model='claude-sonnet-4-6' where id='aaad8181-d343-420c-b54b-97aee1631089'`
  **롤백**: 같은 문장에서 `model='claude-opus-4-7'`. (agent_id `f7eb7cd1-6e95-4ecd-98f7-0899d1f38db4` · version 1 · is_current true · 워크스페이스 plan=premium이라 게이트 무관)
  **예상 효과 −40%**: 위 2턴을 Sonnet 단가로 재계산하면 $0.299→**$0.180**, $0.303→**$0.182**.
①-c ✅ **완료: 캐시 무효화 잠복 버그 수정** — `chat/route.ts`의 `agent_knowledge`·`mcp_servers`·`mcp_user_connections` 조회 3곳에 **`.order(created_at, id)` 추가**. 이 결과가 그대로 캐시 프리픽스가 되고, **MCP 도구 정의는 렌더 순서상 system보다 앞**이라 행 순서가 흔들리면 캐시가 통째로 무효가 된다. 지금까지는 우연히 일정했을 뿐.
①-d ✅ **완료 + 정정: "도구 결과 누적"은 문제가 아니었다 (2026-08-14 조사)**
  - **누적은 턴 간에 일어나지 않는다** — `messages` 테이블에는 **텍스트만** 저장되고 도구 호출/결과는 저장되지 않는다. 다음 턴에 재전송되지 않는다.
  - **"입력 10배(46,682 → 485,486)"는 오해였다.** 캐시 읽기 **230,070 = 46,014 × 정확히 5** — 즉 한 턴 안에서 `stepCountIs(5)`로 **5스텝**이 돌며 각 스텝이 캐시 프리픽스를 재읽은 합계다(`totalUsage`는 전 스텝 합산). 하나의 거대한 프롬프트가 아니고, **캐시가 이미 그 반복을 0.1×로 감당하고 있었다.**
  - → 다단계 도구 루프의 컨텍스트 재전송은 에이전트 구조상 내재적이며, 캐싱이 정답이다. 별도 차단 작업은 불필요.
  - 🐛 **대신 실제 버그를 발견해 수정**: `onFinish`의 `text`는 SDK 정의상 **마지막 스텝**의 텍스트다(`OnFinishEvent = StepResult<TOOLS> & { steps }`, `ai/dist/index.d.ts:3262`). 도구를 쓰는 턴이 **도구 호출로 끝나면 `text=""`** 가 되어 **assistant 메시지가 빈 채로 저장**된다.
    - 실측 증거: 2026-08-14 15:21 턴은 출력 **2,463토큰**을 쓰고 원가 $0.303을 냈는데 `messages.content` 길이가 **0자**. 사용자는 스트리밍으로 봤어도 **새로고침하면 답변이 사라진다.**
    - 부수 피해: `turnsForExtraction(messages, text)`도 같은 `text`를 써서 **자동 기억 추출이 빈 답변을 보고** 돌았다.
    - **수정**: `steps`의 전 스텝 텍스트를 `\n\n`으로 이어붙인 `fullText`를 저장하고 기억 추출에도 전달(단일 스텝이면 기존과 동일값·`|| text` 폴백).
  - ✅ **확인 완료(대표 2026-08-14): "답변은 다 잘 됐다"** — 화면에는 정상 출력됐다. 즉 **저장 전용 버그**였고 위 수정으로 종결. **`stepCountIs(5)` 상한 조정은 불필요**(미완 종료가 아니었음).
② **캐시 커버리지 확대** ← 새 1순위. 24곳 중 22곳이 여전히 미적용(워크플로우·리서치5·OCR2·다듬기3·백그라운드3). **워크플로우가 특히 큼**(노드마다 system·도구정의 정가 재전송).
③ **캐시 TTL 5분 → 1시간** — 대화 간격이 5분 넘으면 미스. 재사용 3회↑면 이득.
④ **`Notion clean` 정리**(**대표 확인 필요**: 복잡 판단이면 Opus 유지 + 도구결과 누적만 차단 / 정리·분류면 Sonnet 전환 −40%). 배포 전 데이터지만 **구조는 그대로**라 유효한 문제.
⑤ **내부 작업 Haiku 라우팅**(기억추출·대화요약·MCP도구요약).
⑥ ~~효과 측정 후 필요시 무료 공정사용 3배→1배 · 포함량 축소~~ → **✅ 완료(2026-08-14, 아래 §포함량 재설계)**: 공정사용 3배 → 무료 1.0 / 유료 1.3, 포함량 3,000/7,000 → 750/1,300. **"캐싱 효과가 확인되면 ⑥은 안 해도 될 것"이라던 판단은 틀렸다** — 캐싱은 회당 원가를 줄이지만 공정사용 3배가 곱해진 **최악값 470%**는 그대로였다. 남은 것은 지식파일 RAG(미착수).

⚠️ **미확정**: 정상 히트율의 안정값(배포 후 표본이 **3건뿐**, 관측 32.8%) · 실사용 패턴(표본이 대표 1인 dogfood라 고객 패턴 미지) · Notion MCP 도구 정의 크기(개인 커넥터라 런타임 조회로 DB 측정 불가). ※ "캐시 무산 원인"은 미확정 목록에서 **제거** — 무산 자체가 없었다(위 정정).
⚠️ **구조적 한계**: `agent_usage`에 **라우트 식별 컬럼이 없어** 기능별 원가를 간접 추정만 할 수 있다 → 컬럼 추가 권장.

### 🔴 대표 액션
1. ~~Supabase 신규가입 ON~~ → **✅ 이미 켜져 있음(2026-08-13 라이브 확인)**. 확인 방법 = GoTrue 공개 설정 엔드포인트: `curl -H "apikey: <anon>" https://dutovtfdckhayyvhtuxu.supabase.co/auth/v1/settings` → **`disable_signup: false`**(=가입 허용) · `google: true` · `email: true`(기존 5명 폴백) · `apple: false` · `anonymous_users: false`.
   ⚠️ **세션45 초반에 "가입 OFF"라고 적은 것은 HANDOFF 구절을 근거로 한 오판이었다** — 라이브 설정이 SSOT. **즉 외부인은 이미 가입 가능한데 E2E 검증만 안 된 상태**이므로, 제3자 계정 가입 테스트의 우선순위가 올라간다(대표 보류 중이나 문은 열려 있음).
2. **브랜드 검증 제출 확인** — `complow-login` 인증 센터
3. **나이스페이 전자결제 신청** — 테스트 상점 개설 완료(상점명 `컴플로우`). 신청서에 **"월 정액 구독 SaaS"** 기재 · **"크레딧·충전·포인트" 표현 금지**(KCP 반려 추정 원인 = 포인트충전 위험업종 분류). 요구 서류 목록 공유 필요.
4. **통신판매업 신고번호** → 푸터 반영(대표 보유·미전달. 푸터엔 사업자번호·대표·주소·전화·이메일까지 이미 라이브)
5. 기존 프로젝트 **"제한된 범위" 화면 캡처**

### ✅ 포함량 재설계 — 원가율 40% 역산 (대표 결정 2026-08-14 · 마이그 **138**)

**왜 지금**: 결제 붙이기 전 마지막 기회. **유료 워크스페이스가 0개**(free 1 · premium 2)라 지금은 순수 설정 변경이지만, 유료 고객이 생기면 포함량 축소는 **계약 변경**이 된다.

**바뀐 값** (1 크레딧 = $0.01 원가 · 환율 ₩1,514/$)

| 플랜 | 포함량 | 공정사용 배수 | 최악 원가 | 매출 | 포함량 원가율 | 최악 원가율 |
|---|---|---|---|---|---|---|
| Basic | 500 (유지) | **3 → 1.0** | $15 → **$5** | $0 | n/a | n/a |
| Standard | **3,000 → 750** | **3 → 1.3** | $90 → **$9.75** | $19.15 | 156% → **39.2%** | 470% → **50.9%** |
| Pro | **7,000 → 1,300** | **3 → 1.3** | $210 → **$16.90** | $32.36 | 216% → **40.2%** | 649% → **52.2%** |

> 이전의 진짜 최악값은 156%/216%가 아니라 **470%/649%**였다 — 공정사용 3배가 포함량 위에 곱해지는 걸 앞선 계산에서 빠뜨렸다. 배수를 1.3으로 내린 게 이번 변경의 절반이다.

**바뀐 곳 (3군데 · 반드시 같은 값 유지)**
- `src/lib/plans.ts` — `PlanDef.fairUseMultiplier` 신설 + `includedCredits`. 산정식·환율·부가세 주석 포함.
- `src/lib/credits.ts` — 전역 `FAIR_USE_MULTIPLIER`(=3) **삭제** → `fairUseMultiplierOf(plan)`. `creditBlockReason`이 플랜별 배수 사용.
- `supabase/migrations/138_plan_credits_40pct.sql` — `plan_monthly_credits()`. **차감·충전의 실제 SSOT는 DB**이므로 plans.ts만 바꾸면 "안내는 여유인데 실제로 막히는" 상태가 된다.

**체감 (실측 회당 원가 기준 · Standard 5인 워크스페이스 전체)**

| 무엇을 쓰나 | 회당 | 이전(3,000) | **지금(750)** | 1인 하루 |
|---|---|---|---|---|
| 어시스턴트(컴피) | $0.0122 | 하루 82회 | **하루 20.5회** | 4.1회 |
| 기본 에이전트 | $0.009 | 하루 111회 | **하루 27.8회** | 5.6회 |
| MCP 에이전트 | $0.025 | 하루 40회 | **하루 10회** | 2.0회 |
| MCP 꼬리물기 | $0.18 | 하루 5.6회 | **하루 1.4회** | 0.3회 |

**들어가나?** 대표 실사용 187건/78일을 **캐싱 후 단가로 환산**하면 1인 하루 $0.022 → 5인 $0.11/일 vs 허용 $0.25/일 = **45% 사용, 여유 있음**. 단 **MCP 꼬리물기(Notion형)를 상시 쓰면 즉시 초과**한다 — Standard의 실질 제약은 채팅 횟수가 아니라 꼬리물기 횟수다.

**랜딩 문구 변경 불필요** — 요금표가 크레딧 숫자를 안 쓰고 "AI 채팅 넉넉히 / 공정 사용 / 매일 사용량 제공"으로만 표기한다(PG 환금성 분류 회피 목적, `LandingPage.tsx:55` 주석). 숫자를 안 걸어놨던 게 이번에 이득이 됐다.

**⚠️ 남은 설계 구멍**: 인원 추가(₩4,000/인)가 포함량을 **늘리지 않는다** → 10인 Standard가 5인과 같은 750을 받는다(1인당 150 → 75). 시트 비례 가산 미설계 → 아래 착수 순서에 넣음.

**롤백**: 마이그138 주석의 롤백 SQL(500/3000/7000/null) + `plans.ts` includedCredits·fairUseMultiplier(3) 되돌리기. 데이터 변경 없는 순수 함수 교체라 위험 0.

### 🔜 다음 세션 착수 순서 (2026-08-14 갱신 — 위 항목들은 완료돼 제거함)

**원가 절감 트랙 (결제 연동보다 앞. 근거·수치는 위 §AI 원가 진단)**
1. **캐시 TTL 5분 → 1시간** — `providerOptions.anthropic.cacheControl`에 `ttl:"1h"` 추가(현재 2곳: `assistant/route.ts:96-103`, `agents/[id]/chat/route.ts:269-276`). 쓰기 1.25×→2×지만 재사용 3회↑면 이득. 대화 간격이 5분 넘으면 지금은 매번 미스.
2. **캐시 커버리지 확대** — 24곳 중 **22곳 미적용**. **워크플로우(`workflows/[id]/run/route.ts:275`)가 가장 큼**(노드마다 system·도구정의를 정가 재전송, 최대 6노드). 그다음 리서치 5개·다듬기 3개.
   ⚠️ Haiku는 최소 캐시 **2,048토큰**(Sonnet 1,024) — 내부 작업에 캐시를 걸 때 미달 주의.
3. **내부 작업 Haiku 라우팅**(대표 결정 범위 = 기억추출·대화요약·MCP도구요약. **사용자가 보는 결과물은 제외**). 기억추출·대화요약은 **이미 `MODELS.cheap`** 이므로 실제 남은 건 `lib/mcp/summarize.ts:33`(현재 default).
4. **Sonnet 전환 효과 실측** — 대표가 `Notion clean`을 한 번 쓰면 `agent_usage`로 확인. 예상 $0.18대(전환 전 $0.30).
4-1. **시트 비례 포함량 가산** ← 마이그138의 남은 구멍. 지금은 10인 Standard가 5인과 같은 750크레딧이라 **1인당 포함량이 절반**이 된다. ₩4,000/인 = $2.64 매출이므로 40% 기준 **추가 1인당 +105크레딧**이 정합. 구현 = `plan_monthly_credits(plan)` → `plan_monthly_credits(plan, seats)` 확장 + `credit_sync`가 `workspace_members` 비게스트 수를 넘겨주기. **결제 연동보다 앞에 둘 것** — 좌석 과금과 포함량이 같은 계산에 묶여야 한다.
4-2. **꼬리물기 원가 상한** ← Standard의 실질 제약. 회당 $0.18(채팅 25배)이라 포함량 750이면 하루 1.4회뿐. `stepCountIs(5)`가 스텝마다 캐시 프리픽스를 재읽으므로(실측 46,014×5) **도구 결과를 다음 스텝에 누적 전달하지 않는지** 재확인 + 필요시 스텝 상한을 에이전트별 설정으로.

**결제 트랙 (대표 임계경로)**
5. **나이스페이 전자결제 신청 + 구비서류** — 테스트 상점만 개설된 상태. 심사 며칠~2주라 빨리 시작할수록 좋다. 신청서에 **"월 정액 구독 SaaS"** 기재·**"크레딧/충전/포인트" 표현 금지**.
6. **테스트 상점 MID·상점키 확보 → 결제 연동 착수** — 결제 코드 **0줄**. 착수 지점 설계는 위 §코드 브리핑 참조(`api/payments/nicepay/*` · `lib/nicepay.ts` · `subscriptions`/`payments` 테이블 · 설정 요금제 카드).
   ⚠️ **선행**: 인원 초과 과금 정책(랜딩 "6명째부터 ₩4,000" vs 마이그125가 6번째 초대 거부) — **자동 추가 과금은 정기결제(빌링키) 전용**이라 일반결제로는 "좌석 추가 구매" 반자동이 현실적.

**정리·부채**
7. **`feat/toss-ui-refresh` 브랜치 정리** + 대표 로컬 `git restore`(위 헤더 명령).
8. **`agent_usage`에 라우트 식별 컬럼 추가** — 지금은 기능별 원가를 `agent_id`/`model`로 간접 추정만 할 수 있다. 원가 관리의 구조적 한계.
9. **CLAUDE.md 스펙 갱신** — §6이 "마이그 001~130 · 57테이블"인데 실제 **134**. 세션마다 첫 읽는 문서의 틀린 숫자가 오판을 만든다(이번 세션 오판 2건이 문서 신뢰에서 나왔다).
10. **기존 GCP 프로젝트(`influencer-automation-489610`) "제한된 범위" 확인** — Gmail 계열이 남아 있으면 캘린더 연동 정식화 때 CASA 재발. 스크린샷 한 장.
11. lint 29건(전부 `react-hooks/set-state-in-effect`) 정리 — 신규 위반을 눈으로 못 걸러내는 상태.
12. 기존 5명 구글 연결 유도 → 로그인 폴백 폼 완전 삭제(현재는 접힘).

**⏸️ 보류 (대표가 먼저 꺼낼 때만)**
13. **제3자 구글 계정 E2E 가입 검증** — *"내가 나중에 해볼 테니 정리해놨다가 다른 세션에서 꺼낼 때 진행"*(2026-08-13). **재촉 금지.** 절차는 메모리 `pending-signup-e2e-test.md`. ⚠️ `chowhiwon99@gmail.com`·`complow@complow.kr`은 이미 등록된 오너라 테스트 불가. **실제 고객 가입 경로는 아직 한 번도 실행된 적 없다**(코드·DB 검증만 완료, 신규가입은 ON 상태).

---

## 🔜 (이전) 세션44[원가 레버 재조준·하이브리드 한도·크레딧 표현] 인계 · 2026-08-10

> **✅ 상태 = 프로덕션 `b20ac6d` (2026-08-10 배포·main-first) · 마이그 001~134**(134 원격 적용 완료·파일화 완료) · main=feat=origin 동기 · 각 tsc0·lint29/0(신규0)·build0.
> **롤백 후보 = `0560ea1`**(세션43 마지막 프로덕션) · 그 이전 = `cdb3fdc`

### 세션44 완료 — 세션43 인계의 ①~④ 전부

**⓪ 실측이 ①의 전제를 뒤집었다 (가장 중요)**
- 60일 원가 분포: **Opus 에이전트 채팅 $10.60(61%)** · 큰 프롬프트(어시/리서치) $4.48(26%) · 중간 보조 $1.66(10%) · **Haiku로 보내려던 소형 라우트 $0.08(0.5%)**.
- "Haiku 라우팅 27% 절감"은 **캐싱 전에 계산된 수치**였다. 캐싱 후에는 **Sonnet 캐시읽기 $0.30/MTok < Haiku 정가입력 $1.00/MTok** — 캐시 걸린 Sonnet이 캐시 없는 Haiku보다 3.3배 싸다. **Haiku 4.5는 캐시 최소 프리픽스가 4,096토큰**(Sonnet 1,024)이라 작은 라우트는 옮기면 캐싱을 잃는다.
- → **Haiku 라우팅 보류(대표 결정)**. 남은 원가 레버는 Opus 통제였고 그쪽으로 전환.

**① 고급 모델 Pro 전용(`4dd987d`·마이그134)** — 코드가 Opus를 전 플랜에 열어둔 반면 랜딩은 Pro 전용으로 팔고 있던 불일치도 함께 해소. 에이전트 버전은 **클라가 Supabase로 직접 insert**하므로 앱 게이팅은 우회 가능 → BEFORE INSERT 트리거가 유일 강제 지점. 모델 id 나열 대신 이름 매칭(opus/fable/mythos)으로 **fail-closed**. 기존 Opus 에이전트는 grandfather. 5경로 시뮬 통과.

**② 하이브리드 한도(`aa69d68`·DDL 없음)** — `checkBudget(userId, kind)`. **기본값 "automated"(엄격)** = 새 라우트가 표시를 빼먹으면 막히는 쪽으로 실패. interactive 6곳만 명시(채팅·컴피·수식·기억하기·제작인터뷰·재무코칭). **공정사용 안전밸브 3배** — 채팅도 무한은 아니다. 경계값 11케이스 통과.

**③④ 크레딧 → AI 사용량(`3e66d01`)** — DB·트리거·차감은 그대로, 표시만. 랜딩 "월 500 크레딧"(부정확) → "매일 충전", 요금표에 채팅/자동실행 2행 분리, **FAQ top-up 문구 삭제**, 게이지 "N% 남음". **차감 계수 $0.01 유지**(캐싱으로 크레딧당 대화량이 이미 ~4배 늘어 조정 불필요).

**④ AI 사용량 기록 누락 7곳 봉합(`b20ac6d`·신규 `lib/aiUsage.ts`)** — 배포 직전 발견. AI 호출 23곳 중 **7곳이 `agent_usage` 기록을 아예 안 했다**(명함/영수증 OCR·채팅/메일/회의록 다듬기·프롬프트 생성·MCP 도구 요약). 마이그133 트리거는 **기록하는 라우트만** 잡으므로 이들은 그물 밖 → 원가 집계 누락·크레딧 미차감·예산 미적용. **⓪의 "60일 $17.4"도 이만큼 작았다.** 20줄 insert 복붙이 원인이라 `recordAiUsage` 한 줄 호출로 통일. 검증: 전수 23/23 · DB 시뮬(차감 0.81=$0.0081 정확·실패 미차감) · `consumeStream`이 응답 스트림을 먹지 않는지 실호출 확인.

### 🔴 다음 세션 착수 순서

**① 대표 dogfood(배포 완료 — 이제 확인만)**
- 무료 워크스페이스('테스트')로 전환 → 에이전트 만들기에서 **Opus가 잠기고 'Pro 이상' 배지**가 뜨는지
- 자동 실행 소진 후 **채팅은 계속 되는지** ← ②의 핵심
- 사이드바 **'AI 사용량 N% 남음'** (대표 계정은 premium이라 안 보이는 게 정상)
- 랜딩 요금표는 **라이브 검증 완료**(신규 문구 6종 O·구 문구 4종 0)

**② 무료 범위 결정 + 기능별 게이팅 (대표 방향 = "AI 빼고 다 열기")**
- **기능별 게이팅이 미구현**이다. `features.ts`에 요금제 조건이 없어서 무료가 결재·근태·회의노트·재무를 다 쓴다. 랜딩은 Standard부터라고 파는데 코드는 안 막는다 → **SNS로 받아도 돈 낼 이유가 없다.**
- 대표 방향: 무료 = AI 맛보기 + 나머지 전부 / 유료 = AI 본격 + 인원 추가. 요금표를 이 구조로 다시 쓰고 게이팅은 AI에만.
- 선행: **무료 워크스페이스 파일 용량 상한**(버킷 5개가 크기 무제한 — `docs/ops/infra-limits.md`).

**③ 인원 초과 과금 (판매 약속과 코드 불일치)**
- 랜딩은 "6명째부터 1인당 ₩4,000"인데 마이그125가 **Standard 6번째 초대를 거부**한다. 무료 3명은 그대로 막고 **유료만 상한 해제 + 초과 인원·금액 표시**(수동 청구 반영용). 대표 결론 = 가격 사다리가 10명에서 Pro와 정확히 일치하도록 설계돼 있어 초과 과금이 전제.

**④ 결제(PG) — 나이스페이로 방향 확정 (조사 2026-08-12 · 진행 2026-08-13)**
- 🔑 **핵심 발견: KCP 반려 사유들은 별개가 아니라 인과관계였을 수 있다.** 포트원 위험업종 문서 — *"포인트(크레딧/사이버머니) 충전 형태의 서비스는 결제 가능한 금액 한도나 인증 강화 등 PG사 및 카드사 입점이 제한적"*. → `크레딧 포함 → 위험업종(포인트충전) 분류 → 강화 심사 트랙 → 업력 2년·재무제표 요구`. **업력 2년은 일반 SaaS 기준이 아니다** — 그게 표준이면 나이스페이 포스타트("신청부터 첫 결제까지 하루")나 스텝페이(구독결제 전문)가 성립할 수 없다.
- **사업자 = 개인사업자**(법인 아님 · 사업자등록번호 592-58-00892). 개인사업자는 재무제표를 작성하지 않으므로 **"직전년도 재무제표" 요건 자체가 성립하지 않는다.** ※ 이전 기록의 "신설 법인"은 오기.
- **전략: 일반결제 먼저 → 매출 실적 축적 → 정기결제 특약.** 포트원 FAQ가 *"일반결제(결제창)를 먼저 연동 권장"*. 토스 문서상 **자동결제만** "리스크 검토 및 추가 계약" 대상 = 일반결제는 기본 계약 포함. 네이버페이가 "직전달 매출 내역"을 심사자료로 받는 것을 보면 매출 실적이 업력을 대신하는 자료로 쓰인다.
- **1순위 = 나이스페이 포스타트**(`start.nicepay.co.kr` — 2026-08-12 실물 확인: 개인사업자 전용 신청 가이드 링크 있음·사업자등록증 업로드로 시작·전자계약). 흐름 = 신청 → 2~3영업일 내 담당자 연락 → 1차 가입승인 → 2차 카드사 심사. **정기결제는 특약서 별도 제출 → 전용 MID 발급**(미검증 — 담당자 확인 필요. 포스타트 페이지에는 정기결제 언급이 없다). ⚠️ 기록된 전화 `1661-0808`은 그 페이지에서 확인되지 않았다.
- **✅ 테스트 상점 개설 완료(2026-08-13 · 상점명 `컴플로우`)** — 심사와 무관하게 연동 개발 가능. **MID·상점키 미확보 = 연동 착수 선행조건.**
- **구비서류(개인사업자)**: 서비스 계약서(고객거래확인서) · 사업자등록증 사본 · 대표자 신분증 사본 · **대표자 개인인감증명서 원본** · 정산계좌 사본 · **보증보험증권**(나이스는 기본 구비서류라 협상 카드가 아니다). ⚠️ 실제 요구 목록은 신청 폼에서 확인 후 갱신.
- **예비 PG**: 페이플(정기결제 주력·사업자등록증만으로 계약 가능) · 토스페이먼츠(카드 3.4%·연관리비 11만) · KSNET(가입비+보증보험) · 스텝페이(PG 심사 대행) · 효성CMS+(자동이체 우회로). 네이버페이 결제형은 **자사몰 1개월 운영+직전달 매출** 필수라 지금 불가.
- ⚠️ **되돌리면 안 되는 선**: "AI 사용량 추가 구매/충전"을 붙이는 순간 다시 위험업종이 된다. 한도 증가는 **요금제 업그레이드** 또는 **후불 가산**으로만. → `credits.ts` 상단에 규칙 주석으로 못박아 뒀다.
- **심사 반려 사유 1~3위 대비 현황**: ① 홈페이지 사업자정보 → **✅ 충족**(사업자등록번호·**통신판매업신고번호 제2026-인천계양-0642호**·대표·주소·전화·이메일 전부 푸터 라이브, 2026-08-13) ② 환불정책 → **✅ `/refund`**(7일 청약철회·월연 해지·환불 3~5영업일) ③ BM 리스크 → **화면 노출 "충전" 2곳 제거 완료·배포**. 포트원 SaaS 글의 최다 반려 사유 *"로그인 후에만 서비스가 보임"* → **랜딩이 미로그인 상태로 기능 설명·스크린샷·활용예시를 노출하므로 회피됨(2026-08-13 라이브 확인)**. 심사용 데모 계정은 필요 시 발급.
- **신청 시 기재 원칙**: 업종·서비스 설명에 **"월 정액 구독형 B2B SaaS"**로 쓰고 **"크레딧·충전·포인트" 단어 금지**(위 위험업종 분류 회피).

**⑤ 세션43 미검증분** — 캐싱 실효과 · PWA 설치 · Gmail 만료 후 재연결 · 세션42 HR/근태.

**③ 세션43 미검증분 소진** — 캐싱 실효과(`cache_read_tokens` 축적) · PWA 설치 · Gmail 만료 후 재연결 · 세션42 HR/근태.

> 📊 **매 세션 확인**: `docs/ops/infra-limits.md` — Supabase 한도 감시(현재 무료 플랜·스토리지 14.5%).
> 대표 지시(2026-08-10): "구독할 예정이니 걱정 말고, **데이터 보다가 결제해야 할 것 같으면 알려달라**."
> 그 파일의 SQL 한 번 돌려서 임계치(스토리지 60%·외부 워크스페이스 10곳·SNS 개시 결정) 걸리면 보고할 것.

---

<!-- ↓ 이전 세션43 인계 -->
## 🔜 (이전) 세션43[캐싱·크레딧·PWA + PG 거절] 인계 · 2026-08-10

> **✅ 상태 = 프로덕션 `cdb3fdc` READY(`complow.kr`) · 마이그 001~133**(131~133 이번 세션·전부 프로덕션 DB 적용됨) · main=feat=origin 동기·작업트리 clean. 각 배포 tsc0·lint29/0(신규 0)·build0.
> **롤백 후보**: `9c6bb59`(캐싱·크레딧 전) · `8fd5b16`(세션42 전)

### 세션43 완료 — 시간순

**1. 세션42 배포**(`9c6bb59`) — HR 설정·근태 잔여·컴피가 라이브. 마이그 127~130은 이미 적용돼 있어 DDL 신규 0.

**2. Gmail MCP 차단 해제 확인·검증**(`c37cb13`)
- 구글 프리뷰 승인 메일이 **2026-07-28 17:06에 이미 도착**해 있었음(신청 당일 승인). 수신함이 `complow@complow.kr`이라 10일간 미확인 — 독촉 불필요했음.
- 진단 스크립트로 실호출 검증: refresh 갱신 → initialize 200 → tools/list 13개 → **`list_drafts`·`create_draft` 200**.
- **403 사유 전환이 결정적 증거**: 7/28 `caller does not have permission`(프리뷰 미등록) → 지금 `insufficient authentication scopes`(스코프 부족). 남은 403은 읽기 스코프 미보유로 **설계대로**.

**3. 앱 버그 수정**(`3e00558`) — 구글은 정상인데 라이브 채팅은 빈 `오류:`로 실패하던 건
- ① `connectMcp`는 try/catch로 보호했는데 **`tools()` 로딩은 무방비 + `Promise.all`** → 커넥터 하나 실패가 채팅 전체를 죽임(워크플로우 run 라우트는 이미 try 안이라 무사했음)
- ② access token 만료 시 gmailmcp가 첫 호출에 401을 던지고 **그 뒤에야** SDK가 refresh 저장 → 만료 직후 첫 요청은 항상 실패(실측: 에러 08:31:03Z / 갱신 발급 08:31:09Z)
- 수정: connect+tools()를 같은 try로 + 개인 커넥터는 갱신된 행 다시 읽어 1회 재연결. **대표 라이브 확인 완료**(초안 작성 성공).

**4. 프롬프트 캐싱 + 캐시 인지 원가계산**(`3f1b417`·마이그131)
- 실측: 동일 프리픽스 2회 → 1회차 write 14,560 / 2회차 **read 14,560**(입력의 99.6%). 대화 1회 원가 **약 90% 절감**.
- **핵심 발견 — AI SDK는 Anthropic 원본과 반대**: `inputTokens.total = input_tokens + cache_creation + cache_read`(원본 API는 캐시 제외 잔여만). 기존 `computeCostUsd`로 캐싱을 켰으면 **캐시 읽기를 10배 과다 청구**했을 것 → 크레딧보다 반드시 먼저 고쳐야 했던 이유.
- 채팅·컴피 라우트의 시스템 프롬프트를 안정/변동 구간으로 분리(지식파일·기능레지스트리를 기억·요약·스냅샷 **앞**에). `pricing.ts`에 fable/mythos 티어 신설(폴백 시 3.3배 과소계상 버그).
- 🔒 부수 발견: **`UIMessage.role`에 `'system'` 포함** → 클라이언트가 system 메시지를 섞어 보내면 시스템 지시를 덮어쓰는 프롬프트 주입 가능(변경 전부터 존재). 이력을 user/assistant로 필터링해 봉합. 컴피는 네이티브 도구 권한이 있어 영향이 컸음.

**5. AI 크레딧 시스템**(`a0bf25b`·마이그132·133 / 게이지 `6192f91`)
- 대표 결정: **소진 시 완전 차단 + 하루 지나면 조금씩 충전**.
- 무료 = 가입 시 500 → **매일 17씩 충전·상한 500**(월 환산 510) / 유료 = 월 일괄·이월 없음 / premium = 무제한.
- **설계 판단 2가지**: ⓐ 충전을 크론이 아니라 **호출 시점 lazy 계산**(크론 실패 리스크 0·미사용 워크스페이스 비용 0) ⓑ 차감을 앱이 아니라 **`agent_usage` INSERT 트리거**(AI 라우트가 14곳이라 앱에서 하면 하나는 반드시 누락되고 새 라우트가 조용히 무료가 됨).
- DB 실검증 전부 통과: 최초500 → 같은날 중복지급 없음 → 100차감 → 3일경과 +51 → 60일경과는 상한에서 +49만 / 트리거 $0.045 → 정확히 4.5크레딧 차감·실패호출 미차감. **검증데이터 정리 완료.**
- 사이드바 `CreditMeter`(잔액/상한 + 20%↓ 주황·0 빨강). `/api/budget` GET이 `credit_sync`를 태우므로 **사이드바가 뜨는 것만으로 충전이 겸해짐**.
- ⚠️ **자사 워크스페이스(EQURIA·이큐리아2)는 `plan='premium'`으로 전환**(무제한·세션13 "우리 팀은 사용 가능" 결정). **'테스트'는 free로 남겨 게이팅 실검증용.** 롤백=`update workspaces set plan='free' where id in (...)`.

**6. PWA 앱 설치 지원**(`c8c3a90`·이름정리 `9ec03a4`·`cdb3fdc`)
- Electron/네이티브(B6)는 코드사이닝·스토어 심사로 1~2주+연 비용인데 **고객 0명**이라, 같은 체감을 오늘 주는 PWA를 먼저(네이티브로 갈 때 버리는 작업 없음).
- `app/manifest.ts`(start_url=/dashboard) · 아이콘 192/512/maskable(기존 로고에서 생성) · `public/sw.js` · `/download` 안내 페이지 + `InstallButton` · 푸터 '준비 중' → `/download`.
- ⚠️ **캐시 정책이 이 작업의 핵심 위험**: 멀티테넌트라 응답을 함부로 캐시하면 타사 데이터가 기기에 남아 RLS를 캐시가 뚫는다 → 캐시 대상을 `/_next/static/`·`/icons/`로만 한정, `/api/*`·`/auth/*`·**모든 HTML**(SSR이라 본문에 사용자 데이터)은 캐시도 가로채기도 안 함.
- ⚠️ **proxy OPEN_PATHS에 PWA 5종 추가** — 빠지면 /login으로 튕겨 설치가 조용히 실패(sitemap·robots 전례). 로컬 실검증 5경로 전부 200·리다이렉트 0.
- 앱 이름·창 제목을 `Complow`로 통일(랜딩은 자기 SEO 제목을 따로 가져 영향 없음).

**7. 🔐 격리 재감사(크레딧 테이블 포함)** — 구조: `workspace_id` 보유 **51테이블 · RLS off 0 · sentinel DEFAULT 0 · ws인덱스 누락 0**. 동작: 실제 RLS 시뮬로 **타사 잔액·원장 0행** 확인.

---

### 🔴 다음 세션 착수 순서 (이 순서 권장)

**⓪ 먼저 읽을 것**: 아래 "PG 거절" 블록 — 결제 전략이 바뀌었다.

**① 모델 라우팅 (가장 큰 남은 원가 레버 · 미착수)**
- Haiku는 Sonnet의 **정확히 1/3 가격**($1/$5 vs $3/$15). 현재 `MODELS.cheap`을 기억 추출에만 쓰는 중.
- 요약·분류·간단질의를 Haiku로 보내면 **전체 원가 약 27% 절감**(대화 40% 전환 가정) → 그만큼 한도를 넉넉히 줄 수 있음.
- **착수 방식**: 어떤 작업을 Haiku로 보낼지 후보를 뽑아 대표 확인 후 적용(품질 민감 영역 구분 필요).

**② 하이브리드 한도 설계 (노션 벤치 반영 · 대표 결정 대기)**
- 노션 확인 결과: 일반 AI는 **무제한**(공정사용), **자율 에이전트만 크레딧**($10/1,000)이고 **소진 시 정지**(우리와 동일).
- ⚠️ **구조적 사실**: 노션 Business 5명 = $100/월 vs 우리 Standard 5명 ≈ $21/월 → **우리가 1/5 가격**. 같은 AI 여유를 주는 건 불가능.
- 후보: A 현 가격 유지(한도 빡빡) / B 가격 인상 / **C 하이브리드**(채팅·요약은 넉넉히, 워크플로우 자동실행·자율에이전트만 한도) ← 권장.

**③ "크레딧" → "AI 사용량 %" 표현 정리 (PG 대비 · 코드 변경 적음)**
- KCP 거절 사유 ①이 "구독료에 크레딧 포함 = 환금성 분류". **표현만 바꾸면 다음 PG에서 이 사유가 안 생긴다.**
- DB·트리거·차감 로직은 **그대로**. `CreditMeter`를 %로, 랜딩·약관의 "크레딧" 단어 교체, **top-up(추가 구매) 판매는 계획에서 삭제**(초과분은 상위 플랜 전환으로 유도).
- ⚠️ **함께 결정 필요 — 차감 계수**: 현재 1크레딧 = 원가 $0.01이라 **Standard 3,000 소진 시 원가 $30 > 매출 ₩29,000(≈$21) = 구조적 적자**. 지금은 "어차피 다 안 쓴다(breakage)"에 기대는 중. 계수를 $0.005로 바꾸면 마진 +29%가 되지만 **실질 한도가 절반**이 됨 → ②의 결론과 함께 정해야 함.

**④ 랜딩 문구 정정(현재 부정확)** — "월 500 크레딧" → 실제는 "매일 17 충전(최대 500)". ③과 함께 처리.

---

### 🔴 대표 액션 (막힌 것 · 개발이 못 함)

**PG 결제 — KCP 거절(2026-08-10)**
- 사유 ① 구독료에 크레딧 포함 → **환금성 분류로 심사 강화** (③으로 해소 가능)
- 사유 ② **업력 2년 + 직전년도 재무제표** → 신생 사업자라 조회 불가 (시간 문제·해소 불가)
- **크레딧을 없애도 KCP는 안 됨**(②가 남음). ③은 "다음 PG를 위한" 작업.
- **선택지**: ⓐ **수동 청구(세금계산서+계좌이체)** ← 초기 B2B 표준·개발 0·지금 당장 가능 ⓑ 다른 1차 PG(토스·나이스·이니시스) 문의 ⓒ **2차 PG/결제 플랫폼**(신생 사업자 우회 구조·수수료 조금 높음) ⓓ 해외 MoR(세금계산서 문제로 국내 B2B엔 부적합할 수 있음).
- ⚠️ 각 PG의 정확한 기준은 **확인 후 단정할 것**([[verify-before-asserting-external-domains]] 교훈).
- **고객 0명이라 지금 매출 손실은 0.** 첫 10곳까지는 ⓐ로 충분.

**기타 대기**: 통신판매업 신고번호(발급됨 — 푸터 게재 미반영) · 공용비번 `4321` 교체 · 구글 브랜드 검증(앱 게시) 제출.

---

### ⚠️ 대표 dogfood 미검증 (오늘 배포분 전부 육안 확인 0)
1. **PWA 설치** — 크롬 주소창 설치 아이콘 / iOS '홈 화면에 추가' / 앱 이름이 `Complow`로 뜨는지
2. **크레딧 게이지** — 대표 계정은 premium이라 **안 보이는 게 정상**. **'테스트' 워크스페이스로 전환**해야 보임(소진→차단→다음날 회복까지)
3. **캐싱 효과** — 채팅 몇 번 후 `agent_usage.cache_read_tokens`가 쌓이는지 / `cost_usd`가 떨어지는지
4. **Gmail 만료 후 재연결** — 토큰 만료(1시간) 뒤 첫 요청이 실패 없이 도는지(수정 ②의 실검증 경로)
5. 세션42분(HR 설정·근태 잔여·컴피 도구호출) — 여전히 미검증

<!-- ↓ 이전 세션42 인계(참고 유지) -->
## 🔜 (이전) 세션42[HR·근태 설정 + 범용 비서 컴피] 인계 · 2026-08-01

> **✅ 상태 = 프로덕션 `9c6bb59` READY(2026-08-07 배포·`complow.kr`) + 마이그 001~130**(127~130 세션42·전부 프로덕션 DB 적용됨). **롤백 후보 = `8fd5b16`**(직전 프로덕션). main=feat=origin 동기. 세션42 = (1) 후속 정리(app-ambient 제거·죽은컬럼 drop 127) (2) **회사별 HR 설정 + 근태 잔여** (3) **범용 비서 '컴피'**. 배포 게이트: tsc0·lint29/0·build0 통과. **이제 대표 dogfood 차례(아래 🔴 2번).**

### 세션42 완료(플랜 승인 `~/.claude/plans/memoized-sprouting-cascade.md` · 대표 결정: 범용비서 1개·이름 컴피·근태HR 먼저)
- **HR(근태) 설정**: `hr_settings`(마이그128·workspace_id PK+jsonb·오너 write RLS)+`profiles.hire_date`+'월차'(status/leave_type enum). 오너 전용 `HrSettingsCard`(휴가기준·근무시간·휴무일)+구성원 입사일 칸. `lib/hr.ts` 순수 산식(근로기준법 기본값·근속 가산·1년미만 월부여·resolve* 병합).
- **근태 잔여**: `attendance_balances` RPC(마이그129·`can_view_attendance` 오너/위임자 게이트·실호출 검증 6명/비오너0)→ `AttendanceAdmin` 인원별 '연차 잔여 N/부여 M·월차 K' 표시.
- **컴피(Compi)**: `api/assistant` 승격 — 워크스페이스 현황 스냅샷+`features.ts` 기능 레지스트리 주입 + 네이티브 도구(`lib/agentTools`: get_attendance_balances·list_projects·list_calendar_events·list_my_tasks, 전부 `getUserWorkspaceId`·RLS 스코프)+게스트403. "누가 연차 며칠 남았어"→도구로 근태 RPC 조회해 정확 응답. `DashboardAssistant` 브랜딩(인사말·예시 칩·플레이스홀더).
- **옛 8종**: 신규 워크스페이스=clean-slate(마이그130 `clone_seed_agents` no-op — 컴피가 대체). 기존 활성 시드 16개(3워크스페이스)는 **컴피 검증 후 소프트삭제**(대표 결정·I30).
- **후속(2026-08-03)**: ① **/code-review 7건 반영**(`a061bd3`) — 컴피 도구·스냅샷 활성 워크스페이스 명시 스코프(이큐리아/이큐리아2 혼입 차단·마이그126 회귀)·onFinish `usage`→`totalUsage`·스냅샷 첫 턴만·빈 답변 미저장·`lib/hr` 근속 가산 `start_year` 반영. ② **컴피 캐릭터/위젯 헤더는 대표 요청으로 되돌림(제외)** — reflog `ee18201`에 보존(복구 가능). 컴피 UI는 캐릭터 없는 직전 브랜딩 상태.

### 🔴 다음 액션
1. ✅ **배포 완료(2026-08-07 세션43)**: 세션42 14커밋 main-first 푸시 → 프로덕션 `9c6bb59` READY. 롤백=`8fd5b16`.
2. **대표 dogfood(배포 후 필수)**: 설정→'인사(HR) 설정' 저장·구성원 입사일 지정 → 근태 화면 연차 잔여 표시 확인 → 컴피에 "이번 달 연차 누가 몇 개 남았어?"·"진행 중 프로젝트"·"오늘 일정" 물어 **도구호출·정확도** 확인(모바일/다크 포함).
3. **컴피 검증 후**: 옛 8종 소프트삭제(마이그131 후보 `update agents set is_active=false where created_by is null`) · 컴피 도구 확장(재무·회의)·이월/셀프 잔여(I27~I30).

<!-- ↓ 이전 세션41 인계(참고 유지) -->
## 🔜 (이전) 세션41[디자인 통일+대개편+게이팅] 인계 · 2026-08-01

> **프로덕션 = `a274fab`**(READY·`complow.kr`·main=feat=origin 동기) · 마이그 001~**126**(121=finance_snapshot_open[미사용·I22]·122=ledger_categories·123=task_timeline·124=task_color·**125=plan_seat_gating**·**126=personal_tasks_workspace** — 전부 추가형·DB 적용됨) · 세션41 = 대표 실시간 dogfood **대규모 반복 개편**. tsc0·lint29/0·build0 · **3중 적대 리뷰(데이터/UI/보안) HIGH 3·MED 6 + /code-review 2라운드(12+7건) 전부 수정** → main-first 배포.

### 세션41 완료(대표 지시·즉시 반복)
- **디자인 통일**: 글래스 → 랜딩 언어(불투명 카드·헤어라인·emerald/blue 정합, globals.css 토큰 레벨·롤백=revert). 랜딩 픽셀 무변화·다크 유지.
- **손익 대개편**: 그리드 요약/펼침 편집 행·분류=독립 컬럼·셀렉트(마스터=설정 '분류 관리'·마이그122)·**계산값=결과값 단일화(입력 변경→장부 자동 동기화·직렬화)**·기록=같은 달 갱신(+부가세 칸)·채널판매 legacy·장부연동 기능 제거·캔버스 높이 드래그.
- **프로젝트**: 노션식 타임라인(목록=상태 그룹×기간 바 드래그 / 상세=태스크 타임라인+색상·클릭 편집·주 눈금·일정별 파일[마이그123·124]) · 기간 편집+진행 바 · AI 연동=생성 시(metadata) · 재무 카드/정산 제거.
- **대시보드**: 한 화면(좌 카드|우 AI 세로 풀·컨테이너 쿼리 반응) · **AI 작업 제안**(`/api/task-suggestions` — 앱 내부+Gmail 소스→우선순위·출처·오늘 할 일 등록 플로우, budget 가드+게스트 403) · 진행/예정 작업 · 손익 필/최근기록 제거.
- **리사이즈 일습**: 사이드바 폭·대시보드 AI 폭/높이·어시 대화목록 폭·손익 캔버스 높이 — 보이지 않는 경계 드래그+기기별 기억.
- **근태**: flex식 일별 시간축 그래프+날짜 선택+사유 기입(연차·반차 등, 본인 upsert).
- **캘린더**: 프로젝트 기간 📁 막대 표시+막대 드래그 이동(일정·프로젝트 공통·Undo·토스트).
- **플랜/시트 게이팅(마이그125)**: 무료 3석·초과 시 `accept_workspace_invite` 서버 강제 거부(게스트 미소비·기존 grandfathered)+노션식 업그레이드 프롬프트·`lib/plans.ts` SSOT.
- **오늘 할 일 회사별 분리(마이그126)**: `personal_tasks.workspace_id`(추가형·백필) — 이큐리아/이큐리아2 겹침 해소. **에이전트 개인 기억은 개인 유지**(대표 결정).
- **초대 역할 노션식 피커**: 소유자/멤버/제한된멤버 카드+시트 게이팅 '플러스' 배지 · **초대 링크 방식 유지**.
- **에이전트 생성 단순화**: 첫 화면 예시 칩+타이핑 플레이스홀더 · **원샷 생성은 반려(단계별 과정이 강점 — 대표 결정)**.
- **회의노트 토글(접기) 블록**: 슬래시 메뉴에 노션식 토글 추가(수학/차트/DB/임베드 등은 신뢰성 불가로 제외). **메일 기능=완성 확인(부실 아님)**.
- 상세 = WORKLOG 세션41 · 리뷰 보류분 = known-issues **I19~I24**.

### 🔴 다음 액션
1. **대표 육안 확인(배포됨)**: 대시보드 한 화면·작업 제안(제안 받기 1회)·손익 그리드·프로젝트 타임라인 드래그·근태 그래프·캘린더 프로젝트 막대 · **회의노트 `/`→토글**·**초대 역할 피커/시트 초과 업그레이드**·**회사 전환 시 오늘 할 일 분리** — 모바일/다크 포함.
2. **플랜 게이팅 = 시트 부분 완료(마이그125)**. 남은 것 = **기능별 게이팅**(`features.ts` minPlan + `workspaces.plan`) + **크레딧 시스템**(PortOne API키 대기·`agent_usage.cost_usd` 차감·소진 차단·top-up[일반결제]). I21(rate limit)은 크레딧 시스템에서 정식 해결. (PG·통신판매업 진행상황 = 아래 세션40 블록 🔴.)
3. 후속 정리(비긴급): app-ambient div 제거·Surface glass variant·랜딩 리터럴 토큰화·마이그121 컬럼 drop 검토(I22)·Notion/Slack MCP를 작업 제안 소스로 확장 · 회의노트 토글 외 노션 블록(동영상 임베드 등)은 요청 시 개별 정식 추가.

<!-- ↓ 이전 세션40(결제/가격 B3) 인계 -->
## 🔜 (이전) 세션40 인계 (2026-07-30 결제/가격 B3 트랙)

> 프로덕션 `a761bd3` · 마이그 001~120(이번 세션 신규 마이그 0 · 전부 코드/문서) · main=feat=origin 동기 · tsc0·lint29/0.

### 이번 세션(B3 결제/가격) 완료
- **4티어 가격 설계 확정** — 과금 = 워크스페이스 정액 + 초과 시트 + AI 크레딧. Basic ₩0(3석·500크레딧·맛보기 영구무료) / Standard ₩29,000(5석·3,000) / Pro ₩49,000(10석·7,000·+Opus) / Premium 문의. 초과 ₩4,000/석. 연 2개월 무료. 기능차등=혼합(인프라 게이팅+AI 크레딧·모델). **설계 상세·근거 = 플랜 `~/.claude/plans/prancy-whistling-tome.md`.**
- **랜딩 4티어 가격표 공개(`503166f`)** — `LandingPage.tsx` #pricing ₩— → 실가격 4카드 + 4열 기능표. '14일 체험'→'무료로 시작' 정합.
- **배포 3건**: 푸터 사업자정보(번호·주소·전화, `ea3d5dc`) · X링크 `x.com/Complowkr`(`185a93f`) · 사이드바 로고 이미지 교체(`a761bd3`).
- **산출물(Desktop/complow)**: `가격_프로모션_논의자료.pdf` · `Complow_회사소개서.pdf`(6p·크레딧/토큰 비용 시각화).
- **비용 조사**: Claude 공식단가(Sonnet $3/$15·Opus $5/$25·Haiku $1/$5·Fable $10/$50·캐싱 60~84%↓·배치 50%↓) · 실측(agent_usage: Opus $0.41/콜·**입력이 원가 93~99% 지배**·유저당 월 ~$2) · 노션/잔디 벤치(둘 다 per-seat, 우리 10명 팀 = 1/2~1/6).
- **DB 멀티테넌트 재감사 통과(2026-07-30)**: workspace_id 보유 **47테이블** — DEFAULT 0 · RLS off 0 · ws인덱스 미보유 0 · 헬퍼 guest fail-closed ✅. 격리 견고, 구멍 0.

### 🔴 대표 진행 중(행정·대기)
- **PortOne PG 신청**(KPN+KCP 정기결제, 추천패키지=가입비 무료로 토스 직접 33만원 우회) → **가맹점 검토중**. 가격표 라이브라 **"가격 설정 완료" 콜백 가능**.
- **통신판매업 신고 + 등록면허세 ₩40,500 납부 완료(2026-07-29)** → **신고번호 발급 임박**(정부24). 나오면 ① PortOne 구비서류 제출 ② 푸터에 통신판매업신고번호 추가(개발).
- 결제 모델: **구독=빌링(정기결제) / 크레딧 추가구매=일반결제**(PortOne에 추가 신청 필요·미완).

### 다음 개발 (승인된 플랜 순서)
1. **플랜 게이팅** — `features.ts`에 minPlan + `workspaces.plan`('free' 컬럼) 기반 접근제어(GuestGuard 패턴) + free AI 정책.
2. **크레딧 시스템**(PortOne API키 후) — credit 잔액/원장 테이블 + `agent_usage.cost_usd`→차감 + 소진 차단(`budget.ts` 인접) + top-up(일반결제).
3. (여유) 프롬프트 캐싱(원가 60~84%↓·마진 개선) · 사이드바 기능별 기본 에이전트+호버 소환 UX(경로 조사 완료).

> ⚠️ **신규 교훈(메모리 기록됨)**: 외부·규제 도메인(PG·결제·법령·세무) 구체는 **검증 후 단정**. 빌링 top-up을 "빌링키로 다 됨"이라 오안내→PG가 "정기결제만"이라 정정, 흐름 꼬임. → [[verify-before-asserting-external-domains]].
> 📝 WORKLOG.md는 다음 세션 초에 이번 세션 상세 추가 필요(이번엔 HANDOFF만 갱신).

<!-- ↓ 이전 세션39(B1-b/B2/구글로그인) 인계 — 참고용 유지 -->
## 🔜 (이전) 세션39후속 인계 (2026-07-28)

> 프로덕션 `a283d4c`(코드 `9f77e33`) · 마이그 001~117.
> **완료**: B1-b 쓰기 격리 전체(A1~A5·DEFAULT 제거) · B2 노션식 개방(초대 링크·게스트·온보딩·가입 ON) · 구글 로그인 체제(전원 구글·가입폼 제거·초대제) · 브랜딩 인증·게시 · 트랙2 대화 요약 압축 · 파비콘 · UX 픽스 4종.

### 1순위 — 결제 트랙(B3 슬라이스, 대표 "에바 아님" 승인 2026-07-28)
1. **토큰 원가 분석 → 가격 제안**: agent_usage 실데이터로 인당/워크스페이스당 월 원가 → 구독가 제안(랜딩 ₩— 채우기). 결정 불요·즉시 착수 가능.
2. **플랜 게이팅**: workspaces.plan('free') 기존 컬럼 활용 — free=AI 실행 차단(세션13 결정: 무료=AI 불가·우리 팀=내부 enable). checkBudget 인접 배선.
3. **토스페이먼츠 빌링**(국내 B2B 구독 권장): 대표의 통신판매업 신고(정부24) 선행 — PG 심사와 개발 병행.

### 2순위 — 프리뷰 승인 오면(메일: complow@complow.kr, ~2026-08-04 예상)
- Gmail MCP 서버 재검증(진단 스크립트 있음: job tmp/probe_mcp.mjs 패턴) → **개인 gmail 최종사용자 지원 실측** → 대표 데모 영상 촬영 가이드 → 스코프 심사 제출.

### 3순위 — 소품/후속(아무거나 자투리에)
- 직원 구글 전환(대표가 Gmail 주소 주면 이메일 교체 SQL — 오너 때 한 방식) → 옛 계정 정리 → 공용비번 자연 소멸.
- Header 워크스페이스 전환 드롭다운+last-ws 쿠키(멀티멤버십) · 마이그118 후보=mcp_oauth_clients 회사별 스코프(플랜 B-5 잔여) · 게스트→멤버 승급 버튼 · 어시스턴트 대화에도 요약 압축.

### 대표 dogfood 미검증 체크리스트(쌓임 주의)
- 가입 개방 스모크 5종(신규 계정→회사 생성→타사 데이터 0·멤버/게스트 링크) ← **최우선**
- 대화 압축(14턴+ 후 앞 내용 질문) · 초대 링크 발급/회수 UI · 세션39 손익/재무(대표가 "임의 데이터라 무시" 선언했으나 탭 동작은 확인 권장)

> 현재 프로덕션 상태 참고(구 세션37 기록은 아래 유지).

### 트랙1 — MCP 정적 클라이언트 인프라 → 구글 연결 (✅✅ 실연결 검증 완료 2026-07-24)
- **🟢 실연결 성공(2026-07-24, 대표 dogfood 육안 검증):** 대표가 Google Cloud에 OAuth 앱 등록(client_id/secret) → 설정 'MCP 앱 크리덴셜'에 입력 → **Gmail·Google캘린더 실제 연결됨**. `mcp_user_connections`에 access+**refresh token** 저장 확인(access_type=offline 주입 작동), **Gmail 13도구·캘린더 9도구 디스커버리 성공**(토큰이 `*mcp.googleapis.com`에 실인증). **정적 client seed→DCR 건너뜀 가설이 실환경에서 통함 = 인프라 100% 검증.** 계정=`complow@complow.kr`(테스트 사용자 4명 등록·External+Testing 모드).
- **⚙️ 최종 구성(대표 결정 — 검증비 회피):** Gmail=**compose(작성)만**(gmail.readonly=제한스코프·CASA비용 제외) · 캘린더=events.readonly · **Drive 커넥터 제거**(drive.readonly=제한스코프) · **네이티브 메일 탭 사이드바 숨김**(hiddenFromNav — 메일은 에이전트가 compose로). 남은 스코프 2개 다 '민감'(제한 아님)→ 게시 시 **가벼운 브랜드 검증만**(CASA 없음).
- **🔜 자동 연동(자가서비스 SaaS) 조건 = 앱 브랜드 검증 1회:** 현재 Testing(테스트 사용자·100명)이라 신규 유저마다 등록 필요. **게시+브랜드 검증** 통과하면 아무 구글 계정이나 가입 즉시 연결(등록0). **격리는 유지**(OAuth 앱은 공유해도 토큰은 사용자별 RLS 격리 — Zapier/Notion 방식). 검증 준비물: 도메인 인증·`/privacy`·`/terms` 페이지(초안 `docs/legal/` 존재)·로고·스코프 사유. **다음 작업 후보=검증 준비물 만들기.**
- **인프라(배포 `a8b832f`~`f99edcc`, 세션38):** 오너 설정 **'MCP 앱 크리덴셜'** UI(client_id/secret 저장·리디렉션 URI 복사, `McpCredentialsCard`) + `/api/mcp/oauth-clients`(오너 게이트·`is_static=true` seed) · 카탈로그 Gmail·캘린더·Slack·PayPal(available·oauth·requiresAppCredential) · McpView **'관리자 설정 필요'** 게이팅. 엔진 검증(@ai-sdk/mcp 소스): 정적 client→DCR 건너뜀·secret 있으면 confidential 자동·`is_static` 가드(마이그102 자가치유 방지)·구글 `access_type=offline` 주입. 마이그107(`is_static`·`custom_url` 추가형).
- **Slack·PayPal(미완·대표 액션 남음):** 구글과 동일 방식 — 각 개발자 콘솔에 앱 등록(리디렉션 `https://complow.kr/api/mcp/oauth/slack/callback`·`.../paypal/callback`) → client_id/secret을 설정 'MCP 앱 크리덴셜'에 입력. 아직 미입력이라 '관리자 설정 필요' 상태. (구글은 완료)
- **fast-follow:** **Zapier 커스텀 URL**(URL에 시크릿 포함→저장 스키마 조정 필요·준비중, `custom_url` 컬럼 선반영).
- **보류/후순위:** Vercel=승인된 클라만(whitelist·불확실) · **Figma=사실상 막힘**(DCR 403·승인 클라 전용) · Exa=이미 활성(직원 각자 API키·대표 액션 0).

### ~~트랙2 — 대화 요약 압축(compaction)~~ → ✅ 완료(2026-07-28 `9f77e33`·마이그117 — 위 헤더 참조)
- 채팅이 길어지면 오래된 턴을 요약해 "지금까지 맥락"으로 압축 주입. 현재 `src/app/api/agents/[id]/chat/route.ts`의 `HISTORY_WINDOW=10` 슬라이딩 윈도우에서 뚝 잘림 → 앞 내용 망각. "질문하다 앞 내용 까먹음"의 정면 해결(대표 원안 "멍청 게이지"는 반려·이게 정직한 대안). lib/agentMemory 인접 설계.

### ✅✅ 구글 로그인 실검증 성공(2026-07-28 대표 실왕복) + 가입=구글 전용 전환(`35813ed`)
- **🟢 실검증 완료**: 대표가 'Google로 계속하기'로 **실제 로그인 성공**. 트러블슈팅 2건: ① Supabase에 잘못된 secret 입력 → auth 로그 `invalid_client` 확인 → `.env.local` 값(구글 token 엔드포인트로 유효성 검증)으로 교정 ② 연결 단계 불편("바로 구글로 되야지") → **오너 계정 이메일 직접 교체**로 해결.
- **⚠️ 데이터 변경(2026-07-28)**: 오너(조휘원 `c6817c63`) 이메일 `ueca1b0ed9c98ec9b90@equria.local` → `chowhiwon99@gmail.com` (auth.users+auth.identities+profiles 3곳, MCP SQL). 이메일 일치 자동 연결로 구글 직행 로그인. **롤백=역방향 동일 3문**. 부작용: 오너는 이름+비밀번호 로그인 불가(구글 전용).
- **🔜 직원 전환(권장=이메일 교체, 탈퇴 아님)**: 직원별 Gmail 받으면 동일 SQL로 교체 → 데이터 보존한 채 전원 구글 직행. 탈퇴+재초대는 개인 데이터 소실이라 차선. 테스트 모드 동안 콘솔 '테스트 사용자'에도 각 Gmail 추가 필요.
- **설정 완료 확인**: 구글 콘솔 리디렉션 URI 추가 ✅ · Supabase 신규가입 OFF ✅ · manual linking ON ✅ · Google provider ON(워크스페이스 웹 클라이언트) ✅ — authorize 엔드포인트 **302→accounts.google.com 서버 검증됨** · Redirect URLs 2개 ✅(Site URL은 complow.kr 확인 권장).
- **🆕 가입=구글 전용(`35813ed`, 대표 결정)**: 이름+공용비번 가입 폼 제거(가입 화면=구글 버튼+안내만) · **설정(오너) '구성원 초대'** 신설 — 구글 이메일 사전등록(`/api/members/invite`, admin.createUser) → 당사자는 'Google로 계속하기'만 누르면 이메일 일치 자동 연결·로그인. 가입 OFF 유지=외부인 차단 그대로. 로그인 폼(이름+비밀번호)은 구글 미연결 기존 계정 폴백으로 유지. `signupAction`은 미사용화(파일 유지).
- **⚠️ 기존 멤버 강제 탈퇴(대표 의사)**: 새 흐름(초대→구글 로그인) **실검증 후** 설정 UI에서 대표가 직접(개인 데이터 연쇄 삭제라 파괴적·2단계 확인 있음). 순서: ①대표 본인 마이페이지 구글 연결+재로그인 검증 → ②직원 1명 초대 실검증 → ③기존 계정 정리.
- **테스트 모드 한계**: 구글 앱 게시 전엔 콘솔 '테스트 사용자'(최대 100명) 등록 계정만 OAuth 가능 → 초대 시 콘솔에도 이메일 추가 필요. **브랜드 검증(게시) 진행 중(아래)** — 통과 시 제한 해제(우리 로그인은 초대제라 외부인 여전히 차단).

### (완료) 구글 로그인 활성화 콘솔 5단계 (기록 보존)
> ⚠️ **순서 중요: ②(신규가입 OFF)를 ③(provider ON)보다 반드시 먼저.** 순서 틀리면 아무 구글 계정이나 로그인→`handle_new_user`(043)가 equria 워크스페이스에 자동 등록→회사 데이터 접근. ②를 꺼도 공용비번 가입은 `admin.createUser`(service_role) 경유라 **안 깨짐**(코드 확인됨).
1. Google Cloud Console → 기존 OAuth 클라이언트(웹) → 승인된 리디렉션 URI 추가: `https://dutovtfdckhayyvhtuxu.supabase.co/auth/v1/callback`
2. Supabase 대시보드 → Authentication → Sign In/Up: **"Allow new users to sign up" OFF**
3. Authentication → Providers → **Google ON** + client_id/secret 입력(MCP용으로 등록한 그 앱 재사용 가능)
4. Authentication → **"Allow manual linking" ON**(마이페이지 '구글 계정 연결'의 전제 — 안 켜면 연결 버튼이 에러)
5. Authentication → URL Configuration: Site URL=`https://complow.kr` + Redirect URLs에 `https://complow.kr/auth/callback`·`http://localhost:3000/auth/callback` 추가
- **검증 루틴(설정 후)**: 로그인 상태로 마이페이지→'로그인 방법'→구글 연결 → 로그아웃 → 로그인 화면 'Google로 계속하기'로 재로그인 → (별도 크롬 프로필의 미등록 구글 계정으로 로그인 시도 → "등록된 멤버가 없어요" 뜨는지 = 가입 차단 확인).
- 참고: 기존 멤버는 이메일이 합성 주소라 **연결 없이 구글 로그인 불가**(자동연결 안 됨·의도된 동작). 신규 직원 = 공용비번 가입 → 마이페이지에서 구글 연결.

### ✅ 멀티테넌시 격리 적대 감사(2026-07-28, 코드+실DB RLS 시뮬) — 구멍 4종 봉합·배포 `bbc8143`
- **DB 시뮬 통과**: 교차 테넌트 열람 0 · 게스트 권한상승(workspace_id 획득/INSERT) 차단 · 무소속 유저 0열람 · 타사 초대 발급 차단 · sentinel DEFAULT 0/nullable 0.
- **수정된 실취약점**(가입 개방 후 "회사 소유=전역 권한" 가정 붕괴): **V1(HIGH)** mcp/oauth-clients 게이트를 플랫폼 운영자(equria 오너)로 한정 — 임의 유저가 전역 공유 구글/슬랙 OAuth secret 탈취·삭제하던 구멍 / **V2** members DELETE를 전역계정 삭제→멤버십 행만 제거(타 회사 계정 파괴 방지) / **V3** getUserWorkspaceId·budget guest 제외+order(게스트 호스트예산 소진 방지) / **V4** 마이그118 공지 fan-out guest 제외(공지 유출 차단, 실DB 검증=게스트 0건).
- **✅ V5·읽기격리 완결(`dd92f54`·마이그119)**: 워크스페이스 전환기(사이드바 상단·노션식)+**활성 워크스페이스 헤더 스코핑** — auth_user_workspace_ids()가 x-workspace-id(쿠키→클라/서버 Supabase 헤더) 읽어 활성 회사로 읽기 스코프(스푸핑/가비지 안전 폴백). getUserWorkspaceId 쓰기도 활성 우선. 실환경 헤더 전달 curl 검증+DB 시뮬 통과. **잔여**: **S2** 전역 profiles.role='admin' → B-5(회사별 권한) 전환 시 workspace_members.role로(비차단·전역 admin은 대표뿐).

### 🔴 B2 컷오버 스위치 (대표 1클릭 + 스모크 — 이게 마지막)
> 코드·DB·순서 안전장치 전부 완료(마이그 116까지 적용됨). 이제 켜기만 하면 가입 개방.
1. https://supabase.com/dashboard/project/dutovtfdckhayyvhtuxu/settings/auth → **"Allow new users to sign up" ON**
2. **스모크 5종**: ① 기존 멤버 로그인·데이터 그대로 ② 새 구글 계정(테스트 사용자 등록된 것)으로 로그인 → /onboarding → "새 워크스페이스 만들기" → 자기 회사 대시보드(에이전트 8종 자동) ③ 그 계정에서 equria 데이터 안 보이는지 ④ 설정→구성원 초대→멤버 링크 발급→다른 계정으로 수락 ⑤ 게스트 링크(프로젝트 선택)→수락→그 프로젝트만 보이는지
- ⚠️ 구글 앱 게시 심사 통과 전엔 **콘솔 테스트 사용자 등록 계정만** 구글 인증 가능(심사 통과 시 전면 개방).
- 도입 문의 기업 셋업(운영자): SQL Editor에서 `select admin_create_workspace('회사명','slug');` → 반환 토큰으로 `https://complow.kr/join/<토큰>` 전달(7일·1회·owner 승계). Runbook=마이그 115 주석.

### ✅ Gmail MCP 프리뷰 차단 해제 — 실호출 검증 완료 (2026-08-07 세션43)
- **🟢 승인 확인**: 구글 프리뷰 승인 메일이 **2026-07-28 17:06**에 이미 도착해 있었음(`workspace-devrel-dpp-starter@google.com` → "Your Google Cloud Project(s) has been registered", 프로젝트 **353073448998**). **신청 당일 승인.** 수신함이 `complow@complow.kr`이라 10일간 미확인 상태였던 것 — 독촉 불필요.
- **🟢 실호출 검증 통과(2026-08-07)**: 진단 스크립트로 refresh_token 갱신(7/28 저장분 유효·scope=gmail.compose) → initialize 200 → tools/list 13개 → **`list_drafts` tools/call = HTTP 200·isError=false·실데이터 반환**. HANDOFF 검증 기준("실제 도구 호출 1회 성공") 충족.
- **🔑 결정적 구분 — 403 사유가 바뀜**: 7/28 `The caller does not have permission`(프리뷰 미등록=프로젝트 거부) → 지금 `Request had insufficient authentication scopes`(스코프 부족). 즉 **프로젝트 차단은 사라졌고**, 남은 403은 읽기 스코프 미보유(`search_threads`/`get_message` 등 — CASA 비용 회피로 의도적 제외)라 **설계대로**. 진단 스크립트=scratchpad `probe_gmail_mcp.mjs`(env→DB→복호화→갱신→JSON-RPC, 토큰 미출력).
- **🐛 앱 버그 발견·수정(`3e00558`)**: 구글은 정상인데 라이브 에이전트 채팅은 빈 `오류:`로 실패 → 런타임 로그 = `MCPClientError HTTP 401` at `/api/agents/[id]/chat`. **원인 2가지** ① `connectMcp`는 try/catch로 보호했는데 **`tools()` 로딩은 무방비 + `Promise.all`** → 커넥터 하나 실패가 채팅 전체를 죽임(워크플로우 run 라우트는 이미 try 안이라 무사) ② access token 만료 시 gmailmcp가 첫 호출에 401을 던지고 **그 뒤에야** SDK가 refresh 저장 → 만료 직후 첫 요청은 항상 실패(실측: 에러 08:31:03Z / 갱신 발급 08:31:09Z). **수정** = connect+tools()를 같은 try로(Promise.all 제거) + 개인 커넥터는 갱신된 행 다시 읽어 **1회 재연결**. 실패해도 그 커넥터만 빠지고 채팅 계속.
- **🟢 대표 라이브 실사용 확인(2026-08-07, 프로덕션 `7a43ccc`)**: 에이전트 채팅에서 **Gmail 초안 작성 성공**. 세션38부터 이어진 "Gmail 에이전트가 실제로 되는가" 트랙이 여기서 닫힘. **단 절반 검증** — 당시 토큰이 살아 있어 ①(채팅 안 죽음)만 탔고, ②(만료 직후 자동 재연결)는 **토큰 만료 1시간 뒤 첫 요청**에서만 타는 경로라 미검증. 다음 세션에 한참 뒤 재시도로 확인할 것.
- **잔여**: ① **캘린더는 `mcp_user_connections`에 연결 행 자체가 없음** → 재연결 후 같은 방식 재검증 필요 ② 검증은 **대표 계정 1건** 기준 — 다른 사용자는 각자 연결(+Testing 모드라 콘솔 테스트 사용자 등록) ③ 개인 gmail 최종사용자 지원 실측 ④ 데모 영상 촬영→스코프 심사 제출.
- **원인 기록(보존)**: gmailmcp/calendarmcp.googleapis.com은 **Developer Preview Program 전용(Pre-GA)** — 미등록 프로젝트는 도구 호출 거부. 연결·tools/list는 등록 없이도 성공해서 7/24 검증(디스커버리까지)이 통과했던 것. **교훈: 연동 검증 = "실제 도구 호출 1회 성공"까지.**
- **약관 제약(로드맵)**: Pre-GA 기능은 **GA 전 고객 제공 금지** — Gmail/캘린더 에이전트 기능의 B2B 판매는 구글 GA 후. 사내 사용 OK. 개인 gmail 최종사용자 지원 여부=승인 후 실측 예정. 폴백=네이티브 Gmail 통합(자체 googleapis, 개인 계정 지원, 메일 탭 숨김 상태).
- **오늘 배포된 관련 개선**(각 tsc0·lint29/0·build0): 위저드 MCP 팝업 제자리 연결(`d82409b`)·목적 카드 중복 제거(`e20bbb8`)·Gmail 작성전용 usageNote 주입(`18deb72`)·**연결 직후 자동 점검**(tokeninfo 스코프 검사, `c0f4749`).

### 🔴 기존 미해결 대표 액션(연동/보안)
- 🏠 **랜딩 트랙 진행(최신 `5d47732`)**: 공개→새 로고(순검정)→미니멀 전환→잔디식 모션(플로팅 AI 칩·제품 화면 재현)→**구독제 섹션 UX 틀**(가격 ₩— 플레이스홀더·얼리버드/연간/리퍼럴 자리). [대표 결정] 수익모델=**구독제**(가격은 토큰 원가 측정 후 확정)·프로모션=얼리버드+연간 2개월+리퍼럴·CTA=도입 문의 유지(보류). 다음=토큰 원가 측정→가격 확정→사전 신청 CTA.
- 🏠 이전 랜딩 공개(`bfa92d2`): complow.kr 루트=마케팅 랜딩(도입 문의 CTA·기능 6종·가격 없음—대표 결정). 로그인 상태는 대시보드로. 법적 문서 로그인 리다이렉트 버그 픽스.
- 📄 **구글 브랜드 검증 제출(대표 액션·준비물 완료)**: /privacy·/terms 게시됨(`4eb9499`) → ① ✅ Search Console complow.kr 도메인 소유 인증 **완료(2026-07-28**, 가비아 TXT `uo_Wtvs...b45w` 추가·통과. 후속: 루트 색인 생성 요청 ✅ 완료·sitemap.xml은 구글 자동 재시도 대기) ② 콘솔 Google 인증 플랫폼→브랜딩: 앱 로고·홈페이지 `https://complow.kr`·개인정보처리방침 `https://complow.kr/privacy`·서비스 약관 `https://complow.kr/terms`·승인된 도메인 `complow.kr` ③ 대상 탭 "앱 게시"→확인 요청 제출. **PG(결제) 심사 병렬 준비(대표 체크리스트)**: /terms·/privacy·/refund(`42d72c7`)·서비스URL=완료 4칸 / 대표 남은 것=통신판매업 신고(정부24·구매안전서비스 이용확인증 필요)·사업자 명의 통장 사본(스코프 사유: "사내 업무용 AI가 사용자를 대신해 메일 초안 작성(gmail.compose)·일정 조회(calendar.events.readonly)"). 심사 며칠~2주·통과 시 가입 즉시 구글 연동. ⚠️ 법적 문서는 변호사 검토 전 게시 상태.
- 🔑 공용비번 `4321`→강한 값 · 📧 **네이티브 구글(메일/드라이브 탭) 복구** = Vercel `GOOGLE_OAUTH_REDIRECT_URI=https://complow.kr/api/google/callback` + Google Console '승인된 리디렉션 URI' 등록(트랙1의 에이전트용 구글 MCP와는 별개 통합).

---

## 📂 문서 지도 (무엇을 어디서 읽나)

| 문서 | 내용 |
|------|------|
| **HANDOFF.md** (이 파일) | 현재 상태·다음 할 일·합의된 정책 (제일 먼저) |
| **WORKLOG.md** | 작업 로그(매 작업 무엇·왜·쪼갠내용·예상이슈) + 기능 마스터 체크리스트 |
| `CLAUDE.md` | 프로젝트 정체성·스택·절대원칙·파일구조·DB요약 (단, **브랜드 규칙은 아래 '합의된 정책'이 최신**) |
| `PRODUCTIZATION.md` | B2B SaaS 전체 로드맵(B1~B6)·리스크 레지스터·핵심 의사결정 |
| `B1-DESIGN.md` | 테넌트 격리(B1) 상세 설계·RLS 메커니즘 |
| `AGENTS-MCP-STRATEGY.md` | 에이전트·MCP 차별화 전략·시한폭탄(생각만/나중) |
| `AGENTS-LEARNING-DESIGN.md` | 🆕세션35 **학습/기억 + 생성물 파이프라인** 기획·기술병목분석(설계 前, 대표 결정 §7) |
| `.claude/skills/safe-changes.md` | 변경 안전 원칙 (모든 코드/DB 변경 전 — 최우선) |
| `.claude/skills/latest-stack.md` | AI SDK v6·Supabase 최신 패턴 |
| `.claude/skills/known-issues.md` | 비차단 기술부채·보류분 |
| `.claude/skills/work-harness.md` | **작업 방식(하네스/루프)** — 작업 SOP·검증 게이트·멀티에이전트 기준·진행 가시성 |
| `STUDY.md` | (대표용) 비개발자 학습 코스 — 시스템 이해. 온보딩 필수는 아님 |

> 옛 설계 문서(`PLAN`·`CHAT-HUB`·`GOOGLE-MCP` 아키텍처)는 구현 완료/대체되어 `docs/archive/`로 이동. **현재 상태=이 파일, 구현 진실=코드/마이그레이션.**

---

## 🎯 지금 상태 (2026-07-13)

- **제품**: 사내 직원용 AI 워크스페이스 → **B2B 멀티테넌트(회사별 판매) SaaS로 전환 중**.
  - ✅ **브랜드 = Complow(컴플로우) 확정(세션26).** 코드 UI 브랜드 문자열 EQURIA/이큐리아 → Complow 교체 완료. 잔존 `equria:*`(이벤트/스토리지 키)·`equria-*`(CSS 애니명)·`equria.local`(내부 이메일 도메인 폴백)은 **내부 식별자라 의도적 유지**(바꾸면 로그인·상태 깨짐). `K-뷰티`는 첫 사내 고객 맥락 잔재. 철학 = **"회사별 커스터마이징"**(각 회사 업무에 AI가 진짜 작동하게).
- **배포**: 프로덕션 `main` = **세션34 에이전트 재설계(Phase1~3 + MCP 바인딩): 기본제거·삭제UI·예시갤러리·카테고리/업무영역/직무 자유입력·지식파일첨부·개인MCP바인딩**(코드 **`23cca30`**, 마이그 096·097·098, Vercel READY icn1·`complow.kr`) · Vercel 프로젝트 `complow-workflow-sass`(서울 icn1) · **롤백 후보**: **`9adcddd`**(MCP 바인딩 전) · `595558a`(지식파일 전) · `af87697`(Phase1) · `ecadc69`(세션33). *feat=main=origin 모두 `23cca30` 동기(클린).* ⚠️ 마이그 096 롤백=`update agents set is_active=true where created_by is null`. ⚠️ **배포 교훈: main·feat 같은 SHA 연달아 push 시 Vercel 중복제거 → main 먼저 push하고 프로덕션 빌드 확인 후 feat.** ⚠️ **배포 교훈(재확인): main·feat를 같은 SHA로 연달아 push하면 Vercel 중복제거로 프로덕션 승격 스킵됨 → main 먼저 push하고 프로덕션 빌드 생성 확인 후 feat push(간격 필수).**
  - ⚠️ **프로덕션 Drive/Gmail 실사용 전제(대표 액션)**: 구글 OAuth Vercel env + Google 콘솔 `drive.readonly` 스코프 + **prod redirect URI**(`https://<도메인>/api/google/callback`) 등록. 미설정 시 "구글 연결" 실패(코드는 라이브).
  - **과거 배포 상세 = git 커밋·WORKLOG.** 굵직한 묶음: **세션15~21 현금흐름 손익계산기**(마이그 078~085, 아래 §현금흐름 블록) · **세션13~14 그룹채팅**(카카오톡식 전체방+다중방·읽음, 마이그 071~076)·**회의노트 노션식 에디터 + AI 리서치/꼬리물기 지식그래프**(마이그 070·077, `d3-force`·Tiptap v3 확장·`lib/safeFetch` SSRF)·getUser 왕복 제거(`CurrentUserProvider`) · **세션10~12** 멀티에이전트 코드리뷰 픽스·캘린더 팀수정(062)·**icn1 리전 이동**(전환속도) · **세션9** 전자결재·드롭다운 복구. ⚠️ AI 리서치 실사용 전제 = Anthropic 콘솔 web search 활성+결제(대표 액션).
  - ⚠️ **배포 팁**: `main`·`feat`를 같은 SHA로 동시 push하면 Vercel이 중복제거해 프로덕션 승격을 건너뛸 때가 있음 → **`main`을 먼저 push해 프로덕션 빌드 확인 후 `feat` push**.
  - 작업 흐름: `feat`에서 작업 → 브랜치 push마다 **프리뷰 자동배포**(라이브 무영향) → `main` push만 프로덕션 배포.
  - 🆕 **작업 하네스(세션12 `cacf4f4`)**: `/deploy`(이 시퀀스 자동)·`/verify`(tsc+lint 게이트) 명령 + `.claude/settings.json` 훅(**pre-push 게이트**=push 전 tsc 강제·lint 회귀차단 / **Stop 검증**=src 변경 턴 tsc 자동) + `work-harness` 스킬(작업 SOP·멀티에이전트 기준·진행상황 가시성). ⚠️ 훅은 `/hooks` 1회 열거나 재시작해야 활성.
- **라이브 기능**: 에이전트 허브(우하단 위젯)·에이전트 빌더(위저드)·워크플로우(n8n 캔버스+순차실행)·캘린더·프로젝트·재무·명함·파일·**메일(Gmail)**·**MCP**·구성원 디렉터리 + **직원 채팅**(상태표시·이모지반응·답장/스레드·리치텍스트·다중첨부·**드래그앤드롭/붙여넣기**·AI보조/맞춤법) · 전역 ⌘Z Undo/휴지통.
- **🔐 멀티테넌시 B1-a (읽기 격리) = 완료** (마이그 033~043, DB 라이브·검증). 회사 간 데이터 격리 활성 → 아래 §멀티테넌시.
- **💰 비용 추적 = 켜짐** (마이그 042): Claude 호출별 모델·달러비용 기록 + 마이페이지 "추정 비용". **🆕 예산 한도(세션28, 마이그 087)**: `workspaces.monthly_budget_usd` 월 예산 + 실행당 상한($2, `PER_RUN_MAX_USD`) 초과 시 비관리자 AI 실행 **429 차단**(admin 예외) · AI 라우트 9개 프리플라이트(`lib/budget.ts checkBudget`) · 설정 "AI 비용 예산"에서 관리자 설정.
- **🆕 사내 행정 일습(✅ 라이브)**:
  - **근태·결재 `/work`**(마이그 045): 근태(출퇴근)·지출결의서·휴가 통합. 본인/관리자 비공개 RLS, 관리자 승인/반려.
  - **회의 노트 `/meetings`**(마이그 046~049): 워크스페이스 **공유** 회의록. **노션식 Tiptap 블록 에디터** — `/` 슬래시 메뉴(텍스트·제목1~4·목록3종·인용·콜아웃·코드·구분선·표 + **이미지/파일(pdf·zip·xlsx·ppt 등 모든 형식) 인라인 업로드**, lucide 아이콘 통일), 빈줄 "'/'를 입력해 명령어 사용". 본문=HTML 저장, 읽기전용은 동일 확장 렌더(=sanitize). 상시 AI 보조(요약/액션아이템/정리, `/api/meeting-notes/assist`, 슬래시에서도). 인라인 미디어=공개 `meeting-media` 버킷(048, svg/html 차단·50MB 049). 적대 리뷰 2라운드(IDOR·저장형 XSS 등) 수정 완료. **🆕 세션13 에디터 퀄리티 Part 1**(표 편집 컨트롤·선택 인라인 툴바·코드 하이라이트·이미지 정렬) — 위 배포 참조.
  - **🆕 전자결재 `/approval`**(카카오워크식, 마이그 054~056): 양식→폼→**결재선(순차 N명, 결재/참조)**→상신→순차 승인/반려(의견)→**문서함**(결재할문서·기안함·참조함)→문서상세(**결재선 도장** 진행·회수·의견). RPC `submit/act/recall`(상신 후 변경 RPC 전용). **self-approval DB 3중 차단**·realtime·초안 프라이버시. 메뉴 `/work`를 **전자결재+근태**로 분리(구 지출/휴가 단일승인 패널 제거). Phase B 근태개편·C 연차/관리자는 로드맵.
  - **공지사항**(051): 대시보드 상단 칸, 전 직원 열람·**오너(workspaces.owner_id)만 작성**(`AnnouncementsBoard`).
  - **비용/매출 다중 화폐**(052): `finance_entries.currency`(KRW/USD/EUR/JPY/CNY/BTC), 통화별 합계 분리·OCR 통화 인식. `lib/finance`의 `money()`.
  - **사이드바 "직원 채팅" 미읽음 빨간 배지**(`useUnreadDms`) · 근태/지출 UI 개선(근무시간·상태요약).
  - 작은 수정(1·2단계): 채팅 알림 자동삭제·스크롤바 간격·파일 다중업로드·파일 공개범위(공개/부서/개인, 044).
  - **다음(5단계 예정)**: 노션식 새 페이지(블록 에디터 + `/`슬래시 + AI 상시) — 가장 큼·단독.
- **안정도**: `tsc` 0 · `pnpm lint` **30 errors/0 warnings**(전부 기존 `set-state-in-effect`·refs 부채, 신규 0이 베이스라인) · `any` 0 · 마이그 **001~098 적용·drift 없음** · **`next build` 성공** → **Vercel 빌드가 실제 게이트**.
- **🆕 세션14 (✅ 라이브)**: 그룹채팅 알림(마이그 076)·카카오톡식 인앱 토스트(NotificationBell)·ChatList 그룹 미읽음 회귀수정 · `(app)/loading.tsx`(전환 스켈레톤) · pricing Opus $5/$25 정정 · **getUser 왕복 제거**(`CurrentUserProvider` — 서버 레이아웃 user.id를 client context로, 클라 ~27곳 마운트 왕복 제거).
- **🆕 현금흐름 → 손익(P&L) 계산기 `/finance` (✅ 라이브, 세션15~21, 마이그 078~085)**: 매출·비용·보유를 **드래그 캔버스**(박스 자유 배치 + **그룹 컨테이너**(이름·접기·소계, 박스 드래그로 묶기/빼기, flex 오토레이아웃) · **회사 가용현금 pool**(시작보유 인라인 편집)) ↔ **노션DB식 표**(`CashGrid`, 그룹 섹션·소계)가 같은 데이터(SSOT). **계산 칸을 회사가 직접 편집** — 표 헤더 라벨 인라인 + "칸 편집"으로 부가세 등 칸 추가·삭제 + **수식 스텝 빌더**(기존 AST를 스텝으로 분해해 바로 수정). "회사 기본 계산 유형"(`cash_calc_types` 1개)을 표의 **동적 컬럼**으로 승격 → 캔버스·엑셀 자동 반영(구분 강제 안 함=매출·비용 공용). **함수 살아있는 엑셀/CSV**(`exceljs` lazy, 셀=AST 실수식·입력칸 동적, 그룹 섹션·소계 SUMIF, 색·테두리·헤더고정). 구분 색 자동(매출 초록/비용 빨강/보유 파랑, 개별 변경 가능). **단일 AST 엔진**(`calcFormula`: `evalFormula`=앱 · `toExcelFormula`=엑셀)으로 앱=엑셀 일치. 신규 lib: `calcFormula`·`cashflowGraph`(buildSlotGraph 롤업)·`cashAccounts`(fieldsOf·SLOT_TYPES)·`xlsx`·`inline`. 마이그 078(슬롯)·079(amount)·080(카테고리·설정)·081(item_type)·082(calc_types·field_values)·083(note·pool_pos)·084(그룹 x/y/collapsed)·085(default_calc_type_id), RLS=035/080 패턴. **남은 것:** 급여(P2 대표전용)·예측/오픈뱅킹(P3). (알려진 한계: 시드 insert RLS 멀티테넌트 견고화·다통화 pool 표시 = `known-issues`.)
  - **🆕 현금흐름 AI 코칭 `/finance` (✅ 라이브, 세션23, 배포 `84b55ab`)**: 손익 헤더 **"AI 코칭"** 버튼 → 현재 스냅샷(슬롯 + 통화별 요약 + pool)을 Claude가 분석해 **건강도 배지·절감 제안·이상 신호** 구조화 카드로. **DDL 0·RLS 변경 0·읽기 전용**(기존 데이터 분석만). `generateObject` 원샷 + `cashCoachSchema`(zod), sonnet-4-6·온도 0.3, `agent_usage` 비용추적. 신규: `lib/cashCoach`(payload/prompt 순수)·`api/finance/cashflow-coach`·`components/finance/CashCoachPanel`(자체 완결·열면 1회 자동 분석). 프롬프트가 비용률·항목비중 사전계산 → 산술오류 방지, "근거 없으면 빈 배열" 강제로 환각 억제. **다음:** 추세(finance_entries 월별)·저장/히스토리·요금제 게이팅(무료=AI 불가).

> 최근 작업 상세(세션7·8: UI 리프레시·채팅 단계0~6·에이전트/위젯 재설계·캘린더·삭제RLS버그·B1격리·비용추적 등)는 **git 커밋 메시지**에 충실히 기록됨. 여기 중복 안 함.

---

## 🔴 다음 할 일 (우선순위)

### 🔥 활성 — 에이전트 시스템 재설계 (세션34~, 계획 = `~/.claude/plans/stateful-zooming-quiche.md`)
> **배포됨(LIVE, 프로덕션 `aed63d9`(세션34 코드 `23cca30`+세션35 P2.2·채팅픽스), 마이그 096~098):** 기본 에이전트 제거+삭제UI · 예시 갤러리(백오피스 8종) · 카테고리/업무영역/직무 **자유입력** · 업계특화 문구 중립화 · 위젯 드래그 버그수정 · **지식파일 첨부**(AI-readable, 채팅 주입) · **개인 MCP 커넥터 바인딩**(빌더 '내 MCP 연결', 채팅·워크플로우 일관).
> **⚠️ 동작 변경:** 에이전트는 이제 **선택한** 개인 MCP만 씀(예전엔 전부 자동). 기존 에이전트는 수정에서 재선택 필요.
> 🆕 **세션35 — 대표 dogfood 피드백으로 대거 진행(개인화·학습·프라이버시·MCP UX). 방침=만들고 쓰면서 점검 후 고도화:**
> - ✅ **① 만들기 진입 재설계** — 예시 갤러리 **제거** → 열린 "무슨 일 맡길지" 입력 + 배경 **떠다니는 에이전트 아이콘**(`animate-float`). **MCP 연결 스텝(직무 다음, `McpConnectorPicker`)** = 사용 가능한 커넥터 **전체** 표시(연결됨=선택·미연결='연결하고 오세요'·개인계정 안내) + **실제 로고**(공용 `ConnectorLogo` 파비콘). 미검증(대표): 아이콘 톤·MCP 목록 자동갱신(새 연결 후 스텝 재진입 필요).
> - ✅ **② 학습/기억 v1(수동 루프)** — 마이그099 `agent_memories`(본인RLS·격리검증) + 위젯 '기억 관리'(추가·삭제) + 채팅 주입. 설계=`AGENTS-LEARNING-DESIGN.md`(대표 결정 §9). **다음=자동추출·중복병합 / 프로젝트공유=v1.5**(에이전트↔프로젝트 연결 선행 필요).
> - ✅ **MCP 도구설명 한국어 요약**(`lib/mcp/summarize`, AI·테스트 시 1회 캐시) — 영어 원문 대신 일목요연. 하드코딩 아니라 어떤 MCP든 자동.
> - ✅ **프라이버시 3종** — 지식파일 **개인전용**(마이그100 `is_personal`, 공유 에이전트여도 올린 본인만·RLS검증) / 관리자 **구성원별 AI 사용량**(마이그101 RPC 오너전용·**내용 미노출·사용량만**·게이팅검증) / 개인 파일 '🔒 개인(나만)' 명확화. **합의**: AI채팅·기억·개인연결·개인전용지식=본인만(관리자도 내용 못 봄) / 관리자는 사용량·비용만.
> - ⏳ **③ 파일 자료 추천**(첨부 유용/중복/저품질 평가+추천, agent_knowledge 위 AI 1패스) · **④ 대용량→RAG**(<~150쪽 full-context+캐싱 / 초과 pgvector HNSW+iterative_scan) · **생성물(higgsfield) 파이프라인**(비동기 잡큐 pgmq+cron+Edge+Storage+승인→레퍼런스 재투입, Vercel 분단위 대기 금지) — **대기**.
> - ⚠️ **미해결(대표 액션)**: **Notion 연결 401(invalid_token) = 재연결** 필요(안 되면 OAuth refresh 버그 조사). 대표 dogfood 중 실기기 육안 미검증.
> **다음 할 것(기존 우선순위):**
> 1. **MCP 3b** — 워크플로우 StepPicker에 개인 MCP를 **독립 툴노드**로 추가(Notion을 에이전트 없이 워크플로우 단계로). 노드모델(mcp_tool: 회사UUID↔개인slug 구분) + 실행엔진 변경. **3c** — 워크플로우 에이전트 노드에 바인딩 MCP 배지.
> 2. ✅ **P2.2 출력형식 재설계 (세션35 완료·배포 `cf5684e`)** — 업무언어 6개 단일선택 + 미니예시 + 직무기반 추천배지(`OUTPUT_FORMATS`·`recommendOutputFormat`, `WizardField.hint`).
> 3. **P2.3 가드레일/메타프롬프트 정합** — `SKILL_MD_SYSTEM` 11↔7섹션 통일 + 가드레일 기본탑재(환각완화).
> 4. **P2.4 위저드↔직접작성 통합** — "직접 작성" 전환 시 빈 textarea 뚝 떨어짐 해소.
> 5. **Phase 4 내보내기**(SKILL.md·AGENTS.md·시스템프롬프트 복사·MCP스니펫) · **Phase 5 워크플로우 사람검토 게이트**(재개형 실행+승인RPC. ✅ 선행 `save_file`/`notify` CHECK 위반 버그 = **세션36 마이그104로 수정 완료**).
> **대표 확인 대기:** 아이콘(가이드 결과폼 상단에 이미 있음 — 별도 스텝 원하는지) · MCP 선택 UI를 "직무 다음 스텝"으로 옮길지(현재 결과 리뷰에 있음) · 세션34 전체 실사용 육안.



### ⭐ 세션31 이후 (최우선 — 대표 확인/결정 대기)
1. **리디자인 최종 디테일 튜닝** — 대표가 **3테마(라이트/다크/공개화면 force-light)·모바일** 육안 확인 후: 글래스 강도·배경 톤(`.app-ambient`)·강조색(`--accent-dot`)·비용매출 레이아웃 비율(추세 2/5 vs 분류 3/5)·추세 그라데이션 진하기 등 미세조정. 전부 `globals.css` 토큰/소수 파일로 즉시 반영. **(세션32: 대표 피드백으로 `.app-ambient` 다크 그라데이션 alpha 0.18~0.2→0.10~0.11·라이트 0.1→0.06으로 완화. 추가 조정은 언제든.)**
   - 🆕 **모바일 픽스(세션32·배포)**: ① 채팅 입력 시 iOS 자동확대(전송버튼 밀림) → root `viewport{maximumScale:1}` + 컴포저 `text-base md:text-sm`. ② **사이드바 드로어 "뒤로 빠짐" 근본원인 = 글래스 리디자인 회귀**: 드로어가 헤더(`glass-panel`=`backdrop-filter`) 안에 있어 `fixed`의 containing block이 헤더(56px)로 잡힘 → **`createPortal(document.body)`** 로 탈출(뷰포트 기준 보장). 불투명 `bg-sidebar`+스크림 blur 제거는 iOS 투명버그 보조. ③ **공용 `shared/Modal`도 body portal**(glass/transform 조상 트랩 예방). 둘 다 `typeof document` 가드(하이드레이션·lint 안전). **⚠️ 대표 실기기 재확인 필요.**
2. **비용·매출 '분류 관리' 화면 (대표 결정 대기)** — 현재는 *"쓰면 자동 제안 축적"*(자유입력+datalist, 업종무관). 원하면 설정에 **명시적 분류 추가/이름변경/삭제·저장**(DB 테이블 신설+RLS+UI, 팀 공유). → 자동방식으로 충분한지 vs 관리화면 필요한지 대표 결정.
3. **미검증 검증(대표 액션)** — macOS Safari `backdrop-filter` 블러 성능·모바일 드로어(프로스트 상속)·`prefers-reduced-transparency`/`reduced-motion` 폴백 육안.
4. ✅ **오늘 할 일 시각 알림(2차·완료·세션32, 마이그 095)** — `personal_tasks.reminded_at` + `remind_due_personal_tasks()`(security definer·KST 기준) + 매일 09:00 KST pg_cron 잡. 기한 도래 미완료 → notifications(system)·realtime로 NotificationBell 표시. due_date 변경 시 리셋 트리거. **⚠️ 실기기 알림 도착 육안 확인 권장.**
5. **백로그(대표 연동 필요)** — 회사 카드/금융 실연동(오픈뱅킹, "담당자만"=`064` 위임패턴 재사용) · 카카오(카톡) 연결. *(백로그=제품 로직은 준비됐고 대표가 외부 계정/키[오픈뱅킹 API 신청·카카오 개발자앱 등록]를 확보해야 켜지는 연동.)*
6. ✅ **워크플로우 모바일 질문 — 해결(세션33)**: 대표 확인 결과 **"일반 모바일 흐름"** 의미였음(세션32 채팅확대·사이드바 픽스로 이미 해소). n8n 드래그 캔버스 자체 재설계는 불필요. **🆕 다음 착수(세션33 대표 지시): "워크플로우 사용하기 쉽도록" 개선** — 범위(온보딩·템플릿·실행 UX·모바일 보기 등) 세션 시작 시 구체화.
7. **멀티테넌시 B1-b 착수 준비됨(대표 결정 2개 대기)** — "어떻게 시작?"에 대한 답: ⓐ 무위험 첫 단계 = `WorkspaceProvider`+`useWorkspace()`(DB 무변경, 결정 없이 착수 가능) → ⓑ 전 INSERT `workspace_id` 배선 → ⓒ service_role 라우트 가드 → ⓓ 맨 마지막 DEFAULT 제거(파괴적·별도 승인 게이트). **선행 대표 결정**: ① 프리셋 자원 복제/공용/하이브리드(권장 복제) ② MCP 시크릿 전역env→회사별 DB 암호화. 상세 = `B1-DESIGN.md §6`·§B. ⚠️ **B1-b 완료 전 두 번째 회사 온보딩 금지.**

> 아래는 이전 세션 로드맵(대부분 완료). 두 번째 회사 받기 전 필수 = **§B 멀티테넌시 B1-b**.

> **현금흐름 손익계산기(세션15~21) ✅ · 현금흐름 AI 코칭(세션23) ✅ 완료·배포.** 다음 후보: 코칭 고도화(추세 분석 — `finance_entries` 월별 연동·저장/히스토리) · 기존 색 일괄정리(구분색으로) · 급여(P2 대표전용) · 예측/오픈뱅킹(P3) · 또는 아래 **멀티테넌시 B1-b**(두 번째 회사 받기 전 필수).

### 🆕 기능 로드맵 (세션10·2026-06-22 · 대표 승인 · 진행 중)
> 8영역 멀티에이전트 코드조사 기반. 순서 = 안전 win→추가형→큰작업→AI고도화. 매 단계 예상이슈+검증(tsc/빌드/RLS시뮬) 후 보고·배포.
- **1차 ✅ 완료·배포**: ⑦위젯 둥둥모션 · ②직급부여(대표만) · ④명함 그룹화(등록자/날짜/회사)
- **2차 ✅ 완료·배포**: ⑤근태 대표열람+위임(마이그 064) · ⑥회의노트 폴더(마이그 065) + **노트 드래그&드롭**
- **🆕 파일 폴더(④ 연장) ✅ 완료**: 마이그 066 `file_folders` + 폴더 사이드바·드래그&드롭(소유자·대표·관리자 이동). 공용 `FolderSidebarItem` 추출.
- **🆕 macOS Finder식 폴더 UX(세션11 ✅ 배포·프론트만)**: 사이드바→**공용 `FolderGrid`**(중립 폴더 아이콘·더블클릭 진입·breadcrumb·다중선택+묶음드래그+**플로팅 이동바**·정렬·날짜그룹·**폴더 안=항상 아이콘**·조건부 컨트롤). 파일은 **종류 아이콘 통일**(썸네일 시도→되돌림). 회의노트 통일, 명함 "직원별" 라벨. 구 `FolderSidebarItem` 삭제.
- **🆕 파일 미리보기 버그픽스(세션11)**: files 버킷이 "본인 폴더만 읽기"(015)라 공개 공유된 남의 파일은 클라가 서명 불가 → 서버 BFF **`/api/files/signed-url`**(유저 RLS 인가→admin 서명). 본인·공유 파일 모두 미리보기/호버 동작. **`pdfjs-dist` 제거**.
- **🆕 근태 개편(세션11)**: 혼합 리스트→**인원별 마스터/디테일**(이름검색+오늘상태) · **월별 보기**(공용 `MonthStepper` ◀▶, 개인·팀) · 24시간제·"0분" 숨김·"내 근태/나" 표시.
- **🆕 직급(position) 전 화면 표시(세션11)**: 채팅·전자결재(결재선)·회의노트 작성자·공지·명함 등록자·프로젝트·지출/휴가·마이페이지·근태에 직급 노출(있을 때만). **설정에 대표 전용 "구성원 직급" 일괄관리** 추가(set_member_position RPC 재사용).
- **3차**: ③재무 UI 재구성 ✅ **완료·배포(2026-06-24 `e1789f3`)** — 962줄 단일 FinanceView → **탭(요약/내역/세금계산서) + 월 기간필터(MonthStepper) + 경량 차트(npm 0, CSS/SVG)**. 신규 `usePeriodFilter`·`financeAgg`(순수 집계, 통화분리 내장)·`financeCharts`(TrendBadge 증감%·TrendBars·BreakdownBars)·모달 2종 분리. **통화합산 금지·RLS·soft-delete·useUndo 유지**. **설계 판정단→적대검증(4/4 pass) 워크플로우**로 진행. **🆕 원화 환산 합계**(마이그 069 `fx_rates` + `/api/finance/fx-rates` BFF, ECB/Frankfurter 일별·무키, 방식A=오늘환율·BTC 제외): 통화 분리는 유지하고 환산 합계만 별도(기준환율·날짜 명시). · ①그룹채팅 ✅ **완료·배포(세션13 `7d41cc6`)** — 전체방+카카오톡식 다중방·초대·읽음표시(마이그 071~075, DM 미접촉·별도 group_* 병렬). 상세=위 배포 블록.
- **4차**: ⑥회의노트 — **에디터 Part 1 + 표 노션강화 + 회의 DB 뷰(마이그 070) + AI 리서치 Part 2(2a~2c) + 인터랙티브 지식그래프(꼬리물기·대화형·플랫노드) + 이미지크기·PDF ✅ 완료·배포(세션13 `0030e9e`)**. **⚠️ AI 리서치 실사용 전제 = Anthropic 콘솔 web search 활성+결제(대표 액션)** — 미활성이면 2a Claude 폴백·2b 출처 없음·그래프는 자료텍스트라 동작. **남은 것:** 그래프 저장(노트 영구 임베드)·Tavily 티어드·요금제 게이팅(무료=AI 불가, 유료/우리팀 enable)·회의 DB 파일컬럼·프로젝트 중요도 연동. 에디터 패스트팔로우(가벼움): 토글(접기)·멘션(@직원)·`/리서치` 슬래시화.
- **확정 결정**: ②직급=`profiles.position` 자유입력·대표(owner)만 / ⑤근태=대표+지정자만(admin 자동열람 제거) / ⑥⑦폴더=공용(멤버 생성), 회의노트 이동=멤버 누구나·파일 이동=소유자/대표/관리자.
- **확정 결정(세션13 · AI 검색백엔드·요금 모델)**: ① **Tavily/Exa는 지금 안 함** → 나중에 추가해 **티어드**(기본=Claude / 프리미엄=Tavily) 구성. ② **AI(Claude) 기능 = 유료 요금제 전용** — **무료 버전은 AI 사용 불가**, 유료만 가능. ③ **단, 우리 팀(첫 회사=현재 워크스페이스)은 지금 사용 가능**(내부 enable). → B2B 멀티테넌트(B1-b·요금제) 구축 시 plan 게이팅으로 구현. 웹서치 자체(Anthropic web_search) 활성·과금은 Part 2 착수 시 결정.
- 신규 마이그: 062(캘린더팀수정)·063(직급)·064(근태열람)·065(회의폴더)·066(파일폴더). 모두 적용·RLS 시뮬 검증.

### A. 지금 안전·고가치 (멀티테넌시 무관 · 리스크 0 · 바로 착수 가능)
1. ✅ **기본(템플릿) 에이전트 똑똑하게 (2026-06-23·완료·라이브)** — 8개 매뉴얼을 7섹션 백본(역할&경계·회사 컨텍스트(교체 블록)·절차·금지선·예시3·엣지케이스·출력형식&성공정의)으로 재작성. seed.sql 재작성 + **마이그 067**로 라이브 version 2 버전업(296~458자→973~1629자, 3~5배). 모델·온도 무변(A②=온도 별건). 롤백 후보 **`067` 직전**(v1을 is_current로). 상세 = `AGENTS-MCP-STRATEGY.md §5`.
2. ✅ **seed 온도값 차등 (2026-06-24·완료·라이브, 마이그 068)** — 정확성(번역·세금·법무)=0.3 / 균형(CS·데이터=0.5·문서=0.6) / 창의(SNS·이미지)=0.9. ※067로 라이브 v2가 이미 있어 seed만으론 라이브 반영 안 됨 → 마이그 068로 현재 버전 temperature UPDATE + seed.sql 동기. 롤백=전부 0.7.
3. **회사 지식 수집** — 차별화 자산(정책·실제 사례·톤)을 슬롯 콘텐츠로.

### B. 두 번째 회사 받기 "전" 반드시 (멀티테넌시 본작업)
- **B1-b(쓰기 강제)**: `WorkspaceProvider`(현재 워크스페이스 컨텍스트) → 전 INSERT에 `workspace_id` 명시 → presence 채널 동적화 → service_role 라우트 가드 → sentinel 백필 → DEFAULT 제거. **앱-DB 동기 배포 필요** = 별도 승인 게이트. (설계 `B1-DESIGN.md` §6)
- **대표 결정 필요(코드보다 먼저)**: ① 프리셋 자원(기본 에이전트·MCP) 전략 = 복제/공용/하이브리드(권장 초기 복제) ② MCP 시크릿 = 전역환경변수→회사별 DB 암호화(`AGENTS-MCP-STRATEGY.md` §3·6).
- ⚠️ **B1-b 완료 전 두 번째 회사 온보딩 금지**(현재 앱이 workspace_id를 sentinel default에 의존 → DEFAULT 제거 전엔 신규 회사 데이터가 섞임).

### C. 사용자(대표) 작업
- **첫 파일럿 고객 1곳 물색**(영업주도+수동셋업 — 제품은 충분히 동작).
- **Gmail 프로덕션 연동**: Vercel 환경변수 5개(`GOOGLE_CLIENT_ID`·`_SECRET`·`_TOKEN_ENC_KEY`·`GOOGLE_OAUTH_REDIRECT_URI=https://equria-workflow-sass.vercel.app/api/google/callback`·`NEXT_PUBLIC_APP_URL`) + Google Console 리디렉션 URI. 안 하면 메일 연결 시 `GOOGLE_OAUTH_NOT_CONFIGURED`.
- **service_role 키 rotation**(레거시 노출분) · **Supabase Auth leaked-password protection 켜기**(대시보드 토글, 무료 보안강화).

### D. 나중
- 채팅 단계7(초대·권한, B2와 합류) · 과금 자동화(Stripe) · SSO/SCIM·SOC2(엔터프라이즈) · Electron/모바일(B6) · **Google Drive 탭**(`FilesView` 미구현 — 설계 `docs/archive/GOOGLE-MCP-ARCHITECTURE.md` §4).
> 비차단 기술부채는 `known-issues.md`.

---

## 💡 합의된 정책 (코드만으론 안 보이는 결정 — 깨지 말 것)

- **🏷️ 브랜드/철학(최신 — CLAUDE.md §1 브랜딩 규칙을 대체)**: 제품 브랜드 = **Complow(컴플로우)** 확정. **도메인** = `complow.kr`(국내 B2B 메인) + 방어용 `.ai`/`.io`/복합`.com`(`complow.com`은 1999년 선점 → 후순위 매입). **상표** = 9류(소프트웨어)·42류(SaaS·AI 서비스)·35류(비즈니스)로 변리사 출원 예정(국내 "컴플로우" 무경합; 38류 COMPLOW는 "포기"건이라 무관). 코드 UI 브랜드 문자열은 Complow로 교체 완료(내부 식별자 `equria:*`/`equria-*`/`equria.local`은 유지). `K-뷰티`는 첫 사내 고객 잔재. 철학 = **회사별 커스터마이징**(도메인·브랜드는 고정값 아닌 **슬롯/설정** 지향).
- **🎯 타겟 고객·제품 목표(대표 진술 2026-08-13 — 기능·가격·UX 판단의 기준선)**: **기업이 아니라 소규모 팀·스타트업·대학생 수준의 작은 조직**이 타겟. 목표 = *"작은 단위의 사람들이 AI를 프로페셔널하게·조직적으로 다루게 하는 것."*
  - **따라서 후순위**: 기업용 요건(SSO·감사로그·온프레미스·조달). 요금표에서 API·SSO를 뺀 판단(세션44)과 일치.
  - **따라서 우선**: IT 담당자가 없는 조직도 스스로 세팅 가능한 온보딩 · 1인당 부담이 작은 가격(Standard ₩29,000/5석 = 1인 ₩5,800) · 설치·심사 없는 진입(**PWA**).
  - **판단 기준**: "이 기능이 5~10명 팀에게 필요한가"로 되묻는다. 대기업 기준으로 설계하면 이 층이 못 쓴다.
- **📲 앱 배포 = PWA(대표 결정 2026-08-13)**: 설치 파일·스토어 배포를 하지 않는다. 이유 = ① 서명 고정비(Apple 연 $99 + Windows 코드서명 인증서)가 없으면 미서명 실행 파일이 OS 경고로 차단돼 **오히려 신뢰를 깎는다**(타겟층은 보안 경고에 취약) ② 데스크톱 앱은 구버전이 남아 서버 API와 어긋나는 사고가 나는데 **우리는 거의 매일 배포** ③ Electron은 데스크톱 전용이라 모바일은 별도. 얻는 것(독립 창·아이콘·알림)은 PWA가 이미 제공. **Electron 재검토 조건 = 유료 고객 요구 또는 배포 주기가 주 1회 이하로 안정될 때**(그때도 최소 2종: mac Universal + Windows x64).
- **삭제 = soft-delete(휴지통)**: 목록 `deleted_at is null` 필터, Storage 파일 보존, ⌘Z Undo(무음·실패 시만 토스트).
  - **⚠️ `is_active` 소프트삭제 테이블의 SELECT 정책에 `is_active=true`를 USING에 넣지 말 것**(마이그 031·032 교훈): 삭제 시 결과 행이 SELECT 가시성을 잃어 42501로 막힘. 소유자는 활성/비활성 무관 조회 + 앱에서 `is_active=true` 필터.
- **🔐 RLS 멀티테넌시 패턴(B1)**: 모든 데이터 SELECT = `workspace_id in (select public.auth_user_workspace_ids())`, INSERT with check = `public.is_workspace_member(workspace_id)`. 헬퍼는 `security definer stable`. ⚠️ **옛 기록 정정(2026-08-13 DB 실물 확인)**: "신규 가입자는 `handle_new_user()`가 equria 멤버로 자동 등록(043)"은 **더 이상 사실이 아니다** — 마이그 **116**이 그 자동 등록을 제거했고(가입 개방 시 외부인이 우리 회사로 들어오던 구조), 지금 `handle_new_user`는 `profiles`만 만든다. 워크스페이스 귀속은 **온보딩(`create_workspace`)·초대 수락(`accept_workspace_invite`)** 이 담당한다.
- **에이전트 = 우하단 위젯 only**(/agents는 빌더/관리). 내 핀 기준. 비공개 기본 + 공유(이제 "내 워크스페이스 안 공개").
- **워크플로우 = n8n 캔버스**. 비공개 기본 + `is_public`. 소유자만 수정, 끈 위상정렬 실행(노드≤6·60s).
- **채팅 SSOT**: `content`(plain)가 모든 텍스트 소비자의 단일 진실. 리치는 `body_json`(버블만). Tiptap 확장 추가 시 `MessageBody` 렌더러 케이스도 함께(`lib/tiptap.ts` 규약).
- **연락처 = `directory_contact` RPC로만**(이메일/전화 컬럼 RLS 차단, 마이그 023b·024). B1(040)에서 **같은 워크스페이스 동료만** 반환하도록 강화.
- **캘린더** = 네이티브 Date·**종일 전용**·첨부 jsonb 메타+`calendar-files` 버킷.
- **DDL 규칙(SSOT)**: MCP `apply_migration`(`project_id` 필수) **+** `supabase/migrations/` 파일 **둘 다**.

---

## 🏢 멀티테넌시 (슬랙형 — 회사별 격리, B2B 판매 대비)

> 목표: 회사별 데이터 완전 격리. **A단계(구조)=완료 · B1-a(읽기 격리)=완료 · B1-b(쓰기 강제)=차후.**

- **확정 결정(대표)**: 격리=단일 Supabase 프로젝트+RLS · 과금=시트 고정요금(+비용추적 병행) · 인증=Supabase Auth **소셜 로그인(구글·애플·카카오)** + 매직링크 병행(카카오=커스텀 OIDC, SSO는 나중 WorkOS) · 시장=영업주도+수동셋업 · MCP=회사별 격리.
- **A단계(마이그 030)**: `workspaces`·`workspace_members`(다대다) + 24개 테이블 `workspace_id`(NOT NULL DEFAULT=sentinel `00000000-0000-0000-0000-0000000000e1`=equria). 제외: `profiles`(전역)·`google_connections`(개인).
- **B1-a 읽기 격리(마이그 033~043, 2026-06-09~10, DB 라이브·검증)**:
  - 헬퍼 3종(033) + 24테이블 RLS를 workspace 멤버십 격리로 재작성(034 profiles 동료한정·035 완전개방7·036 agents/workflows·037 사용자별·038 채팅·039 MCP·040 함수/RPC/트리거·041 하드닝) + **043 신규가입자 자동 멤버등록(회귀수정)** + 042 비용추적 컬럼.
  - **검증**(롤백 트랜잭션): 멤버=회귀 0, 비멤버=전 테이블 0 + 연락처 PII 0, 신규가입=프로필+멤버십 생성·write-gate 통과. 독립 재검증(적대 9에이전트)으로 '신규가입 시한폭탄' 잡아 043으로 수정.
  - 앱 코드 변경 없음(읽기는 멤버십 함수 기반).
- **B1-b 알려진 한계(쓰기 강제 전까지)**: ① 앱이 INSERT에 workspace_id 미명시·sentinel DEFAULT 의존 → DEFAULT 제거 전 `useWorkspace()`+전 INSERT 배선 선행 필수 ② presence 채널 전역(비차단) ③ 채팅 라우트 MCP 로딩 workspace 미검증(`AGENTS-MCP-STRATEGY.md` §3) ④ 비용추적은 성공 호출 기준 추정 ⑤ 어드바이저 WARN(헬퍼 SECURITY DEFINER 실행가능=RLS 필요·무해).
- **B1-b 남은 작업 + B2~B6 로드맵**: `PRODUCTIZATION.md`·`B1-DESIGN.md` 참조. 포장(Electron/모바일)은 같은 코드 래핑으로 후순위.
- **🆕 세션26 정합(2026-07-06)** — *로드맵 SSOT는 PRODUCTIZATION.md, 아래는 이번 세션 결정만*:
  - **브랜드 = Complow** 확정(코드·문서·로고·파비콘 반영 완료·배포).
  - **개인정보처리방침·이용약관 초안 완료** = `docs/legal/{privacy,terms}.md` → PRODUCTIZATION §B5/MVP의 "TOS·Privacy(한국)" **착수분**. 남은 것: `[___]` 값·`/privacy`·`/terms` 페이지·가입 동의·변호사 검토·hard-delete·public 버킷 점검.
  - **인증 방식 결정** = B2를 **소셜 로그인(구글·애플·카카오)** 우선으로(카카오=커스텀 OIDC). OAuth 콜백은 **데스크톱(B6 Electron) 딥링크까지 고려**해 설계.
  - **🆕 랜딩/마케팅 페이지** = 신규 필요 트랙(공개 사이트: 소개·가격·CTA "앱 열기/데스크톱 다운로드"). **앱과 분리**(예: `complow.kr`=마케팅 / 앱=로그인 후). **멀티테넌시와 무관 → 독립 착수 가능**. 격리는 "로그인해 워크스페이스 진입 시점"부터(랜딩은 공개).
  - **착수 우선순위 권장**: B1-b(쓰기 격리·선행 차단막) → B2(소셜로그인+초대+워크스페이스 관리) → B3(과금+rate limit). 랜딩페이지·AI 고도화(에이전트/워크플로우/MCP)는 병행 가능.

---

## 🚀 환경 / 접근

- **GitHub**: `chowhiwon99-code/equria-workflow-Sass` (main=프로덕션, 작업브랜치 `feat/toss-ui-refresh`).
- **Vercel**: team `team_wcW0NMU7oiIxNndyV1afigbp` · project `prj_CcCTUr8eIYpaStaj6RNq7VoLPZG6` (`equria-workflow-sass`) · 배포보호 off.
- **Supabase**: project `dutovtfdckhayyvhtuxu` (ap-northeast-2 서울) · 마이그 **001~107**. 🆕세션38: 107=`mcp_static_oauth_clients`(추가형·Supabase MCP 적용·drift0 — `mcp_oauth_clients.is_static` boolean default false[대표 등록 정적 크리덴셜 표시, DCR 자가치유가 안 덮게] + `mcp_user_connections.custom_url` text[Zapier 커스텀URL 대비, 아직 미사용] / 롤백=두 컬럼 drop). 세션37이어서2: 106=`agent_memory_importance`(추가형 — `agent_memories.importance` smallint default 2[1낮음·2보통·3높음], AI 기억 정리·중요도순 주입용 / 롤백=drop column). 105=`project_collab`(세션36 작성·세션37 Supabase MCP 적용 — `projects.notes`·`deleted_at` 추가형 컬럼 + `projects_update` RLS에 project_members OR 추가·멤버도 중요도/상태 변경 / 롤백=update 정책을 035 원문 created_by·owner_id만으로). 세션36: 104=`workflow_tool_check`(추가형 — `files.source`에 'workflow', `notifications.type`에 'workflow' 추가 → 워크플로우 save_file/notify 도구 CHECK 위반 버그 수정 · 프로덕션 SQL Editor 직접적용). 세션35: 103=`agents_category_check` 제거(카테고리 자유입력 정합), 102=`mcp_oauth_clients.redirect_uri`(OAuth 자가치유·주소변경 자동 재등록), 101=`admin_usage_by_member()` RPC(오너전용 구성원별 사용량 집계·security definer·게이팅검증 오너4/비오너0), 100=`agent_knowledge.is_personal`(지식파일 개인전용 — true면 올린 본인만 열람·주입, RLS 검증 소유자2/타인1), 099=`agent_memories`(에이전트 개인 기억 v1, `user_id`+`agent_id`, 본인만 RLS·격리검증, 프로젝트공유·벡터는 v1.5·v2 추가형). 세션34: 096=`remove_default_agents`(기본 8개 소프트삭제·복구가능) · 097=`agent_knowledge`(지식파일, 부모 상속 RLS) · 098=`agent_mcp_connectors`(agent_versions.mcp_connectors 개인 커넥터 바인딩).
- **.env.local**: ANTHROPIC · Supabase(URL·anon·service_role) · Google 4종 · `WORKSPACE_PASSWORD` 채워짐. ⚠️ **시크릿 값은 문서/채팅에 적지 말 것**(HANDOFF는 git 추적).
- **테스트 계정**: 조휘원(`c6817c63-943f-4257-8500-f9840ad39bde`)·이동규·김건 (워크스페이스 비번 로그인). 모델: 기본 `claude-sonnet-4-6` / 복잡 `claude-opus-4-7`.
- 링크: [GitHub](https://github.com/chowhiwon99-code/equria-workflow-Sass) · [Vercel](https://vercel.com/chowhiwon99-2151s-projects/equria-workflow-sass) · [Supabase](https://supabase.com/dashboard/project/dutovtfdckhayyvhtuxu) · 메모리 `~/.claude/projects/-Users-johwiwon-equria-workspace/memory/`
