# HANDOFF — Complow (회사 AX 도입 플랫폼 · B2B SaaS)

> **이 파일은 "지금 상태 · 다음 할 일 · 합의된 정책"만 담는다.**
> 과거 세션 상세는 `WORKLOG.md`와 git 커밋 메시지에 있다. 여기에 이력을 쌓지 말 것.
> 읽는 순서: **이 파일 → `CLAUDE.md` → `.claude/skills/{safe-changes,latest-stack,known-issues,work-harness}.md`**

---

## 📣 남은 미검증 하나 — 부분취소 금액 필드명

> **`readInquiry`가 읽는 `cancelledAmount`/`canceledAmount`가 실물 응답에 있는지 아직 모른다.**
> 2026-08-22 조회 실호출로 확인한 건 **승인 거래**(`status:"paid"`)뿐이고, 그 응답엔 두 이름이
> **둘 다 없었다**. 취소 거래에서만 나타나는 필드라 정상일 수 있지만 확인된 게 아니다.
>
> 왜 중요한가 — 못 읽으면 `canceledAmountKrw`가 null이 되고, `billing_apply_cancellation`이
> **전액 취소로 본다**. 즉 **부분환불이 전액 환불로 처리되어 유료 고객이 통째로 강등**된다.
>
> **확인 방법**: 실취소 1건(최소금액) 후 같은 조회를 돌려 필드명을 눈으로 볼 것 → 아래 §남은 것 3번.
> 후보 대안: 응답에 **`balanceAmt`(잔액)**·**`cancels`(배열)**가 실재하므로,
> 필드명이 다르면 `amount − balanceAmt`로 유도하는 편이 철자 추측보다 안전하다.

---

## 🎯 지금 상태 (2026-08-27)

| 항목 | 값 |
|---|---|
| 프로덕션 | **`2755c3b`** · `complow.kr` (Vercel `icn1` · **플랜 = Hobby**) · 배포 2026-08-27 READY 확인(`dpl_EiTLPcj…`) · **롤백 후보 `88b996c`** |
| 작업 브랜치 | **`main` 직접**(루트 체크아웃이 main). `worktree-gmail-scope-narrow`도 있으나 51f4100에 정지. ⚠️ `feat/toss-ui-refresh`(`4b7fe08`)는 **죽은 브랜치** — 쓰지 말 것 |
| 스모크(배포 직후) | `/` 200 · 웹훅 GET 200 · `/api/billing/reconcile` **401**(크론 시크릿 가드) · `/billing` 307(로그인) · Playwright로 **프로덕션에서 직접 로그인해 대시보드·결제화면 렌더 확인**(아래 §참고) |
| 마이그레이션 | **001~143** 적용 · drift 없음 (135~137은 결번) |
| 게이트 | `tsc` 0 · `pnpm lint` **29 errors/0 warnings**(전부 기존 부채, **신규 0이 베이스라인**) · `next build` 성공 |
| 유료 고객 | **외부 고객 0명.** 워크스페이스 3개 = premium 2(EQURIA·이큐리아2, 자사 내부 무제한) + **standard 1**(테스트 — 2026-08-27 대표 요청으로 **해지 처리 완료**, `auto_renew=false`·9/21 자동청구 취소됨. 9/20까지는 정상 이용 후 free로 자동 전환). 가격·스키마 변경 리스크가 아직 낮은 시점 |
| 결제 | **빌키(정기결제) 전 구간 완료**. 실결제 1건 성공(08-20) · **매달 자동청구**(`d21716a`) · **환불 계산 + 취소 통보 반영**(`8b4a188`·마이그142). 코드로 할 건 다 했다 — 남은 건 **카드사 심사**와 실물 확인뿐 |

**배포 규칙 (어기면 프로덕션 승격이 스킵된다)**
`main`을 **먼저** push → Vercel 프로덕션 빌드 생성 확인 → 그 다음 feat 브랜치 push.
같은 SHA를 동시에 올리면 Vercel이 중복제거해 프로덕션이 안 올라간다. `/deploy`가 이 순서를 자동화한다.

**⚠️ 세션 시작 시 브랜치부터 확인할 것** — `git rev-parse --short HEAD` vs `origin/main`.
2026-08-23 세션이 `feat/toss-ui-refresh`(**main보다 74커밋 뒤**)에서 출발해 **세션44 시점의 낡은
HANDOFF를 읽고** 이미 끝난 과제를 "다음 할 일"로 잘못 집계했다. HANDOFF를 읽어도 낡은 것이면 소용없다.

---

## ✅ 완료 — 기능별 요금제 게이팅 (2026-08-24)

전자결재·근태·회의노트·명함·비용/매출 = Standard+, 워크플로우·MCP 연동 = Pro+ (랜딩 `PLAN_ROWS`와 정확히 일치).
**서버 강제 = DB `BEFORE INSERT` 트리거 16개**(마이그143, `agent_version_enforce_model_plan`(134)·
`plan_seat_limit`(125)과 같은 패턴 — RLS·service_role 우회와 무관하게 항상 발동).
화면은 `PlanGate`(`components/shared`)가 `FEATURES`의 `minPlan`을 GuestGuard와 같은 방식으로 대조해
미달이면 업그레이드 카드로 대체(사이드바는 그대로 노출, 클릭 후 유도 — 대표 결정). 그랜드파더 없음(대표 결정) —
적용 전 확인 결과 테스트(standard)·이큐리아2(premium) 모두 대상 테이블 0건이라 소급 충돌 없었음.
범위 제외: `mcp_user_connections`(workspace_id 없는 개인 커넥터, 대표 확인).
검증: tsc 0·lint 29/0(신규 0)·build 통과·트랜잭션 시뮬레이션(rollback)으로 서버 강제 확인.
⚠️ **브라우저 실사용 확인은 못 함**(Claude-in-Chrome 확장 미연결) — 아래 §마케팅 개시 조건 D참고 계정으로 직접 볼 것.
`lib/plans.ts`에 새 기능/플랜을 추가할 때는 `features.ts`의 `minPlan`과 마이그143 트리거 인자를 **같이** 고칠 것.

**같이 하면 좋은 것 — 파일 용량 상한 (미착수)**
5개 버킷 중 `meeting-media`(50MB)만 상한이 있고 나머지는 무제한, 워크스페이스별 총량 상한도 없다.
Supabase Free는 1GB고 **넘으면 과금이 아니라 중단**이라 **한 사람이 큰 파일을 올려 전 서비스를 멈출 수 있다.**
상세 = `docs/ops/infra-limits.md` §아직 안 한 것.

### 🚦 마케팅 개시 조건 (2026-08-23 판정 = 아직 아니다)

| # | 조건 | 상태 |
|---|---|---|
| A | **기능별 게이팅** | ✅ 완료(2026-08-24, 배포됨) |
| B | **파일 용량 상한** | ⬜ 미착수(서비스 중단 리스크) |
| C | **제3자 신규가입 E2E 1회** | ⬜ 대표 보류 항목. 광고로 온 첫 사람이 가입 실패하면 그날 유입을 통째로 잃는다 |
| D | **카드사 심사 통과** | ⬜ 대기(시한 **9/17**). 기록상 지금은 **정산(입금)이 안 열려 돈이 안 들어온다** — 담당자에게 한 번 직접 확인할 것 |
| E | Vercel Pro + Supabase Pro | ⬜ **첫 유료 고객 직전**에만(그전에 사면 돈만 나감). Hobby는 *"non-commercial only"* = 돈 받는 순간 약관 위반 |
| F | 부분환불 필드 확인 | ⬜ 위 §📣 — 심사 후 실취소 1건과 함께 |

**A~D 전에는 유입을 켜지 않는다.** 콘텐츠 제작·채널 세팅·대기자 명단은 지금 해도 된다.
"무료 베타"로 먼저 여는 선택지도 있으나 **B·C가 먼저**고, 무료로 다 쓰게 한 뒤 막으면 반발이 크므로
하려면 **처음부터 "베타 기간 한정"이라고 못박고** 시작해야 한다.

---

## ▶ 진행 중 — 나이스페이 결제 연동

> **설계·근거·함정은 `BILLING-PLAN.md`에 있다. 결제 코드를 건드리기 전에 반드시 읽을 것.**

**⏰ 시한 = 2026-09-17** (임시오픈 30일). 그 안에 심사 완료 + 연동이 끝나야 실테스트가 된다.

### ✅ 완료 — 코드로 할 건 다 끝났다

| 커밋 | 내용 |
|---|---|
| `6df4bdd`·`5acb250` | **마이그139~140** 테이블 4개 + RLS + 처리 RPC 7개 |
| `0982585`·`1f80c46`·`cde652d` | 연간 요금 · `/terms/billing` · 요금제 카드 + **24시간 해지** · 결제 화면 + 별도 동의 |
| `c21af5b` | 🔴 프록시 차단 해제(결제가 조용히 전부 실패하던 버그) · 웹훅 · 크론 · **마이그141**(복구·만료·7일고지) |
| `ac1cf27`·`9ae7883` | **PG 규격 전환** — MID+상점키 → `CLIENT_KEY`+`SECRET_KEY` · `Authorization: Basic` · 성공판정 `resultCode==="0000"` · 결제창 없이 **카드정보 입력창을 우리가** 만듦 |
| `d21716a` | **매달 자동청구**(`lib/billing/renew.ts`) + `auto_renew` 켜지는 경로 + 결제 대상 회사를 활성 워크스페이스로 |
| `8b4a188` | **환불 계산**(`lib/billing/refund.ts`) + **마이그142** 취소 통보 반영 + 웹훅 연결 |

**실결제 1건으로 증명됨**(2026-08-20): 빌키 발급 → 승인 → 정산 → 요금제 승급.
29,000원 · 하나(외환) 끝 2590 · DB에 남은 건 `bid`와 **끝 4자리뿐**(카드번호 전체 없음).
세부 경위·실측치는 **`WORKLOG.md` 세션47~49**에 있다. 여기에 다시 쌓지 말 것.

### 🔴 결제 코드를 만질 때 깨지 말 것 (이유는 각 파일 헤더 주석에)

1. **카드 원문**은 브라우저 → 요청본문 → 서버 암호화 직전까지만 존재. DB·로그·에러메시지·PG raw
   **어디에도** 남기지 않는다. 저장은 `bid` + 끝 4자리. 암호화는 **AES-256-CBC**(ECB는 카드에 부적합).
2. **중복 청구 방어 4겹**(`renew.ts` 헤더) — ①주문번호=워크스페이스×한국날짜(`order_id` unique가 뮤텍스)
   ②확정 표식(`refund_reason`이 `renew:`로 시작)이 없으면 **전부 미결로 보고 재청구 안 함**
   ③미결은 PG **주문번호 조회**로만 확정("거래 없음"은 `A118·A243·A251·U107·U126` 코드로만 판정)
   ④청구 판정은 `current_period_end` 하나만. **하나라도 빼면 뚫린다.**
   ⚠️ ②는 `billing_fail_stale_checkouts`가 `refund_reason`을 `coalesce`로 **보존**하는 성질에 기댄다 — 그 함수를 고칠 때 깨지 말 것.
3. **크론 스윕 순서** ⓪갱신 → ①고아정리 → ②해지만료 → ③기간만료 → ④7일고지. 갱신이 뒤로 가면
   성공한 구독까지 `past_due`를 찍고 미결 판단이 흐려진다.
4. **실제 청구는 크론(GET)만.** 화면 진입 시 지연 대사(POST)는 로그인한 아무나 부를 수 있다(`runSweep(charge)`).
5. **웹훅은 본문을 믿지 않는다.** 승인이든 취소든 TID로 조회 API를 다시 때려 확인한 뒤에만 반영한다 —
   특히 취소는 **주문번호만 알면 가짜 통보로 유료 고객을 강등**시킬 수 있다.
6. **환불 계산은 `/refund` 페이지와 세트다.** `computeFairRefundKrw()`와 공표 문구 중 한쪽만 고치면 분쟁에서 진다.
7. 취소 반영은 **현재 기간의 결제일 때만** 강등(`current_period_start` 일치) · 취소액은 합산이 아니라 `greatest`.
8. **크레딧은 승급 시 즉시 올리고 강등 시엔 깎지 않는다**(회수는 분쟁 소재).

### ✅ 해소 — 예약돼 있던 09-21 자동청구 해지 완료 (2026-08-27)

8/20 결제분의 `auto_renew`가 2026-08-21에 켜져 09-21에 29,000원이 자동 청구될 예정이었으나,
2026-08-27 대표 요청으로 `billing_request_cancel` RPC(앱의 실제 해지 버튼과 같은 경로)를 통해
정식 해지 처리했다. 확인된 상태: `auto_renew=false`·`next_charge_at=null`·
`cancel_effective_at='2026-09-20 06:56:52Z'`. **9/20까지는 정상 이용, 그 이후 자동으로 free 전환.**
09-21 청구는 없다. (다시 켜려면 `/billing`에서 재결제하거나 설정 > 요금제에서 "해지 취소".)

### 🔜 결제 트랙에 남은 것

**코드로 할 수 있는 건 다 했다.** 남은 건 전부 실물 확인이거나 대표 액션이다.

| # | 남은 것 | 막고 있는 것 |
|---|---|---|
| ~~1~~ | ~~`inquireByOrderId` 실API 왕복 검증~~ | ✅ **2026-08-22 통과.** 8/20 주문 조회 → `resultCode "0000"` · `approved=true` · 29,000원 · tid `UT0036025m01162608201556531803`. Basic 인증·URL 조립·`readInquiry` 해석 전부 정상. 시크릿키 32자 = AES-256-CBC 경로 확정 |
| ~~2~~ | ~~웹훅 URL 등록~~ | ✅ **2026-08-22 등록.** 상점관리자 > 개발정보 > 웹훅에 `https://complow.kr/api/billing/nicepay/webhook` · 결제수단 **4종 전부** 체크(가상계좌는 입금 통보가 이 콜백뿐이라 미리 켜둠). ⚠️ 같은 화면 **IP보안은 비워 둔 상태 유지** |
| 3 | 실승인 1건 + 실취소(최소금액) | 카드사 심사 통과 후. ⚠️ 옛 EUC-KR 우려는 **해소됐다**(REST는 JSON/UTF-8). 🔴 **취소 시 위 §📣의 부분취소 필드명을 반드시 같이 확인할 것** |
| 4 | **웹훅 통보 규격 실물 확인** | 문서가 Content-Type·필드명을 단정하지 않아 REST(camelCase)와 옛 필드를 **둘 다** 읽게 해뒀다. 통보 1건 받아 확정할 것. 등록은 끝났으니 이제 **통보가 오기만 하면 된다** — `billing_events`에서 `kind='webhook_received'`로 조회 |

✅ **Vercel 플랜은 `Hobby`** — 크론 **하루 1회가 상한**(더 잦으면 배포 실패). `0 0 * * *`가 유일한 선택.

### ✅ 해소 — "서비스 제공기간 3개월 초과 상품은 판매불가" → 연간 요금제 삭제 (2026-08-25)

나이스페이(포스타트)가 홈페이지 심사 미비사항으로 연간 결제 상품 삭제를 요구 → **대표 결정: 삭제**.
랜딩 프로모션 문구·결제화면 월/연 토글·체크아웃 API(`body.cycle` 무시하고 항상 monthly로 고정 —
UI만 지우면 크래프팅된 요청으로 yearly가 여전히 통과하던 서버측 구멍도 같이 막음)를 제거.
기존 연간 가입자가 없어(유일한 실결제 고객=월간) DB 제약조건·환불계산(`refund.ts`)·자동갱신(`renew.ts`)·
약관 문구는 그대로 둠(되돌리기 쉽게). `d044e9a`로 배포·라이브 확인(`complow.kr`에서 "연간 결제" 문구 0건).

**🔴 여전히 확인 필요 — 같은 메일의 "바로오픈" 안내**: "정기결제의 경우 **실사업자전환일부터 사용 가능**"이라는
문구가 있음. 실사업자 전환은 카드사 심사 **완료 후**에나 일어나는데, 이 문구가 "지금 나이스정보통신
명의로 도는 정기결제도 실사업자 전환 전엔 정식이 아니다"라는 뜻인지 **확인 안 됨**(테스트 구독은
2026-08-27에 해지해 이 특정 청구 자체는 더 이상 안 걸리지만, 실 고객이 늘면 다시 걸리는 질문이라
회신 메일에 여전히 물어볼 것). PG 규정을 추측으로 단정하지 말 것(과거 사고 전례).

### 📬 2026-08-26 담당자 재회신 — 미비사항 2건

포스타트가 08-25 회신을 확인 후 다시 2건을 지적함(회신 확인 후 카드사 심사 접수 예정이라고 명시):
1. **테스트계정 미비** — "기존 계정(이름·비밀번호)으로 로그인" 경로는 확인됐으나 로그인 계정(ID/PW) 자체를
   못 받았다고 함(08-24에 발급·전달했다고 기록했는데 미도달 추정). ✅ **2026-08-27 비밀번호 재발급 +
   실제 로그인 성공 검증**(`signInWithPassword` API 직접 호출로 세션 발급까지 확인, 문서엔 비밀번호 미기재
   원칙 유지 — 회신 메일 본문에만 기재). 2차 인증 관련 재확인 요청은 이미 답변 완료(위 §3번).
2. **상품 가격 조정 관련 안내** — "조정하려는 가격 변동 금액이 얼마로 예상되는지" 확인 요청.
   → 현재 구체적 변동 계획 없음, 통상 범위(10~20% 이내) 소폭 조정 가능성만 언급하고 회신 예정
   (지금 큰 폭 인상이 필요한 원가구조가 아님 — 위 §요금제·AI 원가 정책 참고).

### 🔴 카드사 승인심사 — 회신 발송 완료, 답 대기 중

절차: **전자계약 ✅ → 카드사 승인심사 ◀ 현재 → 보증보험 → 정식 오픈**
지금은 **바로오픈** 상태다(관리자 접속 = 심사 통과 아님). 신용카드 결제·연동은 지금 되고,
**간편결제와 정산(입금)만** 심사 완료 후 열린다. ⏰ 시한 **2026-09-17**.

**2026-08-22 담당자 메일에 회신 발송함** — 결제경로 5장(PDF) + 정기결제 상품·실제 금액·
판매 상품 상세내용 + 카드정보 입력창 4칸 + 가맹점명 문의.
⚠️ 담당자는 "**PPT**로 제작"이라고 명시했는데 **PDF로 보냈다** — 형식 지적이 오면 변환해서 재발송할 것
(원본 이미지 5장은 그 PDF 안에 있다). 스크린샷에 **주소창이 안 보이는 것**도 지적 가능 지점이다.

| # | 심사 요구 | 상태 |
|---|---|---|
| 1 | 홈페이지 하단정보 7종 + 약관 본문에 상호 | ✅ 라이브 확인 |
| 2 | 홈페이지 URL | ✅ `https://complow.kr` |
| 3 | 로그인 페이지 + **심사용 ID/PW** | ✅ **2026-08-24 발급 → 08-26 담당자가 미도달 재문의 → 08-27 비밀번호 재발급 + 실로그인 성공 검증 완료**(`signInWithPassword` API 직접 호출로 세션 발급까지 확인 — Chrome 확장 미연결이라 클릭 재현은 못 했지만 인증 자체는 확실). 격리된 워크스페이스("나이스페이 심사", free plan — 실회사 데이터와 안 섞임)의 소유자 계정. `/login` → "기존 계정(이름·비밀번호)로 로그인" 클릭 → 이름 `nicepayreview` / 비밀번호는 회신 메일 본문(문서에 시크릿 값 금지 원칙, git 추적 문서엔 절대 기재 금지). 2차 인증 없음 확인·답변 완료(`AuthForm.tsx` 로그인 경로 2가지: 구글 버튼 / 이름·비밀번호, 둘 다 추가 인증 단계 없음 — 구글 버튼을 누르면 구글 계정 자체 2FA에 걸릴 수 있어 그건 피하라고 안내) |
| 4 | 상품 목록 ⚠️ "sample·test" 문구 있으면 불가 | ✅ Standard ₩29,000 · Pro ₩49,000 |
| 5 | 카드정보 입력창(카드번호·유효기간·생년월일·비밀번호앞2자리) | ✅ `/billing` 4칸 라이브 |

**남은 대표 액션**

| # | 할 일 | 왜 |
|---|---|---|
| ~~1~~ | ~~웹훅(URL 통보) 등록~~ | ✅ **2026-08-22 완료** |
| 2 | 계약서 사본 보관 — 알림톡 「이메일 전달」 | 카톡 삭제 시 열람 불가 |
| ~~3~~ | ~~심사용 계정~~ | ✅ **2026-08-24 발급 완료**(위 §심사 요구 3번) |
| ~~4~~ | ~~연간상품 삭제 요구에 어떻게 답할지 결정~~ | ✅ **2026-08-25 삭제 결정 + 배포 완료**(`d044e9a`) |
| 5 | 🔴 회신 메일 발송(08-26 재문의 2건에 대한 답) | 담당자가 "회신 확인 후 카드사 심사 접수"라고 명시 — 안 보내면 접수 자체가 안 걸림. 보낼 내용: ① 재발급된 심사용 계정 ID/PW ② 가격 조정 폭 답변(통상 10~20% 이내, 구체 계획 없음) ③ 🔴 "실사업자전환일부터 정기결제 사용 가능" 문구가 향후 실 고객 정기결제에 영향 있는지 문의(신규 발견, 미확인 — 위 §참고) |

⚠️ **키 재발급 시**: `Server 승인` + `Basic 인증`(기본값) · **IP보안(REST API 접근제한)은 비워둘 것** —
Vercel은 아웃바운드 IP가 고정이 아니라 등록하면 승인 요청이 통째로 막힌다.
환경변수는 **재배포해야 적용**되고, Sensitive로 저장하면 값을 다시 못 읽는다(분실 시 새 값으로 덮어쓰기).

**담당자에게 아직 못 받은 답**: 베이직 → 프리미엄 전환 가능 여부(급하지 않음 — 손익분기 누적 약 1.83억) ·
가맹점명 표기(승인 문자가 "나이스정보통신"으로 찍힘 — 이번 회신에 문의 포함).

---

## ▶ 트랙 2 — 첫 사용 경험(Activation) · 코드 완료, 눈으로 볼 것만 남음

> **점검 결과·근거는 `ACTIVATION-PLAN.md`. 이 트랙을 만지기 전에 읽을 것.**

진단 한 문장: **기능이 부족한 게 아니라, 17개 중 실사용 5~6개이고 신규 팀의 첫 5분이 비어 있다.**
대표 결정: ① Activation 먼저 ② 기능은 다 유지하고 완성도만 ③ 시트 한도는 가격 무변경·UX만.

**코드 10개 전부 완료·배포됨**(2026-08-20 `f02864e`) · **DB 마이그레이션 0건** ·
프로덕션 DB에 `검증01`을 만들어 RLS·판정 로직까지 수치로 검증 후 삭제(잔여 0).
커밋·검증 상세는 `WORKLOG.md` 세션47 / `ACTIVATION-PLAN.md` §5-0.

**🔜 남음**
1. **화면 확인(브라우저)** — 로직·권한은 검증됐고 눈으로 볼 것만 남았다: 시작하기 카드 레이아웃 ·
   닫은 뒤 재방문 · 컴피 예시칩 · "제안 받기"에 Gmail 403이 안 뜨는지.
   ⚠️ 신규가입 E2E는 **구글 전용이라 새 구글 계정이 필요**하다(대표 보류 항목). 대표 계정으로
   `/dashboard`만 봐도 시작하기 카드·사이드바 '팀 초대'는 바로 확인된다.
2. **§4 컴피 쓰기 툴** — 선택. 대표 판단 대기(**착수 금지**).

🔴 **깨지 말 것 2개**
- **시드 에이전트 DB 함수를 부활시키지 말 것.** 마이그130의 clean-slate 사유(옛 8종은 이큐리아 전용 ·
  앱 데이터를 못 읽는 "혼자 글쓰기 도우미")는 지금도 유효하다. 앱(`src/lib/seedAgents.ts`)이 업종 무관 3개를 만든다.
- **`agents.category = '시작하기'`는 표식이다.** 시작하기 카드 3단계와 예시 갤러리가 "이 값이 아닌
  에이전트가 있는가"로 판정한다. 빼면 가입 즉시 3단계가 완료로 찍혀 안내가 통째로 무의미해진다.

---

## ▶ 트랙 3 — 랜딩페이지 리디자인 · 시안 확정 중(코드 미착수)

대표 요청(2026-08-27): 정렬 정리·"더 있어보이게"·스크롤 리빌 모션·좌우교차 이미지블록 강화.
실제 코드(`src/components/landing/LandingPage.tsx`, 549줄)는 **아직 한 줄도 안 건드렸다** —
`design` 스킬로 별도 시안(Claude Design 캔버스, Artifact)을 먼저 만들어 대표가 보면서 방향을 잡는 중.

**시안 링크**: https://claude.ai/code/artifact/3395df82-a5e2-4d1e-a2ad-a2f3ccd09c1a
(대표 소유 Artifact — `Artifact` 도구로 `action:"read"` 하거나 `url`로 재게시하면 이어서 수정 가능)

**시안에 반영된 것** (전부 실제 프로덕션 카피·숫자 그대로, 새로 지어낸 콘텐츠 없음):
- 히어로 목업 고도화(레이어드 그림자·은은한 배경 글로우·미니 바차트) + 실제 작동하는 스크롤 리빌
  (IntersectionObserver, 정적 목업 안에서도 진짜로 스크롤하면 섹션이 하나씩 나타남)
- 4개 좌우교차 블록(손익·팀협업·전자결재·외부연동)의 미니 UI 카드를 옆 텍스트 칸 높이만큼 꽉 채우고
  콘텐츠도 보강(대표 지적 — "작게 하지 말고 꽉 채워서")
- FAQ를 실제 클릭 가능한 네이티브 `<details>` 아코디언으로(6개 전부 실답변 포함 — 최초 시안엔 2~6번
  답변이 빠져있던 걸 백그라운드 검증에서 잡아 수정함)
- AI·기능·보안 3개 섹션 제목을 가운데→왼쪽 정렬로 통일(그리드와 정렬 체계 일치, 대표 확정)
- 마케팅 카피(설명·불릿·FAQ·요금제·통계·테이블·푸터) 전반 폰트 크기·명암비 상향(대표 지적 —
  "색상이나 글씨 크기가 작다"). 목업 카드 안쪽 미니 UI 라벨은 실제 앱처럼 보이게 의도적으로 작게 유지.
- 폰트는 Pretendard 대신 시스템 폰트 대체(캔버스 환경이 구글 폰트만 허용 — 실제 코드로 옮길 땐 원래대로)

**🔜 다음 세션 시작할 것**
1. 대표가 시안을 마저 보고 최종 확정(추가 피드백 있으면 캔버스에서 계속 반영).
2. 확정되면 **실제 `LandingPage.tsx`에 반영** — 이게 진짜 작업이다: 정렬·타이포·미니카드 CSS를
   Tailwind로 옮기고, 스크롤 리빌은 IntersectionObserver 훅으로 구현(시안의 로직 그대로 이식 가능).
   `prefers-reduced-motion` 대응 포함(시안에도 이미 넣어둠).
3. 배포 전 라이트/다크 테마 확인 — 시안은 라이트 전용으로 만들었는데 실제 페이지도 라이트 고정
   (`bg-white` 하드코딩)이라 문제 없음, 확인만.

---

## 💳 나이스페이 계약 — 확정 조건

서명 완료 2026-08-18 · 문서 `5925800892_컴플로우(Complow)_개인_휴대폰인증` · 요청자 `untact@nicepay.co.kr`

**요금 (포스타트 베이직)**

| 항목 | 값 |
|---|---|
| 등록비 · 연관리비 | **면제** (프로모션 조건) |
| 신용카드 · 간편결제 3사 | **3.2%** (VAT 별도) |
| 계좌이체 / 가상계좌 | 2% (최소 200원) / 300원 건당 |
| 휴대폰 | 실물 4% / **컨텐츠 7%** — ⚠️ **쓰지 말 것**(카드의 2배 + 정산이 거래월+3개월째) |
| 정산주기 | **일일(+5일)** |
| 보증보험 | 면제 (단, **정산한도 200만원**) |

**🎉 영세사업자 우대수수료율 = 카드 2.0%** — 계약서 명시: *"우대수수료율 적용은 이용계약에서 정한
계약조건보다 **우선하여 적용**"*. 영세(~3억) 2.0 / 중소1 2.5 / 중소2 2.6 / 중소3 2.8 / 일반 3.2%.
국세청 자료로 나이스가 **일괄 적용**, 시기는 매반기 종료일부터 1개월 이내.
→ Standard ₩29,000 기준 수수료 ₩1,021 → **₩638**.

**⚠️ 주의 조항 3개**
- **정산한도 200만원 초과분은 담보 제공 시까지 지급 보류**(제14조 ⑦). Standard 약 **69건**에서 도달 → 넘기 전에 협의.
- **계약기간 1년 · 자동연장** — 만료 1개월 전까지 의사표시 없으면 1년 연장.
- **이행보증보험은 한 번 걸면 못 뺀다**(제15조) — 거래 1건이라도 발생하면 계약기간 중 해지 불가, 2년 청구권.

**프리미엄(등록비 55만원·카드 2.9%) 손익분기 = 누적 거래액 약 1.83억**(Standard 약 6,300건) → 현 단계는 베이직이 맞다.

---

## ⚖️ 결제 법적 의무 4가지 (계약 제22조 · 여신전문금융업법 시행령)

**①번 위반 시 이용자 민원·손해배상을 전부 가맹점이 부담한다**고 계약서에 박혀 있다. 지우지 말 것.

| # | 의무 | 구현 |
|---|---|---|
| ① | 자동결제 약관을 **회원가입 약관과 별도로** 고지 + **사전 동의** | `/terms/billing` (별도 문서) + `/billing` 결제 화면의 **분리된 체크박스** → `billing_record_consent` |
| ② | 금액 변경·유료 전환 시 **결제 7일 전 고지** | ✅ `billing_notice_upcoming`(마이그141) + 크론 `/api/billing/reconcile` 매일 1회. 앱 내 알림 + `billing_events` 증빙. ⚠️ **`past_due` 재시도도 승인요청이라 고지 대상** → 재시도를 원 결제일 ±7일 안에 끝낼 것 |
| ③ | 영업시간 외에도 **24시간 해지 채널** | ✅ 설정 > 요금제 해지 버튼 + `/api/billing/subscription/cancel` + `/refund` 제6조 1항 |
| ④ | 사용여부·기간 고려한 **공정 환불** | ✅ `/refund` 제1~2조 + **`computeFairRefundKrw()`**(`lib/billing/refund.ts`·마이그142와 세트). ⚠️ `/refund` 문구를 고치면 **이 함수도 반드시 같이** 고칠 것 — 한쪽만 바뀌면 분쟁에서 진다 |

**설계 원칙 (깨지 말 것)**
- **금액 위변조 방어**: 클라는 `plan/cycle`만 보낸다. 서버가 `quoteAmountKrw()`로 산출 → `ready` 행에 못박음 → 승인·웹훅·조회 금액을 전부 대조. **클라 금액은 어느 경로로도 DB에 닿지 않는다.**
- **멱등성 3중**: `order_id` unique + `tid` unique + RPC의 `select … for update`. 하나만으론 못 막는다.
- **정산은 RPC 하나로 원자 처리**: 영수증·구독·plan 승급·**사용량 즉시 재설정**·감사로그. 나눠 쏘면 "돈은 받았는데 요금제는 그대로"가 남는다.
- **크레딧은 승급 시 즉시 올리고, 강등 시엔 깎지 않는다**(회수는 분쟁 소재).
- **갱신 실패 시 즉시 강등 금지** — `past_due` 유예 최소 3일. 한도초과·카드 재발급은 정상 빈도다.
- **빌링키는 별도 테이블 + RLS 정책 0개** — 구독 테이블은 멤버 SELECT를 열어야 해서 같은 행에 두면 브라우저로 샌다.
- **RPC는 전부 service_role 전용** — 앱이 admin client로만 호출. 인증은 앱, 원자성은 DB.
- **화면 문구에 "충전·크레딧·포인트" 금지** — PG 위험업종(포인트충전) 분류 회피. KCP 반려 추정 원인이 이 분류였다.

---

## 💰 요금제·AI 원가 정책

**포함량 = 원가율 40% 역산** (대표 결정 2026-08-14 · 마이그138). 1크레딧 = $0.01 원가, 환율 ₩1,514/$.

| 플랜 | 가격(**부가세 포함**) | 인원 | 포함량 | 공정사용 배수 | 포함량 원가율 | 최악 원가율 |
|---|---|---|---|---|---|---|
| Basic | ₩0 | 3 | 500 | **1.0** | n/a | n/a |
| Standard | ₩29,000 (연 ₩290,000) | 5 | **750** | **1.3** | 39.2% | 50.9% |
| Pro | ₩49,000 (연 ₩490,000) | 10 | **1,300** | **1.3** | 40.2% | 52.2% |

> 이전 값(3,000·7,000 + 배수 3)의 진짜 최악값은 156%/216%가 아니라 **470%/649%**였다 — 공정사용 배수가
> 포함량 위에 곱해지는 걸 빠뜨렸었다. **연간가 = 월가 × 10**(2개월 무료).

**AI 원가 실측** (2026-08-14 기준)
- 프롬프트 캐싱은 **정상 작동 중**(커밋 `3f1b417`, 08-08 배포). 어시스턴트 회당 **$0.1074 → $0.0121**(1/9).
- 캐시 적용은 **2곳**(에이전트 채팅·어시스턴트). ⚠️ "나머지 22곳도 다 걸면 된다"고 적혀 있었으나 **틀렸다** — 아래 §재실측 참조.
- 에이전트별: `Notion clean`(Opus→Sonnet 전환됨) 평균 입력 81,846토큰 · `Gmail agent`(Sonnet) 19,393 · 기본 에이전트 318~1,900.
  **원가를 결정하는 건 모델이 아니라 MCP 도구 정의 크기다**(60~100배 차이).
- Standard 체감: 어시스턴트 1인 하루 약 4회, 기본 에이전트 5~6회, MCP 꼬리물기는 워크스페이스 전체 하루 1.4회.
  **실질 제약은 채팅 횟수가 아니라 꼬리물기 횟수.**

**🔬 재실측 (2026-08-22) — 계획 2개가 틀렸다. 착수 전에 이 절을 읽을 것.**

`agent_usage` 60일치 + 라이브 API 프로브로 다시 쟀다. 결론부터: **지금은 손댈 때가 아니다.**

| 확인한 것 | 결과 |
|---|---|
| 캐싱 코드 경로(AI SDK v6 `providerOptions.cacheControl`) | ✅ **정상.** 프로브 1차 쓰기 3,774 → 2차 읽기 3,774 |
| 캐싱 배포(08-08) **이후** 프로덕션 호출 | **6건뿐** (어시스턴트 4 · Notion clean 2). 회당 $0.0116 / $0.30 |
| 그 6건의 캐시 적중 | 어시스턴트 4건 중 **3건 적중** · Notion clean 2건 중 1건. 위 "1/9 절감" 기록과 일치 ✅ |

⚠️ **60일 전체 평균을 보면 "캐시가 거의 안 걸린다"로 보이는데 착시다** — 대부분이 캐싱 배포 *이전*
호출이다. 다음 세션이 같은 함정에 빠지지 않게 적어 둔다: **`created_at >= '2026-08-09'`로 필터링할 것.**

🔴 **계획에서 빼야 할 것 2개**
1. ~~캐시 TTL 5분→1시간~~ — **이 워크로드에선 손해다.** 호출 간격 실측: 직전 호출과 **5분 이내가
   67~73%**, 1시간까지 늘려도 어시스턴트 +12%p · **Notion clean +0%p** · Gmail +9%p.
   추가 적중은 거의 없는데 **쓰기 비용만 1.25배 → 2배**가 된다(1시간 TTL은 손익분기가 3회 읽기).
2. ~~캐시 커버리지 24곳 전부~~ — **일회성 라우트에 캐시를 걸면 오히려 비싸진다.**
   OCR(`cards/ocr`·`finance/ocr`)·`generate-prompt` 같은 곳은 입력이 매번 달라 재사용 프리픽스가
   없다 → 읽기 0회 + 쓰기 1.25배 = 순손실. **큰 공통 프리픽스를 반복해서 쓰는 곳만** 대상이다
   (에이전트 채팅·어시스턴트는 이미 적용됨, 워크플로우는 노드끼리 프리픽스를 공유할 때만).

✅ **이번에 고친 것**: MCP 도구를 **이름순 정렬**(`1cc6709`). 도구는 프롬프트 맨 앞에 렌더돼
1바이트만 달라져도 전체 캐시가 무효인데, MCP 서버 응답 순서와 재연결 경로 때문에 호출마다
순서가 달라질 수 있었다. (무효화 소지는 규격상 분명하지만 표본이 6건뿐이라 **실측 증명은 못 했다.**)

**남은 것 (사용량이 늘면 그때)**: 꼬리물기 스텝 상한 · 내부 작업 Haiku 라우팅
(`lib/mcp/summarize.ts:33`) · 워크플로우 캐시. ⚠️ **모델 최소 캐시 프리픽스가 다르다** —
Sonnet 4.6 **1,024** · Opus 4.7 **2,048** · Haiku 4.5 **4,096**토큰. 그보다 짧으면 마커를 걸어도
**조용히 캐시되지 않는다**(에러도 안 난다). "기본 에이전트 318~1,900토큰"은 상당수가 미달이다.

⚠️ **시트 비례 크레딧은 결제 뒤로**: 초과 좌석은 지금 생길 수 없고(`accept_workspace_invite`가 차단),
유일한 발생 경로인 다운그레이드에서는 **미결제 좌석**이라 크레딧을 주면 안 된다. 좌석 과금(`paid_seats` 기록)이 먼저다.

---

## 🧾 인프라 업그레이드 시점 (2026-08-19 조사)

**지금은 둘 다 무료 플랜이고, 그대로 두는 게 맞다.** 사는 시점은 **"외부 고객에게 결제를 여는 직전"** 하나뿐이다.
(대표 본인 카드로 하는 실결제 테스트까지는 무료 플랜으로 해도 된다 — 매출이 아니다.)

| 서비스 | 지금 | 올려야 하는 이유 | 가격 |
|---|---|---|---|
| **Vercel** | Hobby | 🔴 **약관**: 공식 문서에 *"the Hobby plan restricts users to **non-commercial, personal use only**"*. 돈을 받는 순간 위반이고, 적발 시 **계정·배포 정지** 가능 → 결제받는 서비스가 멈추는 최악의 사고. 부수 효과로 **런타임 로그 1시간 → 1일**(결제 사고는 몇 시간 뒤 발견된다), 크론 1일 1회 → 1분 단위 | **$20 / 사용자·월** |
| **Supabase** | Free | 🔴 **백업**: Free는 **자동 백업이 없다**. 고객사 결제·회계·인사 데이터를 백업 없이 굴리면 사고 시 복구 불가. 용량은 여유(2026-08-19 실측 **DB 20MB / 500MB = 4%**, 사용자 8·워크스페이스 3) | **$25 / 월** (8GB + 일일백업 7일 보관 + 자동정지 없음) |

**합계 월 약 $45 ≈ ₩68,000** (환율 ₩1,514/$).
손익분기 = **Standard 고객 3명**(수수료·부가세 제외 실수령 약 ₩25,800 × 3 = ₩77,400 > ₩68,130).
그 전까지는 대표 자비 부담 구간이다.

> Vercel은 **Pro 무료 체험**이 있다 — 실결제 테스트 시점에 체험으로 시작해 로그 1일 보관을 먼저 확보하는 것도 방법.

---

## 💡 합의된 정책 (코드만으론 안 보이는 결정 — 깨지 말 것)

- **🏷️ 브랜드**: **Complow(컴플로우)** 확정 · 도메인 `complow.kr` · 상표 9·42·35류 출원 예정.
  코드 UI는 교체 완료, 내부 식별자(`equria:*`·`equria-*`·`equria.local`)는 **의도적 유지**(바꾸면 로그인·상태가 깨진다).
  철학 = **회사별 커스터마이징**(도메인·브랜드는 고정값이 아니라 슬롯/설정).
- **🎯 타겟 (2026-08-13)**: 기업이 아니라 **소규모 팀·스타트업·대학생 수준 조직**.
  목표 = *"작은 조직이 AI를 프로페셔널하게·조직적으로 다루게 하는 것."*
  → **후순위**: SSO·감사로그·온프레미스·조달. **우선**: IT 담당자 없이 되는 온보딩 · 낮은 1인 부담 · 설치 없는 진입.
  판단 기준 = **"이 기능이 5~10명 팀에게 필요한가"**.
- **📲 앱 배포 = PWA (2026-08-13)**: 설치 파일·스토어 배포 안 한다. 미서명 실행 파일은 OS 경고로 오히려 신뢰를 깎고,
  거의 매일 배포하는 지금은 구버전 사고 위험이 크다. **Electron 재검토 조건 = 유료 고객 요구 또는 배포 주기가 주 1회 이하로 안정될 때.**
- **🔐 구글 OAuth 스코프**: `gmail.send`까지만. **제한(restricted) 스코프 절대 추가 금지** —
  `gmail.modify`·`gmail.readonly`·`drive.readonly`는 프로덕션 공개 시 **CASA Tier 2 연간 유료 감사**가 강제된다.
  로그인 전용 GCP 프로젝트를 분리해 **100명 상한도 제거**됨.
- **삭제 = soft-delete(휴지통)**: 목록은 `deleted_at is null`, Storage 보존, ⌘Z Undo.
  ⚠️ **`is_active` 소프트삭제 테이블의 SELECT USING에 `is_active=true`를 넣지 말 것**(마이그031·032 교훈) — 삭제 행이 가시성을 잃어 42501로 막힌다.
- **🔐 RLS 패턴**: SELECT = `workspace_id in (select public.auth_user_workspace_ids())`,
  INSERT with check = `public.is_workspace_member(workspace_id)`. 헬퍼는 `security definer stable`.
  신규 테이블은 `workspace_id uuid not null`(**sentinel DEFAULT 금지**, 마이그112) + 선두 인덱스.
  쓰기 정책을 만들지 않고 `security definer` RPC로만 쓰는 게 이 DB의 관례다.
- **가입·귀속**: `handle_new_user`는 **profiles만** 만든다(마이그116이 equria 자동등록 제거).
  귀속은 온보딩(`create_workspace`)·초대 수락(`accept_workspace_invite`)이 담당.
- **에이전트 = 우하단 위젯 only**(`/agents`는 빌더/관리) · **워크플로우 = n8n 캔버스**(노드≤6·60s).
- **채팅 SSOT**: `content`(plain)가 단일 진실, 리치는 `body_json`(버블만).
- **연락처 = `directory_contact` RPC로만**(이메일·전화 컬럼은 RLS 차단).
- **DDL 규칙**: MCP `apply_migration` **+** `supabase/migrations/` 파일 **둘 다**. 헤더에 근거·롤백 SQL 필수.

---

## 🏢 멀티테넌시 (회사별 격리)

- **B1-a 읽기 격리 = 완료**(마이그 033~043) · **B1-b 쓰기 강제 = 완료**(마이그112가 45개 테이블 sentinel DEFAULT 제거).
  이제 신규 워크스페이스 데이터가 equria로 오귀속될 수 없다.
- **B2 가입 개방 = 완료**(마이그113~116): 초대 링크 + guest role + `create_workspace` + 온보딩.
  신규가입은 **이미 켜져 있다**(GoTrue `disable_signup: false` 라이브 확인). 구글 로그인 동작 확인됨.
- 로드맵(B3~B6) 상세 = `PRODUCTIZATION.md` · 격리 설계 = `B1-DESIGN.md`.
- ⏸️ **보류(재촉 금지)**: 제3자 구글 계정 신규가입 E2E 검증 — 대표가 꺼낼 때 진행.

---

## 📂 문서 지도

| 문서 | 내용 |
|---|---|
| **HANDOFF.md** (이 파일) | 지금 상태·다음 할 일·합의된 정책 — **제일 먼저** |
| **WORKLOG.md** | 세션별 작업 로그(세션34~) + 기능 체크리스트 — **과거 이력은 전부 여기** |
| `CLAUDE.md` | 정체성·스택·절대원칙·파일구조·DB요약 (브랜드 규칙은 위 §합의된 정책이 최신) |
| **`BILLING-PLAN.md`** | **결제 연동 설계·작업 순서 + 놓치기 쉬운 함정** — 결제 작업 전 필독 (레포 추적, 모바일에서도 열림) |
| **`ACTIVATION-PLAN.md`** | **제품 점검 결과 + 첫 사용 경험 개선 계획** — 제품(비결제) 작업 전 필독 |
| `PRODUCTIZATION.md` / `B1-DESIGN.md` | B2B 로드맵·리스크 / 테넌트 격리 상세 |
| `AGENTS-MCP-STRATEGY.md` / `AGENTS-LEARNING-DESIGN.md` | 에이전트·MCP 전략 / 학습·기억 설계 |
| `.claude/skills/safe-changes.md` | 변경 안전 원칙 — **모든 코드/DB 변경 전 최우선** |
| `.claude/skills/{latest-stack,known-issues,work-harness}.md` | 최신 패턴 / 기술부채 / 작업 SOP·검증 게이트 |
| `src/lib/billing/*.ts` 헤더 주석 | 🔴 **결제 설계 근거의 1차 출처.** 중복청구 방어·환불 조항 대응·카드 취급 규칙이 코드 옆에 있다 |
| `STUDY.md` | (대표용) 비개발자 학습 코스 |

> 옛 설계 문서(`PLAN`·`CHAT-HUB`·`GOOGLE-MCP`)는 구현 완료·대체되어 `docs/archive/`.
> **현재 상태 = 이 파일, 구현 진실 = 코드·마이그레이션.**

---

## 🚀 환경 / 접근

- **GitHub**: `chowhiwon99-code/equria-workflow-Sass` (main=프로덕션)
- **Vercel**: team `team_wcW0NMU7oiIxNndyV1afigbp` · project `prj_CcCTUr8eIYpaStaj6RNq7VoLPZG6` · 배포보호 **off**
- **Supabase**: project `dutovtfdckhayyvhtuxu` (ap-northeast-2 서울) · 마이그 **001~142**
- **사업자**: 개인사업자 · 등록번호 592-58-00892 · 통신판매업 제2026-인천계양-0642호
- **.env.local**: ANTHROPIC · Supabase 3종 · Google 4종 · `WORKSPACE_PASSWORD` · **나이스페이 2종 = `NICEPAY_CLIENT_KEY`·`NICEPAY_SECRET_KEY`(2026-08-22 입력 완료)** · `CRON_SECRET`(Vercel 전용)
  ⚠️ **시크릿 값을 문서·채팅에 적지 말 것** (HANDOFF는 git 추적됨)
- 링크: [GitHub](https://github.com/chowhiwon99-code/equria-workflow-Sass) · [Vercel](https://vercel.com/chowhiwon99-2151s-projects/equria-workflow-sass) · [Supabase](https://supabase.com/dashboard/project/dutovtfdckhayyvhtuxu)

---

## 📜 과거 이력

세션 34~49의 작업 내용은 **`WORKLOG.md`**에 세션별로 정리돼 있고, 더 상세한 것은 git 커밋 메시지에 있다.

🔴 **이 파일에 과거 세션 블록을 다시 쌓지 말 것.** 2026-08-18에 697→약 400줄로 줄였는데 세션47~49를 거치며
**526줄로 되돌아왔다**(완료 보고를 여기에 쌓아서다). 2026-08-22에 다시 370줄로 줄였다.
판단 기준: **"앞으로 무엇을 해야 하나 / 무엇을 깨면 안 되나"만 여기 남기고, "무엇을 했나"는 WORKLOG로.**
